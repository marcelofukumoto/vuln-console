(("undefined"!==typeof self?self:this)["webpackChunkvuln_console_0_2_1"]=("undefined"!==typeof self?self:this)["webpackChunkvuln_console_0_2_1"]||[]).push([[376],{6376:function(e,n,t){"use strict";t.r(n),t.d(n,{default:function(){return Pt}});var a=t(9274),o=t(5359),r=t(9103),s=t(1300),i=t(8846),l=t(4850),d=t(9861);const c="vuln-console",h="rancher/dashboard",u="marcelofukumoto/dashboard",p=u,m="settings",v="gh_token",f="extension-studio",g="settings",b="vuln-console.rancher.io/owns",w="snapshot",y="vuln-workspace",k="/workspaces",x="__agents",_="agents:ready";function $(e){return e}const E="0.1.40";function S(){return window[x]||null}function R(e,n){const t=String(e||"").split(".").map(e=>parseInt(e,10)||0),a=n.split(".").map(e=>parseInt(e,10)||0);for(let o=0;o<Math.max(t.length,a.length);o++){const e=t[o]||0,n=a[o]||0;if(e!==n)return e>n}return!0}async function N(){const e=S();if(!e)return{state:"missing",version:null,pod:null,detail:"The Agents extension is not installed in this Rancher. Install codyrancher/agents — this extension runs every gather and every fix through the agent pod it provides."};if(!R(e.version,E))return{state:"outdated",version:e.version,pod:null,detail:`The Agents extension is ${e.version}; this needs ${E} or later for conversations that can be started with an opening prompt.`};const n=await e.agent.pod().catch(()=>null);return n?{state:"ready",version:e.version,pod:n,detail:`Agents ${e.version} · agent pod ${n}`}:{state:"no-pod",version:e.version,pod:null,detail:"The agent pod is not running yet. It comes up on its own — give it a minute after a Rancher restart, then refresh."}}function T(e=8e3){const n=S();return n?Promise.resolve(n):new Promise(n=>{const t=e=>{window.removeEventListener(_,a),clearTimeout(o),n(e)},a=()=>t(S()),o=setTimeout(()=>t(S()),e);window.addEventListener(_,a)})}const A="local",C=`/k8s/clusters/${A}`;function D(e,n,t){const a=window.location.origin.replace(/^http/,"ws"),o=new URLSearchParams({container:e.container,stdin:t?"1":"0",stdout:"1",stderr:"1",tty:"0"});for(const r of n)o.append("command",r);return`${a}${C}/api/v1/namespaces/${e.namespace}/pods/${e.pod}/exec?${o}`}function V(e){const n=Number(e.slice(0,1));try{return{channel:n,bytes:Uint8Array.from(atob(e.slice(1)),e=>e.charCodeAt(0))}}catch(t){return null}}const O=12e4;function P(e,n,t={}){var a;const o=null!==(a=t.timeoutMs)&&void 0!==a?a:O;return new Promise(r=>{const s={stdout:"",stderr:"",status:"",code:-1};let i=!1,l=null;const d={1:new TextDecoder,2:new TextDecoder},c=(e="")=>{if(!i){i=!0,clearTimeout(h),e&&(s.status=s.status||e);try{null===l||void 0===l||l.close()}catch(a){}r(s)}},h=setTimeout(()=>c(`no answer from the pod in ${Math.round(o/1e3)}s`),o);try{l=new WebSocket(D(e,n,!!t.onStdin),"base64.channel.k8s.io")}catch(u){return void c((null===u||void 0===u?void 0:u.message)||String(u))}t.onStdin&&(l.onopen=()=>{var e;null===(e=t.onStdin)||void 0===e||e.call(t,e=>{const n=(new TextEncoder).encode(e);let t="";for(const a of n)t+=String.fromCharCode(a);null===l||void 0===l||l.send(`0${btoa(t)}`)})}),l.onmessage=e=>{var n;const t=V(String(e.data||""));if(t){if(1===t.channel||2===t.channel){const e=d[t.channel].decode(t.bytes,{stream:!0});return void(1===t.channel?s.stdout+=e:s.stderr+=e)}if(3===t.channel){const e=(new TextDecoder).decode(t.bytes);try{const t=JSON.parse(e);if("Success"===t.status)s.code=0;else{s.status=t.message||"the command failed";const e=((null===(n=t.details)||void 0===n?void 0:n.causes)||[]).find(e=>"ExitCode"===e.reason);s.code=e&&Number(e.message)||1}}catch(a){s.status=e,s.code=-1===s.code?1:s.code}}}},l.onclose=()=>{if(s.stdout+=d[1].decode(),s.stderr+=d[2].decode(),-1===s.code){if(!s.stdout&&!s.stderr)return void c(s.status||"the exec was refused or dropped");s.code=0}c()},l.onerror=()=>c("could not open an exec to the pod")})}const B="__VC_OK__";async function I(e,n,t,a){const o=await P(e,["/bin/sh","-c",`set -e\n${n}\necho ${B}`],{timeoutMs:a});if(!o.stdout.includes(B)){const e=o.stderr.trim()||o.status||(o.code>0?`exit ${o.code}`:"it reported nothing");throw new Error(`Could not ${t}: ${e}`)}return o.stdout.replace(B,"").trim()}function H(e){return`'${e.split("'").join("'\\''")}'`}const j=4096,W="__END_OF_FILE__";function q(e){const n=(new TextEncoder).encode(e);let t="";for(let a=0;a<n.length;a+=8192)t+=String.fromCharCode(...n.subarray(a,a+8192));return btoa(t)}async function U(e,n,t,a={}){const o=a.mode||"644",r=a.owner||"",s=H(n),i=[`mkdir -p "$(dirname ${s})"`,`: > ${s}.b64`,`: > ${s} && chmod ${o} ${s}`,r?`chown ${r} ${s} 2>/dev/null || true`:"",`while IFS= read -r line; do [ "$line" = "${W}" ] && break; printf '%s' "$line" >> ${s}.b64; done`,`base64 -d < ${s}.b64 > ${s}`,`rm -f ${s}.b64`,`chmod ${o} ${s}`,r?`chown ${r} ${s} 2>/dev/null || true`:"",`echo WROTE $(wc -c < ${s})`].filter(Boolean).join("\n"),l=q(t),d=await P(e,["/bin/sh","-c",i],{timeoutMs:6e4,onStdin:e=>{for(let n=0;n<l.length;n+=j)e(`${l.slice(n,n+j)}\n`);e(`${W}\n`)}}),c=d.stdout.match(/WROTE\s+(\d+)/);if(!c||Number(c[1])!==(new TextEncoder).encode(t).length){const e=d.stderr.trim()||d.status||`exit ${d.code}`;throw new Error(`Could not write ${n} into the agent pod: ${e}`)}}const L="local",M=`/k8s/clusters/${L}/v1`,F=`/k8s/clusters/${L}/api/v1`;function G(){const e=document.cookie.match(/(?:^|;\s*)CSRF=([^;]*)/);return{"X-Api-Csrf":e?decodeURIComponent(e[1]):"CSRF"}}async function z(e,n){const t=!!(null===n||void 0===n?void 0:n.method)&&"GET"!==n.method,a=await fetch(e,{...n,headers:{"Content-Type":"application/json",Accept:"application/json",...t?G():{},...(null===n||void 0===n?void 0:n.headers)||{}}});if(404===a.status)return null;const o=await a.json().catch(()=>({}));if(!a.ok)throw new Error(o.message||o.error||`HTTP ${a.status}`);return o}function K(e,n){return z(`${M}${e}`,n)}async function J(){const e=await K(`/namespaces/${c}`).catch(()=>null);e||await K("/namespaces",{method:"POST",body:JSON.stringify({apiVersion:"v1",kind:"Namespace",metadata:{name:c}})}).catch(e=>{if(!/409|already exists|alreadyexists/i.test((null===e||void 0===e?void 0:e.message)||""))throw e})}function Y(e){return`/configmaps/${c}/${e}`}async function X(e,n,t){const a={apiVersion:"v1",kind:"ConfigMap",metadata:{name:e,namespace:c,labels:{[b]:"true",...t}},data:n},o=await K(Y(e)).catch(()=>null);o?await K(Y(e),{method:"PUT",body:JSON.stringify({...a,metadata:{...o.metadata,labels:a.metadata.labels}})}):await K("/configmaps",{method:"POST",body:JSON.stringify(a)}).catch(async n=>{if(!/409|already exists|alreadyexists/i.test((null===n||void 0===n?void 0:n.message)||""))throw n;const t=await K(Y(e));await K(Y(e),{method:"PUT",body:JSON.stringify({...a,metadata:{...t.metadata,labels:a.metadata.labels}})})})}async function Z(e,n){var t;const a=await K(Y(e)).catch(()=>null),o=null===(t=null===a||void 0===a?void 0:a.data)||void 0===t?void 0:t[n];if(!o)return null;try{return JSON.parse(o)}catch(r){return null}}function Q(){return Z(w,"snapshot.json")}function ee(e){const n=e.toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-+|-+$/g,"").slice(0,50);return`job-${n||"unnamed"}`}async function ne(){const e=await K(`/configmaps?labelSelector=${encodeURIComponent(`${b}=true`)}`).catch(()=>null);return((null===e||void 0===e?void 0:e.data)||[]).filter(e=>{var n;return((null===(n=null===e||void 0===e?void 0:e.metadata)||void 0===n?void 0:n.name)||"").startsWith("job-")}).map(e=>{var n;try{return JSON.parse((null===(n=e.data)||void 0===n?void 0:n["job.json"])||"null")}catch(t){return null}}).filter(e=>!!(null===e||void 0===e?void 0:e.library))}async function te(e){await J(),await X(ee(e.library),{"job.json":JSON.stringify(e)},{})}const ae={"workspace/config.yaml":'# The scripts the workspace pod runs, mounted rather than baked into an image.\n#\n# No image is ever built for any of this: a stock `node:24` reads its whole behaviour out of\n# this ConfigMap. Editing a script is editing this object, and the pod picks it up on its next\n# start - which is why the Deployment carries a hash of these in its pod template.\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  namespace: ${namespace}\n  name: vuln-workspace-config\ndata:\n  boot.sh: |\n    #!/bin/sh\n    # Bring one fix workspace up: a rancher/dashboard checkout with its dependencies installed and\n    # the dev server supervised on top of it.\n    #\n    # The container\'s main process ends up being the supervisor, not the server, because a fix run\n    # does things that kill a dev server - switches branch, regenerates a lockfile, runs a build -\n    # and a pod whose main process was the server would restart on each of those and take the run\'s\n    # terminal with it.\n    #\n    # `${install}`, `${repo}` and `${port}` are apps-plus substitutions, filled in when the App is\n    # rendered. Everything written `$LIKE_THIS` is ordinary shell and is left alone: apps-plus only\n    # substitutes the braced form, and only for names its App declares.\n    set -e\n\n    # Force apt onto IPv4 before anything runs one. The node has no working IPv6 route to the Debian\n    # mirror, so an `apt-get update` left alone resolves an IPv6 address and hangs on it for minutes\n    # while holding the apt lock - which is how a workspace comes up with no tmux and no terminal.\n    mkdir -p /etc/apt/apt.conf.d\n    printf \'Acquire::ForceIPv4 "true";\\nAcquire::Retries "3";\\n\' > /etc/apt/apt.conf.d/99vuln-ipv4\n\n    WS=/workspaces/${install}\n    mkdir -p "$WS/.home"\n    chown node:node "$WS" "$WS/.home" 2>/dev/null || true\n    [ -f "$WS/.owned" ] || { chown -R node:node "$WS" 2>/dev/null; touch "$WS/.owned"; }\n\n    # The browser sidecar mounts these as subPaths, and the kubelet creates a missing subPath as\n    # root - leaving the seed unable to write into it.\n    mkdir -p "$WS/.a11y/opt" "$WS/.a11y/init"\n    chown -R node:node "$WS/.a11y" 2>/dev/null || true\n\n    # What every workspace on this node shares rather than keeping its own copy. Measured before\n    # this existed: Cypress 813 MB, yarn 749 MB, and a gigabyte of node_modules - most of a\n    # workspace\'s 3 GB, per workspace.\n    SHARED=/workspaces/.shared\n    mkdir -p "$SHARED/yarn" "$SHARED/cypress" "$SHARED/npm" "$SHARED/template"\n    chown node:node "$SHARED" "$SHARED/yarn" "$SHARED/cypress" "$SHARED/npm" "$SHARED/template" 2>/dev/null || true\n\n    # tmux and kubectl, which the terminal needs. No claude: the conversation runs in the agents pod\n    # and reaches this one through a tunnel, so a claude here is hundreds of megabytes of hostPath\n    # that nothing executes.\n    [ -f /seed/terminal-tools.sh ] \\\n      && (TOOLS_NO_CLAUDE=1 HOME_DIR="$WS/.home" /bin/sh /seed/terminal-tools.sh >"$WS/.terminal-tools.log" 2>&1 &) \\\n      || true\n\n    # What a recording and a lockfile bump need and the image lacks. Backgrounded so the dev server\n    # is not a minute later for it; the lock timeout lets it wait for the tmux install above rather\n    # than colliding with it.\n    (command -v ffmpeg >/dev/null 2>&1 && command -v lsof >/dev/null 2>&1) || (\n      apt-get -o DPkg::Lock::Timeout=300 update -qq \\\n        && DEBIAN_FRONTEND=noninteractive apt-get -o DPkg::Lock::Timeout=300 install -y -qq \\\n          ffmpeg jq lsof iproute2 >"$WS/.apt.log" 2>&1 &\n    ) || true\n\n    exec setpriv --reuid=1000 --regid=1000 --init-groups /bin/sh /seed/checkout.sh\n  checkout.sh: |\n    #!/bin/sh\n    # The workspace\'s checkout and install, as the node user. Split from boot.sh so the privileged\n    # half is short enough to read and this half can be run again by hand.\n    set -e\n\n    WS=/workspaces/${install}\n    SHARED=/workspaces/.shared\n\n    export HOME="$WS/.home"\n    export YARN_CACHE_FOLDER="$SHARED/yarn"\n    export CYPRESS_CACHE_FOLDER="$SHARED/cypress"\n    export npm_config_cache="$SHARED/npm"\n\n    # init + fetch rather than `git clone`, because clone refuses a non-empty directory and this one\n    # is not reliably empty: the agent seed writes `.claude` into the checkout, and if that lands\n    # first - a separate trigger racing this boot - a clone would fail and leave the pod serving\n    # nothing.\n    if [ ! -d "$WS/dashboard/.git" ]; then\n      mkdir -p "$WS/dashboard"\n      cd "$WS/dashboard"\n      git init -q\n      git remote add origin https://github.com/${repo} 2>/dev/null || true\n      DEFAULT=$(git ls-remote --symref origin HEAD | sed -n \'s@^ref: refs/heads/\\(.*\\)[[:space:]]HEAD@\\1@p\')\n      git fetch --depth 1 origin "$DEFAULT"\n      git checkout -f -B "$DEFAULT" FETCH_HEAD\n    fi\n\n    cd "$WS/dashboard"\n\n    # The fork is where fixes are pushed, so it is a remote from the start rather than something the\n    # agent has to remember to add. `upstream` is what a branch is diffed against - never the fork\'s\n    # own master, which is chronically stale and makes a clean fix look like it carries half the\n    # repository with it.\n    git remote add fork "https://github.com/${fork}" 2>/dev/null || true\n    git remote set-url fork "https://github.com/${fork}" 2>/dev/null || true\n    git remote add upstream "https://github.com/${repo}" 2>/dev/null || true\n\n    # node_modules, hard-linked from a template of the same lockfile if this node already has one.\n    # The files are the same inodes, so a second workspace\'s node_modules costs directory entries\n    # and nothing else. Keyed by the lockfile hash, so a template is only ever used by a checkout\n    # that would have installed exactly it.\n    HASH=$(sha1sum yarn.lock 2>/dev/null | cut -c1-12)\n\n    if [ -n "$HASH" ] && [ ! -d node_modules ] && [ -d "$SHARED/template/$HASH/node_modules" ]; then\n      cp -al "$SHARED/template/$HASH/node_modules" node_modules && touch .install-done\n    fi\n\n    [ -f .install-done ] || {\n      yarn install --mutex "file:$SHARED/yarn/.mutex" --network-timeout 600000\n      touch .install-done\n    }\n\n    # The first workspace to install a given lockfile leaves the template for the next one.\n    if [ -n "$HASH" ] && [ -d node_modules ] && [ ! -d "$SHARED/template/$HASH" ]; then\n      mkdir -p "$SHARED/template/$HASH"\n      cp -al node_modules "$SHARED/template/$HASH/node_modules" || rm -rf "$SHARED/template/$HASH"\n    fi\n\n    exec /bin/bash /workspace-config/serve.sh ${port}\n  serve.sh: |\n    #!/bin/bash\n    # The dev server, supervised: the container\'s process is this loop, not the server.\n    #\n    # A fix run does things that kill a dev server - switches branch, regenerates a lockfile, runs a\n    # build. When the pod\'s main process was the server itself, each of those restarted the\n    # container and took the run\'s terminal with it, mid-fix. So the server is restarted when it\n    # exits, and left alone while something else holds the port.\n    PORT=${1:-8005}\n\n    # The boot script runs this from the checkout, so the tree is its parent - which is where the\n    # log and the retarget file live.\n    WS=${WS:-$(dirname "$PWD")}\n    RETARGET=$WS/.dev-server.env\n    LOG=$WS/.dev-server.log\n\n    echo "$PORT" > "$WS/.dev-server.port" 2>/dev/null || true\n    export PORT WS RETARGET LOG IONICE\n\n    held() {\n      node -e "require(\'net\').connect(Number(process.argv[1]),\'127.0.0.1\').on(\'connect\',()=>process.exit(0)).on(\'error\',()=>process.exit(1))" "$PORT" 2>/dev/null\n    }\n\n    # Not all at once. Rancher\'s embedded k3s runs beside these servers, and when it restarts every\n    # workspace pod restarts with it and every webpack compiles at the same moment: a dozen of them\n    # on twelve cores, k3s\'s controllers fail to renew their lease, k3s exits, Rancher restarts -\n    # and the herd forms again. So a server waits, with jitter so they do not all wake together,\n    # while the node\'s one-minute load is above what the cores can carry.\n    CORES=$(nproc 2>/dev/null || echo 4)\n    gate() {\n      load=$(cut -d. -f1 /proc/loadavg 2>/dev/null || echo 0)\n      [ "$load" -le $(( CORES * 3 / 2 )) ]\n    }\n\n    # One dev server per pod, whoever started it. A second webpack is another two gigabytes at every\n    # rebuild and the pod\'s limit is five, so an agent that starts its own gets the port until it is\n    # done and this one comes back afterwards. Only node processes, matched by command line: a shell\n    # or a grep that merely mentions the server would otherwise count and stop this one for nothing.\n    servers() { pgrep -f "^[^ ]*node .*vue-cli-service serve" 2>/dev/null; }\n    foreign() {\n      local p\n      for p in $(servers); do\n        [ "$(ps -o pgid= -p "$p" 2>/dev/null | tr -d \' \')" = "$SERVER" ] || echo "$p"\n      done\n    }\n\n    SERVER=""\n    stop_ours() {\n      if [ -n "$SERVER" ]; then\n        kill -TERM -- -"$SERVER" 2>/dev/null\n        wait "$SERVER" 2>/dev/null\n        SERVER=""\n      fi\n    }\n    trap "stop_ours; exit 0" TERM INT\n\n    while :; do\n      if held || [ -n "$(foreign)" ]; then sleep 10; continue; fi\n\n      sleep $(( RANDOM % 20 ))\n\n      waited=0\n      while ! gate && [ "$waited" -lt 300 ]; do\n        [ "$waited" -eq 0 ] && echo "[workspace] the node is busy; waiting to start the dev server"\n        sleep 10\n        waited=$((waited + 10))\n      done\n\n      # Below the cluster\'s own processes: a compile that takes every core has taken k3s down with\n      # it on a busy node, and a slow first page is the better failure.\n      IONICE=""\n      command -v ionice >/dev/null 2>&1 && IONICE="ionice -c 3"\n\n      # What the server runs against can be changed without a new pod: writing the retarget file\n      # restarts it onto a different Rancher. Read in the server\'s own shell, so it replaces the\n      # pod\'s environment for the server alone.\n      STAMP=$(stat -c %Y "$RETARGET" 2>/dev/null || echo 0)\n      [ -f "$RETARGET" ] && echo "[workspace] the dev server runs against $(grep \'^API=\' "$RETARGET" | cut -d= -f2-)"\n\n      # setsid, so the server and everything it starts are one process group a stop can signal as a\n      # whole; tee, so the log is in the tree as well as in the pod\'s.\n      setsid bash -c \'if [ -f "$RETARGET" ]; then set -a; . "$RETARGET"; set +a; fi; nice -n 15 $IONICE env VUE_CLI_SERVICE_CONFIG_PATH=/workspace-config/vue.config.js yarn dev --port "$PORT" 2>&1 | tee "$LOG"\' &\n      SERVER=$!\n\n      # `sleep & wait`, not `sleep`: a trap runs once the foreground command returns, and a stop\n      # that waited out the sleep left the server running for the check that followed.\n      while kill -0 "$SERVER" 2>/dev/null; do\n        sleep 10 & wait $!\n        if [ -n "$(foreign)" ]; then\n          echo "[workspace] another dev server is running in this pod; standing down until it is gone"\n          stop_ours; sleep 10; continue 2\n        fi\n        if [ "$(stat -c %Y "$RETARGET" 2>/dev/null || echo 0)" != "$STAMP" ]; then\n          echo "[workspace] the dev server\'s target changed; restarting it"\n          stop_ours; continue 2\n        fi\n      done\n\n      SERVER=""\n      echo "[workspace] the dev server exited; starting it again in 5s"\n      sleep 5\n    done\n  vue.config.js: |\n    // Written by the Dev extension. See pkg/dev-extension/workspace-config.ts.\n    //\n    // This dev server is reached one of two ways, and everything below is the consequence of which.\n    //\n    //   - behind the apiserver\'s service proxy, at DEV_PROXY_PATH, on Rancher\'s own origin. The Host\n    //     header is the proxy\'s, the TLS the browser sees is Rancher\'s, the prefix is stripped on the\n    //     way in and has to be put back on every URL handed out.\n    //   - at an origin of its own, on a node port, with no prefix at all.\n    //\n    // A dev server built for one does not work at the other: the router base is baked in. Which one\n    // this is comes from DEV_PROXY_PATH being set or empty, and changing it restarts the pod.\n    //\n    // The second mode exists because of what the first one does to a dashboard. rancher/dashboard\n    // builds its API URLs absolute from the page origin, so behind the prefix its /v1 and /v3 calls\n    // never reach this dev server at all: they go to the Rancher this page was served from, using\n    // that session, against that cluster. Measured, on a workspace with a Rancher of its own: two\n    // requests arrived here and a hundred and thirty-six went to the host. An origin of its own is\n    // the only thing that fixes it, because there is no browser-side API base to set.\n    const proxyPath = (process.env.DEV_PROXY_PATH || \'\').replace(/\\/$/, \'\');\n\n    // The prefix on every in-app link, which has to be the path the page was loaded at. Set before\n    // the shell\'s config runs, because the shell reads it out of the environment as it does.\n    process.env.ROUTER_BASE = proxyPath ? proxyPath + \'/\' : \'/\';\n\n    // The repo\'s own config, untouched in the checkout, rather than a reimplementation of it.\n    // rancher/dashboard is a monorepo whose shell is ./shell rather than a node_modules package,\n    // and which arguments its root config passes to that shell is the repo\'s business and changes\n    // with the repo. Wrapping it means this file only has to know about the proxy. This file is\n    // handed to vue-cli-service as VUE_CLI_SERVICE_CONFIG_PATH (see apps.ts, WORKSPACE_SCRIPT), so\n    // it lives outside the checkout and the checkout\'s own vue.config.js is never rewritten: the\n    // tree stays clean, a branch switch never trips over it, and nothing an agent commits carries it.\n    const base = require(require(\'path\').join(process.cwd(), \'vue.config.js\'));\n\n    // The proxy rewrites absolute URLs in HTML as they pass through, which is what lets a naive UI\n    // work behind it at all, so an asset URL that already carried the prefix would carry it twice.\n    // index.html therefore asks for \'/js/...\' and lets the proxy add the prefix, while webpack\'s\n    // runtime works its own base out from the script URL it was loaded from.\n    base.publicPath = \'auto\';\n\n    const previousChainWebpack = base.chainWebpack;\n\n    base.chainWebpack = (webpackConfig) => {\n      if (typeof previousChainWebpack === \'function\') {\n        previousChainWebpack(webpackConfig);\n      }\n\n      webpackConfig.plugin(\'html-index\').tap((args) => {\n        args[0].publicPath = \'/\';\n\n        return args;\n      });\n    };\n\n    base.devServer = {\n      ...base.devServer,\n\n      // Plain http only behind the proxy, where Rancher terminates TLS and will not talk to the\n      // shell\'s self-signed dev certificate on the way through.\n      //\n      // At its own origin the browser talks to this server directly, and http there is not a smaller\n      // problem than a certificate warning, it is a broken login: the shipped proxy sets\n      // x-forwarded-proto: https on every request it makes, so Rancher issues its session cookie with\n      // Secure, and a browser on http throws that cookie away. What that looks like is a login that\n      // returns 200 and a page that bounces straight back to the login form, which is what it did.\n      // So https here, with the shell\'s own certificate, and a warning to click through once.\n      ...(proxyPath ? { server: { type: \'http\' } } : {}),\n\n      // Requests arrive with whatever Host the proxy chain last set, never this server\'s own.\n      // Without this the proxy answers \'Invalid Host header\'.\n      allowedHosts: \'all\',\n\n      // The proxy cannot rewrite URLs in a compressed body: the response comes back as a stream\n      // error and the browser shows a blank page.\n      compress: false,\n\n      // The prefix is stripped before a request lands here, so everything is served from the root.\n      devMiddleware: { publicPath: \'/\' },\n      static:        { publicPath: \'/\' },\n\n      // A deep link into the dashboard has to load the app rather than 404. disableDotRule matters\n      // in Rancher, where a route routinely carries a resource name with dots in it.\n      historyApiFallback: {\n        index:             \'/index.html\',\n        disableDotRule:    true,\n        htmlAcceptHeaders: [\'text/html\', \'application/xhtml+xml\']\n      },\n\n      hot:             true,\n      webSocketServer: { type: \'ws\', options: { path: \'/ws\' } },\n      client:          {\n        // Hot reload rides the same proxy when there is one, and the page\'s own origin when there is\n        // not. Either way this has to be written out, because the shell\'s config has an address of its\n        // own in it and that address is not where this server is.\n        //\n        // Behind the proxy only the path is ours; the rest are webpack-dev-server\'s "infer it from\n        // window.location" sentinels. \'auto\' is one the *server* substitutes as it serves the client.\n        //\n        // Served at its own origin the sentinels are still needed, and leaving them out was a real\n        // bug rather than a tidy default. The shell\'s own config sets a fixed\n        // wss://localhost:8005/ws, which is right on a laptop running the dev server and wrong\n        // everywhere else: the browser then opened a hot-reload socket to whatever was on port 8005\n        // of the machine the browser is on, and displayed that server\'s compile errors over this\n        // dashboard. Seen here as a webpack overlay full of a completely different project\'s module\n        // resolution failures, sitting on top of the workspace\'s login form and swallowing clicks.\n        //\n        // 0.0.0.0 and 0 are webpack-dev-server\'s own "take it from window.location" sentinels. The\n        // protocol is written out rather than left as \'auto\', because \'auto\' is substituted by the\n        // server as it serves the client and is handed to the WebSocket constructor unchanged when\n        // the page was loaded directly. This server is https at its own origin, so wss.\n        webSocketURL: proxyPath ? {\n          protocol: \'auto\',\n          hostname: \'0.0.0.0\',\n          port:     0,\n          pathname: proxyPath + \'/ws\'\n        } : {\n          protocol: \'wss\',\n          hostname: \'0.0.0.0\',\n          port:     0,\n          pathname: \'/ws\'\n        },\n        // The compile-error overlay would cover the whole framed dashboard for whoever is looking\n        // when an edit fails to compile.\n        overlay: false\n      }\n    };\n\n    module.exports = base;\n',"workspace/deployment.yaml":'# The workspace: a checkout with a supervised dev server, and a Chromium beside it.\n#\n# Two containers in one pod so they share localhost - the browser drives the dev server at\n# 127.0.0.1 and the agent drives the browser at 127.0.0.1:9222, with nothing to route and no\n# service to resolve between them.\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  namespace: ${namespace}\n  name: ${namespace}\n  labels:\n    vuln-console.rancher.io/workspace: ${install}\nspec:\n  replicas: 1\n  # Recreate, not RollingUpdate: two of these would both want the hostPath tree and the same\n  # node_modules, and the second would fight the first for the yarn mutex.\n  strategy:\n    type: Recreate\n  selector:\n    matchLabels:\n      vuln-console.rancher.io/workspace: ${install}\n  template:\n    metadata:\n      labels:\n        vuln-console.rancher.io/workspace: ${install}\n    spec:\n      serviceAccountName: vuln-workspace\n      containers:\n        - name: workspace\n          image: ${image}\n          command: ["/bin/sh", "/workspace-config/boot.sh"]\n          env:\n            # The path this dev server is reached at. The dashboard bakes its router base in at\n            # build time, so it has to know the prefix the proxy strips on the way in.\n            - name: DEV_PROXY_PATH\n              value: /k8s/clusters/${hostCluster}/api/v1/namespaces/${namespace}/services/${scheme}:${namespace}:${port}/proxy\n            - name: API\n              value: ${rancherUrl}\n            - name: RANCHER_URL\n              value: ${rancherUrl}\n            - name: VULN_BROWSER_CDP\n              value: http://localhost:9222\n            # This cluster is k3s inside the Rancher container, so the node\'s own address is\n            # how the dev server reaches the Rancher it belongs to.\n            - name: NODE_IP\n              valueFrom:\n                fieldRef:\n                  fieldPath: status.hostIP\n            # A dashboard build is a large one; the default heap is not enough for it.\n            - name: NODE_OPTIONS\n              value: --max_old_space_size=4096\n          ports:\n            - containerPort: ${port}\n              protocol: TCP\n          # The first start is a clone, a yarn install and a first compile - minutes, not\n          # seconds - so the startup probe is allowed a long time before it gives up, and only\n          # then does the readiness probe take over.\n          startupProbe:\n            httpGet:\n              path: /index.html\n              port: ${port}\n              scheme: HTTPS\n            periodSeconds: 10\n            failureThreshold: 90\n          readinessProbe:\n            httpGet:\n              path: /index.html\n              port: ${port}\n              scheme: HTTPS\n            periodSeconds: 30\n            failureThreshold: 3\n          resources:\n            requests:\n              cpu: 250m\n              memory: 1Gi\n            limits:\n              cpu: "2"\n              memory: 5Gi\n          volumeMounts:\n            - name: work\n              mountPath: /workspaces\n            - name: workspace-config\n              mountPath: /workspace-config\n            - name: seed\n              mountPath: /seed\n        - name: browser\n          image: lscr.io/linuxserver/chromium:latest\n          env:\n            - name: CHROME_CLI\n              value: --remote-debugging-port=9222 --remote-allow-origins=* --ignore-certificate-errors ${scheme}://localhost:${port}\n            - name: DOCKER_MODS\n              value: linuxserver/mods:universal-package-install\n            - name: INSTALL_PACKAGES\n              value: ${a11yPackages}\n          ports:\n            - containerPort: 3000\n              protocol: TCP\n            - containerPort: 9222\n              protocol: TCP\n          resources:\n            requests:\n              cpu: 100m\n              memory: 512Mi\n            limits:\n              cpu: "1"\n              memory: 3Gi\n          volumeMounts:\n            # Chromium needs more shared memory than the default 64 MiB or it dies rendering\n            # anything substantial.\n            - name: dshm\n              mountPath: /dev/shm\n            - name: work\n              mountPath: /artifacts\n              subPath: ${install}/artifacts\n      volumes:\n        # The PARENT of the workspaces, deliberately: the checkout\'s node_modules is hard-linked\n        # from a template shared between workspaces, and a hard link cannot cross mounts.\n        - name: work\n          hostPath:\n            path: /var/lib/rancher/vuln-workspaces\n            type: DirectoryOrCreate\n        - name: workspace-config\n          configMap:\n            name: vuln-workspace-config\n            defaultMode: 365\n        # The agents extension\'s own seed, for the terminal tools. Optional: a workspace still\n        # builds and serves without it, it just has no tmux.\n        - name: seed\n          configMap:\n            name: agent-seed\n            defaultMode: 365\n            optional: true\n        - name: dshm\n          emptyDir:\n            medium: Memory\n            sizeLimit: 1Gi\n',"workspace/namespace.yaml":"# The workspace's own namespace. One per fix in flight, named for the installation, so a run's\n# pod, service and config are deleted together by deleting the installation.\napiVersion: v1\nkind: Namespace\nmetadata:\n  name: ${namespace}\n  labels:\n    vuln-console.rancher.io/workspace: ${install}\n  annotations:\n    vuln-console.rancher.io/port: \"${port}\"\n    vuln-console.rancher.io/scheme: ${scheme}\n","workspace/rbac.yaml":"# What the workspace pod may do, which is only ever within its own namespace.\n#\n# Deliberately not cluster-admin. The agents pod is cluster-admin because it edits the cluster;\n# a workspace only ever builds a branch and serves it, so a RoleBinding - not a ClusterRoleBinding\n# - is what it gets. A fix that needs more than this is a fix worth looking at.\napiVersion: v1\nkind: ServiceAccount\nmetadata:\n  namespace: ${namespace}\n  name: vuln-workspace\n---\napiVersion: rbac.authorization.k8s.io/v1\nkind: RoleBinding\nmetadata:\n  namespace: ${namespace}\n  name: vuln-workspace\nroleRef:\n  apiGroup: rbac.authorization.k8s.io\n  kind: ClusterRole\n  name: edit\nsubjects:\n  - kind: ServiceAccount\n    name: vuln-workspace\n    namespace: ${namespace}\n","workspace/service.yaml":"# Two ports, two ways in.\n#\n# The dev server (${port}) is reached through the Kubernetes apiserver's service proxy, on\n# Rancher's own origin - so there is no Ingress, no hostname to choose and no certificate to\n# wait for, and the browser's existing Rancher session authenticates it. The browser sidecar\n# (3000) is framed the same way.\napiVersion: v1\nkind: Service\nmetadata:\n  namespace: ${namespace}\n  name: ${namespace}\n  labels:\n    vuln-console.rancher.io/workspace: ${install}\nspec:\n  selector:\n    vuln-console.rancher.io/workspace: ${install}\n  ports:\n    - name: http\n      port: ${port}\n      targetPort: ${port}\n      protocol: TCP\n    - name: browser\n      port: 3000\n      targetPort: 3000\n      protocol: TCP\n"};const oe="appsplus.io.app",re="appsplus.io.appinstance",se="vuln-console.rancher.io/library";function ie(e){var n,t;return!!(null===(t=(n=e.getters)["management/schemaFor"])||void 0===t?void 0:t.call(n,re))}function le(e){const n=e.toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-+|-+$/g,"").slice(0,40);return`vuln-${n||"fix"}`}function de(){const e=Object.keys(ae).filter(e=>e.startsWith("workspace/")).sort().map(e=>({name:e.replace("workspace/",""),content:ae[e]}));return{apiVersion:"appsplus.io/v1alpha1",kind:"App",metadata:{name:y},spec:{description:"A rancher/dashboard checkout with its dependencies installed, the dev server running, and a browser beside it — where one Dependabot fix is made and verified. The first start is minutes: a clone, a yarn install and a first compile.",values:{repo:h,fork:u,port:8005,scheme:"https",image:"node:24",hostCluster:"local",rancherUrl:"https://$(NODE_IP)",a11yPackages:"at-spi2-core|dbus-x11|gir1.2-atspi-2.0|python3-gi|python3-pyatspi"},valueLabels:{repo:"Repository the fix is made against",fork:"Fork the branch is pushed to",port:"Port the dev server listens on",scheme:"http or https",image:"Container image",hostCluster:"Cluster the workspace runs on",rancherUrl:"Rancher the dev server points at",a11yPackages:"What the browser installs (pipe separated)"},templates:e}}}async function ce(e){var n;const t=de(),a=await e.dispatch("management/find",{type:oe,id:y}).catch(()=>null);if(!a){const n=await e.dispatch("management/create",t);return void await n.save()}JSON.stringify(null===(n=a.spec)||void 0===n?void 0:n.templates)!==JSON.stringify(t.spec.templates)&&(a.spec={...a.spec,...t.spec},await a.save())}async function he(e,n){const t=le(n),a=await e.dispatch("management/find",{type:re,id:t}).catch(()=>null);if(a)return t;await ce(e);const o=await e.dispatch("management/create",{type:re,metadata:{name:t,labels:{[se]:n.replace(/[^A-Za-z0-9_.-]/g,"-").slice(0,63)}},spec:{app:y,namespace:t,targets:[{clusterName:"local"}],values:{},provisionCluster:{enabled:!1}}});return await o.save(),t}const ue={"address-comments.prompt.md":'# Address the review comments on a pull request\n\nRe-read the pull request\'s review comments, make any changes that are actually requested,\nre-verify, and **stage a reply for the human to send**.\n\n## The rule that matters most\n\n**Do not post a comment on the pull request.** Not a reply, not a summary, not an\nacknowledgement. Marcelo answers reviewers himself, in his own words — an auto-posted reply reads\nas his and is not.\n\nWhat you do instead: write the per-point reply as a draft and record where it is, so the board\nshows a **reply draft** pill he can read and then post himself.\n\nEditing the pull request **body** is still wanted — the description should stay true as the\nbranch changes, and the video belongs there.\n\n## What to do\n\n1. Read every review comment and every review state on the pull request.\n2. For each point: decide whether it asks for a change. If it does, make it in the checkout.\n3. If you changed anything: re-run the verification against the dev server, capture a fresh\n   video, and update the `### Screenshot/Video` section of the body.\n4. Write the reply draft — one short paragraph per point, saying what you did or why nothing\n   changed. Never a bare `#<number>` in it.\n5. Record:\n\n```\n$ROOT/job.sh "<library>" phase=Done replyUrl=<path> message="<one line: what changed>"\n```\n\nIf nothing needed changing, say so in the draft and record it the same way.\n',"fix.prompt.md":'# Fix one Dependabot vulnerability\n\nYou are the vulnerability console\'s fix agent. Somebody pressed **Fix** on one library. Bump it,\nprove the bump did not break where it is used, and record what you did. **Do not open a pull\nrequest** — that is a separate button and a separate run.\n\n## Where you are\n\nEvery command you run lands inside this fix\'s workspace, at the path it already has. You do not\nneed to `kubectl exec`, `ssh` or `cd` anywhere unusual; `yarn`, `git` and `node` are simply there.\n\n- the checkout is `$WSD/dashboard`, on the repository\'s default branch\n- `origin` is the upstream repository, `fork` is where branches are pushed\n- `gh` and `jq` are on `$PATH`, and `gh` is already authenticated\n- the dev server is **already running** and supervised. Never start a second one — the pod\'s\n  memory limit fits one, and two have OOM-killed the container out from under a run. Its URL is\n  in the job; a branch you check out is serving within a minute or two of compiling.\n\n## ⛔ You do not get woken up\n\nYou run one-shot. There is no notification, no wake-up, no re-invocation. The moment you stop to\n"wait for a background task" or "await completion", the run ends and everything you did is lost.\n\nRun every slow command **synchronously in the foreground**. If one might time out, block inline\nwith a single foreground loop over its log until a done marker appears. Never hand work to a\nbackground task and yield.\n\nDo **not** run the whole monorepo\'s `yarn lint` or `yarn test` — that is the pull request\'s CI\njob, it is slow, and it will strand you.\n\n## 1. Decide whether to fix, and on which branch\n\nA package can need fixing more than once over time, so branches are **numbered** and every\nre-fix gets its own. **Never reuse or force-push a branch that already has a pull request** —\nthat corrupts the earlier one and makes the new fix link to it.\n\nList what already exists:\n\n```\ngh pr list --repo <fork> --state all --json number,state,title,headRefName \\\n  | jq \'[.[] | select(.title | test("<library>"; "i"))]\'\ngh api repos/<fork>/branches --paginate --jq \'.[].name\' | grep -E "/<library>(-|$)"\n```\n\nThen:\n\n- **an open pull request of ours already covers every still-open line** — stop, and record\n  `phase=Done message="already covered by pull request <n>"`. Do not open another.\n- **an open pull request exists but does not cover this case** (it fixed a different manifest or\n  major line) — this is a separate fix: take the next free numbered branch.\n- **only merged pull requests exist and the advisory is open again** — always a new numbered\n  branch. Never touch the merged one\'s.\n- **nothing exists** — fix it on the base branch name.\n\nBranch names: `dependabot/npm_and_yarn/multi/<library>` for the first fix, then `<library>-1`,\n`<library>-2`, … — the first name not already in use.\n\n## 2. Make the fix\n\nStart from a clean tree on the repository\'s default branch, then bump the vulnerable dependency\nacross **every** manifest it touches and regenerate each lockfile.\n\n**Prefer a real update; a `resolutions` override is the last resort.** When a stale lockfile\nmerely holds a transitive at an old patch and the consuming range already admits a patched\nversion, `yarn upgrade <library>` lets it climb in place. Reach for a root `resolutions` pin only\nwhen neither an in-range update nor bumping the pinning parent can get there.\n\n### The three things reviewers actually catch\n\n1. **Cover every affected manifest, enumerated from the LIVE alerts** — not from the board,\n   which can be hours old:\n   ```\n   gh api repos/<repo>/dependabot/alerts --paginate \\\n     --jq \'.[]|select(.state=="open")|select(.security_vulnerability.package.name=="<library>")|.dependency.manifest_path\' \\\n     | sort -u\n   ```\n   Then grep the repository for every other `yarn.lock` pinning the vulnerable range. Bump them\n   all — root, `shell/`, **`cypress/`**, `storybook/`, `docusaurus/`, `pkg/*`. `cypress/yarn.lock`\n   is the commonly missed one.\n\n2. **Each affected major line needs that line\'s own first patched version.** They differ per\n   line: one advisory can require `1.1.16` *and* `2.1.2` *and* `5.0.7`, and a lower patch in one\n   line can still be vulnerable. Read each alert\'s `vulnerable_version_range` and\n   `first_patched_version`, and confirm the consuming `^x.y` ranges admit what you resolved to.\n\n3. **Introduce no new vulnerable package.** After regenerating, diff each lockfile for *added*\n   entries and check every newly added package and version — including a **different** package\n   the resolver pulled in (the classic is `braces < 3.0.3`). If the regeneration added a\n   vulnerable one, pin it forward, reinstall, and re-diff until the net change adds zero\n   vulnerabilities.\n\n### The lockfile will refuse to change\n\nThe checkout carries a root `.yarnrc` containing `--frozen-lockfile true`, and it is inherited by\nevery subdirectory. `yarn install` will install into `node_modules` and leave `yarn.lock`\nuntouched, so the bump appears to do nothing. Move the root `.yarnrc` aside, clear the package\nfrom `node_modules` and remove `.yarn-integrity`, install, then put `.yarnrc` back. Look for\n`success Saved lockfile.` and confirm the new version in the lock. **Never commit a `.yarnrc`\nchange.**\n\nCommit **only** `package.json` and `yarn.lock` files. No attribution trailers, no co-author\nlines, and no pull request or issue numbers in the message.\n\nRecord the branch as soon as it is pushed:\n\n```\n$ROOT/job.sh "<library>" branch=<branch>\n```\n\n## 3. Prove it still works\n\nThe point is not that it builds — it is that the place this library is actually used still\nbehaves. Grep the checkout for where it is imported, pick two to four real interactions that\nexercise it, and drive them in the browser against the running dev server.\n\nUse the browser that is already in this pod at `$VULN_BROWSER_CDP` — connect over CDP, do not\ninstall a browser. Capture an annotated video and take screenshots of each check.\n\nWrite the verification up as numbered checks, each ending ✅ or ❌, followed by a one-line\nverdict. Then record it:\n\n```\n$ROOT/job.sh "<library>" phase=Fixed previewUrl=<url> videoUrl=<path> infoUrl=<path> \\\n  message="<one line: what was bumped, in how many manifests, and what was verified>"\n```\n\nIf a check fails, that is a real result: record `phase=Failed` with what broke. A fix that\nbreaks the feature is worse than the advisory.\n\n## What you must not do\n\n- do not open a pull request\n- do not comment on anything on GitHub\n- do not touch any repository other than the checkout in your workspace\n- do not start a second dev server\n',"gather.mjs":"#!/usr/bin/env node\n// Gather the Dependabot picture for one repository: every alert, Dependabot's own pull\n// requests, and ours.\n//\n// Dependency-free and shell-free on purpose. This runs inside the agents pod, a stock\n// `node:24` with neither `jq` nor `gh` in it, so the laptop pipeline it replaces\n// (scripts/gather-vulnerabilities.sh, which is `gh api | jq` from end to end) is re-expressed\n// in the one runtime the pod is guaranteed to have.\n//\n// It writes ALL alert states, not just the open ones. The board needs the closed ones to show\n// what we shipped, and - more importantly - closing is the ONLY thing that moves a row out of\n// the actionable list. A gather that dropped them would leave the board unable to tell \"fixed\"\n// from \"never seen\".\n\nimport { readFileSync, writeFileSync } from 'node:fs';\n\nconst REPO = process.env.VULN_REPO || 'rancher/dashboard';\nconst FORK_OWNER = process.env.FORK_OWNER || 'marcelofukumoto';\nconst OUT = process.env.OUT || './snapshot.json';\nconst API = process.env.GITHUB_API || 'https://api.github.com';\n\nfunction fail(message) {\n  process.stderr.write(`gather: ${ message }\\n`);\n  process.exit(1);\n}\n\n/**\n * The token, read from a file rather than the environment.\n *\n * A pod is a place where `ps` and `/proc/<pid>/environ` are readable, and this one is shared:\n * every conversation in it runs as the same user. A token on a command line or in an exported\n * variable is a token every pane can read for as long as the process lives. The file is written\n * 0600, read once here, and removed when the run ends.\n */\nfunction token() {\n  const file = process.env.CREDS_FILE;\n\n  if (!file) {\n    return process.env.GH_TOKEN || '';\n  }\n\n  try {\n    return JSON.parse(readFileSync(file, 'utf8')).GH_TOKEN || '';\n  } catch (e) {\n    fail(`could not read the credentials at ${ file }: ${ e?.message || e }`);\n  }\n}\n\nconst GH_TOKEN = token();\n\nif (!GH_TOKEN) {\n  fail('GH_TOKEN is not set. Dependabot alerts cannot be read without it.');\n}\n\nconst HEADERS = {\n  Authorization: `Bearer ${ GH_TOKEN }`,\n  Accept:        'application/vnd.github+json',\n  'User-Agent':  'vuln-console',\n};\n\nasync function request(url, what) {\n  let resp;\n\n  try {\n    resp = await fetch(url, { headers: HEADERS });\n  } catch (e) {\n    throw new Error(`${ what }: ${ e?.message || e }`, { cause: e });\n  }\n\n  const text = await resp.text();\n\n  if (!resp.ok) {\n    const detail = text.slice(0, 300).replace(/\\s+/g, ' ').trim();\n\n    throw new Error(`${ what }: HTTP ${ resp.status }${ detail ? ` - ${ detail }` : '' }`);\n  }\n\n  try {\n    return { body: JSON.parse(text), link: resp.headers.get('link') || '' };\n  } catch {\n    throw new Error(`${ what }: the answer was not JSON`);\n  }\n}\n\n/** Follow `Link: rel=\"next\"` rather than counting pages - GitHub decides where the end is. */\nasync function paginate(url, what) {\n  const all = [];\n  let next = url;\n\n  while (next) {\n    const { body, link } = await request(next, what);\n\n    if (!Array.isArray(body)) {\n      throw new Error(`${ what }: expected a list, got ${ typeof body }`);\n    }\n\n    all.push(...body);\n\n    const more = /<([^>]+)>;\\s*rel=\"next\"/.exec(link);\n\n    next = more ? more[1] : null;\n  }\n\n  return all;\n}\n\nfunction severityOf(value) {\n  const known = ['critical', 'high', 'medium', 'low'];\n\n  return known.includes(value) ? value : 'low';\n}\n\nasync function fetchAlerts() {\n  // No `state` filter, deliberately. The parameter takes a comma-separated list of the four\n  // real states and has no \"all\" - and an unrecognised value is not an error, it is an empty\n  // list. Asking for `state=all` returns zero alerts with HTTP 200, which is indistinguishable\n  // from a repository that has none.\n  const raw = await paginate(\n    `${ API }/repos/${ REPO }/dependabot/alerts?per_page=100`,\n    'reading the Dependabot alerts',\n  );\n\n  // Trimmed, because this ends up in a ConfigMap and a ConfigMap holds a megabyte. The full\n  // record for all 969 of rancher/dashboard's alerts is 521 KiB, which is half the ceiling on a\n  // list that only grows - so the snapshot carries what the BOARD reads and nothing else:\n  //\n  //   * `url` is derived (`<repo>/security/dependabot/<id>`), not stored.\n  //   * the vulnerable range, scope and relationship are not stored at all. They belong to\n  //     fixing, and the fix prompt already enumerates them from the LIVE alerts rather than the\n  //     snapshot - precisely because a snapshot can be hours old and a manifest list must not be.\n  //   * a CLOSED alert keeps only what the shipped list shows. 959 of the 969 are closed, and\n  //     what they are for is \"we fixed this\" - not the advisory text.\n  return raw.map((a) => {\n    const open = a.state === 'open';\n    const alert = {\n      id:       a.number,\n      library:  a.security_vulnerability?.package?.name || a.dependency?.package?.name || '?',\n      ghsa:     a.security_advisory?.ghsa_id || '',\n      severity: severityOf(a.security_advisory?.severity),\n      manifest: a.dependency?.manifest_path || '',\n      state:    a.state,\n      patched:  a.security_vulnerability?.first_patched_version?.identifier || null,\n      fixedAt:  a.fixed_at || null,\n    };\n\n    if (!open) {\n      return alert;\n    }\n\n    return {\n      ...alert,\n      ecosystem: a.security_vulnerability?.package?.ecosystem || 'npm',\n      cve:       a.security_advisory?.cve_id || null,\n      summary:   a.security_advisory?.summary || '',\n      createdAt: a.created_at,\n    };\n  });\n}\n\nfunction normalisePr(pr) {\n  return {\n    number:      pr.number,\n    url:         pr.html_url,\n    title:       pr.title || '',\n    headRefName: pr.head?.ref || '',\n    status:      pr.merged_at ? 'merged' : (pr.state === 'closed' ? 'closed' : 'open'),\n    author:      pr.user?.login || '',\n    createdAt:   pr.created_at,\n    mergedAt:    pr.merged_at || null,\n    videoUrl:    videoIn(pr.body || ''),\n  };\n}\n\n/**\n * A verification video already attached to a pull request body.\n *\n * Detecting it means the board's recording pill survives losing our own record of the run -\n * the pull request is the durable copy, and a GitHub-hosted URL is the published one.\n */\nfunction videoIn(body) {\n  const found = /https:\\/\\/github\\.com\\/user-attachments\\/assets\\/[0-9a-f-]+/i.exec(body);\n\n  return found ? found[0] : null;\n}\n\n/**\n * Our pull requests, in one GraphQL search.\n *\n * Not by paging `/pulls?state=closed`: rancher/dashboard has tens of thousands and the ones\n * that matter are a few hundred by one author. Not by the REST search either - that returns\n * issue records carrying neither the head branch nor a body, so every hit needed a second\n * call and a few hundred of those take longer than the whole rest of the gather. GraphQL\n * answers the question and returns the fields in the same request.\n *\n * Both fields it adds are load-bearing: a closed alert is attributed to a merge by TIMING, and\n * the branch is the fallback when a title says nothing.\n */\nasync function fetchOurPulls() {\n  const query = `repo:${ REPO } author:${ FORK_OWNER } is:pr`;\n  const found = [];\n  let cursor = null;\n\n  for (;;) {\n    const page = await graphql(\n      `query($q: String!, $after: String) {\n         search(query: $q, type: ISSUE, first: 100, after: $after) {\n           pageInfo { hasNextPage endCursor }\n           nodes {\n             ... on PullRequest {\n               number url title headRefName state createdAt mergedAt body\n               author { login }\n             }\n           }\n         }\n       }`,\n      { q: query, after: cursor },\n    );\n    const { nodes = [], pageInfo = {} } = page.search || {};\n\n    found.push(...nodes.filter((n) => n && n.number));\n\n    if (!pageInfo.hasNextPage) {\n      break;\n    }\n\n    cursor = pageInfo.endCursor;\n  }\n\n  return found\n    .map((n) => ({\n      number:      n.number,\n      url:         n.url,\n      title:       n.title || '',\n      headRefName: n.headRefName || '',\n      status:      n.mergedAt ? 'merged' : (n.state === 'CLOSED' ? 'closed' : 'open'),\n      author:      n.author?.login || '',\n      createdAt:   n.createdAt,\n      mergedAt:    n.mergedAt || null,\n      videoUrl:    videoIn(n.body || ''),\n    }))\n    .filter((p) => p.status !== 'closed');\n}\n\nasync function graphql(query, variables) {\n  let resp;\n\n  try {\n    resp = await fetch(`${ API }/graphql`, {\n      method:  'POST',\n      headers: { ...HEADERS, 'Content-Type': 'application/json' },\n      body:    JSON.stringify({ query, variables }),\n    });\n  } catch (e) {\n    throw new Error(`searching for our pull requests: ${ e?.message || e }`, { cause: e });\n  }\n\n  const text = await resp.text();\n\n  if (!resp.ok) {\n    throw new Error(`searching for our pull requests: HTTP ${ resp.status } - ${ text.slice(0, 300) }`);\n  }\n\n  const body = JSON.parse(text);\n\n  // GraphQL answers 200 with an `errors` array, so a failure here looks like a success to\n  // anything that only checks the status.\n  if (body.errors?.length) {\n    throw new Error(`searching for our pull requests: ${ body.errors.map((e) => e.message).join('; ') }`);\n  }\n\n  return body.data;\n}\n\n/** Dependabot's own open pull requests - the \"there is already one upstream\" signal. */\nasync function fetchDependabotPulls() {\n  const open = await paginate(\n    `${ API }/repos/${ REPO }/pulls?state=open&per_page=100`,\n    'reading the open pull requests',\n  );\n\n  return open.map(normalisePr).filter((p) => p.author === 'dependabot[bot]');\n}\n\nasync function main() {\n  const [alerts, dependabotPrs, ourPrs] = await Promise.all([\n    fetchAlerts(), fetchDependabotPulls(), fetchOurPulls(),\n  ]);\n\n  // A transient failure must never be written as an empty snapshot. The console this replaces\n  // piped `gh api | jq ... || echo \"[]\"`, so a rate-limited page produced a valid 0-alert file\n  // that the watcher then reused for the rest of the day - the board simply went blank while\n  // the repository had 156 open alerts. Any failure above has already thrown; an empty result\n  // here is treated as one too.\n  if (!alerts.length) {\n    fail('no alerts came back at all. Refusing to write a snapshot that would blank the board.');\n  }\n\n  const snapshot = {\n    gatheredAt: new Date().toISOString(),\n    repo:       REPO,\n    alerts,\n    dependabotPrs,\n    ourPrs,\n  };\n\n  writeFileSync(OUT, JSON.stringify(snapshot));\n\n  const open = alerts.filter((a) => a.state === 'open').length;\n\n  process.stderr.write(\n    `gather: ${ alerts.length } alerts (${ open } open), ${ dependabotPrs.length } Dependabot pull requests, ${ ourPrs.length } of ours -> ${ OUT }\\n`,\n  );\n}\n\nmain().catch((e) => fail(e?.message || String(e)));\n","gather.sh":'#!/bin/sh\n# Refresh the board: gather the Dependabot picture and put it in the cluster.\n#\n# Deterministic from end to end - there is no agent in this path and nothing is judged. The old\n# console needed a watcher on somebody\'s laptop to do this on a timer; here it is a script the\n# extension runs in a pod it already has, and the result is a ConfigMap rather than a file in an\n# emptyDir that a pod rollout wiped.\n#\n# It resolves the token itself rather than being handed one. A token pasted into a page and\n# written through the exec socket is a token the browser held; read here, with the pod\'s own\n# ServiceAccount, at the moment it is needed, the browser never has it at all.\n#\n# usage: gather.sh <work-dir>\nset -e\n\nDIR=${1:?gather.sh needs a working directory}\nSEED=$(dirname "$0")\n\n[ -d "$DIR" ] || { echo "gather.sh: no such directory: $DIR" >&2; exit 2; }\n\nNS=vuln-console\nSECRET=settings\n# Extension Studio keeps an account\'s GitHub token under this exact name. Ours is preferred -\n# setting one here is somebody choosing it for this extension - and theirs is the fallback, so\n# nobody has to keep two copies of one token in step.\nSTUDIO_NS=extension-studio\nSTUDIO_SECRET=settings\n\n# kubectl as the pod rather than as whoever opened a terminal in it. shell.sh writes a kubeconfig\n# carrying the Rancher identity of the person who opened the pane, and that identity may not be\n# allowed to read these Secrets - the pod\'s ServiceAccount is. A KUBECONFIG naming nothing is\n# what sends kubectl to the in-cluster config.\nkube() {\n  KUBECONFIG=/dev/null kubectl "$@"\n}\n\nsecret_key() {\n  kube get secret "$2" -n "$1" -o "jsonpath={.data.$3}" 2>/dev/null | base64 -d 2>/dev/null | tr -d \'\\r\\n\'\n}\n\nGH_TOKEN=$(secret_key "$NS" "$SECRET" gh_token)\n[ -n "$GH_TOKEN" ] || GH_TOKEN=$(secret_key "$STUDIO_NS" "$STUDIO_SECRET" gh_token)\n\nif [ -z "$GH_TOKEN" ]; then\n  echo "gather.sh: no GitHub token is stored. Set one from the extension\'s Credentials dialog." >&2\n  exit 2\nfi\n\n# 0600 before anything is in it, so it is never briefly readable. Removed however this ends.\nCREDS="$DIR/creds.json"\nSNAPSHOT="$DIR/snapshot.json"\n\ncleanup() { rm -f "$CREDS"; }\ntrap cleanup EXIT INT TERM\n\n: > "$CREDS"\nchmod 600 "$CREDS"\n\n# Through node rather than printf, so a token containing a quote or a backslash is JSON-encoded\n# rather than pasted into a string and hoped for.\nGH_TOKEN="$GH_TOKEN" node -e \\\n  \'require("fs").writeFileSync(process.argv[1], JSON.stringify({ GH_TOKEN: process.env.GH_TOKEN }))\' \\\n  "$CREDS"\n\nCREDS_FILE="$CREDS" OUT="$SNAPSHOT" VULN_REPO="${VULN_REPO:-rancher/dashboard}" \\\n  FORK_OWNER="${FORK_OWNER:-marcelofukumoto}" node "$SEED/gather.mjs"\n\ncleanup\n\n# A ConfigMap holds a megabyte. A snapshot that does not fit has to fail as a gather somebody can\n# see, not as a kubectl error nobody reads.\nSIZE=$(wc -c < "$SNAPSHOT")\n\nif [ "$SIZE" -gt 950000 ]; then\n  echo "gather.sh: the snapshot is $((SIZE / 1024)) KiB, over what a ConfigMap can hold" >&2\n  exit 2\nfi\n\nkube get namespace "$NS" >/dev/null 2>&1 || kube create namespace "$NS" >/dev/null\n\n# Server-side apply, NOT the client-side default. A client-side `apply` records the whole object\n# it sent in a `kubectl.kubernetes.io/last-applied-configuration` annotation, and annotations are\n# capped at 256 KiB - so a snapshot large enough to be worth having is one the apiserver rejects\n# outright ("metadata.annotations: Too long"). Server-side apply keeps its bookkeeping in\n# managedFields instead, where there is no such limit.\n\nkube create configmap snapshot \\\n  --namespace "$NS" \\\n  --from-file=snapshot.json="$SNAPSHOT" \\\n  --dry-run=client -o json |\n  node -e \'\n    let raw = "";\n\n    process.stdin.on("data", (c) => (raw += c));\n    process.stdin.on("end", () => {\n      const cm = JSON.parse(raw);\n\n      cm.metadata.labels = { "vuln-console.rancher.io/owns": "true" };\n      process.stdout.write(JSON.stringify(cm));\n    });\n  \' |\n  kube apply --server-side --force-conflicts -f - >/dev/null\n\necho "gather.sh: published a $((SIZE / 1024)) KiB snapshot"\n',"job.sh":'#!/bin/sh\n# Record what a run achieved, as fields on its job.\n#\n# The agent calls this rather than composing kubectl itself, for the same reason the report\n# console has a publish script: the object name and the label are what the board selects on, so\n# a job written with a typo\'d label is a job that exists and cannot be found.\n#\n# It MERGES. Each call sets the fields it is given and leaves the rest, so recording a branch\n# early and a pull request later does not erase the branch - which is the failure mode of every\n# "write the whole file" version of this.\n#\n# usage: job.sh <library> phase=Done branch=... prNumber=123 message="..."\nset -e\n\nLIBRARY=${1:?job.sh needs the library}\nshift\n\nNS=vuln-console\n\nkube() { KUBECONFIG=/dev/null kubectl "$@"; }\n\n# The object name, by the same rule the extension uses: lowercased, anything that is not a\n# letter or a digit becomes a hyphen. `@scope/name` is not a legal object name and this is only\n# an address - the record keeps the real library name in a field.\nNAME=job-$(printf \'%s\' "$LIBRARY" | tr \'[:upper:]\' \'[:lower:]\' | sed \'s/[^a-z0-9]\\{1,\\}/-/g; s/^-*//; s/-*$//\' | cut -c1-50)\n\nCURRENT=$(kube get configmap "$NAME" -n "$NS" -o "jsonpath={.data.job\\.json}" 2>/dev/null || true)\n\n# Read the current record, apply the assignments, write it back. Through node because the values\n# are arbitrary text - a message can contain a quote, a URL contains slashes - and sed would\n# mangle both.\nUPDATED=$(CURRENT="$CURRENT" LIBRARY="$LIBRARY" node -e \'\n  const assignments = process.argv.slice(1);\n  let job = {};\n\n  try {\n    job = JSON.parse(process.env.CURRENT || "{}") || {};\n  } catch {\n    job = {};\n  }\n\n  job.library = job.library || process.env.LIBRARY;\n\n  // Numbers stay numbers and the empty string clears a field, so `prNumber=` is how a run says\n  // "there is no pull request" without writing the string "null" into the board.\n  for (const pair of assignments) {\n    const at = pair.indexOf("=");\n\n    if (at < 1) {\n      continue;\n    }\n\n    const key = pair.slice(0, at);\n    const raw = pair.slice(at + 1);\n\n    if (raw === "") {\n      job[key] = null;\n    } else if (/^(prNumber|startedAt|updatedAt)$/.test(key)) {\n      job[key] = Number(raw);\n    } else if (key === "vulnIds") {\n      job[key] = raw.split(",").map((n) => Number(n.trim())).filter((n) => !Number.isNaN(n));\n    } else {\n      job[key] = raw;\n    }\n  }\n\n  job.updatedAt = Date.now();\n  process.stdout.write(JSON.stringify(job));\n\' "$@")\n\n[ -n "$UPDATED" ] || { echo "job.sh: refusing to write an empty job" >&2; exit 1; }\n\nkube get namespace "$NS" >/dev/null 2>&1 || kube create namespace "$NS" >/dev/null\n\nprintf \'%s\' "$UPDATED" > /tmp/job-$$.json\n\nkube create configmap "$NAME" \\\n  --namespace "$NS" \\\n  --from-file=job.json=/tmp/job-$$.json \\\n  --dry-run=client -o json |\n  node -e \'\n    let raw = "";\n\n    process.stdin.on("data", (c) => (raw += c));\n    process.stdin.on("end", () => {\n      const cm = JSON.parse(raw);\n\n      cm.metadata.labels = { "vuln-console.rancher.io/owns": "true" };\n      process.stdout.write(JSON.stringify(cm));\n    });\n  \' |\n  kube apply --server-side --force-conflicts -f - >/dev/null\n\nrm -f /tmp/job-$$.json\n\necho "job.sh: recorded $*"\n',"pr.prompt.md":"# Open the pull request for a verified fix\n\nThe fix is made, pushed and verified. Open the pull request for it, and nothing else.\n\n## Rules that are not negotiable\n\n- **Open it as a draft, and leave it a draft.** The human marks it ready for review themselves.\n  Never run `gh pr ready`.\n- **Never write a bare `#<number>`** in the body. GitHub auto-links it to an unrelated pull\n  request or issue, which is a false cross-reference somebody then has to explain. Use a full URL\n  or a link label.\n- **No attribution trailers** and no reviewer names in the commit history.\n\n## What to do\n\n1. Confirm the branch is pushed and its diff is what you expect. Diff it against **upstream's**\n   default branch, never the fork's own — the fork's is chronically stale, and judging by it\n   makes a clean fix look like it carries half the repository with it.\n\n2. Open the draft pull request from the fix branch. Title it the way Dependabot does:\n   `Bump <library> from <old> to <new>` — the board reads the library out of that title, and a\n   title it cannot read is a row that loses its pull request.\n\n3. The body carries: what was bumped and in which manifests, a `### Vulnerabilities fixed`\n   section listing each advisory by GHSA id with a full URL, and a `### Screenshot/Video`\n   section. Put the verification video's URL **on its own line** — that is what makes GitHub\n   render a player rather than a link.\n\n4. Request the reviewers the board has selected, if any are given to you.\n\n5. Record it, including the alert ids this pull request covers — that record is what ties them\n   together afterwards, and it beats any guess the board could make from titles:\n\n```\n$ROOT/job.sh \"<library>\" phase=Done prUrl=<url> prNumber=<n> vulnIds=<id,id,id>\n```\n\n## What you must not do\n\n- do not mark it ready for review\n- do not merge it\n- do not comment on it\n","publish.prompt.md":'# Attach a staged recording to its pull request\n\nThe recording exists and the pull request exists; put one in the other.\n\n1. Upload the video as a GitHub user-attachment so it renders as an inline player. A release\n   asset link renders as a link, not a player, which is not what this is for.\n2. Splice it into the pull request body\'s `### Screenshot/Video` section, **on its own line**.\n3. Leave the staged copy alone until the upload is confirmed, then record the published URL:\n\n```\n$ROOT/job.sh "<library>" videoUrl=<github-url>\n```\n\nDo not comment on the pull request. Do not mark it ready for review.\n',"record.prompt.md":'# Capture the verification recording again\n\nThe fix exists but its recording does not, or needs redoing. Produce it, and change nothing else.\n\n1. Check out the fix branch in the workspace and let the supervised dev server compile it. Do not\n   start a second server.\n2. Grep the checkout for where the library is used and pick two to four real interactions that\n   exercise it.\n3. Drive them against the dev server through the browser already in this pod\n   (`$VULN_BROWSER_CDP`), connecting over CDP. Capture an annotated video and screenshots.\n4. Write the checks up as a numbered list, each ending ✅ or ❌, then a one-line verdict.\n5. Record:\n\n```\n$ROOT/job.sh "<library>" videoUrl=<path> infoUrl=<path> message="<one line verdict>"\n```\n\nDo not change the branch, do not open or edit a pull request.\n',"resolve-conflict.prompt.md":'# Rebase a fix branch onto the latest default branch\n\nThe pull request has drifted from its base, or conflicts with it. Bring it up to date.\n\n## What to do\n\n1. Fetch upstream and check whether the branch actually conflicts. GitHub computes mergeability\n   asynchronously, so a "conflict" flag can be one poll stale — look for yourself.\n2. If it is clean and merely behind, rebase it onto the latest default branch.\n3. If it conflicts, rebase and **re-apply the fix** rather than resolving by hand where the\n   resolution is a lockfile: regenerate the lockfiles from the rebased tree using the recipe in\n   the fix prompt, so what lands is a real resolution and not a merge artefact.\n4. Re-check that no vulnerable version remains and that the diff adds no new vulnerable package.\n5. Force-push **only this branch**, and only when it is the branch this job owns.\n6. Record:\n\n```\n$ROOT/job.sh "<library>" phase=Done message="<one line: rebased onto <sha>, lockfiles regenerated>"\n```\n\n## What you must not do\n\n- do not force-push any branch other than this one\n- do not comment on the pull request\n- do not mark it ready for review\n',"workspace/boot.sh":'#!/bin/sh\n# Bring one fix workspace up: a rancher/dashboard checkout with its dependencies installed and\n# the dev server supervised on top of it.\n#\n# The container\'s main process ends up being the supervisor, not the server, because a fix run\n# does things that kill a dev server - switches branch, regenerates a lockfile, runs a build -\n# and a pod whose main process was the server would restart on each of those and take the run\'s\n# terminal with it.\n#\n# `${install}`, `${repo}` and `${port}` are apps-plus substitutions, filled in when the App is\n# rendered. Everything written `$LIKE_THIS` is ordinary shell and is left alone: apps-plus only\n# substitutes the braced form, and only for names its App declares.\nset -e\n\n# Force apt onto IPv4 before anything runs one. The node has no working IPv6 route to the Debian\n# mirror, so an `apt-get update` left alone resolves an IPv6 address and hangs on it for minutes\n# while holding the apt lock - which is how a workspace comes up with no tmux and no terminal.\nmkdir -p /etc/apt/apt.conf.d\nprintf \'Acquire::ForceIPv4 "true";\\nAcquire::Retries "3";\\n\' > /etc/apt/apt.conf.d/99vuln-ipv4\n\nWS=/workspaces/${install}\nmkdir -p "$WS/.home"\nchown node:node "$WS" "$WS/.home" 2>/dev/null || true\n[ -f "$WS/.owned" ] || { chown -R node:node "$WS" 2>/dev/null; touch "$WS/.owned"; }\n\n# The browser sidecar mounts these as subPaths, and the kubelet creates a missing subPath as\n# root - leaving the seed unable to write into it.\nmkdir -p "$WS/.a11y/opt" "$WS/.a11y/init"\nchown -R node:node "$WS/.a11y" 2>/dev/null || true\n\n# What every workspace on this node shares rather than keeping its own copy. Measured before\n# this existed: Cypress 813 MB, yarn 749 MB, and a gigabyte of node_modules - most of a\n# workspace\'s 3 GB, per workspace.\nSHARED=/workspaces/.shared\nmkdir -p "$SHARED/yarn" "$SHARED/cypress" "$SHARED/npm" "$SHARED/template"\nchown node:node "$SHARED" "$SHARED/yarn" "$SHARED/cypress" "$SHARED/npm" "$SHARED/template" 2>/dev/null || true\n\n# tmux and kubectl, which the terminal needs. No claude: the conversation runs in the agents pod\n# and reaches this one through a tunnel, so a claude here is hundreds of megabytes of hostPath\n# that nothing executes.\n[ -f /seed/terminal-tools.sh ] \\\n  && (TOOLS_NO_CLAUDE=1 HOME_DIR="$WS/.home" /bin/sh /seed/terminal-tools.sh >"$WS/.terminal-tools.log" 2>&1 &) \\\n  || true\n\n# What a recording and a lockfile bump need and the image lacks. Backgrounded so the dev server\n# is not a minute later for it; the lock timeout lets it wait for the tmux install above rather\n# than colliding with it.\n(command -v ffmpeg >/dev/null 2>&1 && command -v lsof >/dev/null 2>&1) || (\n  apt-get -o DPkg::Lock::Timeout=300 update -qq \\\n    && DEBIAN_FRONTEND=noninteractive apt-get -o DPkg::Lock::Timeout=300 install -y -qq \\\n      ffmpeg jq lsof iproute2 >"$WS/.apt.log" 2>&1 &\n) || true\n\nexec setpriv --reuid=1000 --regid=1000 --init-groups /bin/sh /seed/checkout.sh\n',"workspace/checkout.sh":'#!/bin/sh\n# The workspace\'s checkout and install, as the node user. Split from boot.sh so the privileged\n# half is short enough to read and this half can be run again by hand.\nset -e\n\nWS=/workspaces/${install}\nSHARED=/workspaces/.shared\n\nexport HOME="$WS/.home"\nexport YARN_CACHE_FOLDER="$SHARED/yarn"\nexport CYPRESS_CACHE_FOLDER="$SHARED/cypress"\nexport npm_config_cache="$SHARED/npm"\n\n# init + fetch rather than `git clone`, because clone refuses a non-empty directory and this one\n# is not reliably empty: the agent seed writes `.claude` into the checkout, and if that lands\n# first - a separate trigger racing this boot - a clone would fail and leave the pod serving\n# nothing.\nif [ ! -d "$WS/dashboard/.git" ]; then\n  mkdir -p "$WS/dashboard"\n  cd "$WS/dashboard"\n  git init -q\n  git remote add origin https://github.com/${repo} 2>/dev/null || true\n  DEFAULT=$(git ls-remote --symref origin HEAD | sed -n \'s@^ref: refs/heads/\\(.*\\)[[:space:]]HEAD@\\1@p\')\n  git fetch --depth 1 origin "$DEFAULT"\n  git checkout -f -B "$DEFAULT" FETCH_HEAD\nfi\n\ncd "$WS/dashboard"\n\n# The fork is where fixes are pushed, so it is a remote from the start rather than something the\n# agent has to remember to add. `upstream` is what a branch is diffed against - never the fork\'s\n# own master, which is chronically stale and makes a clean fix look like it carries half the\n# repository with it.\ngit remote add fork "https://github.com/${fork}" 2>/dev/null || true\ngit remote set-url fork "https://github.com/${fork}" 2>/dev/null || true\ngit remote add upstream "https://github.com/${repo}" 2>/dev/null || true\n\n# node_modules, hard-linked from a template of the same lockfile if this node already has one.\n# The files are the same inodes, so a second workspace\'s node_modules costs directory entries\n# and nothing else. Keyed by the lockfile hash, so a template is only ever used by a checkout\n# that would have installed exactly it.\nHASH=$(sha1sum yarn.lock 2>/dev/null | cut -c1-12)\n\nif [ -n "$HASH" ] && [ ! -d node_modules ] && [ -d "$SHARED/template/$HASH/node_modules" ]; then\n  cp -al "$SHARED/template/$HASH/node_modules" node_modules && touch .install-done\nfi\n\n[ -f .install-done ] || {\n  yarn install --mutex "file:$SHARED/yarn/.mutex" --network-timeout 600000\n  touch .install-done\n}\n\n# The first workspace to install a given lockfile leaves the template for the next one.\nif [ -n "$HASH" ] && [ -d node_modules ] && [ ! -d "$SHARED/template/$HASH" ]; then\n  mkdir -p "$SHARED/template/$HASH"\n  cp -al node_modules "$SHARED/template/$HASH/node_modules" || rm -rf "$SHARED/template/$HASH"\nfi\n\nexec /bin/bash /workspace-config/serve.sh ${port}\n',"workspace/serve.sh":'#!/bin/bash\n# The dev server, supervised: the container\'s process is this loop, not the server.\n#\n# A fix run does things that kill a dev server - switches branch, regenerates a lockfile, runs a\n# build. When the pod\'s main process was the server itself, each of those restarted the\n# container and took the run\'s terminal with it, mid-fix. So the server is restarted when it\n# exits, and left alone while something else holds the port.\nPORT=${1:-8005}\n\n# The boot script runs this from the checkout, so the tree is its parent - which is where the\n# log and the retarget file live.\nWS=${WS:-$(dirname "$PWD")}\nRETARGET=$WS/.dev-server.env\nLOG=$WS/.dev-server.log\n\necho "$PORT" > "$WS/.dev-server.port" 2>/dev/null || true\nexport PORT WS RETARGET LOG IONICE\n\nheld() {\n  node -e "require(\'net\').connect(Number(process.argv[1]),\'127.0.0.1\').on(\'connect\',()=>process.exit(0)).on(\'error\',()=>process.exit(1))" "$PORT" 2>/dev/null\n}\n\n# Not all at once. Rancher\'s embedded k3s runs beside these servers, and when it restarts every\n# workspace pod restarts with it and every webpack compiles at the same moment: a dozen of them\n# on twelve cores, k3s\'s controllers fail to renew their lease, k3s exits, Rancher restarts -\n# and the herd forms again. So a server waits, with jitter so they do not all wake together,\n# while the node\'s one-minute load is above what the cores can carry.\nCORES=$(nproc 2>/dev/null || echo 4)\ngate() {\n  load=$(cut -d. -f1 /proc/loadavg 2>/dev/null || echo 0)\n  [ "$load" -le $(( CORES * 3 / 2 )) ]\n}\n\n# One dev server per pod, whoever started it. A second webpack is another two gigabytes at every\n# rebuild and the pod\'s limit is five, so an agent that starts its own gets the port until it is\n# done and this one comes back afterwards. Only node processes, matched by command line: a shell\n# or a grep that merely mentions the server would otherwise count and stop this one for nothing.\nservers() { pgrep -f "^[^ ]*node .*vue-cli-service serve" 2>/dev/null; }\nforeign() {\n  local p\n  for p in $(servers); do\n    [ "$(ps -o pgid= -p "$p" 2>/dev/null | tr -d \' \')" = "$SERVER" ] || echo "$p"\n  done\n}\n\nSERVER=""\nstop_ours() {\n  if [ -n "$SERVER" ]; then\n    kill -TERM -- -"$SERVER" 2>/dev/null\n    wait "$SERVER" 2>/dev/null\n    SERVER=""\n  fi\n}\ntrap "stop_ours; exit 0" TERM INT\n\nwhile :; do\n  if held || [ -n "$(foreign)" ]; then sleep 10; continue; fi\n\n  sleep $(( RANDOM % 20 ))\n\n  waited=0\n  while ! gate && [ "$waited" -lt 300 ]; do\n    [ "$waited" -eq 0 ] && echo "[workspace] the node is busy; waiting to start the dev server"\n    sleep 10\n    waited=$((waited + 10))\n  done\n\n  # Below the cluster\'s own processes: a compile that takes every core has taken k3s down with\n  # it on a busy node, and a slow first page is the better failure.\n  IONICE=""\n  command -v ionice >/dev/null 2>&1 && IONICE="ionice -c 3"\n\n  # What the server runs against can be changed without a new pod: writing the retarget file\n  # restarts it onto a different Rancher. Read in the server\'s own shell, so it replaces the\n  # pod\'s environment for the server alone.\n  STAMP=$(stat -c %Y "$RETARGET" 2>/dev/null || echo 0)\n  [ -f "$RETARGET" ] && echo "[workspace] the dev server runs against $(grep \'^API=\' "$RETARGET" | cut -d= -f2-)"\n\n  # setsid, so the server and everything it starts are one process group a stop can signal as a\n  # whole; tee, so the log is in the tree as well as in the pod\'s.\n  setsid bash -c \'if [ -f "$RETARGET" ]; then set -a; . "$RETARGET"; set +a; fi; nice -n 15 $IONICE env VUE_CLI_SERVICE_CONFIG_PATH=/workspace-config/vue.config.js yarn dev --port "$PORT" 2>&1 | tee "$LOG"\' &\n  SERVER=$!\n\n  # `sleep & wait`, not `sleep`: a trap runs once the foreground command returns, and a stop\n  # that waited out the sleep left the server running for the check that followed.\n  while kill -0 "$SERVER" 2>/dev/null; do\n    sleep 10 & wait $!\n    if [ -n "$(foreign)" ]; then\n      echo "[workspace] another dev server is running in this pod; standing down until it is gone"\n      stop_ours; sleep 10; continue 2\n    fi\n    if [ "$(stat -c %Y "$RETARGET" 2>/dev/null || echo 0)" != "$STAMP" ]; then\n      echo "[workspace] the dev server\'s target changed; restarting it"\n      stop_ours; continue 2\n    fi\n  done\n\n  SERVER=""\n  echo "[workspace] the dev server exited; starting it again in 5s"\n  sleep 5\ndone\n',"workspace/vue.config.js":"// Written by the Dev extension. See pkg/dev-extension/workspace-config.ts.\n//\n// This dev server is reached one of two ways, and everything below is the consequence of which.\n//\n//   - behind the apiserver's service proxy, at DEV_PROXY_PATH, on Rancher's own origin. The Host\n//     header is the proxy's, the TLS the browser sees is Rancher's, the prefix is stripped on the\n//     way in and has to be put back on every URL handed out.\n//   - at an origin of its own, on a node port, with no prefix at all.\n//\n// A dev server built for one does not work at the other: the router base is baked in. Which one\n// this is comes from DEV_PROXY_PATH being set or empty, and changing it restarts the pod.\n//\n// The second mode exists because of what the first one does to a dashboard. rancher/dashboard\n// builds its API URLs absolute from the page origin, so behind the prefix its /v1 and /v3 calls\n// never reach this dev server at all: they go to the Rancher this page was served from, using\n// that session, against that cluster. Measured, on a workspace with a Rancher of its own: two\n// requests arrived here and a hundred and thirty-six went to the host. An origin of its own is\n// the only thing that fixes it, because there is no browser-side API base to set.\nconst proxyPath = (process.env.DEV_PROXY_PATH || '').replace(/\\/$/, '');\n\n// The prefix on every in-app link, which has to be the path the page was loaded at. Set before\n// the shell's config runs, because the shell reads it out of the environment as it does.\nprocess.env.ROUTER_BASE = proxyPath ? proxyPath + '/' : '/';\n\n// The repo's own config, untouched in the checkout, rather than a reimplementation of it.\n// rancher/dashboard is a monorepo whose shell is ./shell rather than a node_modules package,\n// and which arguments its root config passes to that shell is the repo's business and changes\n// with the repo. Wrapping it means this file only has to know about the proxy. This file is\n// handed to vue-cli-service as VUE_CLI_SERVICE_CONFIG_PATH (see apps.ts, WORKSPACE_SCRIPT), so\n// it lives outside the checkout and the checkout's own vue.config.js is never rewritten: the\n// tree stays clean, a branch switch never trips over it, and nothing an agent commits carries it.\nconst base = require(require('path').join(process.cwd(), 'vue.config.js'));\n\n// The proxy rewrites absolute URLs in HTML as they pass through, which is what lets a naive UI\n// work behind it at all, so an asset URL that already carried the prefix would carry it twice.\n// index.html therefore asks for '/js/...' and lets the proxy add the prefix, while webpack's\n// runtime works its own base out from the script URL it was loaded from.\nbase.publicPath = 'auto';\n\nconst previousChainWebpack = base.chainWebpack;\n\nbase.chainWebpack = (webpackConfig) => {\n  if (typeof previousChainWebpack === 'function') {\n    previousChainWebpack(webpackConfig);\n  }\n\n  webpackConfig.plugin('html-index').tap((args) => {\n    args[0].publicPath = '/';\n\n    return args;\n  });\n};\n\nbase.devServer = {\n  ...base.devServer,\n\n  // Plain http only behind the proxy, where Rancher terminates TLS and will not talk to the\n  // shell's self-signed dev certificate on the way through.\n  //\n  // At its own origin the browser talks to this server directly, and http there is not a smaller\n  // problem than a certificate warning, it is a broken login: the shipped proxy sets\n  // x-forwarded-proto: https on every request it makes, so Rancher issues its session cookie with\n  // Secure, and a browser on http throws that cookie away. What that looks like is a login that\n  // returns 200 and a page that bounces straight back to the login form, which is what it did.\n  // So https here, with the shell's own certificate, and a warning to click through once.\n  ...(proxyPath ? { server: { type: 'http' } } : {}),\n\n  // Requests arrive with whatever Host the proxy chain last set, never this server's own.\n  // Without this the proxy answers 'Invalid Host header'.\n  allowedHosts: 'all',\n\n  // The proxy cannot rewrite URLs in a compressed body: the response comes back as a stream\n  // error and the browser shows a blank page.\n  compress: false,\n\n  // The prefix is stripped before a request lands here, so everything is served from the root.\n  devMiddleware: { publicPath: '/' },\n  static:        { publicPath: '/' },\n\n  // A deep link into the dashboard has to load the app rather than 404. disableDotRule matters\n  // in Rancher, where a route routinely carries a resource name with dots in it.\n  historyApiFallback: {\n    index:             '/index.html',\n    disableDotRule:    true,\n    htmlAcceptHeaders: ['text/html', 'application/xhtml+xml']\n  },\n\n  hot:             true,\n  webSocketServer: { type: 'ws', options: { path: '/ws' } },\n  client:          {\n    // Hot reload rides the same proxy when there is one, and the page's own origin when there is\n    // not. Either way this has to be written out, because the shell's config has an address of its\n    // own in it and that address is not where this server is.\n    //\n    // Behind the proxy only the path is ours; the rest are webpack-dev-server's \"infer it from\n    // window.location\" sentinels. 'auto' is one the *server* substitutes as it serves the client.\n    //\n    // Served at its own origin the sentinels are still needed, and leaving them out was a real\n    // bug rather than a tidy default. The shell's own config sets a fixed\n    // wss://localhost:8005/ws, which is right on a laptop running the dev server and wrong\n    // everywhere else: the browser then opened a hot-reload socket to whatever was on port 8005\n    // of the machine the browser is on, and displayed that server's compile errors over this\n    // dashboard. Seen here as a webpack overlay full of a completely different project's module\n    // resolution failures, sitting on top of the workspace's login form and swallowing clicks.\n    //\n    // 0.0.0.0 and 0 are webpack-dev-server's own \"take it from window.location\" sentinels. The\n    // protocol is written out rather than left as 'auto', because 'auto' is substituted by the\n    // server as it serves the client and is handed to the WebSocket constructor unchanged when\n    // the page was loaded directly. This server is https at its own origin, so wss.\n    webSocketURL: proxyPath ? {\n      protocol: 'auto',\n      hostname: '0.0.0.0',\n      port:     0,\n      pathname: proxyPath + '/ws'\n    } : {\n      protocol: 'wss',\n      hostname: '0.0.0.0',\n      port:     0,\n      pathname: '/ws'\n    },\n    // The compile-error overlay would cover the whole framed dashboard for whoever is looking\n    // when an edit fails to compile.\n    overlay: false\n  }\n};\n\nmodule.exports = base;\n"},pe="/workspace/.vuln-console",me="1000:1000",ve="/workspace/conversations",fe="/workspace/.home",ge={fix:"fix.prompt.md",pr:"pr.prompt.md",record:"record.prompt.md",publish:"publish.prompt.md",addresscomment:"address-comments.prompt.md",resolveconflict:"resolve-conflict.prompt.md"},be={fix:"Fix",pr:"Open the pull request for",record:"Record the verification for",publish:"Attach the recording for",addresscomment:"Address the review comments on",resolveconflict:"Rebase"};function we(e){const n=S();return{pod:e,namespace:(null===n||void 0===n?void 0:n.agent.namespace)||"extension-studio",container:(null===n||void 0===n?void 0:n.agent.container)||"agent"}}function ye(e){return["#!/bin/sh","# Written by the vulnerability console. Runs one command inside a fix workspace.","set -e",`NS=${H(e)}`,`WS=${H(`${k}/${e}`)}`,"DIR=${PWD}",'KUBECONFIG=/dev/null exec kubectl exec -i -n "$NS" "deploy/$NS" -c workspace -- \\',"  setpriv --reuid=1000 --regid=1000 --init-groups \\",'  /usr/bin/env HOME="$WS/.home" WSD="$WS" DIR="$DIR" \\','  /bin/bash -lc \'cd "$DIR" 2>/dev/null || cd "$WSD/dashboard"; PATH="$WSD/bin:$PATH"; eval "$1"\' bash "$1"',""].join("\n")}function ke(e,n,t,a){const o=((null===t||void 0===t?void 0:t.vulns)||[]).filter(e=>"open"===e.state).map(e=>`#${e.id} (${e.severity}${e.patched?`, needs ${e.patched}`:", no fix published"}) in ${e.manifest}`);return[`${be[e]} the dependency **${n}** in ${h}.`,"",`Read ${pe}/${ge[e]} IN FULL before you do anything. It is the specification`,"for this action and it is authoritative.","","Facts for this run:",`  library        ${n}`,`  workspace      ${a.workspace} (your commands already run inside it)`,`  checkout       ${k}/${a.workspace}/dashboard`,`  push to        ${u} (remote "fork")`,`  pull requests  ${p}`,a.branch?`  branch         ${a.branch}`:"",a.prNumber?`  pull request   ${a.prNumber}`:"",o.length?`  open alerts    ${o.join("; ")}`:"","","Enumerate the affected manifests from the LIVE alerts, not from the list above - this board","can be hours old and a missed lockfile is the most common thing a reviewer catches.","",`Record what you did by writing the job: ${pe}/job.sh ${H(n)} <field>=<value> ...`,"Call it when you finish and whenever something durable happens (a branch, a preview, a pull","request). If you cannot finish, record why:",`  ${pe}/job.sh ${H(n)} phase=Failed message="one line saying what went wrong"`,"","Then stop."].filter(e=>""!==e).join("\n")}async function xe(e,n){const t=["job.sh",...Object.values(ge)];for(const a of t){const n=ue[a];if(!n)throw new Error(`This build is missing its ${a} - run "yarn gen-seed" and rebuild.`);await U(e,`${pe}/${a}`,n,{mode:"644",owner:me})}await U(e,`${pe}/shell-${n}.sh`,ye(n),{mode:"755",owner:me}),await P(e,["/bin/sh","-c",`chown ${me} ${pe} 2>/dev/null || true`],{timeoutMs:15e3})}async function _e(e){const{store:n,library:t,action:a,group:o=null,by:r}=e,s=S();if(!s)throw new Error("The Agents extension is not available on this page, so there is no agent to do the work.");const i=(await ne()).find(e=>e.library===t&&"Running"===e.phase);if(i&&Date.now()-i.updatedAt<9e5)throw new Error(`${t} already has a run going. Stop it first, or wait for it to finish.`);const l=await s.agent.pod();if(!l)throw new Error("The agent pod is not running, so there is nowhere to do the work.");const d=(await ne()).find(e=>e.library===t),c=Date.now(),h={library:t,phase:"Running",action:a,by:r||"unknown",startedAt:c,updatedAt:c,sessionId:null,workspace:le(t),branch:(null===d||void 0===d?void 0:d.branch)||null,previewUrl:(null===d||void 0===d?void 0:d.previewUrl)||null,prUrl:(null===d||void 0===d?void 0:d.prUrl)||null,prNumber:(null===d||void 0===d?void 0:d.prNumber)||null,videoUrl:(null===d||void 0===d?void 0:d.videoUrl)||null,infoUrl:(null===d||void 0===d?void 0:d.infoUrl)||null,replyUrl:(null===d||void 0===d?void 0:d.replyUrl)||null,message:null,vulnIds:(null===d||void 0===d?void 0:d.vulnIds)||[]};await te(h);try{const e=await he(n,t),r=we(l);await xe(r,e);const i=await s.agent.startInProject($(`${e}-${a}-${c}`),`${be[a]} ${t}`,ke(a,t,o,{...h,workspace:e})),d={...h,workspace:e,sessionId:i,updatedAt:Date.now()};return await te(d),await I(r,["/bin/sh /seed/shell.sh",H(i),H(ve),H(fe),"start",H(`${pe}/shell-${e}.sh`)].join(" "),"start the conversation in the agent pod",12e4),d}catch(u){const e=(null===u||void 0===u?void 0:u.message)||String(u);throw await te({...h,phase:"Failed",message:e,updatedAt:Date.now()}).catch(()=>{}),new Error(e,{cause:u})}}async function $e(e){const n=S();n&&e.sessionId&&await n.agent.end(e.sessionId).catch(()=>{}),await te({...e,phase:"Cancelled",message:"Stopped.",sessionId:null,updatedAt:Date.now()})}function Ee(e,n=Date.now()){return"Running"===e.phase&&n-e.updatedAt>9e5}const Se={class:"steps"},Re=["href","title"],Ne=["href"],Te=["href"],Ae=["href"];var Ce=(0,a.defineComponent)({__name:"StepPills",props:{row:{},job:{},busy:{type:Boolean}},emits:["act","stop","session"],setup(e,{emit:n}){const t=e,o=n,r=(0,a.computed)(()=>t.job),i=(0,a.computed)(()=>{var e;return"Running"===(null===(e=r.value)||void 0===e?void 0:e.phase)&&!Ee(r.value)}),l=(0,a.computed)(()=>!!r.value&&Ee(r.value)),c=(0,a.computed)(()=>{var e;if(null===(e=r.value)||void 0===e?void 0:e.prUrl)return{url:r.value.prUrl,number:r.value.prNumber};const n=t.row.pr;return"open"===(null===n||void 0===n?void 0:n.status)?{url:n.url,number:n.number}:null}),h=(0,a.computed)(()=>{var e,n;return(null===(e=r.value)||void 0===e?void 0:e.branch)||("open"===(null===(n=t.row.pr)||void 0===n?void 0:n.status)?t.row.pr.headRefName:null)||null}),p=(0,a.computed)(()=>{var e,n;return(null===(e=r.value)||void 0===e?void 0:e.videoUrl)||(null===(n=t.row.pr)||void 0===n?void 0:n.videoUrl)||null}),m=(0,a.computed)(()=>!!p.value&&/^https?:\/\//.test(p.value)),v=(0,a.computed)(()=>t.row.unfixable),f=(0,a.computed)(()=>h.value?`https://github.com/${u}/tree/${h.value}`:null);return(n,t)=>{var u,g;return(0,a.openBlock)(),(0,a.createElementBlock)("div",Se,[v.value?((0,a.openBlock)(),(0,a.createBlock)((0,a.unref)(s.BN),{key:0,status:"unknown",title:"No patched version is available — Dependabot has no fix for this advisory, so there is nothing to bump to."},{default:(0,a.withCtx)(()=>[...t[11]||(t[11]=[(0,a.createTextVNode)(" No fix published ",-1)])]),_:1})):((0,a.openBlock)(),(0,a.createElementBlock)(a.Fragment,{key:1},[i.value?((0,a.openBlock)(),(0,a.createElementBlock)(a.Fragment,{key:0},[(0,a.createVNode)(d.A,{variant:"secondary",size:"small",onClick:t[0]||(t[0]=e=>o("session"))},{default:(0,a.withCtx)(()=>{var e;return[t[12]||(t[12]=(0,a.createElementVNode)("i",{class:"icon icon-spinner icon-spin"},null,-1)),(0,a.createElementVNode)("span",null,(0,a.toDisplayString)("fix"===(null===(e=r.value)||void 0===e?void 0:e.action)?"Fixing":"Working")+"…",1)]}),_:1}),(0,a.createVNode)(d.A,{variant:"secondary",size:"small","data-testid":"vc-stop",onClick:t[1]||(t[1]=e=>o("stop"))},{default:(0,a.withCtx)(()=>[...t[13]||(t[13]=[(0,a.createElementVNode)("span",null,"Stop",-1)])]),_:1})],64)):l.value?((0,a.openBlock)(),(0,a.createElementBlock)(a.Fragment,{key:1},[(0,a.createVNode)((0,a.unref)(s.BN),{status:"warning",title:"This run has not reported for a while. The tab that started it may have been closed, or its pod replaced."},{default:(0,a.withCtx)(()=>[...t[14]||(t[14]=[(0,a.createTextVNode)(" Stalled ",-1)])]),_:1}),(0,a.createVNode)(d.A,{variant:"secondary",size:"small",onClick:t[2]||(t[2]=e=>o("session"))},{default:(0,a.withCtx)(()=>[...t[15]||(t[15]=[(0,a.createElementVNode)("span",null,"Session",-1)])]),_:1}),(0,a.createVNode)(d.A,{variant:"secondary",size:"small",onClick:t[3]||(t[3]=e=>o("stop"))},{default:(0,a.withCtx)(()=>[...t[16]||(t[16]=[(0,a.createElementVNode)("span",null,"Stop",-1)])]),_:1})],64)):((0,a.openBlock)(),(0,a.createElementBlock)(a.Fragment,{key:2},[f.value?((0,a.openBlock)(),(0,a.createElementBlock)("a",{key:0,class:"steps__link",href:f.value,target:"_blank",rel:"noopener",title:`fork branch: ${h.value}`},[(0,a.createVNode)((0,a.unref)(s.BN),{status:"success"},{default:(0,a.withCtx)(()=>[(0,a.createTextVNode)((0,a.toDisplayString)(h.value),1)]),_:1})],8,Re)):((0,a.openBlock)(),(0,a.createBlock)(d.A,{key:1,variant:"primary",size:"small",disabled:e.busy,"data-testid":"vc-fix",onClick:t[4]||(t[4]=e=>o("act","fix"))},{default:(0,a.withCtx)(()=>[...t[17]||(t[17]=[(0,a.createElementVNode)("span",null,"Fix",-1)])]),_:1},8,["disabled"])),c.value?((0,a.openBlock)(),(0,a.createElementBlock)("a",{key:2,class:"steps__link",href:c.value.url,target:"_blank",rel:"noopener"},[(0,a.createVNode)((0,a.unref)(s.BN),{status:"success"},{default:(0,a.withCtx)(()=>[(0,a.createTextVNode)("Pull request "+(0,a.toDisplayString)(c.value.number),1)]),_:1})],8,Ne)):h.value?((0,a.openBlock)(),(0,a.createBlock)(d.A,{key:3,variant:"secondary",size:"small",disabled:e.busy,onClick:t[5]||(t[5]=e=>o("act","pr"))},{default:(0,a.withCtx)(()=>[...t[18]||(t[18]=[(0,a.createElementVNode)("span",null,"Create pull request",-1)])]),_:1},8,["disabled"])):(0,a.createCommentVNode)("",!0),m.value?((0,a.openBlock)(),(0,a.createElementBlock)("a",{key:4,class:"steps__link",href:p.value||"#",target:"_blank",rel:"noopener"},[(0,a.createVNode)((0,a.unref)(s.BN),{status:"success"},{default:(0,a.withCtx)(()=>[...t[19]||(t[19]=[(0,a.createTextVNode)("Recording",-1)])]),_:1})],8,Te)):p.value&&c.value?((0,a.openBlock)(),(0,a.createBlock)(d.A,{key:5,variant:"secondary",size:"small",disabled:e.busy,onClick:t[6]||(t[6]=e=>o("act","publish"))},{default:(0,a.withCtx)(()=>[...t[20]||(t[20]=[(0,a.createElementVNode)("span",null,"Add recording to pull request",-1)])]),_:1},8,["disabled"])):h.value||c.value?((0,a.openBlock)(),(0,a.createBlock)(d.A,{key:6,variant:"secondary",size:"small",disabled:e.busy,onClick:t[7]||(t[7]=e=>o("act","record"))},{default:(0,a.withCtx)(()=>[...t[21]||(t[21]=[(0,a.createElementVNode)("span",null,"Record",-1)])]),_:1},8,["disabled"])):(0,a.createCommentVNode)("",!0),c.value?((0,a.openBlock)(),(0,a.createElementBlock)(a.Fragment,{key:7},[(0,a.createVNode)(d.A,{variant:"secondary",size:"small",disabled:e.busy,onClick:t[8]||(t[8]=e=>o("act","addresscomment"))},{default:(0,a.withCtx)(()=>[...t[22]||(t[22]=[(0,a.createElementVNode)("span",null,"Address comments",-1)])]),_:1},8,["disabled"]),(0,a.createVNode)(d.A,{variant:"secondary",size:"small",disabled:e.busy,onClick:t[9]||(t[9]=e=>o("act","resolveconflict"))},{default:(0,a.withCtx)(()=>[...t[23]||(t[23]=[(0,a.createElementVNode)("span",null,"Rebase",-1)])]),_:1},8,["disabled"])],64)):(0,a.createCommentVNode)("",!0),(null===(u=r.value)||void 0===u?void 0:u.replyUrl)?((0,a.openBlock)(),(0,a.createElementBlock)("a",{key:8,class:"steps__link",href:r.value.replyUrl,target:"_blank",rel:"noopener",title:"the per-point reply this run staged — read it here and post it yourself; the console never comments for you"},[(0,a.createVNode)((0,a.unref)(s.BN),{status:"info"},{default:(0,a.withCtx)(()=>[...t[24]||(t[24]=[(0,a.createTextVNode)("Reply draft",-1)])]),_:1})],8,Ae)):(0,a.createCommentVNode)("",!0),(null===(g=r.value)||void 0===g?void 0:g.sessionId)?((0,a.openBlock)(),(0,a.createBlock)(d.A,{key:9,variant:"link",size:"small",onClick:t[10]||(t[10]=e=>o("session"))},{default:(0,a.withCtx)(()=>[...t[25]||(t[25]=[(0,a.createElementVNode)("span",null,"Session",-1)])]),_:1})):(0,a.createCommentVNode)("",!0)],64))],64))])}}}),De=(t(9899),t(7433));const Ve=(0,De.A)(Ce,[["__scopeId","data-v-7594f78c"]]);var Oe=Ve;const Pe=["critical","high","medium","low"],Be=36e5,Ie=6048e5,He=432e5;function je(e){switch(e){case"critical":case"high":return"error";case"medium":return"warning";case"low":return"info";default:return"unknown"}}function We(e){const n=Pe.indexOf(e);return-1===n?Pe.length:n}function qe(e){if(!e)return null;const n=Date.parse(e);return Number.isNaN(n)?null:n}function Ue(e){const n=/^Bump\s+([@/A-Za-z0-9._-]+)/.exec(e.title||"");return n?n[1]:null}function Le(e){const n=e.replace(/^@/,"");return[...new Set([e,n,n.replace(/\//g,"-")])]}function Me(e,n){const t=Le(n),a=Ue(e);if(a)return t.includes(a);const o=e.headRefName||"";return t.some(e=>o.endsWith(`/${e}`)||o.includes(`/${e}-`))}function Fe(e,n,t){var a;const o=n.locks||{};for(const[i,l]of Object.entries(o))if(l.includes(e.id)){const e=t.find(e=>e.number===Number(i));if(e)return e}const r=t.filter(n=>Me(n,e.library)),s=null!==(a=n.now)&&void 0!==a?a:Date.now();if("open"===e.state){const e=r.find(e=>"open"===e.status);if(e)return e;const n=r.filter(e=>"merged"===e.status).filter(e=>{const n=qe(e.mergedAt);return null!==n&&s-n<=He}).sort((e,n)=>(qe(n.mergedAt)||0)-(qe(e.mergedAt)||0))[0];return n||null}if("fixed"===e.state){const n=qe(e.fixedAt);return null===n?null:r.filter(e=>"merged"===e.status).filter(e=>{const t=qe(e.mergedAt);return null!==t&&(n>=t-Be&&n<=t+Ie)}).sort((e,n)=>(qe(n.mergedAt)||0)-(qe(e.mergedAt)||0))[0]||null}return null}function Ge(e){const n=new Map;for(const a of e){const e=n.get(a.alert.library);e?e.push(a):n.set(a.alert.library,[a])}const t=[];for(const[a,o]of n){const e=o.map(e=>e.alert).sort((e,n)=>n.id-e.id),n=e.map(e=>e.severity).sort((e,n)=>We(e)-We(n))[0]||"low";t.push({library:a,severity:n,pr:o.map(e=>e.pr).find(e=>null!==e)||null,vulns:e,unfixable:e.length>0&&e.every(e=>!e.patched)})}return t.sort((e,n)=>We(e.severity)-We(n.severity)||e.library.localeCompare(n.library))}function ze(e){const{snapshot:n}=e,t=n.ourPrs||[],a=(n.alerts||[]).map(n=>({alert:n,pr:Fe(n,e,t)})),o=a.filter(e=>{var n;return"open"===e.alert.state&&"open"!==(null===(n=e.pr)||void 0===n?void 0:n.status)}),r=a.filter(e=>{var n;return"open"===e.alert.state&&"open"===(null===(n=e.pr)||void 0===n?void 0:n.status)}),s=a.filter(e=>{var n;return"open"!==e.alert.state&&"merged"===(null===(n=e.pr)||void 0===n?void 0:n.status)}),i={openNoPr:Ge(o),openPrOpen:Ge(r),prMerged:Ge(s)};return{generatedAt:n.gatheredAt,lists:i,counts:{openNoPr:o.length,openPrOpen:r.length,prMerged:s.length}}}function Ke(e){var n;return"merged"===(null===(n=e.pr)||void 0===n?void 0:n.status)&&e.vulns.some(e=>"open"===e.state)}function Je(e,n){return`https://github.com/${e}/security/dependabot/${n}`}const Ye={class:"vuln-ids"},Xe=["href","title"];var Ze=(0,a.defineComponent)({__name:"VulnIds",props:{vulns:{},repo:{}},setup(e){const n=e,t=(0,a.computed)(()=>n.vulns.map(e=>({id:e.id,url:Je(n.repo,e.id),status:"open"===e.state?je(e.severity):"success",title:[e.ghsa,e.manifest,e.patched?`needs ${e.patched}`:"no fix published","open"===e.state?"open":"closed"].filter(Boolean).join(" · ")})));return(e,n)=>((0,a.openBlock)(),(0,a.createElementBlock)("div",Ye,[((0,a.openBlock)(!0),(0,a.createElementBlock)(a.Fragment,null,(0,a.renderList)(t.value,e=>((0,a.openBlock)(),(0,a.createElementBlock)("a",{key:e.id,class:"vuln-ids__link",href:e.url,target:"_blank",rel:"noopener",title:e.title},[(0,a.createVNode)((0,a.unref)(s.BN),{status:e.status},{default:(0,a.withCtx)(()=>[(0,a.createTextVNode)("#"+(0,a.toDisplayString)(e.id),1)]),_:2},1032,["status"])],8,Xe))),128))]))}});t(9657);const Qe=(0,De.A)(Ze,[["__scopeId","data-v-501df710"]]);var en=Qe;function nn(e,n=Date.now()){const t=e.startedAt;if(!t)return"";const a="Running"===e.phase?n:e.updatedAt||n,o=Math.max(0,Math.round((a-t)/1e3));if(o<60)return`${o}s`;const r=Math.floor(o/60);return r<60?`${r}m ${o%60}s`:`${Math.floor(r/60)}h ${r%60}m`}const tn=["starting","fixing","verifying","recording"],an={starting:"Starting the agent",fixing:"Bumping and regenerating the lockfiles",verifying:"Serving the branch and checking it",recording:"Recording the verification"};function on(e){return e.sessionId?e.branch?e.videoUrl?"recording":"verifying":"fixing":"starting"}const rn={class:"progress","data-testid":"vc-progress"},sn={class:"progress__steps"},ln={class:"progress__marker"},dn={key:0,class:"icon icon-checkmark"},cn={key:1,class:"icon icon-spinner icon-spin"},hn={class:"progress__label"},un={class:"progress__foot"},pn={class:"progress__elapsed"};var mn=(0,a.defineComponent)({__name:"RunProgress",props:{phase:{},elapsed:{},canOpenSession:{type:Boolean}},emits:["open-session"],setup(e){const n=e,t=(0,a.computed)(()=>{const e=tn.indexOf(n.phase);return tn.map((n,t)=>({phase:n,label:an[n]||n,state:t<e?"done":t===e?"active":"todo"}))});return(n,o)=>((0,a.openBlock)(),(0,a.createElementBlock)("div",rn,[(0,a.createElementVNode)("ol",sn,[((0,a.openBlock)(!0),(0,a.createElementBlock)(a.Fragment,null,(0,a.renderList)(t.value,e=>((0,a.openBlock)(),(0,a.createElementBlock)("li",{key:e.phase,class:(0,a.normalizeClass)(["progress__step",`is-${e.state}`])},[(0,a.createElementVNode)("span",ln,["done"===e.state?((0,a.openBlock)(),(0,a.createElementBlock)("i",dn)):"active"===e.state?((0,a.openBlock)(),(0,a.createElementBlock)("i",cn)):(0,a.createCommentVNode)("",!0)]),(0,a.createElementVNode)("span",hn,(0,a.toDisplayString)(e.label),1)],2))),128))]),(0,a.createElementVNode)("div",un,[(0,a.createElementVNode)("span",pn,(0,a.toDisplayString)(e.elapsed),1),e.canOpenSession?((0,a.openBlock)(),(0,a.createElementBlock)("button",{key:0,type:"button",class:"progress__toggle","data-testid":"vc-open-session",onClick:o[0]||(o[0]=(0,a.withModifiers)(e=>n.$emit("open-session"),["stop"]))},[...o[1]||(o[1]=[(0,a.createElementVNode)("i",{class:"icon icon-terminal"},null,-1),(0,a.createTextVNode)(" Watch the agent ",-1)])])):(0,a.createCommentVNode)("",!0)])]))}});t(4105);const vn=(0,De.A)(mn,[["__scopeId","data-v-c97aac58"]]);var fn=vn,gn=t(6725);const bn={class:"agent-terminal"},wn={key:2,class:"agent-terminal__waiting"};var yn=(0,a.defineComponent)({__name:"AgentTerminal",props:{session:{},mode:{default:"claude"}},emits:["state"],setup(e,{emit:n}){const t=e,o=n,s=(0,a.ref)(null),i=(0,a.ref)(!1),l=(0,a.computed)(()=>{var e,n;return(null===(n=null===(e=s.value)||void 0===e?void 0:e.terminal)||void 0===n?void 0:n.component)||null}),d=(0,a.computed)(()=>s.value&&t.session?s.value.agent.command(t.session,t.mode):null),c=(0,a.computed)(()=>i.value&&!l.value);return(0,a.onMounted)(async()=>{s.value=await T(15e3),i.value=!0,s.value||o("state","closed")}),(n,t)=>((0,a.openBlock)(),(0,a.createElementBlock)("div",bn,[c.value?((0,a.openBlock)(),(0,a.createBlock)((0,a.unref)(r.l),{key:0,color:"warning",class:"agent-terminal__missing"},{default:(0,a.withCtx)(()=>[...t[1]||(t[1]=[(0,a.createTextVNode)(" Nothing here draws a terminal: install the ",-1),(0,a.createElementVNode)("b",null,"Agents",-1),(0,a.createTextVNode)(" extension, which brings the agent pod and the terminal every extension borrows, then reload. ",-1)])]),_:1})):l.value&&d.value?((0,a.openBlock)(),(0,a.createBlock)((0,a.resolveDynamicComponent)(l.value),{key:1,session:e.session,command:d.value,mode:e.mode,class:"agent-terminal__pane",onState:t[0]||(t[0]=e=>o("state",e))},null,40,["session","command","mode"])):((0,a.openBlock)(),(0,a.createElementBlock)("div",wn,[...t[2]||(t[2]=[(0,a.createElementVNode)("i",{class:"icon icon-spinner icon-spin"},null,-1),(0,a.createElementVNode)("span",null,"Waiting for the Agents extension…",-1)])]))]))}});t(6739);const kn=(0,De.A)(yn,[["__scopeId","data-v-5584bb48"]]);var xn=kn;const _n={class:"session__id"},$n={class:"session"},En={key:2,class:"session__note session__note--ended"},Sn={key:3,class:"session__note"};var Rn=(0,a.defineComponent)({__name:"AgentSessionPanel",props:{job:{},onBack:{type:Function},width:{},height:{},triggerFocusTrap:{type:Boolean},closeOnRouteChange:{}},emits:["close"],setup(e,{emit:n}){const t=e,o=n,s=(0,a.ref)("");return(n,i)=>((0,a.openBlock)(),(0,a.createBlock)(gn.A,{"aria-target":`the agent session for ${e.job.library}`,onClose:i[2]||(i[2]=e=>o("close"))},{title:(0,a.withCtx)(()=>[(0,a.createTextVNode)(" Agent session · "+(0,a.toDisplayString)(e.job.library)+" ",1),(0,a.createElementVNode)("span",_n,(0,a.toDisplayString)(e.job.sessionId),1)]),body:(0,a.withCtx)(()=>[(0,a.createElementVNode)("div",$n,[e.job.sessionId?((0,a.openBlock)(),(0,a.createBlock)(xn,{key:e.job.sessionId||"none",session:e.job.sessionId||"",class:"session__terminal",onState:i[0]||(i[0]=e=>s.value=e)},null,8,["session"])):((0,a.openBlock)(),(0,a.createBlock)((0,a.unref)(r.l),{key:0,color:"warning"},{default:(0,a.withCtx)(()=>[...i[3]||(i[3]=[(0,a.createTextVNode)(" This run has no conversation yet — it is still being set up. ",-1)])]),_:1})),"closed"===s.value?((0,a.openBlock)(),(0,a.createElementBlock)("p",En," This conversation has ended — the run it belonged to is over, and the agent pod has released it. ")):e.job.sessionId?((0,a.openBlock)(),(0,a.createElementBlock)("p",Sn," Closing this drawer leaves the conversation running — it is the pod that holds it, not this page. ")):(0,a.createCommentVNode)("",!0)])]),"additional-actions":(0,a.withCtx)(()=>[e.onBack?((0,a.openBlock)(),(0,a.createBlock)(d.A,{key:0,variant:"secondary",size:"large","data-testid":"vc-session-back",onClick:i[1]||(i[1]=n=>{var a;return null===(a=t.onBack)||void 0===a?void 0:a.call(t,e.job)})},{default:(0,a.withCtx)(()=>[...i[4]||(i[4]=[(0,a.createTextVNode)(" Back to the board ",-1)])]),_:1})):(0,a.createCommentVNode)("",!0)]),_:1},8,["aria-target"]))}});t(5641);const Nn=(0,De.A)(Rn,[["__scopeId","data-v-09836064"]]);var Tn=Nn;const An="vuln-console.rancher.io/gh-token",Cn="barn.rancher.io/gh-token",Dn={Accept:"application/json;as=PartialObjectMetadata;g=meta.k8s.io;v=v1"};function Vn(e,n){return`${F}/namespaces/${e}/secrets/${n}`}async function On(e,n){const t=await z(Vn(e,n),{headers:Dn}).catch(()=>null);return(null===t||void 0===t?void 0:t.metadata)||null}function Pn(e){const n=new Set;return((null===e||void 0===e?void 0:e.managedFields)||[]).forEach(e=>{var t;Object.keys((null===(t=null===e||void 0===e?void 0:e.fieldsV1)||void 0===t?void 0:t["f:data"])||{}).forEach(e=>{e.startsWith("f:")&&n.add(e.slice(2))})}),[...n]}async function Bn(){const[e,n]=await Promise.all([On(c,m),On(f,g)]),t=Pn(e),a=Pn(n),o=(null===e||void 0===e?void 0:e.annotations)||{},r=(null===n||void 0===n?void 0:n.annotations)||{},s="set"===o[An]||t.includes(v),i="set"===r[Cn]||a.includes(v);return{gh:s?"ours":i?"studio":"none",unreadable:!1}}async function In(){const e=await On(c,m);e||(await z(`${F}/namespaces`,{method:"POST",body:JSON.stringify({apiVersion:"v1",kind:"Namespace",metadata:{name:c}})}).catch(e=>{if(!/409|already exists|alreadyexists/i.test((null===e||void 0===e?void 0:e.message)||""))throw e}),await z(`${F}/namespaces/${c}/secrets`,{method:"POST",headers:Dn,body:JSON.stringify({apiVersion:"v1",kind:"Secret",type:"Opaque",metadata:{name:m,namespace:c}})}).catch(e=>{if(!/409|already exists|alreadyexists/i.test((null===e||void 0===e?void 0:e.message)||""))throw e}))}function Hn(e){return btoa(String.fromCharCode(...(new TextEncoder).encode(e)))}async function jn(e){const n={},t={};void 0!==e.ghToken&&(n[v]=""===e.ghToken?null:Hn(e.ghToken),t[An]=""===e.ghToken?null:"set"),Object.keys(n).length&&(await In(),await z(Vn(c,m),{method:"PATCH",headers:{"Content-Type":"application/merge-patch+json",...Dn},body:JSON.stringify({metadata:{annotations:t},data:n})}))}function Wn(e){return"none"!==e.gh}const qn={class:"creds",role:"dialog","aria-modal":"true","aria-labelledby":"vc-creds-title","data-testid":"vc-credentials"},Un={class:"creds__lede"},Ln={class:"creds__field"},Mn={class:"creds__label"},Fn={class:"creds__hint"},Gn={class:"creds__input"},zn=["type","placeholder"],Kn=["title"],Jn=["disabled"],Yn={class:"creds__actions"},Xn=["disabled"],Zn=["disabled"];var Qn=(0,a.defineComponent)({__name:"CredentialsDialog",props:{status:{},blocking:{type:Boolean},busy:{type:Boolean}},emits:["cancel","saved"],setup(e,{emit:n}){const t=e,o=n,s=(0,a.ref)(""),i=(0,a.ref)(!1),l=(0,a.ref)(!1),d=(0,a.ref)(""),c=(0,a.computed)(()=>"none"!==t.status.gh),h=(0,a.computed)(()=>"studio"===t.status.gh),u=(0,a.computed)(()=>c.value||!!s.value.trim());async function p(){if(u.value&&!l.value){l.value=!0,d.value="";try{await jn(s.value.trim()?{ghToken:s.value.trim()}:{}),s.value="",o("saved")}catch(e){d.value=(null===e||void 0===e?void 0:e.message)||String(e)}finally{l.value=!1}}}async function m(){l.value=!0,d.value="";try{await jn({ghToken:""}),o("saved")}catch(e){d.value=(null===e||void 0===e?void 0:e.message)||String(e)}finally{l.value=!1}}return(n,t)=>((0,a.openBlock)(),(0,a.createElementBlock)("div",{class:"creds-backdrop",onClick:t[4]||(t[4]=(0,a.withModifiers)(n=>!l.value&&!e.busy&&o("cancel"),["self"]))},[(0,a.createElementVNode)("div",qn,[t[21]||(t[21]=(0,a.createElementVNode)("h2",{id:"vc-creds-title",class:"creds__title"}," Credentials ",-1)),(0,a.createElementVNode)("p",Un,[e.blocking?((0,a.openBlock)(),(0,a.createElementBlock)(a.Fragment,{key:0},[t[5]||(t[5]=(0,a.createTextVNode)(" The board is read from ",-1)),t[6]||(t[6]=(0,a.createElementVNode)("code",null,"rancher/dashboard",-1)),t[7]||(t[7]=(0,a.createTextVNode)("'s Dependabot alerts, and a fix pushes a branch to the fork — so it needs a GitHub token. It is stored once — you will not be asked again. ",-1))],64)):((0,a.openBlock)(),(0,a.createElementBlock)(a.Fragment,{key:1},[(0,a.createTextVNode)(" Stored in a Secret and read by the pod that needs it. It never comes back out to this page, so a stored one can be replaced but not shown. ")],64))]),d.value?((0,a.openBlock)(),(0,a.createBlock)((0,a.unref)(r.l),{key:0,color:"error"},{default:(0,a.withCtx)(()=>[(0,a.createTextVNode)((0,a.toDisplayString)(d.value),1)]),_:1})):(0,a.createCommentVNode)("",!0),(0,a.createElementVNode)("label",Ln,[(0,a.createElementVNode)("span",Mn,[t[8]||(t[8]=(0,a.createTextVNode)(" GitHub token ",-1)),(0,a.createElementVNode)("span",{class:(0,a.normalizeClass)(["creds__state",{"is-set":c.value}])},(0,a.toDisplayString)(h.value?"From Extension Studio":c.value?"Stored":"Not set"),3)]),(0,a.createElementVNode)("span",Fn,[t[9]||(t[9]=(0,a.createTextVNode)(" It reads Dependabot alerts and pushes branches, so: a classic token with ",-1)),t[10]||(t[10]=(0,a.createElementVNode)("code",null,"repo",-1)),t[11]||(t[11]=(0,a.createTextVNode)(" and ",-1)),t[12]||(t[12]=(0,a.createElementVNode)("code",null,"security_events",-1)),t[13]||(t[13]=(0,a.createTextVNode)(", or a fine-grained token with ",-1)),t[14]||(t[14]=(0,a.createElementVNode)("em",null,"Dependabot alerts (read)",-1)),t[15]||(t[15]=(0,a.createTextVNode)(" plus ",-1)),t[16]||(t[16]=(0,a.createElementVNode)("em",null,"Contents",-1)),t[17]||(t[17]=(0,a.createTextVNode)(" and ",-1)),t[18]||(t[18]=(0,a.createElementVNode)("em",null,"Pull requests",-1)),t[19]||(t[19]=(0,a.createTextVNode)(" (read and write) on the fork. ",-1)),h.value?((0,a.openBlock)(),(0,a.createElementBlock)(a.Fragment,{key:0},[(0,a.createTextVNode)(" Extension Studio already has one and this borrows it; setting one here uses that instead. ")],64)):c.value?((0,a.openBlock)(),(0,a.createElementBlock)(a.Fragment,{key:1},[(0,a.createTextVNode)(" Leave blank to keep the stored one.")],64)):(0,a.createCommentVNode)("",!0)]),(0,a.createElementVNode)("span",Gn,[(0,a.withDirectives)((0,a.createElementVNode)("input",{"onUpdate:modelValue":t[0]||(t[0]=e=>s.value=e),type:i.value?"text":"password",autocomplete:"off",spellcheck:"false","data-testid":"vc-gh-token",placeholder:c.value?"••••••••  (stored)":"GH_TOKEN",onKeyup:(0,a.withKeys)(p,["enter"])},null,40,zn),[[a.vModelDynamic,s.value]]),(0,a.createElementVNode)("button",{type:"button",class:"creds__peek",title:i.value?"Hide":"Show",onClick:t[1]||(t[1]=e=>i.value=!i.value)},[(0,a.createElementVNode)("i",{class:(0,a.normalizeClass)(["icon",i.value?"icon-hide":"icon-show"])},null,2)],8,Kn)]),"ours"===e.status.gh?((0,a.openBlock)(),(0,a.createElementBlock)("button",{key:0,type:"button",class:"creds__clear",disabled:l.value,onClick:t[2]||(t[2]=e=>m())}," Remove the stored token ",8,Jn)):(0,a.createCommentVNode)("",!0)]),(0,a.createVNode)((0,a.unref)(r.l),{color:"info",class:"creds__note"},{default:(0,a.withCtx)(()=>[...t[20]||(t[20]=[(0,a.createTextVNode)(" Written straight into the Secret and never read back by this page — replacing it is possible, seeing it is not. The pod reads it with its own ServiceAccount at the moment it is needed. ",-1)])]),_:1}),(0,a.createElementVNode)("div",Yn,[(0,a.createElementVNode)("button",{type:"button",class:"btn role-secondary",disabled:l.value||e.busy,onClick:t[3]||(t[3]=e=>o("cancel"))},(0,a.toDisplayString)(e.blocking?"Cancel":"Close"),9,Xn),(0,a.createElementVNode)("button",{type:"button",class:"btn role-primary",disabled:!u.value||l.value||e.busy,"data-testid":"vc-creds-confirm",onClick:p},(0,a.toDisplayString)(l.value?"Saving…":e.busy?"Starting…":e.blocking?"Save and fix":"Save"),9,Zn)])])]))}});t(4006);const et=(0,De.A)(Qn,[["__scopeId","data-v-1089221e"]]);var nt=et;const tt={key:0,class:"shipped__empty"},at={class:"shipped__body"},ot={class:"shipped__lib"},rt=["href"];var st=(0,a.defineComponent)({__name:"ShippedDrawer",props:{rows:{},repo:{},width:{},height:{},triggerFocusTrap:{type:Boolean},closeOnRouteChange:{}},emits:["close"],setup(e,{emit:n}){const t=n;return(n,o)=>((0,a.openBlock)(),(0,a.createBlock)(gn.A,{"aria-target":"the vulnerabilities we have fixed",onClose:o[0]||(o[0]=e=>t("close"))},{title:(0,a.withCtx)(()=>[...o[1]||(o[1]=[(0,a.createTextVNode)(" Shipped ",-1)])]),body:(0,a.withCtx)(()=>[o[2]||(o[2]=(0,a.createElementVNode)("p",{class:"shipped__sub"}," Fixes of ours that merged and whose Dependabot alert has since closed. They age out of this list on their own as GitHub stops reporting them. ",-1)),e.rows.length?(0,a.createCommentVNode)("",!0):((0,a.openBlock)(),(0,a.createElementBlock)("p",tt," Nothing shipped yet. ")),((0,a.openBlock)(!0),(0,a.createElementBlock)(a.Fragment,null,(0,a.renderList)(e.rows,n=>((0,a.openBlock)(),(0,a.createElementBlock)("div",{key:n.library,class:"shipped__row"},[(0,a.createVNode)((0,a.unref)(s.BN),{status:(0,a.unref)(je)(n.severity)},{default:(0,a.withCtx)(()=>[(0,a.createTextVNode)((0,a.toDisplayString)(n.severity),1)]),_:2},1032,["status"]),(0,a.createElementVNode)("div",at,[(0,a.createElementVNode)("div",ot,(0,a.toDisplayString)(n.library),1),(0,a.createVNode)(en,{vulns:n.vulns,repo:e.repo},null,8,["vulns","repo"])]),n.pr?((0,a.openBlock)(),(0,a.createElementBlock)("a",{key:0,class:"shipped__pr",href:n.pr.url,target:"_blank",rel:"noopener"},(0,a.toDisplayString)(n.pr.number),9,rt)):(0,a.createCommentVNode)("",!0)]))),128))]),_:1}))}});t(6732);const it=(0,De.A)(st,[["__scopeId","data-v-398ab162"]]);var lt=it;const dt="/workspace/.vuln-console",ct="1000:1000",ht=18e4;async function ut(){const e=S();if(!e)throw new Error("The Agents extension is not available on this page, so there is no pod to gather in.");const n=await e.agent.pod();if(!n)throw new Error("The agent pod is not running, so there is nowhere to gather.");const t={pod:n,namespace:e.agent.namespace||"extension-studio",container:e.agent.container||"agent"};for(const o of["gather.mjs","gather.sh"]){const e=ue[o];if(!e)throw new Error(`This build is missing its ${o} - run "yarn gen-seed" and rebuild.`);await U(t,`${dt}/${o}`,e,{mode:"644",owner:ct})}const a=`${dt}/gather`;await I(t,`mkdir -p ${H(a)} && chown ${ct} ${H(dt)} ${H(a)} 2>/dev/null || true`,"make the gather directory in the agent pod",2e4),await I(t,`sh ${H(`${dt}/gather.sh`)} ${H(a)}`,"gather the Dependabot alerts",ht)}const pt={class:"vuln"},mt={class:"vuln__head"},vt={class:"vuln__titles"},ft={class:"vuln__lede"},gt={class:"vuln__actions"},bt=["disabled","title"],wt=["disabled"],yt={class:"vuln__running-head"},kt=["onClick"],xt={key:4,class:"vuln__loading"},_t={key:5,class:"vuln__empty","data-testid":"vc-empty"},$t={class:"vuln__toolbar"},Et={class:"vuln__counts"},St={class:"vuln__stamp"},Rt={class:"vuln__lib"},Nt=["href"],Tt={key:0,class:"vuln__none"},At=["href"],Ct={key:1,class:"vuln__none"},Dt={key:0,class:"vuln__message"};var Vt=(0,a.defineComponent)({__name:"VulnPage",setup(e){const n=(0,o.Pj)(),t=(0,a.ref)(null),d=(0,a.ref)([]),c=(0,a.ref)({state:"checking",version:null,pod:null,detail:""}),u=(0,a.ref)({gh:"none",unreadable:!1}),p=(0,a.ref)(""),m=(0,a.ref)(!0),v=(0,a.ref)(!1),f=(0,a.ref)(!1),g=(0,a.ref)(!1);let b=null;const w=(0,a.computed)(()=>t.value?ze({snapshot:t.value}):null),y=(0,a.computed)(()=>d.value.filter(e=>"Running"===e.phase&&!Ee(e)).sort((e,n)=>n.startedAt-e.startedAt));function k(e){return[...new Set(e.vulns.map(e=>e.manifest).filter(Boolean))].sort()}const x=(0,a.computed)(()=>{const e=w.value;return e?[...e.lists.openPrOpen,...e.lists.openNoPr].map(e=>{var n;return{...e,id:e.library,job:d.value.find(n=>n.library===e.library)||null,stale:Ke(e),severityRank:We(e.severity),flightRank:"open"===(null===(n=e.pr)||void 0===n?void 0:n.status)?0:1,manifests:k(e)}}):[]}),_=[{name:"severity",label:"Severity",value:"severity",width:110,sort:["flightRank","severityRank","library"]},{name:"library",label:"Library",value:"library",sort:["library"]},{name:"files",label:"Files",value:"manifests"},{name:"vulns",label:"Vulnerability",value:"vulns"},{name:"dependabot",label:"Dependabot",value:"dependabotPr",width:120},{name:"actions",label:"Status / actions",value:"actions"}];function $(e){var n;return((null===(n=t.value)||void 0===n?void 0:n.dependabotPrs)||[]).find(n=>n.title.startsWith(`Bump ${e} `))||null}const E=(0,a.computed)(()=>"ready"===c.value.state&&Wn(u.value)&&ie(n));async function S(){const[e,n]=await Promise.all([Q().catch(()=>null),ne().catch(()=>[])]);t.value=e,d.value=n,m.value=!1}function R(e){e.sessionId&&n.commit("slideInPanel/open",{component:Tn,componentProps:{width:"wide",height:"full",triggerFocusTrap:!0,closeOnRouteChange:["name","params","query"],onClose:()=>n.commit("slideInPanel/close"),job:e}})}function A(){var e;n.commit("slideInPanel/open",{component:lt,componentProps:{width:"wide",height:"full",triggerFocusTrap:!0,closeOnRouteChange:["name","params","query"],onClose:()=>n.commit("slideInPanel/close"),rows:(null===(e=w.value)||void 0===e?void 0:e.lists.prMerged)||[],repo:h}})}function C(){g.value=!1,f.value=!0,Bn().then(e=>{u.value=e}).catch(()=>{})}async function D(){u.value=await Bn().catch(()=>u.value),g.value?Wn(u.value)&&(f.value=!1,await P()):f.value=!1}async function V(){v.value=!0,p.value="";try{await ut(),await S()}catch(e){p.value=(null===e||void 0===e?void 0:e.message)||String(e)}finally{v.value=!1}}async function O(e,t){var a;p.value="";try{const o=await _e({store:n,library:e.library,action:t,group:e,by:null===(a=n.getters["auth/principal"])||void 0===a?void 0:a.loginName});d.value=[...d.value.filter(n=>n.library!==e.library),o],R(o)}catch(o){p.value=(null===o||void 0===o?void 0:o.message)||String(o)}}async function P(){if(!Wn(u.value))return g.value=!0,void(f.value=!0);const e=x.value.filter(e=>{var n,t;return!e.unfixable&&!(null===(n=e.job)||void 0===n?void 0:n.branch)&&"Running"!==(null===(t=e.job)||void 0===t?void 0:t.phase)}).sort((e,n)=>We(e.severity)-We(n.severity))[0];e?await O(e,"fix"):p.value="Nothing on the board is waiting for a fix."}async function B(e){if(e)try{await $e(e),await S()}catch(n){p.value=(null===n||void 0===n?void 0:n.message)||String(n)}}return(0,a.onMounted)(async()=>{await T();const[e,n]=await Promise.all([N(),Bn().catch(()=>({gh:"none",unreadable:!0}))]);c.value=e,u.value=n,await S(),b=setInterval(()=>S().catch(()=>{}),5e3)}),(0,a.onUnmounted)(()=>{b&&clearInterval(b)}),(e,o)=>{var d,b,k;return(0,a.openBlock)(),(0,a.createElementBlock)("div",pt,[(0,a.createElementVNode)("header",mt,[(0,a.createElementVNode)("div",vt,[o[3]||(o[3]=(0,a.createElementVNode)("h1",{class:"vuln__title"}," Vulnerabilities ",-1)),(0,a.createElementVNode)("p",ft,[o[1]||(o[1]=(0,a.createTextVNode)(" Actionable Dependabot alerts on ",-1)),(0,a.createElementVNode)("code",null,(0,a.toDisplayString)((0,a.unref)(h)),1),o[2]||(o[2]=(0,a.createTextVNode)(" — each with the lockfiles it touches, and a fix that runs in a workspace of its own. ",-1))])]),(0,a.createElementVNode)("div",gt,[(0,a.createElementVNode)("button",{type:"button",class:"btn role-primary",disabled:"ready"!==c.value.state||!(0,a.unref)(ie)((0,a.unref)(n)),"data-testid":"vc-fix-worst",title:E.value?"Fix the highest-severity vulnerability waiting for one":c.value.detail||"Not ready yet",onClick:P},[...o[4]||(o[4]=[(0,a.createElementVNode)("i",{class:"icon icon-play"},null,-1),(0,a.createElementVNode)("span",null,"Fix the worst one",-1)])],8,bt),(0,a.createElementVNode)("button",{type:"button",class:"btn role-secondary",disabled:!E.value||v.value,"data-testid":"vc-refresh",title:"Re-read the Dependabot alerts and our pull requests",onClick:V},[o[5]||(o[5]=(0,a.createElementVNode)("i",{class:"icon icon-refresh"},null,-1)),(0,a.createElementVNode)("span",null,(0,a.toDisplayString)(v.value?"Refreshing…":"Refresh"),1)],8,wt),(0,a.createElementVNode)("button",{type:"button",class:"btn role-secondary","data-testid":"vc-credentials-open",title:"The GitHub token the board is read and fixed with",onClick:C},[...o[6]||(o[6]=[(0,a.createElementVNode)("i",{class:"icon icon-key"},null,-1),(0,a.createElementVNode)("span",null,"Credentials",-1)])])])]),"ready"!==c.value.state&&"checking"!==c.value.state?((0,a.openBlock)(),(0,a.createBlock)((0,a.unref)(r.l),{key:0,color:"no-pod"===c.value.state?"warning":"error","data-testid":"vc-agents-banner"},{default:(0,a.withCtx)(()=>[o[7]||(o[7]=(0,a.createElementVNode)("strong",null,"Agents is not ready.",-1)),(0,a.createTextVNode)(" "+(0,a.toDisplayString)(c.value.detail),1)]),_:1},8,["color"])):(0,a.unref)(ie)((0,a.unref)(n))?(0,a.unref)(Wn)(u.value)?(0,a.createCommentVNode)("",!0):((0,a.openBlock)(),(0,a.createBlock)((0,a.unref)(r.l),{key:2,color:"warning"},{default:(0,a.withCtx)(()=>[...o[9]||(o[9]=[(0,a.createElementVNode)("strong",null,"No GitHub token is stored.",-1),(0,a.createTextVNode)(" The board cannot be refreshed and nothing can be fixed until there is one. ",-1)])]),_:1})):((0,a.openBlock)(),(0,a.createBlock)((0,a.unref)(r.l),{key:1,color:"warning"},{default:(0,a.withCtx)(()=>[...o[8]||(o[8]=[(0,a.createElementVNode)("strong",null,"Apps Plus is not installed.",-1),(0,a.createTextVNode)(" A fix runs in a workspace, and a workspace is an Apps Plus installation — without it there is nowhere for the work to happen. ",-1)])]),_:1})),p.value?((0,a.openBlock)(),(0,a.createBlock)((0,a.unref)(r.l),{key:3,color:"error"},{default:(0,a.withCtx)(()=>[(0,a.createTextVNode)((0,a.toDisplayString)(p.value),1)]),_:1})):(0,a.createCommentVNode)("",!0),((0,a.openBlock)(!0),(0,a.createElementBlock)(a.Fragment,null,(0,a.renderList)(y.value,e=>((0,a.openBlock)(),(0,a.createElementBlock)("section",{key:e.library,class:"vuln__running","data-testid":"vc-running"},[(0,a.createElementVNode)("div",yt,[o[10]||(o[10]=(0,a.createElementVNode)("i",{class:"icon icon-spinner icon-spin"},null,-1)),(0,a.createElementVNode)("strong",null,"Fixing "+(0,a.toDisplayString)(e.library),1),(0,a.createElementVNode)("button",{type:"button",class:"vuln__running-stop",title:"Stop this run",onClick:n=>B(e)}," Stop ",8,kt),(0,a.createElementVNode)("span",null,(0,a.toDisplayString)((0,a.unref)(nn)(e)),1)]),(0,a.createVNode)(fn,{phase:(0,a.unref)(on)(e),elapsed:(0,a.unref)(nn)(e),"can-open-session":!!e.sessionId,onOpenSession:n=>R(e)},null,8,["phase","elapsed","can-open-session","onOpenSession"])]))),128)),m.value?((0,a.openBlock)(),(0,a.createElementBlock)("div",xt,[...o[11]||(o[11]=[(0,a.createElementVNode)("i",{class:"icon icon-spinner icon-spin"},null,-1),(0,a.createElementVNode)("span",null,"Loading the board…",-1)])])):t.value?((0,a.openBlock)(),(0,a.createElementBlock)(a.Fragment,{key:6},[(0,a.createElementVNode)("div",$t,[(0,a.createElementVNode)("div",Et,[(0,a.createVNode)(i.A,{name:"In flight",count:(null===(d=w.value)||void 0===d?void 0:d.counts.openPrOpen)||0,"primary-color-var":"--info"},null,8,["count"]),(0,a.createVNode)(i.A,{name:"To fix",count:(null===(b=w.value)||void 0===b?void 0:b.counts.openNoPr)||0,"primary-color-var":"--error"},null,8,["count"]),(0,a.createVNode)(i.A,{name:"Shipped",count:(null===(k=w.value)||void 0===k?void 0:k.counts.prMerged)||0,"primary-color-var":"--success",clickable:"",onClick:A},null,8,["count"])]),(0,a.createElementVNode)("span",St," gathered "+(0,a.toDisplayString)(new Date(t.value.gatheredAt).toLocaleString()),1)]),(0,a.createVNode)(l.A,{rows:x.value,headers:_,"key-field":"id","table-actions":!1,"row-actions":!1,search:!0,"default-sort-by":"severity","no-rows-key":"No open vulnerabilities.",class:"vuln__table"},{"col:severity":(0,a.withCtx)(({row:e})=>[(0,a.createElementVNode)("td",null,[(0,a.createVNode)((0,a.unref)(s.BN),{status:(0,a.unref)(je)(e.severity)},{default:(0,a.withCtx)(()=>[(0,a.createTextVNode)((0,a.toDisplayString)(e.severity.toUpperCase()),1)]),_:2},1032,["status"])])]),"col:library":(0,a.withCtx)(({row:e})=>{var n;return[(0,a.createElementVNode)("td",null,[(0,a.createElementVNode)("div",Rt,(0,a.toDisplayString)(e.library),1),e.stale?((0,a.openBlock)(),(0,a.createElementBlock)("a",{key:0,class:"vuln__stale",href:null===(n=e.pr)||void 0===n?void 0:n.url,target:"_blank",rel:"noopener",title:"That pull request merged but this alert is still open — the merge did not resolve it. It needs a fresh fix."},[(0,a.createVNode)((0,a.unref)(s.BN),{status:"warning"},{default:(0,a.withCtx)(()=>[...o[15]||(o[15]=[(0,a.createTextVNode)("merged, alert still open",-1)])]),_:1})],8,Nt)):(0,a.createCommentVNode)("",!0)])]}),"col:files":(0,a.withCtx)(({row:e})=>[(0,a.createElementVNode)("td",null,[((0,a.openBlock)(!0),(0,a.createElementBlock)(a.Fragment,null,(0,a.renderList)(e.manifests,e=>((0,a.openBlock)(),(0,a.createElementBlock)("span",{key:e,class:"vuln__file"},(0,a.toDisplayString)(e),1))),128)),e.manifests.length?(0,a.createCommentVNode)("",!0):((0,a.openBlock)(),(0,a.createElementBlock)("span",Tt,"—"))])]),"col:vulns":(0,a.withCtx)(({row:e})=>[(0,a.createElementVNode)("td",null,[(0,a.createVNode)(en,{vulns:e.vulns,repo:(0,a.unref)(h)},null,8,["vulns","repo"])])]),"col:dependabot":(0,a.withCtx)(({row:e})=>{var n,t;return[(0,a.createElementVNode)("td",null,[$(e.library)?((0,a.openBlock)(),(0,a.createElementBlock)("a",{key:0,href:null===(n=$(e.library))||void 0===n?void 0:n.url,target:"_blank",rel:"noopener"},(0,a.toDisplayString)(null===(t=$(e.library))||void 0===t?void 0:t.number),9,At)):((0,a.openBlock)(),(0,a.createElementBlock)("span",Ct,"—"))])]}),"col:actions":(0,a.withCtx)(({row:e})=>{var n;return[(0,a.createElementVNode)("td",null,[(0,a.createVNode)(Oe,{row:e,job:e.job,busy:!1,onAct:n=>O(e,n),onStop:n=>B(e.job),onSession:n=>R(e.job)},null,8,["row","job","onAct","onStop","onSession"]),(null===(n=e.job)||void 0===n?void 0:n.message)?((0,a.openBlock)(),(0,a.createElementBlock)("div",Dt,(0,a.toDisplayString)(e.job.message),1)):(0,a.createCommentVNode)("",!0)])]}),_:1},8,["rows"])],64)):((0,a.openBlock)(),(0,a.createElementBlock)("section",_t,[o[14]||(o[14]=(0,a.createElementVNode)("h2",null,"The board has not been gathered yet",-1)),(0,a.createElementVNode)("p",null,[o[12]||(o[12]=(0,a.createTextVNode)(" Refreshing reads every Dependabot alert on ",-1)),(0,a.createElementVNode)("code",null,(0,a.toDisplayString)((0,a.unref)(h)),1),o[13]||(o[13]=(0,a.createTextVNode)(" and the pull requests that relate to them, and stores the result in the cluster. It takes a few seconds and happens in the agent pod, not in this page. ",-1))])])),f.value?((0,a.openBlock)(),(0,a.createBlock)(nt,{key:7,status:u.value,blocking:g.value,onCancel:o[0]||(o[0]=e=>f.value=!1),onSaved:D},null,8,["status","blocking"])):(0,a.createCommentVNode)("",!0)])}}});t(9202);const Ot=(0,De.A)(Vt,[["__scopeId","data-v-b59e41ec"]]);var Pt=Ot},1785:function(e,n,t){"use strict";t.r(n);var a=t(6758),o=t.n(a),r=t(6173),s=t.n(r),i=s()(o());i.push([e.id,".session[data-v-09836064]{display:flex;flex-direction:column;height:100%;min-height:0;overflow:hidden}.session__id[data-v-09836064]{margin-left:10px;font-family:var(--font-family-mono,monospace);font-size:11px;font-weight:400;color:var(--muted)}.session__terminal[data-v-09836064]{flex:1 1 auto;min-height:0;border:1px solid var(--border);border-radius:6px;overflow:hidden;background:var(--body-bg)}.session__note[data-v-09836064]{margin:10px 0 0;font-size:11px;color:var(--muted)}.session__note--ended[data-v-09836064]{color:var(--warning)}",""]),n["default"]=i},8899:function(e,n,t){"use strict";t.r(n);var a=t(6758),o=t.n(a),r=t(6173),s=t.n(r),i=s()(o());i.push([e.id,".agent-terminal[data-v-5584bb48]{display:flex;flex-direction:column;min-width:0}.agent-terminal[data-v-5584bb48],.agent-terminal__pane[data-v-5584bb48]{flex:1 1 auto;min-height:0}.agent-terminal__waiting[data-v-5584bb48]{display:flex;align-items:center;gap:8px;padding:16px;color:var(--muted);font-size:13px}",""]),n["default"]=i},3478:function(e,n,t){"use strict";t.r(n);var a=t(6758),o=t.n(a),r=t(6173),s=t.n(r),i=s()(o());i.push([e.id,"[data-v-1089221e] code{padding:1px 5px;font-size:.92em;line-height:inherit;vertical-align:baseline;border-radius:3px}.creds-backdrop[data-v-1089221e]{position:fixed;inset:0;z-index:1000;display:flex;align-items:center;justify-content:center;padding:24px;background:rgba(0,0,0,.5)}.creds[data-v-1089221e]{width:100%;max-width:580px;max-height:90vh;overflow-y:auto;padding:24px;border-radius:8px;background:var(--body-bg);border:1px solid var(--border);box-shadow:0 18px 48px rgba(0,0,0,.35)}.creds__title[data-v-1089221e]{margin:0 0 8px;font-size:20px}.creds__lede[data-v-1089221e]{margin:0 0 20px;color:var(--muted);font-size:13px;line-height:19px}.creds__field[data-v-1089221e]{display:block;margin-bottom:18px}.creds__label[data-v-1089221e]{display:flex;align-items:center;gap:10px;font-weight:600;font-size:13px;margin-bottom:2px}.creds__state[data-v-1089221e]{padding:1px 8px;border-radius:10px;border:1px solid var(--border);font-size:10px;font-weight:700;letter-spacing:.04em;text-transform:uppercase;color:var(--muted)}.creds__state.is-set[data-v-1089221e]{color:var(--success);border-color:var(--success)}.creds__hint[data-v-1089221e]{display:block;color:var(--muted);font-size:12px;line-height:17px;margin-bottom:6px}.creds__hint em[data-v-1089221e]{font-style:normal;color:var(--body-text)}.creds__input[data-v-1089221e]{display:flex;align-items:stretch;border:1px solid var(--border);border-radius:4px;background:var(--input-bg);overflow:hidden}.creds__input[data-v-1089221e]:focus-within{border-color:var(--link)}.creds__input input[data-v-1089221e]{flex:1;min-width:0;padding:8px 10px;outline:none;color:var(--input-text);font-family:var(--font-family-mono,monospace);font-size:13px}.creds__input input[data-v-1089221e],.creds__peek[data-v-1089221e]{border:none;background:transparent}.creds__peek[data-v-1089221e]{color:var(--muted);cursor:pointer;padding:0 10px}.creds__peek[data-v-1089221e]:hover{color:var(--body-text)}.creds__clear[data-v-1089221e]{margin-top:6px;border:none;background:transparent;padding:0;color:var(--error);font-size:11px;cursor:pointer}.creds__clear[data-v-1089221e]:hover{text-decoration:underline}.creds__note[data-v-1089221e]{font-size:12px;line-height:18px}.creds__actions[data-v-1089221e]{display:flex;justify-content:flex-end;gap:10px;margin-top:20px}",""]),n["default"]=i},2057:function(e,n,t){"use strict";t.r(n);var a=t(6758),o=t.n(a),r=t(6173),s=t.n(r),i=s()(o());i.push([e.id,'.progress[data-v-c97aac58]{margin-top:10px}.progress__steps[data-v-c97aac58]{display:flex;flex-wrap:wrap;gap:6px 4px;margin:0;padding:0;list-style:none}.progress__step[data-v-c97aac58]{display:inline-flex;align-items:center;gap:6px;padding:3px 10px 3px 6px;border-radius:12px;font-size:11px;color:var(--muted);background:var(--nav-bg)}.progress__step+.progress__step[data-v-c97aac58]{position:relative;margin-left:8px}.progress__step+.progress__step[data-v-c97aac58]:before{content:"›";position:absolute;left:-10px;color:var(--muted);opacity:.6}.progress__step.is-done[data-v-c97aac58]{color:var(--success)}.progress__step.is-active[data-v-c97aac58]{color:var(--body-text);background:var(--accent-btn);font-weight:600}.progress__step.is-todo[data-v-c97aac58]{opacity:.55}.progress__marker[data-v-c97aac58]{display:inline-flex;align-items:center;justify-content:center;width:13px;height:13px}.progress__marker .icon[data-v-c97aac58]{font-size:11px}.progress__step.is-todo .progress__marker[data-v-c97aac58]:before{content:"";width:5px;height:5px;border-radius:50%;border:1px solid currentColor}.progress__foot[data-v-c97aac58]{display:flex;align-items:center;gap:12px;margin-top:8px;font-size:11px;color:var(--muted)}.progress__elapsed[data-v-c97aac58]{font-variant-numeric:tabular-nums}.progress__toggle[data-v-c97aac58]{border:none;background:transparent;color:var(--link);font-size:11px;cursor:pointer;padding:0;display:inline-flex;align-items:center;gap:4px}.progress__toggle[data-v-c97aac58]:hover{text-decoration:underline}',""]),n["default"]=i},1324:function(e,n,t){"use strict";t.r(n);var a=t(6758),o=t.n(a),r=t(6173),s=t.n(r),i=s()(o());i.push([e.id,".shipped__sub[data-v-398ab162]{margin:0 0 16px}.shipped__empty[data-v-398ab162],.shipped__sub[data-v-398ab162]{color:var(--muted)}.shipped__row[data-v-398ab162]{display:flex;align-items:flex-start;gap:12px;padding:10px 0;border-bottom:1px solid var(--border)}.shipped__body[data-v-398ab162]{flex:1 1 auto;min-width:0}.shipped__lib[data-v-398ab162]{font-weight:600;margin-bottom:4px}.shipped__pr[data-v-398ab162]{white-space:nowrap}",""]),n["default"]=i},6555:function(e,n,t){"use strict";t.r(n);var a=t(6758),o=t.n(a),r=t(6173),s=t.n(r),i=s()(o());i.push([e.id,".steps[data-v-7594f78c]{display:flex;flex-wrap:wrap;align-items:center;gap:6px}.steps__link[data-v-7594f78c]{text-decoration:none;max-width:260px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}",""]),n["default"]=i},8361:function(e,n,t){"use strict";t.r(n);var a=t(6758),o=t.n(a),r=t(6173),s=t.n(r),i=s()(o());i.push([e.id,".vuln-ids[data-v-501df710]{display:flex;flex-wrap:wrap;gap:4px}.vuln-ids__link[data-v-501df710]{text-decoration:none}",""]),n["default"]=i},6165:function(e,n,t){"use strict";t.r(n);var a=t(6758),o=t.n(a),r=t(6173),s=t.n(r),i=s()(o());i.push([e.id,".vuln[data-v-b59e41ec]{padding:20px}.vuln__head[data-v-b59e41ec]{display:flex;align-items:flex-start;justify-content:space-between;gap:24px;flex-wrap:wrap;margin-bottom:16px}.vuln__titles[data-v-b59e41ec]{min-width:0}.vuln__title[data-v-b59e41ec]{margin:0 0 4px;font-size:22px;font-weight:600}.vuln__lede[data-v-b59e41ec]{margin:0;max-width:66ch;color:var(--muted);font-size:13px;line-height:19px}.vuln__actions[data-v-b59e41ec]{display:flex;gap:8px;flex-shrink:0}.vuln__running[data-v-b59e41ec]{margin-bottom:18px;padding:12px 16px;border:1px solid var(--info);border-radius:8px;background:var(--body-bg)}.vuln__running-head[data-v-b59e41ec]{display:flex;align-items:center;gap:8px;font-size:13px}.vuln__running-head .icon[data-v-b59e41ec]{color:var(--info)}.vuln__running-head span[data-v-b59e41ec]{margin-left:auto;color:var(--muted);font-size:11px;font-variant-numeric:tabular-nums}.vuln__running-stop[data-v-b59e41ec]{border:none;background:transparent;color:var(--link);font-size:11px;cursor:pointer;padding:0}.vuln__running-stop[data-v-b59e41ec]:hover{text-decoration:underline}.vuln__loading[data-v-b59e41ec]{display:flex;align-items:center;gap:10px;padding:40px 0;color:var(--muted)}.vuln__empty[data-v-b59e41ec]{margin:24px auto 0;max-width:560px;padding:28px 32px;border:1px dashed var(--border);border-radius:10px;text-align:center}.vuln__empty h2[data-v-b59e41ec]{margin:0 0 8px;font-size:16px}.vuln__empty p[data-v-b59e41ec]{margin:0;color:var(--muted);font-size:13px;line-height:19px}.vuln__toolbar[data-v-b59e41ec]{display:flex;align-items:center;justify-content:space-between;gap:16px;flex-wrap:wrap;margin-bottom:12px}.vuln__counts[data-v-b59e41ec]{display:flex;flex-wrap:wrap;gap:12px}.vuln__stamp[data-v-b59e41ec]{font-size:11px;color:var(--muted)}.vuln__lib[data-v-b59e41ec]{font-weight:600;white-space:nowrap}.vuln__stale[data-v-b59e41ec]{display:inline-block;margin-top:4px;text-decoration:none}.vuln__file[data-v-b59e41ec]{display:block;font-family:var(--font-family-mono,monospace);font-size:11px;white-space:nowrap}.vuln__file[data-v-b59e41ec],.vuln__message[data-v-b59e41ec],.vuln__none[data-v-b59e41ec]{color:var(--muted)}.vuln__message[data-v-b59e41ec]{margin-top:4px;font-size:11px}",""]),n["default"]=i},5641:function(e,n,t){var a=t(1785);a.__esModule&&(a=a.default),"string"===typeof a&&(a=[[e.id,a,""]]),a.locals&&(e.exports=a.locals);var o=t(4825).A;o("1043a7bb",a,!0,{sourceMap:!1,shadowMode:!1})},6739:function(e,n,t){var a=t(8899);a.__esModule&&(a=a.default),"string"===typeof a&&(a=[[e.id,a,""]]),a.locals&&(e.exports=a.locals);var o=t(4825).A;o("c0eec4c2",a,!0,{sourceMap:!1,shadowMode:!1})},4006:function(e,n,t){var a=t(3478);a.__esModule&&(a=a.default),"string"===typeof a&&(a=[[e.id,a,""]]),a.locals&&(e.exports=a.locals);var o=t(4825).A;o("3abd9413",a,!0,{sourceMap:!1,shadowMode:!1})},4105:function(e,n,t){var a=t(2057);a.__esModule&&(a=a.default),"string"===typeof a&&(a=[[e.id,a,""]]),a.locals&&(e.exports=a.locals);var o=t(4825).A;o("317dcfb8",a,!0,{sourceMap:!1,shadowMode:!1})},6732:function(e,n,t){var a=t(1324);a.__esModule&&(a=a.default),"string"===typeof a&&(a=[[e.id,a,""]]),a.locals&&(e.exports=a.locals);var o=t(4825).A;o("4b198b39",a,!0,{sourceMap:!1,shadowMode:!1})},9899:function(e,n,t){var a=t(6555);a.__esModule&&(a=a.default),"string"===typeof a&&(a=[[e.id,a,""]]),a.locals&&(e.exports=a.locals);var o=t(4825).A;o("8a4837d8",a,!0,{sourceMap:!1,shadowMode:!1})},9657:function(e,n,t){var a=t(8361);a.__esModule&&(a=a.default),"string"===typeof a&&(a=[[e.id,a,""]]),a.locals&&(e.exports=a.locals);var o=t(4825).A;o("9e7c0046",a,!0,{sourceMap:!1,shadowMode:!1})},9202:function(e,n,t){var a=t(6165);a.__esModule&&(a=a.default),"string"===typeof a&&(a=[[e.id,a,""]]),a.locals&&(e.exports=a.locals);var o=t(4825).A;o("01071497",a,!0,{sourceMap:!1,shadowMode:!1})}}]);
//# sourceMappingURL=vuln-console-0.2.1.umd.min.376.js.map