(("undefined"!==typeof self?self:this)["webpackChunkvuln_console_0_1_1"]=("undefined"!==typeof self?self:this)["webpackChunkvuln_console_0_1_1"]||[]).push([[840],{1840:function(e,n,t){"use strict";t.r(n),t.d(n,{default:function(){return ot}});var a=t(9274),o=t(5359),r=t(9103),s=t(6590),i=t(8846),l=t(4850),d=t(9117),c=(0,a.defineComponent)({__name:"SeverityBadge",props:{severity:{}},setup(e){const n=e,t={critical:"bg-error",high:"bg-error",medium:"bg-warning",low:"bg-info"},o=(0,a.computed)(()=>t[n.severity]||"bg-info"),r=(0,a.computed)(()=>n.severity.toUpperCase());return(n,t)=>((0,a.openBlock)(),(0,a.createBlock)(d.A,{color:o.value,label:r.value,class:(0,a.normalizeClass)(["severity",`severity--${e.severity}`])},null,8,["color","label","class"]))}}),h=(t(8046),t(7433));const u=(0,h.A)(c,[["__scopeId","data-v-7b8a0a54"]]);var p=u;const m="vuln-console",v="rancher/dashboard",f="marcelofukumoto/dashboard",g=f,w="settings",b="gh_token",y="extension-studio",k="settings",$="vuln-console.rancher.io/owns",x="snapshot",_="vuln-workspace",E="/workspaces",S="__agents",R="agents:ready";function T(e){return e}const A="0.1.40";function N(){return window[S]||null}function C(e,n){const t=String(e||"").split(".").map(e=>parseInt(e,10)||0),a=n.split(".").map(e=>parseInt(e,10)||0);for(let o=0;o<Math.max(t.length,a.length);o++){const e=t[o]||0,n=a[o]||0;if(e!==n)return e>n}return!0}async function D(){const e=N();if(!e)return{state:"missing",version:null,pod:null,detail:"The Agents extension is not installed in this Rancher. Install codyrancher/agents — this extension runs every gather and every fix through the agent pod it provides."};if(!C(e.version,A))return{state:"outdated",version:e.version,pod:null,detail:`The Agents extension is ${e.version}; this needs ${A} or later for conversations that can be started with an opening prompt.`};const n=await e.agent.pod().catch(()=>null);return n?{state:"ready",version:e.version,pod:n,detail:`Agents ${e.version} · agent pod ${n}`}:{state:"no-pod",version:e.version,pod:null,detail:"The agent pod is not running yet. It comes up on its own — give it a minute after a Rancher restart, then refresh."}}function O(e=8e3){const n=N();return n?Promise.resolve(n):new Promise(n=>{const t=e=>{window.removeEventListener(R,a),clearTimeout(o),n(e)},a=()=>t(N()),o=setTimeout(()=>t(N()),e);window.addEventListener(R,a)})}const P="local",B=`/k8s/clusters/${P}`;function H(e,n,t){const a=window.location.origin.replace(/^http/,"ws"),o=new URLSearchParams({container:e.container,stdin:t?"1":"0",stdout:"1",stderr:"1",tty:"0"});for(const r of n)o.append("command",r);return`${a}${B}/api/v1/namespaces/${e.namespace}/pods/${e.pod}/exec?${o}`}function I(e){const n=Number(e.slice(0,1));try{return{channel:n,bytes:Uint8Array.from(atob(e.slice(1)),e=>e.charCodeAt(0))}}catch(t){return null}}const V=12e4;function j(e,n,t={}){var a;const o=null!==(a=t.timeoutMs)&&void 0!==a?a:V;return new Promise(r=>{const s={stdout:"",stderr:"",status:"",code:-1};let i=!1,l=null;const d={1:new TextDecoder,2:new TextDecoder},c=(e="")=>{if(!i){i=!0,clearTimeout(h),e&&(s.status=s.status||e);try{null===l||void 0===l||l.close()}catch(a){}r(s)}},h=setTimeout(()=>c(`no answer from the pod in ${Math.round(o/1e3)}s`),o);try{l=new WebSocket(H(e,n,!!t.onStdin),"base64.channel.k8s.io")}catch(u){return void c((null===u||void 0===u?void 0:u.message)||String(u))}t.onStdin&&(l.onopen=()=>{var e;null===(e=t.onStdin)||void 0===e||e.call(t,e=>{const n=(new TextEncoder).encode(e);let t="";for(const a of n)t+=String.fromCharCode(a);null===l||void 0===l||l.send(`0${btoa(t)}`)})}),l.onmessage=e=>{var n;const t=I(String(e.data||""));if(t){if(1===t.channel||2===t.channel){const e=d[t.channel].decode(t.bytes,{stream:!0});return void(1===t.channel?s.stdout+=e:s.stderr+=e)}if(3===t.channel){const e=(new TextDecoder).decode(t.bytes);try{const t=JSON.parse(e);if("Success"===t.status)s.code=0;else{s.status=t.message||"the command failed";const e=((null===(n=t.details)||void 0===n?void 0:n.causes)||[]).find(e=>"ExitCode"===e.reason);s.code=e&&Number(e.message)||1}}catch(a){s.status=e,s.code=-1===s.code?1:s.code}}}},l.onclose=()=>{if(s.stdout+=d[1].decode(),s.stderr+=d[2].decode(),-1===s.code){if(!s.stdout&&!s.stderr)return void c(s.status||"the exec was refused or dropped");s.code=0}c()},l.onerror=()=>c("could not open an exec to the pod")})}const W="__VC_OK__";async function q(e,n,t,a){const o=await j(e,["/bin/sh","-c",`set -e\n${n}\necho ${W}`],{timeoutMs:a});if(!o.stdout.includes(W)){const e=o.stderr.trim()||o.status||(o.code>0?`exit ${o.code}`:"it reported nothing");throw new Error(`Could not ${t}: ${e}`)}return o.stdout.replace(W,"").trim()}function U(e){return`'${e.split("'").join("'\\''")}'`}const L=4096,M="__END_OF_FILE__";function F(e){const n=(new TextEncoder).encode(e);let t="";for(let a=0;a<n.length;a+=8192)t+=String.fromCharCode(...n.subarray(a,a+8192));return btoa(t)}async function G(e,n,t,a={}){const o=a.mode||"644",r=a.owner||"",s=U(n),i=[`mkdir -p "$(dirname ${s})"`,`: > ${s}.b64`,`: > ${s} && chmod ${o} ${s}`,r?`chown ${r} ${s} 2>/dev/null || true`:"",`while IFS= read -r line; do [ "$line" = "${M}" ] && break; printf '%s' "$line" >> ${s}.b64; done`,`base64 -d < ${s}.b64 > ${s}`,`rm -f ${s}.b64`,`chmod ${o} ${s}`,r?`chown ${r} ${s} 2>/dev/null || true`:"",`echo WROTE $(wc -c < ${s})`].filter(Boolean).join("\n"),l=F(t),d=await j(e,["/bin/sh","-c",i],{timeoutMs:6e4,onStdin:e=>{for(let n=0;n<l.length;n+=L)e(`${l.slice(n,n+L)}\n`);e(`${M}\n`)}}),c=d.stdout.match(/WROTE\s+(\d+)/);if(!c||Number(c[1])!==(new TextEncoder).encode(t).length){const e=d.stderr.trim()||d.status||`exit ${d.code}`;throw new Error(`Could not write ${n} into the agent pod: ${e}`)}}const z="local",K=`/k8s/clusters/${z}/v1`,J=`/k8s/clusters/${z}/api/v1`;function Y(){const e=document.cookie.match(/(?:^|;\s*)CSRF=([^;]*)/);return{"X-Api-Csrf":e?decodeURIComponent(e[1]):"CSRF"}}async function X(e,n){const t=!!(null===n||void 0===n?void 0:n.method)&&"GET"!==n.method,a=await fetch(e,{...n,headers:{"Content-Type":"application/json",Accept:"application/json",...t?Y():{},...(null===n||void 0===n?void 0:n.headers)||{}}});if(404===a.status)return null;const o=await a.json().catch(()=>({}));if(!a.ok)throw new Error(o.message||o.error||`HTTP ${a.status}`);return o}function Z(e,n){return X(`${K}${e}`,n)}async function Q(){const e=await Z(`/namespaces/${m}`).catch(()=>null);e||await Z("/namespaces",{method:"POST",body:JSON.stringify({apiVersion:"v1",kind:"Namespace",metadata:{name:m}})}).catch(e=>{if(!/409|already exists|alreadyexists/i.test((null===e||void 0===e?void 0:e.message)||""))throw e})}function ee(e){return`/configmaps/${m}/${e}`}async function ne(e,n,t){const a={apiVersion:"v1",kind:"ConfigMap",metadata:{name:e,namespace:m,labels:{[$]:"true",...t}},data:n},o=await Z(ee(e)).catch(()=>null);o?await Z(ee(e),{method:"PUT",body:JSON.stringify({...a,metadata:{...o.metadata,labels:a.metadata.labels}})}):await Z("/configmaps",{method:"POST",body:JSON.stringify(a)}).catch(async n=>{if(!/409|already exists|alreadyexists/i.test((null===n||void 0===n?void 0:n.message)||""))throw n;const t=await Z(ee(e));await Z(ee(e),{method:"PUT",body:JSON.stringify({...a,metadata:{...t.metadata,labels:a.metadata.labels}})})})}async function te(e,n){var t;const a=await Z(ee(e)).catch(()=>null),o=null===(t=null===a||void 0===a?void 0:a.data)||void 0===t?void 0:t[n];if(!o)return null;try{return JSON.parse(o)}catch(r){return null}}function ae(){return te(x,"snapshot.json")}function oe(e){const n=e.toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-+|-+$/g,"").slice(0,50);return`job-${n||"unnamed"}`}async function re(){const e=await Z(`/configmaps?labelSelector=${encodeURIComponent(`${$}=true`)}`).catch(()=>null);return((null===e||void 0===e?void 0:e.data)||[]).filter(e=>{var n;return((null===(n=null===e||void 0===e?void 0:e.metadata)||void 0===n?void 0:n.name)||"").startsWith("job-")}).map(e=>{var n;try{return JSON.parse((null===(n=e.data)||void 0===n?void 0:n["job.json"])||"null")}catch(t){return null}}).filter(e=>!!(null===e||void 0===e?void 0:e.library))}async function se(e){await Q(),await ne(oe(e.library),{"job.json":JSON.stringify(e)},{})}const ie={"workspace/config.yaml":'# The scripts the workspace pod runs, mounted rather than baked into an image.\n#\n# No image is ever built for any of this: a stock `node:24` reads its whole behaviour out of\n# this ConfigMap. Editing a script is editing this object, and the pod picks it up on its next\n# start - which is why the Deployment carries a hash of these in its pod template.\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  namespace: ${namespace}\n  name: vuln-workspace-config\ndata:\n  boot.sh: |\n    #!/bin/sh\n    # Bring one fix workspace up: a rancher/dashboard checkout with its dependencies installed and\n    # the dev server supervised on top of it.\n    #\n    # The container\'s main process ends up being the supervisor, not the server, because a fix run\n    # does things that kill a dev server - switches branch, regenerates a lockfile, runs a build -\n    # and a pod whose main process was the server would restart on each of those and take the run\'s\n    # terminal with it.\n    #\n    # `${install}`, `${repo}` and `${port}` are apps-plus substitutions, filled in when the App is\n    # rendered. Everything written `$LIKE_THIS` is ordinary shell and is left alone: apps-plus only\n    # substitutes the braced form, and only for names its App declares.\n    set -e\n\n    # Force apt onto IPv4 before anything runs one. The node has no working IPv6 route to the Debian\n    # mirror, so an `apt-get update` left alone resolves an IPv6 address and hangs on it for minutes\n    # while holding the apt lock - which is how a workspace comes up with no tmux and no terminal.\n    mkdir -p /etc/apt/apt.conf.d\n    printf \'Acquire::ForceIPv4 "true";\\nAcquire::Retries "3";\\n\' > /etc/apt/apt.conf.d/99vuln-ipv4\n\n    WS=/workspaces/${install}\n    mkdir -p "$WS/.home"\n    chown node:node "$WS" "$WS/.home" 2>/dev/null || true\n    [ -f "$WS/.owned" ] || { chown -R node:node "$WS" 2>/dev/null; touch "$WS/.owned"; }\n\n    # The browser sidecar mounts these as subPaths, and the kubelet creates a missing subPath as\n    # root - leaving the seed unable to write into it.\n    mkdir -p "$WS/.a11y/opt" "$WS/.a11y/init"\n    chown -R node:node "$WS/.a11y" 2>/dev/null || true\n\n    # What every workspace on this node shares rather than keeping its own copy. Measured before\n    # this existed: Cypress 813 MB, yarn 749 MB, and a gigabyte of node_modules - most of a\n    # workspace\'s 3 GB, per workspace.\n    SHARED=/workspaces/.shared\n    mkdir -p "$SHARED/yarn" "$SHARED/cypress" "$SHARED/npm" "$SHARED/template"\n    chown node:node "$SHARED" "$SHARED/yarn" "$SHARED/cypress" "$SHARED/npm" "$SHARED/template" 2>/dev/null || true\n\n    # tmux and kubectl, which the terminal needs. No claude: the conversation runs in the agents pod\n    # and reaches this one through a tunnel, so a claude here is hundreds of megabytes of hostPath\n    # that nothing executes.\n    [ -f /seed/terminal-tools.sh ] \\\n      && (TOOLS_NO_CLAUDE=1 HOME_DIR="$WS/.home" /bin/sh /seed/terminal-tools.sh >"$WS/.terminal-tools.log" 2>&1 &) \\\n      || true\n\n    # What a recording and a lockfile bump need and the image lacks. Backgrounded so the dev server\n    # is not a minute later for it; the lock timeout lets it wait for the tmux install above rather\n    # than colliding with it.\n    (command -v ffmpeg >/dev/null 2>&1 && command -v lsof >/dev/null 2>&1) || (\n      apt-get -o DPkg::Lock::Timeout=300 update -qq \\\n        && DEBIAN_FRONTEND=noninteractive apt-get -o DPkg::Lock::Timeout=300 install -y -qq \\\n          ffmpeg jq lsof iproute2 >"$WS/.apt.log" 2>&1 &\n    ) || true\n\n    exec setpriv --reuid=1000 --regid=1000 --init-groups /bin/sh /seed/checkout.sh\n  checkout.sh: |\n    #!/bin/sh\n    # The workspace\'s checkout and install, as the node user. Split from boot.sh so the privileged\n    # half is short enough to read and this half can be run again by hand.\n    set -e\n\n    WS=/workspaces/${install}\n    SHARED=/workspaces/.shared\n\n    export HOME="$WS/.home"\n    export YARN_CACHE_FOLDER="$SHARED/yarn"\n    export CYPRESS_CACHE_FOLDER="$SHARED/cypress"\n    export npm_config_cache="$SHARED/npm"\n\n    # init + fetch rather than `git clone`, because clone refuses a non-empty directory and this one\n    # is not reliably empty: the agent seed writes `.claude` into the checkout, and if that lands\n    # first - a separate trigger racing this boot - a clone would fail and leave the pod serving\n    # nothing.\n    if [ ! -d "$WS/dashboard/.git" ]; then\n      mkdir -p "$WS/dashboard"\n      cd "$WS/dashboard"\n      git init -q\n      git remote add origin https://github.com/${repo} 2>/dev/null || true\n      DEFAULT=$(git ls-remote --symref origin HEAD | sed -n \'s@^ref: refs/heads/\\(.*\\)[[:space:]]HEAD@\\1@p\')\n      git fetch --depth 1 origin "$DEFAULT"\n      git checkout -f -B "$DEFAULT" FETCH_HEAD\n    fi\n\n    cd "$WS/dashboard"\n\n    # The fork is where fixes are pushed, so it is a remote from the start rather than something the\n    # agent has to remember to add. `upstream` is what a branch is diffed against - never the fork\'s\n    # own master, which is chronically stale and makes a clean fix look like it carries half the\n    # repository with it.\n    git remote add fork "https://github.com/${fork}" 2>/dev/null || true\n    git remote set-url fork "https://github.com/${fork}" 2>/dev/null || true\n    git remote add upstream "https://github.com/${repo}" 2>/dev/null || true\n\n    # node_modules, hard-linked from a template of the same lockfile if this node already has one.\n    # The files are the same inodes, so a second workspace\'s node_modules costs directory entries\n    # and nothing else. Keyed by the lockfile hash, so a template is only ever used by a checkout\n    # that would have installed exactly it.\n    HASH=$(sha1sum yarn.lock 2>/dev/null | cut -c1-12)\n\n    if [ -n "$HASH" ] && [ ! -d node_modules ] && [ -d "$SHARED/template/$HASH/node_modules" ]; then\n      cp -al "$SHARED/template/$HASH/node_modules" node_modules && touch .install-done\n    fi\n\n    [ -f .install-done ] || {\n      yarn install --mutex "file:$SHARED/yarn/.mutex" --network-timeout 600000\n      touch .install-done\n    }\n\n    # The first workspace to install a given lockfile leaves the template for the next one.\n    if [ -n "$HASH" ] && [ -d node_modules ] && [ ! -d "$SHARED/template/$HASH" ]; then\n      mkdir -p "$SHARED/template/$HASH"\n      cp -al node_modules "$SHARED/template/$HASH/node_modules" || rm -rf "$SHARED/template/$HASH"\n    fi\n\n    exec /bin/bash /workspace-config/serve.sh ${port}\n  serve.sh: |\n    #!/bin/bash\n    # The dev server, supervised: the container\'s process is this loop, not the server.\n    #\n    # A fix run does things that kill a dev server - switches branch, regenerates a lockfile, runs a\n    # build. When the pod\'s main process was the server itself, each of those restarted the\n    # container and took the run\'s terminal with it, mid-fix. So the server is restarted when it\n    # exits, and left alone while something else holds the port.\n    PORT=${1:-8005}\n\n    # The boot script runs this from the checkout, so the tree is its parent - which is where the\n    # log and the retarget file live.\n    WS=${WS:-$(dirname "$PWD")}\n    RETARGET=$WS/.dev-server.env\n    LOG=$WS/.dev-server.log\n\n    echo "$PORT" > "$WS/.dev-server.port" 2>/dev/null || true\n    export PORT WS RETARGET LOG IONICE\n\n    held() {\n      node -e "require(\'net\').connect(Number(process.argv[1]),\'127.0.0.1\').on(\'connect\',()=>process.exit(0)).on(\'error\',()=>process.exit(1))" "$PORT" 2>/dev/null\n    }\n\n    # Not all at once. Rancher\'s embedded k3s runs beside these servers, and when it restarts every\n    # workspace pod restarts with it and every webpack compiles at the same moment: a dozen of them\n    # on twelve cores, k3s\'s controllers fail to renew their lease, k3s exits, Rancher restarts -\n    # and the herd forms again. So a server waits, with jitter so they do not all wake together,\n    # while the node\'s one-minute load is above what the cores can carry.\n    CORES=$(nproc 2>/dev/null || echo 4)\n    gate() {\n      load=$(cut -d. -f1 /proc/loadavg 2>/dev/null || echo 0)\n      [ "$load" -le $(( CORES * 3 / 2 )) ]\n    }\n\n    # One dev server per pod, whoever started it. A second webpack is another two gigabytes at every\n    # rebuild and the pod\'s limit is five, so an agent that starts its own gets the port until it is\n    # done and this one comes back afterwards. Only node processes, matched by command line: a shell\n    # or a grep that merely mentions the server would otherwise count and stop this one for nothing.\n    servers() { pgrep -f "^[^ ]*node .*vue-cli-service serve" 2>/dev/null; }\n    foreign() {\n      local p\n      for p in $(servers); do\n        [ "$(ps -o pgid= -p "$p" 2>/dev/null | tr -d \' \')" = "$SERVER" ] || echo "$p"\n      done\n    }\n\n    SERVER=""\n    stop_ours() {\n      if [ -n "$SERVER" ]; then\n        kill -TERM -- -"$SERVER" 2>/dev/null\n        wait "$SERVER" 2>/dev/null\n        SERVER=""\n      fi\n    }\n    trap "stop_ours; exit 0" TERM INT\n\n    while :; do\n      if held || [ -n "$(foreign)" ]; then sleep 10; continue; fi\n\n      sleep $(( RANDOM % 20 ))\n\n      waited=0\n      while ! gate && [ "$waited" -lt 300 ]; do\n        [ "$waited" -eq 0 ] && echo "[workspace] the node is busy; waiting to start the dev server"\n        sleep 10\n        waited=$((waited + 10))\n      done\n\n      # Below the cluster\'s own processes: a compile that takes every core has taken k3s down with\n      # it on a busy node, and a slow first page is the better failure.\n      IONICE=""\n      command -v ionice >/dev/null 2>&1 && IONICE="ionice -c 3"\n\n      # What the server runs against can be changed without a new pod: writing the retarget file\n      # restarts it onto a different Rancher. Read in the server\'s own shell, so it replaces the\n      # pod\'s environment for the server alone.\n      STAMP=$(stat -c %Y "$RETARGET" 2>/dev/null || echo 0)\n      [ -f "$RETARGET" ] && echo "[workspace] the dev server runs against $(grep \'^API=\' "$RETARGET" | cut -d= -f2-)"\n\n      # setsid, so the server and everything it starts are one process group a stop can signal as a\n      # whole; tee, so the log is in the tree as well as in the pod\'s.\n      setsid bash -c \'if [ -f "$RETARGET" ]; then set -a; . "$RETARGET"; set +a; fi; nice -n 15 $IONICE env VUE_CLI_SERVICE_CONFIG_PATH=/workspace-config/vue.config.js yarn dev --port "$PORT" 2>&1 | tee "$LOG"\' &\n      SERVER=$!\n\n      # `sleep & wait`, not `sleep`: a trap runs once the foreground command returns, and a stop\n      # that waited out the sleep left the server running for the check that followed.\n      while kill -0 "$SERVER" 2>/dev/null; do\n        sleep 10 & wait $!\n        if [ -n "$(foreign)" ]; then\n          echo "[workspace] another dev server is running in this pod; standing down until it is gone"\n          stop_ours; sleep 10; continue 2\n        fi\n        if [ "$(stat -c %Y "$RETARGET" 2>/dev/null || echo 0)" != "$STAMP" ]; then\n          echo "[workspace] the dev server\'s target changed; restarting it"\n          stop_ours; continue 2\n        fi\n      done\n\n      SERVER=""\n      echo "[workspace] the dev server exited; starting it again in 5s"\n      sleep 5\n    done\n  vue.config.js: |\n    // Written by the Dev extension. See pkg/dev-extension/workspace-config.ts.\n    //\n    // This dev server is reached one of two ways, and everything below is the consequence of which.\n    //\n    //   - behind the apiserver\'s service proxy, at DEV_PROXY_PATH, on Rancher\'s own origin. The Host\n    //     header is the proxy\'s, the TLS the browser sees is Rancher\'s, the prefix is stripped on the\n    //     way in and has to be put back on every URL handed out.\n    //   - at an origin of its own, on a node port, with no prefix at all.\n    //\n    // A dev server built for one does not work at the other: the router base is baked in. Which one\n    // this is comes from DEV_PROXY_PATH being set or empty, and changing it restarts the pod.\n    //\n    // The second mode exists because of what the first one does to a dashboard. rancher/dashboard\n    // builds its API URLs absolute from the page origin, so behind the prefix its /v1 and /v3 calls\n    // never reach this dev server at all: they go to the Rancher this page was served from, using\n    // that session, against that cluster. Measured, on a workspace with a Rancher of its own: two\n    // requests arrived here and a hundred and thirty-six went to the host. An origin of its own is\n    // the only thing that fixes it, because there is no browser-side API base to set.\n    const proxyPath = (process.env.DEV_PROXY_PATH || \'\').replace(/\\/$/, \'\');\n\n    // The prefix on every in-app link, which has to be the path the page was loaded at. Set before\n    // the shell\'s config runs, because the shell reads it out of the environment as it does.\n    process.env.ROUTER_BASE = proxyPath ? proxyPath + \'/\' : \'/\';\n\n    // The repo\'s own config, untouched in the checkout, rather than a reimplementation of it.\n    // rancher/dashboard is a monorepo whose shell is ./shell rather than a node_modules package,\n    // and which arguments its root config passes to that shell is the repo\'s business and changes\n    // with the repo. Wrapping it means this file only has to know about the proxy. This file is\n    // handed to vue-cli-service as VUE_CLI_SERVICE_CONFIG_PATH (see apps.ts, WORKSPACE_SCRIPT), so\n    // it lives outside the checkout and the checkout\'s own vue.config.js is never rewritten: the\n    // tree stays clean, a branch switch never trips over it, and nothing an agent commits carries it.\n    const base = require(require(\'path\').join(process.cwd(), \'vue.config.js\'));\n\n    // The proxy rewrites absolute URLs in HTML as they pass through, which is what lets a naive UI\n    // work behind it at all, so an asset URL that already carried the prefix would carry it twice.\n    // index.html therefore asks for \'/js/...\' and lets the proxy add the prefix, while webpack\'s\n    // runtime works its own base out from the script URL it was loaded from.\n    base.publicPath = \'auto\';\n\n    const previousChainWebpack = base.chainWebpack;\n\n    base.chainWebpack = (webpackConfig) => {\n      if (typeof previousChainWebpack === \'function\') {\n        previousChainWebpack(webpackConfig);\n      }\n\n      webpackConfig.plugin(\'html-index\').tap((args) => {\n        args[0].publicPath = \'/\';\n\n        return args;\n      });\n    };\n\n    base.devServer = {\n      ...base.devServer,\n\n      // Plain http only behind the proxy, where Rancher terminates TLS and will not talk to the\n      // shell\'s self-signed dev certificate on the way through.\n      //\n      // At its own origin the browser talks to this server directly, and http there is not a smaller\n      // problem than a certificate warning, it is a broken login: the shipped proxy sets\n      // x-forwarded-proto: https on every request it makes, so Rancher issues its session cookie with\n      // Secure, and a browser on http throws that cookie away. What that looks like is a login that\n      // returns 200 and a page that bounces straight back to the login form, which is what it did.\n      // So https here, with the shell\'s own certificate, and a warning to click through once.\n      ...(proxyPath ? { server: { type: \'http\' } } : {}),\n\n      // Requests arrive with whatever Host the proxy chain last set, never this server\'s own.\n      // Without this the proxy answers \'Invalid Host header\'.\n      allowedHosts: \'all\',\n\n      // The proxy cannot rewrite URLs in a compressed body: the response comes back as a stream\n      // error and the browser shows a blank page.\n      compress: false,\n\n      // The prefix is stripped before a request lands here, so everything is served from the root.\n      devMiddleware: { publicPath: \'/\' },\n      static:        { publicPath: \'/\' },\n\n      // A deep link into the dashboard has to load the app rather than 404. disableDotRule matters\n      // in Rancher, where a route routinely carries a resource name with dots in it.\n      historyApiFallback: {\n        index:             \'/index.html\',\n        disableDotRule:    true,\n        htmlAcceptHeaders: [\'text/html\', \'application/xhtml+xml\']\n      },\n\n      hot:             true,\n      webSocketServer: { type: \'ws\', options: { path: \'/ws\' } },\n      client:          {\n        // Hot reload rides the same proxy when there is one, and the page\'s own origin when there is\n        // not. Either way this has to be written out, because the shell\'s config has an address of its\n        // own in it and that address is not where this server is.\n        //\n        // Behind the proxy only the path is ours; the rest are webpack-dev-server\'s "infer it from\n        // window.location" sentinels. \'auto\' is one the *server* substitutes as it serves the client.\n        //\n        // Served at its own origin the sentinels are still needed, and leaving them out was a real\n        // bug rather than a tidy default. The shell\'s own config sets a fixed\n        // wss://localhost:8005/ws, which is right on a laptop running the dev server and wrong\n        // everywhere else: the browser then opened a hot-reload socket to whatever was on port 8005\n        // of the machine the browser is on, and displayed that server\'s compile errors over this\n        // dashboard. Seen here as a webpack overlay full of a completely different project\'s module\n        // resolution failures, sitting on top of the workspace\'s login form and swallowing clicks.\n        //\n        // 0.0.0.0 and 0 are webpack-dev-server\'s own "take it from window.location" sentinels. The\n        // protocol is written out rather than left as \'auto\', because \'auto\' is substituted by the\n        // server as it serves the client and is handed to the WebSocket constructor unchanged when\n        // the page was loaded directly. This server is https at its own origin, so wss.\n        webSocketURL: proxyPath ? {\n          protocol: \'auto\',\n          hostname: \'0.0.0.0\',\n          port:     0,\n          pathname: proxyPath + \'/ws\'\n        } : {\n          protocol: \'wss\',\n          hostname: \'0.0.0.0\',\n          port:     0,\n          pathname: \'/ws\'\n        },\n        // The compile-error overlay would cover the whole framed dashboard for whoever is looking\n        // when an edit fails to compile.\n        overlay: false\n      }\n    };\n\n    module.exports = base;\n',"workspace/deployment.yaml":'# The workspace: a checkout with a supervised dev server, and a Chromium beside it.\n#\n# Two containers in one pod so they share localhost - the browser drives the dev server at\n# 127.0.0.1 and the agent drives the browser at 127.0.0.1:9222, with nothing to route and no\n# service to resolve between them.\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  namespace: ${namespace}\n  name: ${namespace}\n  labels:\n    vuln-console.rancher.io/workspace: ${install}\nspec:\n  replicas: 1\n  # Recreate, not RollingUpdate: two of these would both want the hostPath tree and the same\n  # node_modules, and the second would fight the first for the yarn mutex.\n  strategy:\n    type: Recreate\n  selector:\n    matchLabels:\n      vuln-console.rancher.io/workspace: ${install}\n  template:\n    metadata:\n      labels:\n        vuln-console.rancher.io/workspace: ${install}\n    spec:\n      serviceAccountName: vuln-workspace\n      containers:\n        - name: workspace\n          image: ${image}\n          command: ["/bin/sh", "/workspace-config/boot.sh"]\n          env:\n            # The path this dev server is reached at. The dashboard bakes its router base in at\n            # build time, so it has to know the prefix the proxy strips on the way in.\n            - name: DEV_PROXY_PATH\n              value: /k8s/clusters/${hostCluster}/api/v1/namespaces/${namespace}/services/${scheme}:${namespace}:${port}/proxy\n            - name: API\n              value: ${rancherUrl}\n            - name: RANCHER_URL\n              value: ${rancherUrl}\n            - name: VULN_BROWSER_CDP\n              value: http://localhost:9222\n            # This cluster is k3s inside the Rancher container, so the node\'s own address is\n            # how the dev server reaches the Rancher it belongs to.\n            - name: NODE_IP\n              valueFrom:\n                fieldRef:\n                  fieldPath: status.hostIP\n            # A dashboard build is a large one; the default heap is not enough for it.\n            - name: NODE_OPTIONS\n              value: --max_old_space_size=4096\n          ports:\n            - containerPort: ${port}\n              protocol: TCP\n          # The first start is a clone, a yarn install and a first compile - minutes, not\n          # seconds - so the startup probe is allowed a long time before it gives up, and only\n          # then does the readiness probe take over.\n          startupProbe:\n            httpGet:\n              path: /index.html\n              port: ${port}\n              scheme: HTTPS\n            periodSeconds: 10\n            failureThreshold: 90\n          readinessProbe:\n            httpGet:\n              path: /index.html\n              port: ${port}\n              scheme: HTTPS\n            periodSeconds: 30\n            failureThreshold: 3\n          resources:\n            requests:\n              cpu: 250m\n              memory: 1Gi\n            limits:\n              cpu: "2"\n              memory: 5Gi\n          volumeMounts:\n            - name: work\n              mountPath: /workspaces\n            - name: workspace-config\n              mountPath: /workspace-config\n            - name: seed\n              mountPath: /seed\n        - name: browser\n          image: lscr.io/linuxserver/chromium:latest\n          env:\n            - name: CHROME_CLI\n              value: --remote-debugging-port=9222 --remote-allow-origins=* --ignore-certificate-errors ${scheme}://localhost:${port}\n            - name: DOCKER_MODS\n              value: linuxserver/mods:universal-package-install\n            - name: INSTALL_PACKAGES\n              value: ${a11yPackages}\n          ports:\n            - containerPort: 3000\n              protocol: TCP\n            - containerPort: 9222\n              protocol: TCP\n          resources:\n            requests:\n              cpu: 100m\n              memory: 512Mi\n            limits:\n              cpu: "1"\n              memory: 3Gi\n          volumeMounts:\n            # Chromium needs more shared memory than the default 64 MiB or it dies rendering\n            # anything substantial.\n            - name: dshm\n              mountPath: /dev/shm\n            - name: work\n              mountPath: /artifacts\n              subPath: ${install}/artifacts\n      volumes:\n        # The PARENT of the workspaces, deliberately: the checkout\'s node_modules is hard-linked\n        # from a template shared between workspaces, and a hard link cannot cross mounts.\n        - name: work\n          hostPath:\n            path: /var/lib/rancher/vuln-workspaces\n            type: DirectoryOrCreate\n        - name: workspace-config\n          configMap:\n            name: vuln-workspace-config\n            defaultMode: 365\n        # The agents extension\'s own seed, for the terminal tools. Optional: a workspace still\n        # builds and serves without it, it just has no tmux.\n        - name: seed\n          configMap:\n            name: agent-seed\n            defaultMode: 365\n            optional: true\n        - name: dshm\n          emptyDir:\n            medium: Memory\n            sizeLimit: 1Gi\n',"workspace/namespace.yaml":"# The workspace's own namespace. One per fix in flight, named for the installation, so a run's\n# pod, service and config are deleted together by deleting the installation.\napiVersion: v1\nkind: Namespace\nmetadata:\n  name: ${namespace}\n  labels:\n    vuln-console.rancher.io/workspace: ${install}\n  annotations:\n    vuln-console.rancher.io/port: \"${port}\"\n    vuln-console.rancher.io/scheme: ${scheme}\n","workspace/rbac.yaml":"# What the workspace pod may do, which is only ever within its own namespace.\n#\n# Deliberately not cluster-admin. The agents pod is cluster-admin because it edits the cluster;\n# a workspace only ever builds a branch and serves it, so a RoleBinding - not a ClusterRoleBinding\n# - is what it gets. A fix that needs more than this is a fix worth looking at.\napiVersion: v1\nkind: ServiceAccount\nmetadata:\n  namespace: ${namespace}\n  name: vuln-workspace\n---\napiVersion: rbac.authorization.k8s.io/v1\nkind: RoleBinding\nmetadata:\n  namespace: ${namespace}\n  name: vuln-workspace\nroleRef:\n  apiGroup: rbac.authorization.k8s.io\n  kind: ClusterRole\n  name: edit\nsubjects:\n  - kind: ServiceAccount\n    name: vuln-workspace\n    namespace: ${namespace}\n","workspace/service.yaml":"# Two ports, two ways in.\n#\n# The dev server (${port}) is reached through the Kubernetes apiserver's service proxy, on\n# Rancher's own origin - so there is no Ingress, no hostname to choose and no certificate to\n# wait for, and the browser's existing Rancher session authenticates it. The browser sidecar\n# (3000) is framed the same way.\napiVersion: v1\nkind: Service\nmetadata:\n  namespace: ${namespace}\n  name: ${namespace}\n  labels:\n    vuln-console.rancher.io/workspace: ${install}\nspec:\n  selector:\n    vuln-console.rancher.io/workspace: ${install}\n  ports:\n    - name: http\n      port: ${port}\n      targetPort: ${port}\n      protocol: TCP\n    - name: browser\n      port: 3000\n      targetPort: 3000\n      protocol: TCP\n"};const le="appsplus.io.app",de="appsplus.io.appinstance",ce="vuln-console.rancher.io/library";function he(e){var n,t;return!!(null===(t=(n=e.getters)["management/schemaFor"])||void 0===t?void 0:t.call(n,de))}function ue(e){const n=e.toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-+|-+$/g,"").slice(0,40);return`vuln-${n||"fix"}`}function pe(){const e=Object.keys(ie).filter(e=>e.startsWith("workspace/")).sort().map(e=>({name:e.replace("workspace/",""),content:ie[e]}));return{apiVersion:"appsplus.io/v1alpha1",kind:"App",metadata:{name:_},spec:{description:"A rancher/dashboard checkout with its dependencies installed, the dev server running, and a browser beside it — where one Dependabot fix is made and verified. The first start is minutes: a clone, a yarn install and a first compile.",values:{repo:v,fork:f,port:8005,scheme:"https",image:"node:24",hostCluster:"local",rancherUrl:"https://$(NODE_IP)",a11yPackages:"at-spi2-core|dbus-x11|gir1.2-atspi-2.0|python3-gi|python3-pyatspi"},valueLabels:{repo:"Repository the fix is made against",fork:"Fork the branch is pushed to",port:"Port the dev server listens on",scheme:"http or https",image:"Container image",hostCluster:"Cluster the workspace runs on",rancherUrl:"Rancher the dev server points at",a11yPackages:"What the browser installs (pipe separated)"},templates:e}}}async function me(e){var n;const t=pe(),a=await e.dispatch("management/find",{type:le,id:_}).catch(()=>null);if(!a){const n=await e.dispatch("management/create",t);return void await n.save()}JSON.stringify(null===(n=a.spec)||void 0===n?void 0:n.templates)!==JSON.stringify(t.spec.templates)&&(a.spec={...a.spec,...t.spec},await a.save())}async function ve(e,n){const t=ue(n),a=await e.dispatch("management/find",{type:de,id:t}).catch(()=>null);if(a)return t;await me(e);const o=await e.dispatch("management/create",{type:de,metadata:{name:t,labels:{[ce]:n.replace(/[^A-Za-z0-9_.-]/g,"-").slice(0,63)}},spec:{app:_,namespace:t,targets:[{clusterName:"local"}],values:{},provisionCluster:{enabled:!1}}});return await o.save(),t}const fe={"address-comments.prompt.md":'# Address the review comments on a pull request\n\nRe-read the pull request\'s review comments, make any changes that are actually requested,\nre-verify, and **stage a reply for the human to send**.\n\n## The rule that matters most\n\n**Do not post a comment on the pull request.** Not a reply, not a summary, not an\nacknowledgement. Marcelo answers reviewers himself, in his own words — an auto-posted reply reads\nas his and is not.\n\nWhat you do instead: write the per-point reply as a draft and record where it is, so the board\nshows a **reply draft** pill he can read and then post himself.\n\nEditing the pull request **body** is still wanted — the description should stay true as the\nbranch changes, and the video belongs there.\n\n## What to do\n\n1. Read every review comment and every review state on the pull request.\n2. For each point: decide whether it asks for a change. If it does, make it in the checkout.\n3. If you changed anything: re-run the verification against the dev server, capture a fresh\n   video, and update the `### Screenshot/Video` section of the body.\n4. Write the reply draft — one short paragraph per point, saying what you did or why nothing\n   changed. Never a bare `#<number>` in it.\n5. Record:\n\n```\n$ROOT/job.sh "<library>" phase=Done replyUrl=<path> message="<one line: what changed>"\n```\n\nIf nothing needed changing, say so in the draft and record it the same way.\n',"fix.prompt.md":'# Fix one Dependabot vulnerability\n\nYou are the vulnerability console\'s fix agent. Somebody pressed **Fix** on one library. Bump it,\nprove the bump did not break where it is used, and record what you did. **Do not open a pull\nrequest** — that is a separate button and a separate run.\n\n## Where you are\n\nEvery command you run lands inside this fix\'s workspace, at the path it already has. You do not\nneed to `kubectl exec`, `ssh` or `cd` anywhere unusual; `yarn`, `git` and `node` are simply there.\n\n- the checkout is `$WSD/dashboard`, on the repository\'s default branch\n- `origin` is the upstream repository, `fork` is where branches are pushed\n- `gh` and `jq` are on `$PATH`, and `gh` is already authenticated\n- the dev server is **already running** and supervised. Never start a second one — the pod\'s\n  memory limit fits one, and two have OOM-killed the container out from under a run. Its URL is\n  in the job; a branch you check out is serving within a minute or two of compiling.\n\n## ⛔ You do not get woken up\n\nYou run one-shot. There is no notification, no wake-up, no re-invocation. The moment you stop to\n"wait for a background task" or "await completion", the run ends and everything you did is lost.\n\nRun every slow command **synchronously in the foreground**. If one might time out, block inline\nwith a single foreground loop over its log until a done marker appears. Never hand work to a\nbackground task and yield.\n\nDo **not** run the whole monorepo\'s `yarn lint` or `yarn test` — that is the pull request\'s CI\njob, it is slow, and it will strand you.\n\n## 1. Decide whether to fix, and on which branch\n\nA package can need fixing more than once over time, so branches are **numbered** and every\nre-fix gets its own. **Never reuse or force-push a branch that already has a pull request** —\nthat corrupts the earlier one and makes the new fix link to it.\n\nList what already exists:\n\n```\ngh pr list --repo <fork> --state all --json number,state,title,headRefName \\\n  | jq \'[.[] | select(.title | test("<library>"; "i"))]\'\ngh api repos/<fork>/branches --paginate --jq \'.[].name\' | grep -E "/<library>(-|$)"\n```\n\nThen:\n\n- **an open pull request of ours already covers every still-open line** — stop, and record\n  `phase=Done message="already covered by pull request <n>"`. Do not open another.\n- **an open pull request exists but does not cover this case** (it fixed a different manifest or\n  major line) — this is a separate fix: take the next free numbered branch.\n- **only merged pull requests exist and the advisory is open again** — always a new numbered\n  branch. Never touch the merged one\'s.\n- **nothing exists** — fix it on the base branch name.\n\nBranch names: `dependabot/npm_and_yarn/multi/<library>` for the first fix, then `<library>-1`,\n`<library>-2`, … — the first name not already in use.\n\n## 2. Make the fix\n\nStart from a clean tree on the repository\'s default branch, then bump the vulnerable dependency\nacross **every** manifest it touches and regenerate each lockfile.\n\n**Prefer a real update; a `resolutions` override is the last resort.** When a stale lockfile\nmerely holds a transitive at an old patch and the consuming range already admits a patched\nversion, `yarn upgrade <library>` lets it climb in place. Reach for a root `resolutions` pin only\nwhen neither an in-range update nor bumping the pinning parent can get there.\n\n### The three things reviewers actually catch\n\n1. **Cover every affected manifest, enumerated from the LIVE alerts** — not from the board,\n   which can be hours old:\n   ```\n   gh api repos/<repo>/dependabot/alerts --paginate \\\n     --jq \'.[]|select(.state=="open")|select(.security_vulnerability.package.name=="<library>")|.dependency.manifest_path\' \\\n     | sort -u\n   ```\n   Then grep the repository for every other `yarn.lock` pinning the vulnerable range. Bump them\n   all — root, `shell/`, **`cypress/`**, `storybook/`, `docusaurus/`, `pkg/*`. `cypress/yarn.lock`\n   is the commonly missed one.\n\n2. **Each affected major line needs that line\'s own first patched version.** They differ per\n   line: one advisory can require `1.1.16` *and* `2.1.2` *and* `5.0.7`, and a lower patch in one\n   line can still be vulnerable. Read each alert\'s `vulnerable_version_range` and\n   `first_patched_version`, and confirm the consuming `^x.y` ranges admit what you resolved to.\n\n3. **Introduce no new vulnerable package.** After regenerating, diff each lockfile for *added*\n   entries and check every newly added package and version — including a **different** package\n   the resolver pulled in (the classic is `braces < 3.0.3`). If the regeneration added a\n   vulnerable one, pin it forward, reinstall, and re-diff until the net change adds zero\n   vulnerabilities.\n\n### The lockfile will refuse to change\n\nThe checkout carries a root `.yarnrc` containing `--frozen-lockfile true`, and it is inherited by\nevery subdirectory. `yarn install` will install into `node_modules` and leave `yarn.lock`\nuntouched, so the bump appears to do nothing. Move the root `.yarnrc` aside, clear the package\nfrom `node_modules` and remove `.yarn-integrity`, install, then put `.yarnrc` back. Look for\n`success Saved lockfile.` and confirm the new version in the lock. **Never commit a `.yarnrc`\nchange.**\n\nCommit **only** `package.json` and `yarn.lock` files. No attribution trailers, no co-author\nlines, and no pull request or issue numbers in the message.\n\nRecord the branch as soon as it is pushed:\n\n```\n$ROOT/job.sh "<library>" branch=<branch>\n```\n\n## 3. Prove it still works\n\nThe point is not that it builds — it is that the place this library is actually used still\nbehaves. Grep the checkout for where it is imported, pick two to four real interactions that\nexercise it, and drive them in the browser against the running dev server.\n\nUse the browser that is already in this pod at `$VULN_BROWSER_CDP` — connect over CDP, do not\ninstall a browser. Capture an annotated video and take screenshots of each check.\n\nWrite the verification up as numbered checks, each ending ✅ or ❌, followed by a one-line\nverdict. Then record it:\n\n```\n$ROOT/job.sh "<library>" phase=Fixed previewUrl=<url> videoUrl=<path> infoUrl=<path> \\\n  message="<one line: what was bumped, in how many manifests, and what was verified>"\n```\n\nIf a check fails, that is a real result: record `phase=Failed` with what broke. A fix that\nbreaks the feature is worse than the advisory.\n\n## What you must not do\n\n- do not open a pull request\n- do not comment on anything on GitHub\n- do not touch any repository other than the checkout in your workspace\n- do not start a second dev server\n',"gather.mjs":"#!/usr/bin/env node\n// Gather the Dependabot picture for one repository: every alert, Dependabot's own pull\n// requests, and ours.\n//\n// Dependency-free and shell-free on purpose. This runs inside the agents pod, a stock\n// `node:24` with neither `jq` nor `gh` in it, so the laptop pipeline it replaces\n// (scripts/gather-vulnerabilities.sh, which is `gh api | jq` from end to end) is re-expressed\n// in the one runtime the pod is guaranteed to have.\n//\n// It writes ALL alert states, not just the open ones. The board needs the closed ones to show\n// what we shipped, and - more importantly - closing is the ONLY thing that moves a row out of\n// the actionable list. A gather that dropped them would leave the board unable to tell \"fixed\"\n// from \"never seen\".\n\nimport { readFileSync, writeFileSync } from 'node:fs';\n\nconst REPO = process.env.VULN_REPO || 'rancher/dashboard';\nconst FORK_OWNER = process.env.FORK_OWNER || 'marcelofukumoto';\nconst OUT = process.env.OUT || './snapshot.json';\nconst API = process.env.GITHUB_API || 'https://api.github.com';\n\nfunction fail(message) {\n  process.stderr.write(`gather: ${ message }\\n`);\n  process.exit(1);\n}\n\n/**\n * The token, read from a file rather than the environment.\n *\n * A pod is a place where `ps` and `/proc/<pid>/environ` are readable, and this one is shared:\n * every conversation in it runs as the same user. A token on a command line or in an exported\n * variable is a token every pane can read for as long as the process lives. The file is written\n * 0600, read once here, and removed when the run ends.\n */\nfunction token() {\n  const file = process.env.CREDS_FILE;\n\n  if (!file) {\n    return process.env.GH_TOKEN || '';\n  }\n\n  try {\n    return JSON.parse(readFileSync(file, 'utf8')).GH_TOKEN || '';\n  } catch (e) {\n    fail(`could not read the credentials at ${ file }: ${ e?.message || e }`);\n  }\n}\n\nconst GH_TOKEN = token();\n\nif (!GH_TOKEN) {\n  fail('GH_TOKEN is not set. Dependabot alerts cannot be read without it.');\n}\n\nconst HEADERS = {\n  Authorization: `Bearer ${ GH_TOKEN }`,\n  Accept:        'application/vnd.github+json',\n  'User-Agent':  'vuln-console',\n};\n\nasync function request(url, what) {\n  let resp;\n\n  try {\n    resp = await fetch(url, { headers: HEADERS });\n  } catch (e) {\n    throw new Error(`${ what }: ${ e?.message || e }`, { cause: e });\n  }\n\n  const text = await resp.text();\n\n  if (!resp.ok) {\n    const detail = text.slice(0, 300).replace(/\\s+/g, ' ').trim();\n\n    throw new Error(`${ what }: HTTP ${ resp.status }${ detail ? ` - ${ detail }` : '' }`);\n  }\n\n  try {\n    return { body: JSON.parse(text), link: resp.headers.get('link') || '' };\n  } catch {\n    throw new Error(`${ what }: the answer was not JSON`);\n  }\n}\n\n/** Follow `Link: rel=\"next\"` rather than counting pages - GitHub decides where the end is. */\nasync function paginate(url, what) {\n  const all = [];\n  let next = url;\n\n  while (next) {\n    const { body, link } = await request(next, what);\n\n    if (!Array.isArray(body)) {\n      throw new Error(`${ what }: expected a list, got ${ typeof body }`);\n    }\n\n    all.push(...body);\n\n    const more = /<([^>]+)>;\\s*rel=\"next\"/.exec(link);\n\n    next = more ? more[1] : null;\n  }\n\n  return all;\n}\n\nfunction severityOf(value) {\n  const known = ['critical', 'high', 'medium', 'low'];\n\n  return known.includes(value) ? value : 'low';\n}\n\nasync function fetchAlerts() {\n  // No `state` filter, deliberately. The parameter takes a comma-separated list of the four\n  // real states and has no \"all\" - and an unrecognised value is not an error, it is an empty\n  // list. Asking for `state=all` returns zero alerts with HTTP 200, which is indistinguishable\n  // from a repository that has none.\n  const raw = await paginate(\n    `${ API }/repos/${ REPO }/dependabot/alerts?per_page=100`,\n    'reading the Dependabot alerts',\n  );\n\n  // Trimmed, because this ends up in a ConfigMap and a ConfigMap holds a megabyte. The full\n  // record for all 969 of rancher/dashboard's alerts is 521 KiB, which is half the ceiling on a\n  // list that only grows - so the snapshot carries what the BOARD reads and nothing else:\n  //\n  //   * `url` is derived (`<repo>/security/dependabot/<id>`), not stored.\n  //   * the vulnerable range, scope and relationship are not stored at all. They belong to\n  //     fixing, and the fix prompt already enumerates them from the LIVE alerts rather than the\n  //     snapshot - precisely because a snapshot can be hours old and a manifest list must not be.\n  //   * a CLOSED alert keeps only what the shipped list shows. 959 of the 969 are closed, and\n  //     what they are for is \"we fixed this\" - not the advisory text.\n  return raw.map((a) => {\n    const open = a.state === 'open';\n    const alert = {\n      id:       a.number,\n      library:  a.security_vulnerability?.package?.name || a.dependency?.package?.name || '?',\n      ghsa:     a.security_advisory?.ghsa_id || '',\n      severity: severityOf(a.security_advisory?.severity),\n      manifest: a.dependency?.manifest_path || '',\n      state:    a.state,\n      patched:  a.security_vulnerability?.first_patched_version?.identifier || null,\n      fixedAt:  a.fixed_at || null,\n    };\n\n    if (!open) {\n      return alert;\n    }\n\n    return {\n      ...alert,\n      ecosystem: a.security_vulnerability?.package?.ecosystem || 'npm',\n      cve:       a.security_advisory?.cve_id || null,\n      summary:   a.security_advisory?.summary || '',\n      createdAt: a.created_at,\n    };\n  });\n}\n\nfunction normalisePr(pr) {\n  return {\n    number:      pr.number,\n    url:         pr.html_url,\n    title:       pr.title || '',\n    headRefName: pr.head?.ref || '',\n    status:      pr.merged_at ? 'merged' : (pr.state === 'closed' ? 'closed' : 'open'),\n    author:      pr.user?.login || '',\n    createdAt:   pr.created_at,\n    mergedAt:    pr.merged_at || null,\n    videoUrl:    videoIn(pr.body || ''),\n  };\n}\n\n/**\n * A verification video already attached to a pull request body.\n *\n * Detecting it means the board's recording pill survives losing our own record of the run -\n * the pull request is the durable copy, and a GitHub-hosted URL is the published one.\n */\nfunction videoIn(body) {\n  const found = /https:\\/\\/github\\.com\\/user-attachments\\/assets\\/[0-9a-f-]+/i.exec(body);\n\n  return found ? found[0] : null;\n}\n\n/**\n * Our pull requests, in one GraphQL search.\n *\n * Not by paging `/pulls?state=closed`: rancher/dashboard has tens of thousands and the ones\n * that matter are a few hundred by one author. Not by the REST search either - that returns\n * issue records carrying neither the head branch nor a body, so every hit needed a second\n * call and a few hundred of those take longer than the whole rest of the gather. GraphQL\n * answers the question and returns the fields in the same request.\n *\n * Both fields it adds are load-bearing: a closed alert is attributed to a merge by TIMING, and\n * the branch is the fallback when a title says nothing.\n */\nasync function fetchOurPulls() {\n  const query = `repo:${ REPO } author:${ FORK_OWNER } is:pr`;\n  const found = [];\n  let cursor = null;\n\n  for (;;) {\n    const page = await graphql(\n      `query($q: String!, $after: String) {\n         search(query: $q, type: ISSUE, first: 100, after: $after) {\n           pageInfo { hasNextPage endCursor }\n           nodes {\n             ... on PullRequest {\n               number url title headRefName state createdAt mergedAt body\n               author { login }\n             }\n           }\n         }\n       }`,\n      { q: query, after: cursor },\n    );\n    const { nodes = [], pageInfo = {} } = page.search || {};\n\n    found.push(...nodes.filter((n) => n && n.number));\n\n    if (!pageInfo.hasNextPage) {\n      break;\n    }\n\n    cursor = pageInfo.endCursor;\n  }\n\n  return found\n    .map((n) => ({\n      number:      n.number,\n      url:         n.url,\n      title:       n.title || '',\n      headRefName: n.headRefName || '',\n      status:      n.mergedAt ? 'merged' : (n.state === 'CLOSED' ? 'closed' : 'open'),\n      author:      n.author?.login || '',\n      createdAt:   n.createdAt,\n      mergedAt:    n.mergedAt || null,\n      videoUrl:    videoIn(n.body || ''),\n    }))\n    .filter((p) => p.status !== 'closed');\n}\n\nasync function graphql(query, variables) {\n  let resp;\n\n  try {\n    resp = await fetch(`${ API }/graphql`, {\n      method:  'POST',\n      headers: { ...HEADERS, 'Content-Type': 'application/json' },\n      body:    JSON.stringify({ query, variables }),\n    });\n  } catch (e) {\n    throw new Error(`searching for our pull requests: ${ e?.message || e }`, { cause: e });\n  }\n\n  const text = await resp.text();\n\n  if (!resp.ok) {\n    throw new Error(`searching for our pull requests: HTTP ${ resp.status } - ${ text.slice(0, 300) }`);\n  }\n\n  const body = JSON.parse(text);\n\n  // GraphQL answers 200 with an `errors` array, so a failure here looks like a success to\n  // anything that only checks the status.\n  if (body.errors?.length) {\n    throw new Error(`searching for our pull requests: ${ body.errors.map((e) => e.message).join('; ') }`);\n  }\n\n  return body.data;\n}\n\n/** Dependabot's own open pull requests - the \"there is already one upstream\" signal. */\nasync function fetchDependabotPulls() {\n  const open = await paginate(\n    `${ API }/repos/${ REPO }/pulls?state=open&per_page=100`,\n    'reading the open pull requests',\n  );\n\n  return open.map(normalisePr).filter((p) => p.author === 'dependabot[bot]');\n}\n\nasync function main() {\n  const [alerts, dependabotPrs, ourPrs] = await Promise.all([\n    fetchAlerts(), fetchDependabotPulls(), fetchOurPulls(),\n  ]);\n\n  // A transient failure must never be written as an empty snapshot. The console this replaces\n  // piped `gh api | jq ... || echo \"[]\"`, so a rate-limited page produced a valid 0-alert file\n  // that the watcher then reused for the rest of the day - the board simply went blank while\n  // the repository had 156 open alerts. Any failure above has already thrown; an empty result\n  // here is treated as one too.\n  if (!alerts.length) {\n    fail('no alerts came back at all. Refusing to write a snapshot that would blank the board.');\n  }\n\n  const snapshot = {\n    gatheredAt: new Date().toISOString(),\n    repo:       REPO,\n    alerts,\n    dependabotPrs,\n    ourPrs,\n  };\n\n  writeFileSync(OUT, JSON.stringify(snapshot));\n\n  const open = alerts.filter((a) => a.state === 'open').length;\n\n  process.stderr.write(\n    `gather: ${ alerts.length } alerts (${ open } open), ${ dependabotPrs.length } Dependabot pull requests, ${ ourPrs.length } of ours -> ${ OUT }\\n`,\n  );\n}\n\nmain().catch((e) => fail(e?.message || String(e)));\n","gather.sh":'#!/bin/sh\n# Refresh the board: gather the Dependabot picture and put it in the cluster.\n#\n# Deterministic from end to end - there is no agent in this path and nothing is judged. The old\n# console needed a watcher on somebody\'s laptop to do this on a timer; here it is a script the\n# extension runs in a pod it already has, and the result is a ConfigMap rather than a file in an\n# emptyDir that a pod rollout wiped.\n#\n# It resolves the token itself rather than being handed one. A token pasted into a page and\n# written through the exec socket is a token the browser held; read here, with the pod\'s own\n# ServiceAccount, at the moment it is needed, the browser never has it at all.\n#\n# usage: gather.sh <work-dir>\nset -e\n\nDIR=${1:?gather.sh needs a working directory}\nSEED=$(dirname "$0")\n\n[ -d "$DIR" ] || { echo "gather.sh: no such directory: $DIR" >&2; exit 2; }\n\nNS=vuln-console\nSECRET=settings\n# Extension Studio keeps an account\'s GitHub token under this exact name. Ours is preferred -\n# setting one here is somebody choosing it for this extension - and theirs is the fallback, so\n# nobody has to keep two copies of one token in step.\nSTUDIO_NS=extension-studio\nSTUDIO_SECRET=settings\n\n# kubectl as the pod rather than as whoever opened a terminal in it. shell.sh writes a kubeconfig\n# carrying the Rancher identity of the person who opened the pane, and that identity may not be\n# allowed to read these Secrets - the pod\'s ServiceAccount is. A KUBECONFIG naming nothing is\n# what sends kubectl to the in-cluster config.\nkube() {\n  KUBECONFIG=/dev/null kubectl "$@"\n}\n\nsecret_key() {\n  kube get secret "$2" -n "$1" -o "jsonpath={.data.$3}" 2>/dev/null | base64 -d 2>/dev/null | tr -d \'\\r\\n\'\n}\n\nGH_TOKEN=$(secret_key "$NS" "$SECRET" gh_token)\n[ -n "$GH_TOKEN" ] || GH_TOKEN=$(secret_key "$STUDIO_NS" "$STUDIO_SECRET" gh_token)\n\nif [ -z "$GH_TOKEN" ]; then\n  echo "gather.sh: no GitHub token is stored. Set one from the extension\'s Credentials dialog." >&2\n  exit 2\nfi\n\n# 0600 before anything is in it, so it is never briefly readable. Removed however this ends.\nCREDS="$DIR/creds.json"\nSNAPSHOT="$DIR/snapshot.json"\n\ncleanup() { rm -f "$CREDS"; }\ntrap cleanup EXIT INT TERM\n\n: > "$CREDS"\nchmod 600 "$CREDS"\n\n# Through node rather than printf, so a token containing a quote or a backslash is JSON-encoded\n# rather than pasted into a string and hoped for.\nGH_TOKEN="$GH_TOKEN" node -e \\\n  \'require("fs").writeFileSync(process.argv[1], JSON.stringify({ GH_TOKEN: process.env.GH_TOKEN }))\' \\\n  "$CREDS"\n\nCREDS_FILE="$CREDS" OUT="$SNAPSHOT" VULN_REPO="${VULN_REPO:-rancher/dashboard}" \\\n  FORK_OWNER="${FORK_OWNER:-marcelofukumoto}" node "$SEED/gather.mjs"\n\ncleanup\n\n# A ConfigMap holds a megabyte. A snapshot that does not fit has to fail as a gather somebody can\n# see, not as a kubectl error nobody reads.\nSIZE=$(wc -c < "$SNAPSHOT")\n\nif [ "$SIZE" -gt 950000 ]; then\n  echo "gather.sh: the snapshot is $((SIZE / 1024)) KiB, over what a ConfigMap can hold" >&2\n  exit 2\nfi\n\nkube get namespace "$NS" >/dev/null 2>&1 || kube create namespace "$NS" >/dev/null\n\n# Server-side apply, NOT the client-side default. A client-side `apply` records the whole object\n# it sent in a `kubectl.kubernetes.io/last-applied-configuration` annotation, and annotations are\n# capped at 256 KiB - so a snapshot large enough to be worth having is one the apiserver rejects\n# outright ("metadata.annotations: Too long"). Server-side apply keeps its bookkeeping in\n# managedFields instead, where there is no such limit.\n\nkube create configmap snapshot \\\n  --namespace "$NS" \\\n  --from-file=snapshot.json="$SNAPSHOT" \\\n  --dry-run=client -o json |\n  node -e \'\n    let raw = "";\n\n    process.stdin.on("data", (c) => (raw += c));\n    process.stdin.on("end", () => {\n      const cm = JSON.parse(raw);\n\n      cm.metadata.labels = { "vuln-console.rancher.io/owns": "true" };\n      process.stdout.write(JSON.stringify(cm));\n    });\n  \' |\n  kube apply --server-side --force-conflicts -f - >/dev/null\n\necho "gather.sh: published a $((SIZE / 1024)) KiB snapshot"\n',"job.sh":'#!/bin/sh\n# Record what a run achieved, as fields on its job.\n#\n# The agent calls this rather than composing kubectl itself, for the same reason the report\n# console has a publish script: the object name and the label are what the board selects on, so\n# a job written with a typo\'d label is a job that exists and cannot be found.\n#\n# It MERGES. Each call sets the fields it is given and leaves the rest, so recording a branch\n# early and a pull request later does not erase the branch - which is the failure mode of every\n# "write the whole file" version of this.\n#\n# usage: job.sh <library> phase=Done branch=... prNumber=123 message="..."\nset -e\n\nLIBRARY=${1:?job.sh needs the library}\nshift\n\nNS=vuln-console\n\nkube() { KUBECONFIG=/dev/null kubectl "$@"; }\n\n# The object name, by the same rule the extension uses: lowercased, anything that is not a\n# letter or a digit becomes a hyphen. `@scope/name` is not a legal object name and this is only\n# an address - the record keeps the real library name in a field.\nNAME=job-$(printf \'%s\' "$LIBRARY" | tr \'[:upper:]\' \'[:lower:]\' | sed \'s/[^a-z0-9]\\{1,\\}/-/g; s/^-*//; s/-*$//\' | cut -c1-50)\n\nCURRENT=$(kube get configmap "$NAME" -n "$NS" -o "jsonpath={.data.job\\.json}" 2>/dev/null || true)\n\n# Read the current record, apply the assignments, write it back. Through node because the values\n# are arbitrary text - a message can contain a quote, a URL contains slashes - and sed would\n# mangle both.\nUPDATED=$(CURRENT="$CURRENT" LIBRARY="$LIBRARY" node -e \'\n  const assignments = process.argv.slice(1);\n  let job = {};\n\n  try {\n    job = JSON.parse(process.env.CURRENT || "{}") || {};\n  } catch {\n    job = {};\n  }\n\n  job.library = job.library || process.env.LIBRARY;\n\n  // Numbers stay numbers and the empty string clears a field, so `prNumber=` is how a run says\n  // "there is no pull request" without writing the string "null" into the board.\n  for (const pair of assignments) {\n    const at = pair.indexOf("=");\n\n    if (at < 1) {\n      continue;\n    }\n\n    const key = pair.slice(0, at);\n    const raw = pair.slice(at + 1);\n\n    if (raw === "") {\n      job[key] = null;\n    } else if (/^(prNumber|startedAt|updatedAt)$/.test(key)) {\n      job[key] = Number(raw);\n    } else if (key === "vulnIds") {\n      job[key] = raw.split(",").map((n) => Number(n.trim())).filter((n) => !Number.isNaN(n));\n    } else {\n      job[key] = raw;\n    }\n  }\n\n  job.updatedAt = Date.now();\n  process.stdout.write(JSON.stringify(job));\n\' "$@")\n\n[ -n "$UPDATED" ] || { echo "job.sh: refusing to write an empty job" >&2; exit 1; }\n\nkube get namespace "$NS" >/dev/null 2>&1 || kube create namespace "$NS" >/dev/null\n\nprintf \'%s\' "$UPDATED" > /tmp/job-$$.json\n\nkube create configmap "$NAME" \\\n  --namespace "$NS" \\\n  --from-file=job.json=/tmp/job-$$.json \\\n  --dry-run=client -o json |\n  node -e \'\n    let raw = "";\n\n    process.stdin.on("data", (c) => (raw += c));\n    process.stdin.on("end", () => {\n      const cm = JSON.parse(raw);\n\n      cm.metadata.labels = { "vuln-console.rancher.io/owns": "true" };\n      process.stdout.write(JSON.stringify(cm));\n    });\n  \' |\n  kube apply --server-side --force-conflicts -f - >/dev/null\n\nrm -f /tmp/job-$$.json\n\necho "job.sh: recorded $*"\n',"pr.prompt.md":"# Open the pull request for a verified fix\n\nThe fix is made, pushed and verified. Open the pull request for it, and nothing else.\n\n## Rules that are not negotiable\n\n- **Open it as a draft, and leave it a draft.** The human marks it ready for review themselves.\n  Never run `gh pr ready`.\n- **Never write a bare `#<number>`** in the body. GitHub auto-links it to an unrelated pull\n  request or issue, which is a false cross-reference somebody then has to explain. Use a full URL\n  or a link label.\n- **No attribution trailers** and no reviewer names in the commit history.\n\n## What to do\n\n1. Confirm the branch is pushed and its diff is what you expect. Diff it against **upstream's**\n   default branch, never the fork's own — the fork's is chronically stale, and judging by it\n   makes a clean fix look like it carries half the repository with it.\n\n2. Open the draft pull request from the fix branch. Title it the way Dependabot does:\n   `Bump <library> from <old> to <new>` — the board reads the library out of that title, and a\n   title it cannot read is a row that loses its pull request.\n\n3. The body carries: what was bumped and in which manifests, a `### Vulnerabilities fixed`\n   section listing each advisory by GHSA id with a full URL, and a `### Screenshot/Video`\n   section. Put the verification video's URL **on its own line** — that is what makes GitHub\n   render a player rather than a link.\n\n4. Request the reviewers the board has selected, if any are given to you.\n\n5. Record it, including the alert ids this pull request covers — that record is what ties them\n   together afterwards, and it beats any guess the board could make from titles:\n\n```\n$ROOT/job.sh \"<library>\" phase=Done prUrl=<url> prNumber=<n> vulnIds=<id,id,id>\n```\n\n## What you must not do\n\n- do not mark it ready for review\n- do not merge it\n- do not comment on it\n","publish.prompt.md":'# Attach a staged recording to its pull request\n\nThe recording exists and the pull request exists; put one in the other.\n\n1. Upload the video as a GitHub user-attachment so it renders as an inline player. A release\n   asset link renders as a link, not a player, which is not what this is for.\n2. Splice it into the pull request body\'s `### Screenshot/Video` section, **on its own line**.\n3. Leave the staged copy alone until the upload is confirmed, then record the published URL:\n\n```\n$ROOT/job.sh "<library>" videoUrl=<github-url>\n```\n\nDo not comment on the pull request. Do not mark it ready for review.\n',"record.prompt.md":'# Capture the verification recording again\n\nThe fix exists but its recording does not, or needs redoing. Produce it, and change nothing else.\n\n1. Check out the fix branch in the workspace and let the supervised dev server compile it. Do not\n   start a second server.\n2. Grep the checkout for where the library is used and pick two to four real interactions that\n   exercise it.\n3. Drive them against the dev server through the browser already in this pod\n   (`$VULN_BROWSER_CDP`), connecting over CDP. Capture an annotated video and screenshots.\n4. Write the checks up as a numbered list, each ending ✅ or ❌, then a one-line verdict.\n5. Record:\n\n```\n$ROOT/job.sh "<library>" videoUrl=<path> infoUrl=<path> message="<one line verdict>"\n```\n\nDo not change the branch, do not open or edit a pull request.\n',"resolve-conflict.prompt.md":'# Rebase a fix branch onto the latest default branch\n\nThe pull request has drifted from its base, or conflicts with it. Bring it up to date.\n\n## What to do\n\n1. Fetch upstream and check whether the branch actually conflicts. GitHub computes mergeability\n   asynchronously, so a "conflict" flag can be one poll stale — look for yourself.\n2. If it is clean and merely behind, rebase it onto the latest default branch.\n3. If it conflicts, rebase and **re-apply the fix** rather than resolving by hand where the\n   resolution is a lockfile: regenerate the lockfiles from the rebased tree using the recipe in\n   the fix prompt, so what lands is a real resolution and not a merge artefact.\n4. Re-check that no vulnerable version remains and that the diff adds no new vulnerable package.\n5. Force-push **only this branch**, and only when it is the branch this job owns.\n6. Record:\n\n```\n$ROOT/job.sh "<library>" phase=Done message="<one line: rebased onto <sha>, lockfiles regenerated>"\n```\n\n## What you must not do\n\n- do not force-push any branch other than this one\n- do not comment on the pull request\n- do not mark it ready for review\n',"workspace/boot.sh":'#!/bin/sh\n# Bring one fix workspace up: a rancher/dashboard checkout with its dependencies installed and\n# the dev server supervised on top of it.\n#\n# The container\'s main process ends up being the supervisor, not the server, because a fix run\n# does things that kill a dev server - switches branch, regenerates a lockfile, runs a build -\n# and a pod whose main process was the server would restart on each of those and take the run\'s\n# terminal with it.\n#\n# `${install}`, `${repo}` and `${port}` are apps-plus substitutions, filled in when the App is\n# rendered. Everything written `$LIKE_THIS` is ordinary shell and is left alone: apps-plus only\n# substitutes the braced form, and only for names its App declares.\nset -e\n\n# Force apt onto IPv4 before anything runs one. The node has no working IPv6 route to the Debian\n# mirror, so an `apt-get update` left alone resolves an IPv6 address and hangs on it for minutes\n# while holding the apt lock - which is how a workspace comes up with no tmux and no terminal.\nmkdir -p /etc/apt/apt.conf.d\nprintf \'Acquire::ForceIPv4 "true";\\nAcquire::Retries "3";\\n\' > /etc/apt/apt.conf.d/99vuln-ipv4\n\nWS=/workspaces/${install}\nmkdir -p "$WS/.home"\nchown node:node "$WS" "$WS/.home" 2>/dev/null || true\n[ -f "$WS/.owned" ] || { chown -R node:node "$WS" 2>/dev/null; touch "$WS/.owned"; }\n\n# The browser sidecar mounts these as subPaths, and the kubelet creates a missing subPath as\n# root - leaving the seed unable to write into it.\nmkdir -p "$WS/.a11y/opt" "$WS/.a11y/init"\nchown -R node:node "$WS/.a11y" 2>/dev/null || true\n\n# What every workspace on this node shares rather than keeping its own copy. Measured before\n# this existed: Cypress 813 MB, yarn 749 MB, and a gigabyte of node_modules - most of a\n# workspace\'s 3 GB, per workspace.\nSHARED=/workspaces/.shared\nmkdir -p "$SHARED/yarn" "$SHARED/cypress" "$SHARED/npm" "$SHARED/template"\nchown node:node "$SHARED" "$SHARED/yarn" "$SHARED/cypress" "$SHARED/npm" "$SHARED/template" 2>/dev/null || true\n\n# tmux and kubectl, which the terminal needs. No claude: the conversation runs in the agents pod\n# and reaches this one through a tunnel, so a claude here is hundreds of megabytes of hostPath\n# that nothing executes.\n[ -f /seed/terminal-tools.sh ] \\\n  && (TOOLS_NO_CLAUDE=1 HOME_DIR="$WS/.home" /bin/sh /seed/terminal-tools.sh >"$WS/.terminal-tools.log" 2>&1 &) \\\n  || true\n\n# What a recording and a lockfile bump need and the image lacks. Backgrounded so the dev server\n# is not a minute later for it; the lock timeout lets it wait for the tmux install above rather\n# than colliding with it.\n(command -v ffmpeg >/dev/null 2>&1 && command -v lsof >/dev/null 2>&1) || (\n  apt-get -o DPkg::Lock::Timeout=300 update -qq \\\n    && DEBIAN_FRONTEND=noninteractive apt-get -o DPkg::Lock::Timeout=300 install -y -qq \\\n      ffmpeg jq lsof iproute2 >"$WS/.apt.log" 2>&1 &\n) || true\n\nexec setpriv --reuid=1000 --regid=1000 --init-groups /bin/sh /seed/checkout.sh\n',"workspace/checkout.sh":'#!/bin/sh\n# The workspace\'s checkout and install, as the node user. Split from boot.sh so the privileged\n# half is short enough to read and this half can be run again by hand.\nset -e\n\nWS=/workspaces/${install}\nSHARED=/workspaces/.shared\n\nexport HOME="$WS/.home"\nexport YARN_CACHE_FOLDER="$SHARED/yarn"\nexport CYPRESS_CACHE_FOLDER="$SHARED/cypress"\nexport npm_config_cache="$SHARED/npm"\n\n# init + fetch rather than `git clone`, because clone refuses a non-empty directory and this one\n# is not reliably empty: the agent seed writes `.claude` into the checkout, and if that lands\n# first - a separate trigger racing this boot - a clone would fail and leave the pod serving\n# nothing.\nif [ ! -d "$WS/dashboard/.git" ]; then\n  mkdir -p "$WS/dashboard"\n  cd "$WS/dashboard"\n  git init -q\n  git remote add origin https://github.com/${repo} 2>/dev/null || true\n  DEFAULT=$(git ls-remote --symref origin HEAD | sed -n \'s@^ref: refs/heads/\\(.*\\)[[:space:]]HEAD@\\1@p\')\n  git fetch --depth 1 origin "$DEFAULT"\n  git checkout -f -B "$DEFAULT" FETCH_HEAD\nfi\n\ncd "$WS/dashboard"\n\n# The fork is where fixes are pushed, so it is a remote from the start rather than something the\n# agent has to remember to add. `upstream` is what a branch is diffed against - never the fork\'s\n# own master, which is chronically stale and makes a clean fix look like it carries half the\n# repository with it.\ngit remote add fork "https://github.com/${fork}" 2>/dev/null || true\ngit remote set-url fork "https://github.com/${fork}" 2>/dev/null || true\ngit remote add upstream "https://github.com/${repo}" 2>/dev/null || true\n\n# node_modules, hard-linked from a template of the same lockfile if this node already has one.\n# The files are the same inodes, so a second workspace\'s node_modules costs directory entries\n# and nothing else. Keyed by the lockfile hash, so a template is only ever used by a checkout\n# that would have installed exactly it.\nHASH=$(sha1sum yarn.lock 2>/dev/null | cut -c1-12)\n\nif [ -n "$HASH" ] && [ ! -d node_modules ] && [ -d "$SHARED/template/$HASH/node_modules" ]; then\n  cp -al "$SHARED/template/$HASH/node_modules" node_modules && touch .install-done\nfi\n\n[ -f .install-done ] || {\n  yarn install --mutex "file:$SHARED/yarn/.mutex" --network-timeout 600000\n  touch .install-done\n}\n\n# The first workspace to install a given lockfile leaves the template for the next one.\nif [ -n "$HASH" ] && [ -d node_modules ] && [ ! -d "$SHARED/template/$HASH" ]; then\n  mkdir -p "$SHARED/template/$HASH"\n  cp -al node_modules "$SHARED/template/$HASH/node_modules" || rm -rf "$SHARED/template/$HASH"\nfi\n\nexec /bin/bash /workspace-config/serve.sh ${port}\n',"workspace/serve.sh":'#!/bin/bash\n# The dev server, supervised: the container\'s process is this loop, not the server.\n#\n# A fix run does things that kill a dev server - switches branch, regenerates a lockfile, runs a\n# build. When the pod\'s main process was the server itself, each of those restarted the\n# container and took the run\'s terminal with it, mid-fix. So the server is restarted when it\n# exits, and left alone while something else holds the port.\nPORT=${1:-8005}\n\n# The boot script runs this from the checkout, so the tree is its parent - which is where the\n# log and the retarget file live.\nWS=${WS:-$(dirname "$PWD")}\nRETARGET=$WS/.dev-server.env\nLOG=$WS/.dev-server.log\n\necho "$PORT" > "$WS/.dev-server.port" 2>/dev/null || true\nexport PORT WS RETARGET LOG IONICE\n\nheld() {\n  node -e "require(\'net\').connect(Number(process.argv[1]),\'127.0.0.1\').on(\'connect\',()=>process.exit(0)).on(\'error\',()=>process.exit(1))" "$PORT" 2>/dev/null\n}\n\n# Not all at once. Rancher\'s embedded k3s runs beside these servers, and when it restarts every\n# workspace pod restarts with it and every webpack compiles at the same moment: a dozen of them\n# on twelve cores, k3s\'s controllers fail to renew their lease, k3s exits, Rancher restarts -\n# and the herd forms again. So a server waits, with jitter so they do not all wake together,\n# while the node\'s one-minute load is above what the cores can carry.\nCORES=$(nproc 2>/dev/null || echo 4)\ngate() {\n  load=$(cut -d. -f1 /proc/loadavg 2>/dev/null || echo 0)\n  [ "$load" -le $(( CORES * 3 / 2 )) ]\n}\n\n# One dev server per pod, whoever started it. A second webpack is another two gigabytes at every\n# rebuild and the pod\'s limit is five, so an agent that starts its own gets the port until it is\n# done and this one comes back afterwards. Only node processes, matched by command line: a shell\n# or a grep that merely mentions the server would otherwise count and stop this one for nothing.\nservers() { pgrep -f "^[^ ]*node .*vue-cli-service serve" 2>/dev/null; }\nforeign() {\n  local p\n  for p in $(servers); do\n    [ "$(ps -o pgid= -p "$p" 2>/dev/null | tr -d \' \')" = "$SERVER" ] || echo "$p"\n  done\n}\n\nSERVER=""\nstop_ours() {\n  if [ -n "$SERVER" ]; then\n    kill -TERM -- -"$SERVER" 2>/dev/null\n    wait "$SERVER" 2>/dev/null\n    SERVER=""\n  fi\n}\ntrap "stop_ours; exit 0" TERM INT\n\nwhile :; do\n  if held || [ -n "$(foreign)" ]; then sleep 10; continue; fi\n\n  sleep $(( RANDOM % 20 ))\n\n  waited=0\n  while ! gate && [ "$waited" -lt 300 ]; do\n    [ "$waited" -eq 0 ] && echo "[workspace] the node is busy; waiting to start the dev server"\n    sleep 10\n    waited=$((waited + 10))\n  done\n\n  # Below the cluster\'s own processes: a compile that takes every core has taken k3s down with\n  # it on a busy node, and a slow first page is the better failure.\n  IONICE=""\n  command -v ionice >/dev/null 2>&1 && IONICE="ionice -c 3"\n\n  # What the server runs against can be changed without a new pod: writing the retarget file\n  # restarts it onto a different Rancher. Read in the server\'s own shell, so it replaces the\n  # pod\'s environment for the server alone.\n  STAMP=$(stat -c %Y "$RETARGET" 2>/dev/null || echo 0)\n  [ -f "$RETARGET" ] && echo "[workspace] the dev server runs against $(grep \'^API=\' "$RETARGET" | cut -d= -f2-)"\n\n  # setsid, so the server and everything it starts are one process group a stop can signal as a\n  # whole; tee, so the log is in the tree as well as in the pod\'s.\n  setsid bash -c \'if [ -f "$RETARGET" ]; then set -a; . "$RETARGET"; set +a; fi; nice -n 15 $IONICE env VUE_CLI_SERVICE_CONFIG_PATH=/workspace-config/vue.config.js yarn dev --port "$PORT" 2>&1 | tee "$LOG"\' &\n  SERVER=$!\n\n  # `sleep & wait`, not `sleep`: a trap runs once the foreground command returns, and a stop\n  # that waited out the sleep left the server running for the check that followed.\n  while kill -0 "$SERVER" 2>/dev/null; do\n    sleep 10 & wait $!\n    if [ -n "$(foreign)" ]; then\n      echo "[workspace] another dev server is running in this pod; standing down until it is gone"\n      stop_ours; sleep 10; continue 2\n    fi\n    if [ "$(stat -c %Y "$RETARGET" 2>/dev/null || echo 0)" != "$STAMP" ]; then\n      echo "[workspace] the dev server\'s target changed; restarting it"\n      stop_ours; continue 2\n    fi\n  done\n\n  SERVER=""\n  echo "[workspace] the dev server exited; starting it again in 5s"\n  sleep 5\ndone\n',"workspace/vue.config.js":"// Written by the Dev extension. See pkg/dev-extension/workspace-config.ts.\n//\n// This dev server is reached one of two ways, and everything below is the consequence of which.\n//\n//   - behind the apiserver's service proxy, at DEV_PROXY_PATH, on Rancher's own origin. The Host\n//     header is the proxy's, the TLS the browser sees is Rancher's, the prefix is stripped on the\n//     way in and has to be put back on every URL handed out.\n//   - at an origin of its own, on a node port, with no prefix at all.\n//\n// A dev server built for one does not work at the other: the router base is baked in. Which one\n// this is comes from DEV_PROXY_PATH being set or empty, and changing it restarts the pod.\n//\n// The second mode exists because of what the first one does to a dashboard. rancher/dashboard\n// builds its API URLs absolute from the page origin, so behind the prefix its /v1 and /v3 calls\n// never reach this dev server at all: they go to the Rancher this page was served from, using\n// that session, against that cluster. Measured, on a workspace with a Rancher of its own: two\n// requests arrived here and a hundred and thirty-six went to the host. An origin of its own is\n// the only thing that fixes it, because there is no browser-side API base to set.\nconst proxyPath = (process.env.DEV_PROXY_PATH || '').replace(/\\/$/, '');\n\n// The prefix on every in-app link, which has to be the path the page was loaded at. Set before\n// the shell's config runs, because the shell reads it out of the environment as it does.\nprocess.env.ROUTER_BASE = proxyPath ? proxyPath + '/' : '/';\n\n// The repo's own config, untouched in the checkout, rather than a reimplementation of it.\n// rancher/dashboard is a monorepo whose shell is ./shell rather than a node_modules package,\n// and which arguments its root config passes to that shell is the repo's business and changes\n// with the repo. Wrapping it means this file only has to know about the proxy. This file is\n// handed to vue-cli-service as VUE_CLI_SERVICE_CONFIG_PATH (see apps.ts, WORKSPACE_SCRIPT), so\n// it lives outside the checkout and the checkout's own vue.config.js is never rewritten: the\n// tree stays clean, a branch switch never trips over it, and nothing an agent commits carries it.\nconst base = require(require('path').join(process.cwd(), 'vue.config.js'));\n\n// The proxy rewrites absolute URLs in HTML as they pass through, which is what lets a naive UI\n// work behind it at all, so an asset URL that already carried the prefix would carry it twice.\n// index.html therefore asks for '/js/...' and lets the proxy add the prefix, while webpack's\n// runtime works its own base out from the script URL it was loaded from.\nbase.publicPath = 'auto';\n\nconst previousChainWebpack = base.chainWebpack;\n\nbase.chainWebpack = (webpackConfig) => {\n  if (typeof previousChainWebpack === 'function') {\n    previousChainWebpack(webpackConfig);\n  }\n\n  webpackConfig.plugin('html-index').tap((args) => {\n    args[0].publicPath = '/';\n\n    return args;\n  });\n};\n\nbase.devServer = {\n  ...base.devServer,\n\n  // Plain http only behind the proxy, where Rancher terminates TLS and will not talk to the\n  // shell's self-signed dev certificate on the way through.\n  //\n  // At its own origin the browser talks to this server directly, and http there is not a smaller\n  // problem than a certificate warning, it is a broken login: the shipped proxy sets\n  // x-forwarded-proto: https on every request it makes, so Rancher issues its session cookie with\n  // Secure, and a browser on http throws that cookie away. What that looks like is a login that\n  // returns 200 and a page that bounces straight back to the login form, which is what it did.\n  // So https here, with the shell's own certificate, and a warning to click through once.\n  ...(proxyPath ? { server: { type: 'http' } } : {}),\n\n  // Requests arrive with whatever Host the proxy chain last set, never this server's own.\n  // Without this the proxy answers 'Invalid Host header'.\n  allowedHosts: 'all',\n\n  // The proxy cannot rewrite URLs in a compressed body: the response comes back as a stream\n  // error and the browser shows a blank page.\n  compress: false,\n\n  // The prefix is stripped before a request lands here, so everything is served from the root.\n  devMiddleware: { publicPath: '/' },\n  static:        { publicPath: '/' },\n\n  // A deep link into the dashboard has to load the app rather than 404. disableDotRule matters\n  // in Rancher, where a route routinely carries a resource name with dots in it.\n  historyApiFallback: {\n    index:             '/index.html',\n    disableDotRule:    true,\n    htmlAcceptHeaders: ['text/html', 'application/xhtml+xml']\n  },\n\n  hot:             true,\n  webSocketServer: { type: 'ws', options: { path: '/ws' } },\n  client:          {\n    // Hot reload rides the same proxy when there is one, and the page's own origin when there is\n    // not. Either way this has to be written out, because the shell's config has an address of its\n    // own in it and that address is not where this server is.\n    //\n    // Behind the proxy only the path is ours; the rest are webpack-dev-server's \"infer it from\n    // window.location\" sentinels. 'auto' is one the *server* substitutes as it serves the client.\n    //\n    // Served at its own origin the sentinels are still needed, and leaving them out was a real\n    // bug rather than a tidy default. The shell's own config sets a fixed\n    // wss://localhost:8005/ws, which is right on a laptop running the dev server and wrong\n    // everywhere else: the browser then opened a hot-reload socket to whatever was on port 8005\n    // of the machine the browser is on, and displayed that server's compile errors over this\n    // dashboard. Seen here as a webpack overlay full of a completely different project's module\n    // resolution failures, sitting on top of the workspace's login form and swallowing clicks.\n    //\n    // 0.0.0.0 and 0 are webpack-dev-server's own \"take it from window.location\" sentinels. The\n    // protocol is written out rather than left as 'auto', because 'auto' is substituted by the\n    // server as it serves the client and is handed to the WebSocket constructor unchanged when\n    // the page was loaded directly. This server is https at its own origin, so wss.\n    webSocketURL: proxyPath ? {\n      protocol: 'auto',\n      hostname: '0.0.0.0',\n      port:     0,\n      pathname: proxyPath + '/ws'\n    } : {\n      protocol: 'wss',\n      hostname: '0.0.0.0',\n      port:     0,\n      pathname: '/ws'\n    },\n    // The compile-error overlay would cover the whole framed dashboard for whoever is looking\n    // when an edit fails to compile.\n    overlay: false\n  }\n};\n\nmodule.exports = base;\n"},ge="/workspace/.vuln-console",we="1000:1000",be="/workspace/conversations",ye="/workspace/.home",ke={fix:"fix.prompt.md",pr:"pr.prompt.md",record:"record.prompt.md",publish:"publish.prompt.md",addresscomment:"address-comments.prompt.md",resolveconflict:"resolve-conflict.prompt.md"},$e={fix:"Fix",pr:"Open the pull request for",record:"Record the verification for",publish:"Attach the recording for",addresscomment:"Address the review comments on",resolveconflict:"Rebase"};function xe(e){const n=N();return{pod:e,namespace:(null===n||void 0===n?void 0:n.agent.namespace)||"extension-studio",container:(null===n||void 0===n?void 0:n.agent.container)||"agent"}}function _e(e){return["#!/bin/sh","# Written by the vulnerability console. Runs one command inside a fix workspace.","set -e",`NS=${U(e)}`,`WS=${U(`${E}/${e}`)}`,"DIR=${PWD}",'KUBECONFIG=/dev/null exec kubectl exec -i -n "$NS" "deploy/$NS" -c workspace -- \\',"  setpriv --reuid=1000 --regid=1000 --init-groups \\",'  /usr/bin/env HOME="$WS/.home" WSD="$WS" DIR="$DIR" \\','  /bin/bash -lc \'cd "$DIR" 2>/dev/null || cd "$WSD/dashboard"; PATH="$WSD/bin:$PATH"; eval "$1"\' bash "$1"',""].join("\n")}function Ee(e,n,t,a){const o=((null===t||void 0===t?void 0:t.vulns)||[]).filter(e=>"open"===e.state).map(e=>`#${e.id} (${e.severity}${e.patched?`, needs ${e.patched}`:", no fix published"}) in ${e.manifest}`);return[`${$e[e]} the dependency **${n}** in ${v}.`,"",`Read ${ge}/${ke[e]} IN FULL before you do anything. It is the specification`,"for this action and it is authoritative.","","Facts for this run:",`  library        ${n}`,`  workspace      ${a.workspace} (your commands already run inside it)`,`  checkout       ${E}/${a.workspace}/dashboard`,`  push to        ${f} (remote "fork")`,`  pull requests  ${g}`,a.branch?`  branch         ${a.branch}`:"",a.prNumber?`  pull request   ${a.prNumber}`:"",o.length?`  open alerts    ${o.join("; ")}`:"","","Enumerate the affected manifests from the LIVE alerts, not from the list above - this board","can be hours old and a missed lockfile is the most common thing a reviewer catches.","",`Record what you did by writing the job: ${ge}/job.sh ${U(n)} <field>=<value> ...`,"Call it when you finish and whenever something durable happens (a branch, a preview, a pull","request). If you cannot finish, record why:",`  ${ge}/job.sh ${U(n)} phase=Failed message="one line saying what went wrong"`,"","Then stop."].filter(e=>""!==e).join("\n")}async function Se(e,n){const t=["job.sh",...Object.values(ke)];for(const a of t){const n=fe[a];if(!n)throw new Error(`This build is missing its ${a} - run "yarn gen-seed" and rebuild.`);await G(e,`${ge}/${a}`,n,{mode:"644",owner:we})}await G(e,`${ge}/shell-${n}.sh`,_e(n),{mode:"755",owner:we}),await j(e,["/bin/sh","-c",`chown ${we} ${ge} 2>/dev/null || true`],{timeoutMs:15e3})}async function Re(e){const{store:n,library:t,action:a,group:o=null,by:r}=e,s=N();if(!s)throw new Error("The Agents extension is not available on this page, so there is no agent to do the work.");const i=(await re()).find(e=>e.library===t&&"Running"===e.phase);if(i&&Date.now()-i.updatedAt<9e5)throw new Error(`${t} already has a run going. Stop it first, or wait for it to finish.`);const l=await s.agent.pod();if(!l)throw new Error("The agent pod is not running, so there is nowhere to do the work.");const d=(await re()).find(e=>e.library===t),c=Date.now(),h={library:t,phase:"Running",action:a,by:r||"unknown",startedAt:c,updatedAt:c,sessionId:null,workspace:ue(t),branch:(null===d||void 0===d?void 0:d.branch)||null,previewUrl:(null===d||void 0===d?void 0:d.previewUrl)||null,prUrl:(null===d||void 0===d?void 0:d.prUrl)||null,prNumber:(null===d||void 0===d?void 0:d.prNumber)||null,videoUrl:(null===d||void 0===d?void 0:d.videoUrl)||null,infoUrl:(null===d||void 0===d?void 0:d.infoUrl)||null,replyUrl:(null===d||void 0===d?void 0:d.replyUrl)||null,message:null,vulnIds:(null===d||void 0===d?void 0:d.vulnIds)||[]};await se(h);try{const e=await ve(n,t),r=xe(l);await Se(r,e);const i=await s.agent.startInProject(T(`${e}-${a}-${c}`),`${$e[a]} ${t}`,Ee(a,t,o,{...h,workspace:e})),d={...h,workspace:e,sessionId:i,updatedAt:Date.now()};return await se(d),await q(r,["/bin/sh /seed/shell.sh",U(i),U(be),U(ye),"start",U(`${ge}/shell-${e}.sh`)].join(" "),"start the conversation in the agent pod",12e4),d}catch(u){const e=(null===u||void 0===u?void 0:u.message)||String(u);throw await se({...h,phase:"Failed",message:e,updatedAt:Date.now()}).catch(()=>{}),new Error(e,{cause:u})}}async function Te(e){const n=N();n&&e.sessionId&&await n.agent.end(e.sessionId).catch(()=>{}),await se({...e,phase:"Cancelled",message:"Stopped.",sessionId:null,updatedAt:Date.now()})}function Ae(e,n=Date.now()){return"Running"===e.phase&&n-e.updatedAt>9e5}const Ne={class:"steps"},Ce={key:0,class:"steps__nofix",title:"No patched version is available — Dependabot has no fix for this advisory, so there is nothing to bump to."},De=["href","title"],Oe=["href"],Pe=["href"],Be=["href"];var He=(0,a.defineComponent)({__name:"StepPills",props:{row:{},job:{},busy:{type:Boolean}},emits:["act","stop","session"],setup(e,{emit:n}){const t=e,o=n,r=(0,a.computed)(()=>t.job),i=(0,a.computed)(()=>{var e;return"Running"===(null===(e=r.value)||void 0===e?void 0:e.phase)&&!Ae(r.value)}),l=(0,a.computed)(()=>!!r.value&&Ae(r.value)),d=(0,a.computed)(()=>{var e;if(null===(e=r.value)||void 0===e?void 0:e.prUrl)return{url:r.value.prUrl,number:r.value.prNumber};const n=t.row.pr;return"open"===(null===n||void 0===n?void 0:n.status)?{url:n.url,number:n.number}:null}),c=(0,a.computed)(()=>{var e,n;return(null===(e=r.value)||void 0===e?void 0:e.branch)||("open"===(null===(n=t.row.pr)||void 0===n?void 0:n.status)?t.row.pr.headRefName:null)||null}),h=(0,a.computed)(()=>{var e,n;return(null===(e=r.value)||void 0===e?void 0:e.videoUrl)||(null===(n=t.row.pr)||void 0===n?void 0:n.videoUrl)||null}),u=(0,a.computed)(()=>!!h.value&&/^https?:\/\//.test(h.value)),p=(0,a.computed)(()=>t.row.unfixable),m=(0,a.computed)(()=>c.value?`https://github.com/${f}/tree/${c.value}`:null);return(n,t)=>{var v,f;return(0,a.openBlock)(),(0,a.createElementBlock)("div",Ne,[p.value?((0,a.openBlock)(),(0,a.createElementBlock)("span",Ce," No fix published ")):((0,a.openBlock)(),(0,a.createElementBlock)(a.Fragment,{key:1},[i.value?((0,a.openBlock)(),(0,a.createElementBlock)(a.Fragment,{key:0},[(0,a.createVNode)(s.A,{variant:"secondary",size:"small",onClick:t[0]||(t[0]=e=>o("session"))},{default:(0,a.withCtx)(()=>{var e;return[t[11]||(t[11]=(0,a.createElementVNode)("span",{class:"steps__spin"},null,-1)),(0,a.createElementVNode)("span",null,(0,a.toDisplayString)("fix"===(null===(e=r.value)||void 0===e?void 0:e.action)?"Fixing":"Working")+"…",1)]}),_:1}),(0,a.createVNode)(s.A,{variant:"secondary",size:"small","data-testid":"vc-stop",onClick:t[1]||(t[1]=e=>o("stop"))},{default:(0,a.withCtx)(()=>[...t[12]||(t[12]=[(0,a.createElementVNode)("span",null,"Stop",-1)])]),_:1})],64)):l.value?((0,a.openBlock)(),(0,a.createElementBlock)(a.Fragment,{key:1},[t[15]||(t[15]=(0,a.createElementVNode)("span",{class:"steps__stalled",title:"This run has not reported for a while. The tab that started it may have been closed, or its pod replaced."}," Stalled ",-1)),(0,a.createVNode)(s.A,{variant:"secondary",size:"small",onClick:t[2]||(t[2]=e=>o("session"))},{default:(0,a.withCtx)(()=>[...t[13]||(t[13]=[(0,a.createElementVNode)("span",null,"Session",-1)])]),_:1}),(0,a.createVNode)(s.A,{variant:"secondary",size:"small",onClick:t[3]||(t[3]=e=>o("stop"))},{default:(0,a.withCtx)(()=>[...t[14]||(t[14]=[(0,a.createElementVNode)("span",null,"Stop",-1)])]),_:1})],64)):((0,a.openBlock)(),(0,a.createElementBlock)(a.Fragment,{key:2},[m.value?((0,a.openBlock)(),(0,a.createElementBlock)("a",{key:0,class:"steps__done",href:m.value,target:"_blank",rel:"noopener",title:`fork branch: ${c.value}`},(0,a.toDisplayString)(c.value),9,De)):((0,a.openBlock)(),(0,a.createBlock)(s.A,{key:1,variant:"primary",size:"small",disabled:e.busy,"data-testid":"vc-fix",onClick:t[4]||(t[4]=e=>o("act","fix"))},{default:(0,a.withCtx)(()=>[...t[16]||(t[16]=[(0,a.createElementVNode)("span",null,"Fix",-1)])]),_:1},8,["disabled"])),d.value?((0,a.openBlock)(),(0,a.createElementBlock)("a",{key:2,class:"steps__done",href:d.value.url,target:"_blank",rel:"noopener"}," Pull request "+(0,a.toDisplayString)(d.value.number),9,Oe)):c.value?((0,a.openBlock)(),(0,a.createBlock)(s.A,{key:3,variant:"secondary",size:"small",disabled:e.busy,onClick:t[5]||(t[5]=e=>o("act","pr"))},{default:(0,a.withCtx)(()=>[...t[17]||(t[17]=[(0,a.createElementVNode)("span",null,"Create pull request",-1)])]),_:1},8,["disabled"])):(0,a.createCommentVNode)("",!0),u.value?((0,a.openBlock)(),(0,a.createElementBlock)("a",{key:4,class:"steps__done",href:h.value||"#",target:"_blank",rel:"noopener"}," Recording ",8,Pe)):h.value&&d.value?((0,a.openBlock)(),(0,a.createBlock)(s.A,{key:5,variant:"secondary",size:"small",disabled:e.busy,onClick:t[6]||(t[6]=e=>o("act","publish"))},{default:(0,a.withCtx)(()=>[...t[18]||(t[18]=[(0,a.createElementVNode)("span",null,"Add recording to pull request",-1)])]),_:1},8,["disabled"])):c.value||d.value?((0,a.openBlock)(),(0,a.createBlock)(s.A,{key:6,variant:"secondary",size:"small",disabled:e.busy,onClick:t[7]||(t[7]=e=>o("act","record"))},{default:(0,a.withCtx)(()=>[...t[19]||(t[19]=[(0,a.createElementVNode)("span",null,"Record",-1)])]),_:1},8,["disabled"])):(0,a.createCommentVNode)("",!0),d.value?((0,a.openBlock)(),(0,a.createElementBlock)(a.Fragment,{key:7},[(0,a.createVNode)(s.A,{variant:"secondary",size:"small",disabled:e.busy,onClick:t[8]||(t[8]=e=>o("act","addresscomment"))},{default:(0,a.withCtx)(()=>[...t[20]||(t[20]=[(0,a.createElementVNode)("span",null,"Address comments",-1)])]),_:1},8,["disabled"]),(0,a.createVNode)(s.A,{variant:"secondary",size:"small",disabled:e.busy,onClick:t[9]||(t[9]=e=>o("act","resolveconflict"))},{default:(0,a.withCtx)(()=>[...t[21]||(t[21]=[(0,a.createElementVNode)("span",null,"Rebase",-1)])]),_:1},8,["disabled"])],64)):(0,a.createCommentVNode)("",!0),(null===(v=r.value)||void 0===v?void 0:v.replyUrl)?((0,a.openBlock)(),(0,a.createElementBlock)("a",{key:8,class:"steps__done",href:r.value.replyUrl,target:"_blank",rel:"noopener",title:"the per-point reply this run staged — read it here and post it yourself; the console never comments for you"}," Reply draft ",8,Be)):(0,a.createCommentVNode)("",!0),(null===(f=r.value)||void 0===f?void 0:f.sessionId)?((0,a.openBlock)(),(0,a.createBlock)(s.A,{key:9,variant:"link",size:"small",onClick:t[10]||(t[10]=e=>o("session"))},{default:(0,a.withCtx)(()=>[...t[22]||(t[22]=[(0,a.createElementVNode)("span",null,"Session",-1)])]),_:1})):(0,a.createCommentVNode)("",!0)],64))],64))])}}});t(4763);const Ie=(0,h.A)(He,[["__scopeId","data-v-338a864c"]]);var Ve=Ie;function je(e,n){return`https://github.com/${e}/security/dependabot/${n}`}const We={class:"vuln-ids"},qe=["href","title"];var Ue=(0,a.defineComponent)({__name:"VulnIds",props:{vulns:{},repo:{}},setup(e){const n=e,t=(0,a.computed)(()=>n.vulns.map(e=>({id:e.id,url:je(n.repo,e.id),fixed:"open"!==e.state,class:"open"!==e.state?"vuln-id--fixed":`vuln-id--${e.severity}`,title:[e.ghsa,e.manifest,e.patched?`needs ${e.patched}`:"no fix published",e.state].filter(Boolean).join(" · ")})));return(e,n)=>((0,a.openBlock)(),(0,a.createElementBlock)("div",We,[((0,a.openBlock)(!0),(0,a.createElementBlock)(a.Fragment,null,(0,a.renderList)(t.value,e=>((0,a.openBlock)(),(0,a.createElementBlock)("a",{key:e.id,class:(0,a.normalizeClass)(["vuln-id",e.class]),href:e.url,target:"_blank",rel:"noopener",title:e.title},"#"+(0,a.toDisplayString)(e.id),11,qe))),128))]))}});t(2136);const Le=(0,h.A)(Ue,[["__scopeId","data-v-bdc6d74a"]]);var Me=Le,Fe=t(6725);const Ge={class:"agent-terminal"},ze={key:2,class:"agent-terminal__waiting"};var Ke=(0,a.defineComponent)({__name:"AgentTerminal",props:{session:{},mode:{default:"claude"}},emits:["state"],setup(e,{emit:n}){const t=e,o=n,s=(0,a.ref)(null),i=(0,a.ref)(!1),l=(0,a.computed)(()=>{var e,n;return(null===(n=null===(e=s.value)||void 0===e?void 0:e.terminal)||void 0===n?void 0:n.component)||null}),d=(0,a.computed)(()=>s.value&&t.session?s.value.agent.command(t.session,t.mode):null),c=(0,a.computed)(()=>i.value&&!l.value);return(0,a.onMounted)(async()=>{s.value=await O(15e3),i.value=!0,s.value||o("state","closed")}),(n,t)=>((0,a.openBlock)(),(0,a.createElementBlock)("div",Ge,[c.value?((0,a.openBlock)(),(0,a.createBlock)((0,a.unref)(r.l),{key:0,color:"warning",class:"agent-terminal__missing"},{default:(0,a.withCtx)(()=>[...t[1]||(t[1]=[(0,a.createTextVNode)(" Nothing here draws a terminal: install the ",-1),(0,a.createElementVNode)("b",null,"Agents",-1),(0,a.createTextVNode)(" extension, which brings the agent pod and the terminal every extension borrows, then reload. ",-1)])]),_:1})):l.value&&d.value?((0,a.openBlock)(),(0,a.createBlock)((0,a.resolveDynamicComponent)(l.value),{key:1,session:e.session,command:d.value,mode:e.mode,class:"agent-terminal__pane",onState:t[0]||(t[0]=e=>o("state",e))},null,40,["session","command","mode"])):((0,a.openBlock)(),(0,a.createElementBlock)("div",ze,[...t[2]||(t[2]=[(0,a.createElementVNode)("i",{class:"icon icon-spinner icon-spin"},null,-1),(0,a.createElementVNode)("span",null,"Waiting for the Agents extension…",-1)])]))]))}});t(6739);const Je=(0,h.A)(Ke,[["__scopeId","data-v-5584bb48"]]);var Ye=Je;const Xe={class:"session__id"},Ze={class:"session"},Qe={key:2,class:"session__note session__note--ended"},en={key:3,class:"session__note"};var nn=(0,a.defineComponent)({__name:"AgentSessionPanel",props:{job:{},onBack:{type:Function},width:{},height:{},triggerFocusTrap:{type:Boolean},closeOnRouteChange:{}},emits:["close"],setup(e,{emit:n}){const t=e,o=n,i=(0,a.ref)("");return(n,l)=>((0,a.openBlock)(),(0,a.createBlock)(Fe.A,{"aria-target":`the agent session for ${e.job.library}`,onClose:l[2]||(l[2]=e=>o("close"))},{title:(0,a.withCtx)(()=>[(0,a.createTextVNode)(" Agent session · "+(0,a.toDisplayString)(e.job.library)+" ",1),(0,a.createElementVNode)("span",Xe,(0,a.toDisplayString)(e.job.sessionId),1)]),body:(0,a.withCtx)(()=>[(0,a.createElementVNode)("div",Ze,[e.job.sessionId?((0,a.openBlock)(),(0,a.createBlock)(Ye,{key:e.job.sessionId||"none",session:e.job.sessionId||"",class:"session__terminal",onState:l[0]||(l[0]=e=>i.value=e)},null,8,["session"])):((0,a.openBlock)(),(0,a.createBlock)((0,a.unref)(r.l),{key:0,color:"warning"},{default:(0,a.withCtx)(()=>[...l[3]||(l[3]=[(0,a.createTextVNode)(" This run has no conversation yet — it is still being set up. ",-1)])]),_:1})),"closed"===i.value?((0,a.openBlock)(),(0,a.createElementBlock)("p",Qe," This conversation has ended — the run it belonged to is over, and the agent pod has released it. ")):e.job.sessionId?((0,a.openBlock)(),(0,a.createElementBlock)("p",en," Closing this drawer leaves the conversation running — it is the pod that holds it, not this page. ")):(0,a.createCommentVNode)("",!0)])]),"additional-actions":(0,a.withCtx)(()=>[e.onBack?((0,a.openBlock)(),(0,a.createBlock)(s.A,{key:0,variant:"secondary",size:"large","data-testid":"vc-session-back",onClick:l[1]||(l[1]=n=>{var a;return null===(a=t.onBack)||void 0===a?void 0:a.call(t,e.job)})},{default:(0,a.withCtx)(()=>[...l[4]||(l[4]=[(0,a.createTextVNode)(" Back to the board ",-1)])]),_:1})):(0,a.createCommentVNode)("",!0)]),_:1},8,["aria-target"]))}});t(5641);const tn=(0,h.A)(nn,[["__scopeId","data-v-09836064"]]);var an=tn;const on="vuln-console.rancher.io/gh-token",rn="barn.rancher.io/gh-token",sn={Accept:"application/json;as=PartialObjectMetadata;g=meta.k8s.io;v=v1"};function ln(e,n){return`${J}/namespaces/${e}/secrets/${n}`}async function dn(e,n){const t=await X(ln(e,n),{headers:sn}).catch(()=>null);return(null===t||void 0===t?void 0:t.metadata)||null}function cn(e){const n=new Set;return((null===e||void 0===e?void 0:e.managedFields)||[]).forEach(e=>{var t;Object.keys((null===(t=null===e||void 0===e?void 0:e.fieldsV1)||void 0===t?void 0:t["f:data"])||{}).forEach(e=>{e.startsWith("f:")&&n.add(e.slice(2))})}),[...n]}async function hn(){const[e,n]=await Promise.all([dn(m,w),dn(y,k)]),t=cn(e),a=cn(n),o=(null===e||void 0===e?void 0:e.annotations)||{},r=(null===n||void 0===n?void 0:n.annotations)||{},s="set"===o[on]||t.includes(b),i="set"===r[rn]||a.includes(b);return{gh:s?"ours":i?"studio":"none",unreadable:!1}}async function un(){const e=await dn(m,w);e||(await X(`${J}/namespaces`,{method:"POST",body:JSON.stringify({apiVersion:"v1",kind:"Namespace",metadata:{name:m}})}).catch(e=>{if(!/409|already exists|alreadyexists/i.test((null===e||void 0===e?void 0:e.message)||""))throw e}),await X(`${J}/namespaces/${m}/secrets`,{method:"POST",headers:sn,body:JSON.stringify({apiVersion:"v1",kind:"Secret",type:"Opaque",metadata:{name:w,namespace:m}})}).catch(e=>{if(!/409|already exists|alreadyexists/i.test((null===e||void 0===e?void 0:e.message)||""))throw e}))}function pn(e){return btoa(String.fromCharCode(...(new TextEncoder).encode(e)))}async function mn(e){const n={},t={};void 0!==e.ghToken&&(n[b]=""===e.ghToken?null:pn(e.ghToken),t[on]=""===e.ghToken?null:"set"),Object.keys(n).length&&(await un(),await X(ln(m,w),{method:"PATCH",headers:{"Content-Type":"application/merge-patch+json",...sn},body:JSON.stringify({metadata:{annotations:t},data:n})}))}function vn(e){return"none"!==e.gh}const fn=["placeholder"];var gn=(0,a.defineComponent)({__name:"CredentialsDialog",props:{status:{}},emits:["saved","close"],setup(e,{emit:n}){const t=e,o=n,i=(0,a.ref)(""),l=(0,a.ref)(!1),d=(0,a.ref)(""),c=(0,a.computed)(()=>t.status.gh);async function h(){if(i.value){l.value=!0,d.value="";try{await mn({ghToken:i.value}),i.value="",o("saved",await hn()),o("close")}catch(e){d.value=(null===e||void 0===e?void 0:e.message)||String(e)}finally{l.value=!1}}}return(e,n)=>((0,a.openBlock)(),(0,a.createBlock)(Fe.A,{"aria-target":"the GitHub token this extension uses",onClose:n[1]||(n[1]=e=>o("close"))},{title:(0,a.withCtx)(()=>[...n[2]||(n[2]=[(0,a.createTextVNode)(" Credentials ",-1)])]),body:(0,a.withCtx)(()=>[d.value?((0,a.openBlock)(),(0,a.createBlock)((0,a.unref)(r.l),{key:0,color:"error"},{default:(0,a.withCtx)(()=>[(0,a.createTextVNode)((0,a.toDisplayString)(d.value),1)]),_:1})):(0,a.createCommentVNode)("",!0),"studio"===c.value?((0,a.openBlock)(),(0,a.createBlock)((0,a.unref)(r.l),{key:1,color:"info"},{default:(0,a.withCtx)(()=>[...n[3]||(n[3]=[(0,a.createTextVNode)(" Using Extension Studio's GitHub token. Setting one here makes this extension use that one instead — worth doing only if it should differ. ",-1)])]),_:1})):"ours"===c.value?((0,a.openBlock)(),(0,a.createBlock)((0,a.unref)(r.l),{key:2,color:"success"},{default:(0,a.withCtx)(()=>[...n[4]||(n[4]=[(0,a.createTextVNode)(" A GitHub token is stored for this extension. ",-1)])]),_:1})):((0,a.openBlock)(),(0,a.createBlock)((0,a.unref)(r.l),{key:3,color:"warning"},{default:(0,a.withCtx)(()=>[...n[5]||(n[5]=[(0,a.createTextVNode)(" No GitHub token is stored, so the board cannot be refreshed and nothing can be fixed. ",-1)])]),_:1})),n[6]||(n[6]=(0,a.createElementVNode)("label",{class:"creds__label",for:"vc-gh-token"},"GitHub token",-1)),(0,a.withDirectives)((0,a.createElementVNode)("input",{id:"vc-gh-token","onUpdate:modelValue":n[0]||(n[0]=e=>i.value=e),type:"password",class:"creds__input",autocomplete:"off","data-testid":"vc-gh-token",placeholder:"none"===c.value?"ghp_…":"enter a new token to replace the stored one"},null,8,fn),[[a.vModelText,i.value]]),n[7]||(n[7]=(0,a.createElementVNode)("p",{class:"creds__note"},[(0,a.createTextVNode)(" It needs to read Dependabot alerts and open pull requests on the fork: the "),(0,a.createElementVNode)("code",null,"repo"),(0,a.createTextVNode)(" and "),(0,a.createElementVNode)("code",null,"security_events"),(0,a.createTextVNode)(" scopes on a classic token, or read access to Dependabot alerts plus read and write to contents and pull requests on a fine-grained one. ")],-1)),n[8]||(n[8]=(0,a.createElementVNode)("p",{class:"creds__note"}," It is stored in a Secret and read by the pod that needs it. This page never reads it back. ",-1))]),"additional-actions":(0,a.withCtx)(()=>[(0,a.createVNode)(s.A,{variant:"primary",disabled:!i.value||l.value,"data-testid":"vc-save-token",onClick:h},{default:(0,a.withCtx)(()=>[(0,a.createElementVNode)("span",null,(0,a.toDisplayString)(l.value?"Saving…":"ours"===c.value?"Replace":"Save"),1)]),_:1},8,["disabled"])]),_:1}))}});t(5724);const wn=(0,h.A)(gn,[["__scopeId","data-v-12cdeb6e"]]);var bn=wn;const yn={key:0,class:"shipped__empty"},kn={class:"shipped__body"},$n={class:"shipped__lib"},xn=["href"];var _n=(0,a.defineComponent)({__name:"ShippedDrawer",props:{rows:{},repo:{}},emits:["close"],setup(e,{emit:n}){const t=n;return(n,o)=>((0,a.openBlock)(),(0,a.createBlock)(Fe.A,{"aria-target":"the vulnerabilities we have fixed",onClose:o[0]||(o[0]=e=>t("close"))},{title:(0,a.withCtx)(()=>[...o[1]||(o[1]=[(0,a.createTextVNode)(" Shipped ",-1)])]),body:(0,a.withCtx)(()=>[o[2]||(o[2]=(0,a.createElementVNode)("p",{class:"shipped__sub"}," Fixes of ours that merged and whose Dependabot alert has since closed. They age out of this list on their own as GitHub stops reporting them. ",-1)),e.rows.length?(0,a.createCommentVNode)("",!0):((0,a.openBlock)(),(0,a.createElementBlock)("p",yn," Nothing shipped yet. ")),((0,a.openBlock)(!0),(0,a.createElementBlock)(a.Fragment,null,(0,a.renderList)(e.rows,n=>((0,a.openBlock)(),(0,a.createElementBlock)("div",{key:n.library,class:"shipped__row"},[(0,a.createVNode)(p,{severity:n.severity},null,8,["severity"]),(0,a.createElementVNode)("div",kn,[(0,a.createElementVNode)("div",$n,(0,a.toDisplayString)(n.library),1),(0,a.createVNode)(Me,{vulns:n.vulns,repo:e.repo},null,8,["vulns","repo"])]),n.pr?((0,a.openBlock)(),(0,a.createElementBlock)("a",{key:0,class:"shipped__pr",href:n.pr.url,target:"_blank",rel:"noopener"},(0,a.toDisplayString)(n.pr.number),9,xn)):(0,a.createCommentVNode)("",!0)]))),128))]),_:1}))}});t(3274);const En=(0,h.A)(_n,[["__scopeId","data-v-589dde70"]]);var Sn=En;const Rn=["critical","high","medium","low"],Tn=36e5,An=6048e5,Nn=432e5;function Cn(e){const n=Rn.indexOf(e);return-1===n?Rn.length:n}function Dn(e){if(!e)return null;const n=Date.parse(e);return Number.isNaN(n)?null:n}function On(e){const n=/^Bump\s+([@/A-Za-z0-9._-]+)/.exec(e.title||"");return n?n[1]:null}function Pn(e){const n=e.replace(/^@/,"");return[...new Set([e,n,n.replace(/\//g,"-")])]}function Bn(e,n){const t=Pn(n),a=On(e);if(a)return t.includes(a);const o=e.headRefName||"";return t.some(e=>o.endsWith(`/${e}`)||o.includes(`/${e}-`))}function Hn(e,n,t){var a;const o=n.locks||{};for(const[i,l]of Object.entries(o))if(l.includes(e.id)){const e=t.find(e=>e.number===Number(i));if(e)return e}const r=t.filter(n=>Bn(n,e.library)),s=null!==(a=n.now)&&void 0!==a?a:Date.now();if("open"===e.state){const e=r.find(e=>"open"===e.status);if(e)return e;const n=r.filter(e=>"merged"===e.status).filter(e=>{const n=Dn(e.mergedAt);return null!==n&&s-n<=Nn}).sort((e,n)=>(Dn(n.mergedAt)||0)-(Dn(e.mergedAt)||0))[0];return n||null}if("fixed"===e.state){const n=Dn(e.fixedAt);return null===n?null:r.filter(e=>"merged"===e.status).filter(e=>{const t=Dn(e.mergedAt);return null!==t&&(n>=t-Tn&&n<=t+An)}).sort((e,n)=>(Dn(n.mergedAt)||0)-(Dn(e.mergedAt)||0))[0]||null}return null}function In(e){const n=new Map;for(const a of e){const e=n.get(a.alert.library);e?e.push(a):n.set(a.alert.library,[a])}const t=[];for(const[a,o]of n){const e=o.map(e=>e.alert).sort((e,n)=>n.id-e.id),n=e.map(e=>e.severity).sort((e,n)=>Cn(e)-Cn(n))[0]||"low";t.push({library:a,severity:n,pr:o.map(e=>e.pr).find(e=>null!==e)||null,vulns:e,unfixable:e.length>0&&e.every(e=>!e.patched)})}return t.sort((e,n)=>Cn(e.severity)-Cn(n.severity)||e.library.localeCompare(n.library))}function Vn(e){const{snapshot:n}=e,t=n.ourPrs||[],a=(n.alerts||[]).map(n=>({alert:n,pr:Hn(n,e,t)})),o=a.filter(e=>{var n;return"open"===e.alert.state&&"open"!==(null===(n=e.pr)||void 0===n?void 0:n.status)}),r=a.filter(e=>{var n;return"open"===e.alert.state&&"open"===(null===(n=e.pr)||void 0===n?void 0:n.status)}),s=a.filter(e=>{var n;return"open"!==e.alert.state&&"merged"===(null===(n=e.pr)||void 0===n?void 0:n.status)}),i={openNoPr:In(o),openPrOpen:In(r),prMerged:In(s)};return{generatedAt:n.gatheredAt,lists:i,counts:{openNoPr:o.length,openPrOpen:r.length,prMerged:s.length}}}function jn(e){var n;return"merged"===(null===(n=e.pr)||void 0===n?void 0:n.status)&&e.vulns.some(e=>"open"===e.state)}const Wn="/workspace/.vuln-console",qn="1000:1000",Un=18e4;async function Ln(){const e=N();if(!e)throw new Error("The Agents extension is not available on this page, so there is no pod to gather in.");const n=await e.agent.pod();if(!n)throw new Error("The agent pod is not running, so there is nowhere to gather.");const t={pod:n,namespace:e.agent.namespace||"extension-studio",container:e.agent.container||"agent"};for(const o of["gather.mjs","gather.sh"]){const e=fe[o];if(!e)throw new Error(`This build is missing its ${o} - run "yarn gen-seed" and rebuild.`);await G(t,`${Wn}/${o}`,e,{mode:"644",owner:qn})}const a=`${Wn}/gather`;await q(t,`mkdir -p ${U(a)} && chown ${qn} ${U(Wn)} ${U(a)} 2>/dev/null || true`,"make the gather directory in the agent pod",2e4),await q(t,`sh ${U(`${Wn}/gather.sh`)} ${U(a)}`,"gather the Dependabot alerts",Un)}const Mn={class:"vuln"},Fn={class:"vuln__head"},Gn={class:"vuln__sub"},zn={key:4,class:"vuln__counts"},Kn={class:"vuln__toolbar"},Jn={class:"vuln__stamp"},Yn={class:"vuln__lib"},Xn=["href"],Zn={key:0,class:"vuln__none"},Qn=["href"],et={key:1,class:"vuln__none"},nt={key:0,class:"vuln__message"};var tt=(0,a.defineComponent)({__name:"VulnPage",setup(e){const n=(0,o.Pj)(),t=(0,a.ref)(null),d=(0,a.ref)([]),c=(0,a.ref)({state:"checking",version:null,pod:null,detail:""}),h=(0,a.ref)({gh:"none",unreadable:!1}),u=(0,a.ref)(""),m=(0,a.ref)(!1),f=(0,a.ref)(!1),g=(0,a.ref)(!1),w=(0,a.ref)(null);let b=null;const y=(0,a.computed)(()=>t.value?Vn({snapshot:t.value}):null),k=(0,a.computed)(()=>{const e=y.value;return e?[...e.lists.openPrOpen,...e.lists.openNoPr].map(e=>{var n;return{...e,id:e.library,job:d.value.find(n=>n.library===e.library)||null,stale:jn(e),severityRank:Cn(e.severity),flightRank:"open"===(null===(n=e.pr)||void 0===n?void 0:n.status)?0:1,manifests:x(e)}}):[]}),$=[{name:"severity",label:"Severity",value:"severity",width:110,sort:["flightRank","severityRank","library"]},{name:"library",label:"Library",value:"library",sort:["library"]},{name:"files",label:"Files",value:"manifests"},{name:"vulns",label:"Vulnerability",value:"vulns"},{name:"dependabot",label:"Dependabot",value:"dependabotPr",width:120},{name:"actions",label:"Status / actions",value:"actions"}];function x(e){return[...new Set(e.vulns.map(e=>e.manifest).filter(Boolean))].sort()}function _(e){var n;return((null===(n=t.value)||void 0===n?void 0:n.dependabotPrs)||[]).find(n=>n.title.startsWith(`Bump ${e} `))||null}const E=(0,a.computed)(()=>d.value.some(e=>"Running"===e.phase&&!Ae(e))),S=(0,a.computed)(()=>"ready"===c.value.state&&vn(h.value)&&he(n));async function R(){const[e,n]=await Promise.all([ae().catch(()=>null),re().catch(()=>[])]);t.value=e,d.value=n}async function T(){m.value=!0,u.value="";try{await Ln(),await R()}catch(e){u.value=(null===e||void 0===e?void 0:e.message)||String(e)}finally{m.value=!1}}async function A(e,t){var a;u.value="";try{const o=await Re({store:n,library:e.library,action:t,group:e,by:null===(a=n.getters["auth/principal"])||void 0===a?void 0:a.loginName});d.value=[...d.value.filter(n=>n.library!==e.library),o]}catch(o){u.value=(null===o||void 0===o?void 0:o.message)||String(o)}}async function N(){const e=k.value.filter(e=>{var n,t;return!e.unfixable&&!(null===(n=e.job)||void 0===n?void 0:n.branch)&&"Running"!==(null===(t=e.job)||void 0===t?void 0:t.phase)}).sort((e,n)=>Cn(e.severity)-Cn(n.severity))[0];e?await A(e,"fix"):u.value="Nothing on the board is waiting for a fix."}async function C(e){if(e.job)try{await Te(e.job),await R()}catch(n){u.value=(null===n||void 0===n?void 0:n.message)||String(n)}}return(0,a.onMounted)(async()=>{await O();const[e,n]=await Promise.all([D(),hn().catch(()=>({gh:"none",unreadable:!0}))]);c.value=e,h.value=n,await R(),b=setInterval(()=>R().catch(()=>{}),5e3)}),(0,a.onUnmounted)(()=>{b&&clearInterval(b)}),(e,o)=>((0,a.openBlock)(),(0,a.createElementBlock)("div",Mn,[(0,a.createElementVNode)("header",Fn,[o[11]||(o[11]=(0,a.createElementVNode)("h1",null,"Vulnerabilities",-1)),(0,a.createElementVNode)("p",Gn,[o[7]||(o[7]=(0,a.createTextVNode)(" Actionable Dependabot alerts on ",-1)),(0,a.createElementVNode)("code",null,(0,a.toDisplayString)((0,a.unref)(v)),1),o[8]||(o[8]=(0,a.createTextVNode)(". ",-1)),o[9]||(o[9]=(0,a.createElementVNode)("strong",null,"Fix",-1)),o[10]||(o[10]=(0,a.createTextVNode)(" bumps the library in a workspace of its own, serves the branch, and verifies it. Opening the pull request is a separate step. ",-1))])]),"ready"!==c.value.state&&"checking"!==c.value.state?((0,a.openBlock)(),(0,a.createBlock)((0,a.unref)(r.l),{key:0,color:"warning"},{default:(0,a.withCtx)(()=>[(0,a.createTextVNode)((0,a.toDisplayString)(c.value.detail),1)]),_:1})):(0,a.unref)(he)((0,a.unref)(n))?(0,a.unref)(vn)(h.value)?(0,a.createCommentVNode)("",!0):((0,a.openBlock)(),(0,a.createBlock)((0,a.unref)(r.l),{key:2,color:"warning"},{default:(0,a.withCtx)(()=>[o[13]||(o[13]=(0,a.createTextVNode)(" No GitHub token is stored, so the board cannot be refreshed and nothing can be fixed. ",-1)),(0,a.createElementVNode)("a",{href:"#",onClick:o[0]||(o[0]=(0,a.withModifiers)(e=>f.value=!0,["prevent"]))},"Set one"),o[14]||(o[14]=(0,a.createTextVNode)(". ",-1))]),_:1})):((0,a.openBlock)(),(0,a.createBlock)((0,a.unref)(r.l),{key:1,color:"warning"},{default:(0,a.withCtx)(()=>[...o[12]||(o[12]=[(0,a.createTextVNode)(" The Apps Plus extension is not installed. A fix runs in a workspace, and a workspace is an Apps Plus installation — without it there is nowhere for the work to happen. ",-1)])]),_:1})),u.value?((0,a.openBlock)(),(0,a.createBlock)((0,a.unref)(r.l),{key:3,color:"error"},{default:(0,a.withCtx)(()=>[(0,a.createTextVNode)((0,a.toDisplayString)(u.value),1)]),_:1})):(0,a.createCommentVNode)("",!0),y.value?((0,a.openBlock)(),(0,a.createElementBlock)("div",zn,[(0,a.createVNode)(i.A,{name:"In flight",count:y.value.counts.openPrOpen,"primary-color-var":"--info"},null,8,["count"]),(0,a.createVNode)(i.A,{name:"To fix",count:y.value.counts.openNoPr,"primary-color-var":"--error"},null,8,["count"]),(0,a.createVNode)(i.A,{name:"Shipped",count:y.value.counts.prMerged,"primary-color-var":"--success",clickable:"",onClick:o[1]||(o[1]=e=>g.value=!0)},null,8,["count"])])):(0,a.createCommentVNode)("",!0),(0,a.createElementVNode)("div",Kn,[(0,a.createVNode)(s.A,{variant:"primary",disabled:!S.value,onClick:N},{default:(0,a.withCtx)(()=>[...o[15]||(o[15]=[(0,a.createElementVNode)("span",null,"Fix the worst one",-1)])]),_:1},8,["disabled"]),(0,a.createVNode)(s.A,{variant:"secondary",disabled:!S.value||m.value,"data-testid":"vc-refresh",onClick:T},{default:(0,a.withCtx)(()=>[(0,a.createElementVNode)("span",null,(0,a.toDisplayString)(m.value?"Refreshing…":"Refresh"),1)]),_:1},8,["disabled"]),(0,a.createVNode)(s.A,{variant:"secondary",onClick:o[2]||(o[2]=e=>f.value=!0)},{default:(0,a.withCtx)(()=>[...o[16]||(o[16]=[(0,a.createElementVNode)("span",null,"Credentials",-1)])]),_:1}),(0,a.createElementVNode)("span",Jn,[t.value?((0,a.openBlock)(),(0,a.createElementBlock)(a.Fragment,{key:0},[(0,a.createTextVNode)("gathered "+(0,a.toDisplayString)(new Date(t.value.gatheredAt).toLocaleString()),1)],64)):((0,a.openBlock)(),(0,a.createElementBlock)(a.Fragment,{key:1},[(0,a.createTextVNode)("no gather yet — press Refresh")],64))])]),(0,a.createVNode)(l.A,{rows:k.value,headers:$,"key-field":"id","table-actions":!1,"row-actions":!1,search:!0,"default-sort-by":"severity","no-rows-key":"No open vulnerabilities.",class:"vuln__table"},{"col:severity":(0,a.withCtx)(({row:e})=>[(0,a.createElementVNode)("td",null,[(0,a.createVNode)(p,{severity:e.severity},null,8,["severity"])])]),"col:library":(0,a.withCtx)(({row:e})=>{var n;return[(0,a.createElementVNode)("td",null,[(0,a.createElementVNode)("div",Yn,(0,a.toDisplayString)(e.library),1),e.stale?((0,a.openBlock)(),(0,a.createElementBlock)("a",{key:0,class:"vuln__stale",href:null===(n=e.pr)||void 0===n?void 0:n.url,target:"_blank",rel:"noopener",title:"That pull request merged but this alert is still open — the merge did not resolve it. It needs a fresh fix."},"merged, alert still open",8,Xn)):(0,a.createCommentVNode)("",!0)])]}),"col:files":(0,a.withCtx)(({row:e})=>[(0,a.createElementVNode)("td",null,[((0,a.openBlock)(!0),(0,a.createElementBlock)(a.Fragment,null,(0,a.renderList)(e.manifests,e=>((0,a.openBlock)(),(0,a.createElementBlock)("span",{key:e,class:"vuln__file"},(0,a.toDisplayString)(e),1))),128)),e.manifests.length?(0,a.createCommentVNode)("",!0):((0,a.openBlock)(),(0,a.createElementBlock)("span",Zn,"—"))])]),"col:vulns":(0,a.withCtx)(({row:e})=>[(0,a.createElementVNode)("td",null,[(0,a.createVNode)(Me,{vulns:e.vulns,repo:(0,a.unref)(v)},null,8,["vulns","repo"])])]),"col:dependabot":(0,a.withCtx)(({row:e})=>{var n,t;return[(0,a.createElementVNode)("td",null,[_(e.library)?((0,a.openBlock)(),(0,a.createElementBlock)("a",{key:0,href:null===(n=_(e.library))||void 0===n?void 0:n.url,target:"_blank",rel:"noopener"},(0,a.toDisplayString)(null===(t=_(e.library))||void 0===t?void 0:t.number),9,Qn)):((0,a.openBlock)(),(0,a.createElementBlock)("span",et,"—"))])]}),"col:actions":(0,a.withCtx)(({row:e})=>{var n,t;return[(0,a.createElementVNode)("td",null,[(0,a.createVNode)(Ve,{row:e,job:e.job,busy:E.value&&"Running"===(null===(n=e.job)||void 0===n?void 0:n.phase),onAct:n=>A(e,n),onStop:n=>C(e),onSession:n=>w.value=e.job},null,8,["row","job","busy","onAct","onStop","onSession"]),(null===(t=e.job)||void 0===t?void 0:t.message)?((0,a.openBlock)(),(0,a.createElementBlock)("div",nt,(0,a.toDisplayString)(e.job.message),1)):(0,a.createCommentVNode)("",!0)])]}),_:1},8,["rows"]),g.value&&y.value?((0,a.openBlock)(),(0,a.createBlock)(Sn,{key:5,rows:y.value.lists.prMerged,repo:(0,a.unref)(v),onClose:o[3]||(o[3]=e=>g.value=!1)},null,8,["rows","repo"])):(0,a.createCommentVNode)("",!0),w.value?((0,a.openBlock)(),(0,a.createBlock)(an,{key:6,job:w.value,"on-back":()=>w.value=null,onClose:o[4]||(o[4]=e=>w.value=null)},null,8,["job","on-back"])):(0,a.createCommentVNode)("",!0),f.value?((0,a.openBlock)(),(0,a.createBlock)(bn,{key:7,status:h.value,onSaved:o[5]||(o[5]=e=>h.value=e),onClose:o[6]||(o[6]=e=>f.value=!1)},null,8,["status"])):(0,a.createCommentVNode)("",!0)]))}});t(5840);const at=(0,h.A)(tt,[["__scopeId","data-v-49e715d9"]]);var ot=at},1785:function(e,n,t){"use strict";t.r(n);var a=t(6758),o=t.n(a),r=t(6173),s=t.n(r),i=s()(o());i.push([e.id,".session[data-v-09836064]{display:flex;flex-direction:column;height:100%;min-height:0;overflow:hidden}.session__id[data-v-09836064]{margin-left:10px;font-family:var(--font-family-mono,monospace);font-size:11px;font-weight:400;color:var(--muted)}.session__terminal[data-v-09836064]{flex:1 1 auto;min-height:0;border:1px solid var(--border);border-radius:6px;overflow:hidden;background:var(--body-bg)}.session__note[data-v-09836064]{margin:10px 0 0;font-size:11px;color:var(--muted)}.session__note--ended[data-v-09836064]{color:var(--warning)}",""]),n["default"]=i},8899:function(e,n,t){"use strict";t.r(n);var a=t(6758),o=t.n(a),r=t(6173),s=t.n(r),i=s()(o());i.push([e.id,".agent-terminal[data-v-5584bb48]{display:flex;flex-direction:column;min-width:0}.agent-terminal[data-v-5584bb48],.agent-terminal__pane[data-v-5584bb48]{flex:1 1 auto;min-height:0}.agent-terminal__waiting[data-v-5584bb48]{display:flex;align-items:center;gap:8px;padding:16px;color:var(--muted);font-size:13px}",""]),n["default"]=i},6236:function(e,n,t){"use strict";t.r(n);var a=t(6758),o=t.n(a),r=t(6173),s=t.n(r),i=s()(o());i.push([e.id,".creds__label[data-v-12cdeb6e]{display:block;margin:16px 0 4px;font-weight:600}.creds__input[data-v-12cdeb6e]{width:100%}.creds__note[data-v-12cdeb6e]{margin:12px 0 0;font-size:12px;color:var(--muted)}",""]),n["default"]=i},5390:function(e,n,t){"use strict";t.r(n);var a=t(6758),o=t.n(a),r=t(6173),s=t.n(r),i=s()(o());i.push([e.id,".severity[data-v-7b8a0a54]{min-width:74px;justify-content:center;font-size:10px;font-weight:600;letter-spacing:.04em}.severity--high[data-v-7b8a0a54]{opacity:.72}",""]),n["default"]=i},9370:function(e,n,t){"use strict";t.r(n);var a=t(6758),o=t.n(a),r=t(6173),s=t.n(r),i=s()(o());i.push([e.id,".shipped__sub[data-v-589dde70]{margin:0 0 16px}.shipped__empty[data-v-589dde70],.shipped__sub[data-v-589dde70]{color:var(--muted)}.shipped__row[data-v-589dde70]{display:flex;align-items:flex-start;gap:12px;padding:10px 0;border-bottom:1px solid var(--border)}.shipped__body[data-v-589dde70]{flex:1 1 auto;min-width:0}.shipped__lib[data-v-589dde70]{font-weight:600;margin-bottom:4px}.shipped__pr[data-v-589dde70]{white-space:nowrap}",""]),n["default"]=i},7704:function(e,n,t){"use strict";t.r(n);var a=t(6758),o=t.n(a),r=t(6173),s=t.n(r),i=s()(o());i.push([e.id,".steps[data-v-338a864c]{display:flex;flex-wrap:wrap;align-items:center;gap:6px}.steps__done[data-v-338a864c]{display:inline-flex;align-items:center;padding:2px 8px;border:1px solid var(--success);border-radius:12px;font-size:11px;color:var(--success);text-decoration:none;max-width:260px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.steps__nofix[data-v-338a864c],.steps__stalled[data-v-338a864c]{font-size:11px;color:var(--muted)}.steps__stalled[data-v-338a864c]{color:var(--warning)}.steps__spin[data-v-338a864c]{width:10px;height:10px;margin-right:6px;border:2px solid currentColor;border-right-color:transparent;border-radius:50%;animation:steps-spin-338a864c .7s linear infinite}@keyframes steps-spin-338a864c{to{transform:rotate(1turn)}}",""]),n["default"]=i},5323:function(e,n,t){"use strict";t.r(n);var a=t(6758),o=t.n(a),r=t(6173),s=t.n(r),i=s()(o());i.push([e.id,".vuln-ids[data-v-bdc6d74a]{display:flex;flex-wrap:wrap;gap:4px}.vuln-id[data-v-bdc6d74a]{padding:1px 6px;border:1px solid var(--border);border-radius:10px;font-family:var(--font-family-mono,monospace);font-size:11px;line-height:16px;text-decoration:none;color:var(--body-text)}.vuln-id--critical[data-v-bdc6d74a],.vuln-id--high[data-v-bdc6d74a]{border-color:var(--error);color:var(--error)}.vuln-id--medium[data-v-bdc6d74a]{border-color:var(--warning)}.vuln-id--fixed[data-v-bdc6d74a]{border-style:dashed;color:var(--muted);text-decoration:line-through}",""]),n["default"]=i},4224:function(e,n,t){"use strict";t.r(n);var a=t(6758),o=t.n(a),r=t(6173),s=t.n(r),i=s()(o());i.push([e.id,".vuln[data-v-49e715d9]{padding:0 0 40px}.vuln__head[data-v-49e715d9]{margin-bottom:16px}.vuln__head h1[data-v-49e715d9]{margin:0 0 4px}.vuln__sub[data-v-49e715d9]{margin:0;max-width:780px;color:var(--muted)}.vuln__counts[data-v-49e715d9]{display:flex;flex-wrap:wrap;gap:12px;margin-bottom:16px}.vuln__toolbar[data-v-49e715d9]{display:flex;flex-wrap:wrap;align-items:center;gap:8px;margin-bottom:12px}.vuln__stamp[data-v-49e715d9]{margin-left:auto;font-size:11px;color:var(--muted)}.vuln__lib[data-v-49e715d9]{font-weight:600}.vuln__stale[data-v-49e715d9]{margin-top:2px;color:var(--warning)}.vuln__file[data-v-49e715d9],.vuln__stale[data-v-49e715d9]{display:block;font-size:11px}.vuln__file[data-v-49e715d9]{font-family:var(--font-family-mono,monospace);white-space:nowrap}.vuln__file[data-v-49e715d9],.vuln__message[data-v-49e715d9],.vuln__none[data-v-49e715d9]{color:var(--muted)}.vuln__message[data-v-49e715d9]{margin-top:4px;font-size:11px}",""]),n["default"]=i},5641:function(e,n,t){var a=t(1785);a.__esModule&&(a=a.default),"string"===typeof a&&(a=[[e.id,a,""]]),a.locals&&(e.exports=a.locals);var o=t(4825).A;o("1043a7bb",a,!0,{sourceMap:!1,shadowMode:!1})},6739:function(e,n,t){var a=t(8899);a.__esModule&&(a=a.default),"string"===typeof a&&(a=[[e.id,a,""]]),a.locals&&(e.exports=a.locals);var o=t(4825).A;o("c0eec4c2",a,!0,{sourceMap:!1,shadowMode:!1})},5724:function(e,n,t){var a=t(6236);a.__esModule&&(a=a.default),"string"===typeof a&&(a=[[e.id,a,""]]),a.locals&&(e.exports=a.locals);var o=t(4825).A;o("c3bbc3a6",a,!0,{sourceMap:!1,shadowMode:!1})},8046:function(e,n,t){var a=t(5390);a.__esModule&&(a=a.default),"string"===typeof a&&(a=[[e.id,a,""]]),a.locals&&(e.exports=a.locals);var o=t(4825).A;o("14ba0463",a,!0,{sourceMap:!1,shadowMode:!1})},3274:function(e,n,t){var a=t(9370);a.__esModule&&(a=a.default),"string"===typeof a&&(a=[[e.id,a,""]]),a.locals&&(e.exports=a.locals);var o=t(4825).A;o("1bbf7932",a,!0,{sourceMap:!1,shadowMode:!1})},4763:function(e,n,t){var a=t(7704);a.__esModule&&(a=a.default),"string"===typeof a&&(a=[[e.id,a,""]]),a.locals&&(e.exports=a.locals);var o=t(4825).A;o("cf870dd6",a,!0,{sourceMap:!1,shadowMode:!1})},2136:function(e,n,t){var a=t(5323);a.__esModule&&(a=a.default),"string"===typeof a&&(a=[[e.id,a,""]]),a.locals&&(e.exports=a.locals);var o=t(4825).A;o("0acafe74",a,!0,{sourceMap:!1,shadowMode:!1})},5840:function(e,n,t){var a=t(4224);a.__esModule&&(a=a.default),"string"===typeof a&&(a=[[e.id,a,""]]),a.locals&&(e.exports=a.locals);var o=t(4825).A;o("07561566",a,!0,{sourceMap:!1,shadowMode:!1})}}]);
//# sourceMappingURL=vuln-console-0.1.1.umd.min.840.js.map