(("undefined"!==typeof self?self:this)["webpackChunkvuln_console_0_8_2"]=("undefined"!==typeof self?self:this)["webpackChunkvuln_console_0_8_2"]||[]).push([[914],{4914:function(e,n,t){"use strict";t.r(n),t.d(n,{default:function(){return Yt}});var a=t(9274),o=t(5359),r=t(9103),s=t(6265),i=t(6221),l=t(1300),d=t(8846),c=t(9219),h=t(9861);const p="__agents",u="agents:ready",m=40;function f(e){const n=Date.now().toString(36),t=m-n.length-1,a=String(e).toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-+/,"").slice(0,t).replace(/-+$/,"");return`${a||"vc"}-${n}`}const v="0.1.40";function g(){return window[p]||null}function b(e,n){const t=String(e||"").split(".").map(e=>parseInt(e,10)||0),a=n.split(".").map(e=>parseInt(e,10)||0);for(let o=0;o<Math.max(t.length,a.length);o++){const e=t[o]||0,n=a[o]||0;if(e!==n)return e>n}return!0}async function w(){const e=g();if(!e)return{state:"missing",version:null,pod:null,detail:"The Agents extension is not installed in this Rancher. Install codyrancher/agents — this extension runs every gather and every fix through the agent pod it provides."};if(!b(e.version,v))return{state:"outdated",version:e.version,pod:null,detail:`The Agents extension is ${e.version}; this needs ${v} or later for conversations that can be started with an opening prompt.`};const n=await e.agent.pod().catch(()=>null);return n?{state:"ready",version:e.version,pod:n,detail:`Agents ${e.version} · agent pod ${n}`}:{state:"no-pod",version:e.version,pod:null,detail:"The agent pod is not running yet. It comes up on its own — give it a minute after a Rancher restart, then refresh."}}function y(e=8e3){const n=g();return n?Promise.resolve(n):new Promise(n=>{const t=e=>{window.removeEventListener(u,a),clearTimeout(o),n(e)},a=()=>t(g()),o=setTimeout(()=>t(g()),e);window.addEventListener(u,a)})}const k="local",$=`/k8s/clusters/${k}`;function x(e,n,t){const a=window.location.origin.replace(/^http/,"ws"),o=new URLSearchParams({container:e.container,stdin:t?"1":"0",stdout:"1",stderr:"1",tty:"0"});for(const r of n)o.append("command",r);return`${a}${$}/api/v1/namespaces/${e.namespace}/pods/${e.pod}/exec?${o}`}function _(e){const n=Number(e.slice(0,1));try{return{channel:n,bytes:Uint8Array.from(atob(e.slice(1)),e=>e.charCodeAt(0))}}catch(t){return null}}const E=12e4;function S(e,n,t={}){var a;const o=null!==(a=t.timeoutMs)&&void 0!==a?a:E;return new Promise(r=>{const s={stdout:"",stderr:"",status:"",code:-1};let i=!1,l=null;const d={1:new TextDecoder,2:new TextDecoder},c=(e="")=>{if(!i){i=!0,clearTimeout(h),e&&(s.status=s.status||e);try{null===l||void 0===l||l.close()}catch(a){}r(s)}},h=setTimeout(()=>c(`no answer from the pod in ${Math.round(o/1e3)}s`),o);try{l=new WebSocket(x(e,n,!!t.onStdin),"base64.channel.k8s.io")}catch(p){return void c((null===p||void 0===p?void 0:p.message)||String(p))}t.onStdin&&(l.onopen=()=>{var e;null===(e=t.onStdin)||void 0===e||e.call(t,e=>{const n=(new TextEncoder).encode(e);let t="";for(const a of n)t+=String.fromCharCode(a);null===l||void 0===l||l.send(`0${btoa(t)}`)})}),l.onmessage=e=>{var n;const t=_(String(e.data||""));if(t){if(1===t.channel||2===t.channel){const e=d[t.channel].decode(t.bytes,{stream:!0});return void(1===t.channel?s.stdout+=e:s.stderr+=e)}if(3===t.channel){const e=(new TextDecoder).decode(t.bytes);try{const t=JSON.parse(e);if("Success"===t.status)s.code=0;else{s.status=t.message||"the command failed";const e=((null===(n=t.details)||void 0===n?void 0:n.causes)||[]).find(e=>"ExitCode"===e.reason);s.code=e&&Number(e.message)||1}}catch(a){s.status=e,s.code=-1===s.code?1:s.code}}}},l.onclose=()=>{if(s.stdout+=d[1].decode(),s.stderr+=d[2].decode(),-1===s.code){if(!s.stdout&&!s.stderr)return void c(s.status||"the exec was refused or dropped");s.code=0}c()},l.onerror=()=>c("could not open an exec to the pod")})}const R="__VC_OK__";async function N(e,n,t,a){const o=await S(e,["/bin/sh","-c",`set -e\n${n}\necho ${R}`],{timeoutMs:a});if(!o.stdout.includes(R)){const e=o.stderr.trim()||o.status||(o.code>0?`exit ${o.code}`:"it reported nothing");throw new Error(`Could not ${t}: ${e}`)}return o.stdout.replace(R,"").trim()}function A(e){return`'${e.split("'").join("'\\''")}'`}const T=4096,O="__END_OF_FILE__";function C(e){const n=(new TextEncoder).encode(e);let t="";for(let a=0;a<n.length;a+=8192)t+=String.fromCharCode(...n.subarray(a,a+8192));return btoa(t)}async function D(e,n,t,a={}){const o=a.mode||"644",r=a.owner||"",s=A(n),i=[`mkdir -p "$(dirname ${s})"`,`: > ${s}.b64`,`: > ${s} && chmod ${o} ${s}`,r?`chown ${r} ${s} 2>/dev/null || true`:"",`while IFS= read -r line; do [ "$line" = "${O}" ] && break; printf '%s' "$line" >> ${s}.b64; done`,`base64 -d < ${s}.b64 > ${s}`,`rm -f ${s}.b64`,`chmod ${o} ${s}`,r?`chown ${r} ${s} 2>/dev/null || true`:"",`echo WROTE $(wc -c < ${s})`].filter(Boolean).join("\n"),l=C(t),d=await S(e,["/bin/sh","-c",i],{timeoutMs:6e4,onStdin:e=>{for(let n=0;n<l.length;n+=T)e(`${l.slice(n,n+T)}\n`);e(`${O}\n`)}}),c=d.stdout.match(/WROTE\s+(\d+)/);if(!c||Number(c[1])!==(new TextEncoder).encode(t).length){const e=d.stderr.trim()||d.status||`exit ${d.code}`;throw new Error(`Could not write ${n} into the agent pod: ${e}`)}}const V="vuln-console",P=[{id:"dashboard",label:"Dashboard",repo:"rancher/dashboard"},{id:"rancher-ai-ui",label:"Rancher AI UI",repo:"rancher/rancher-ai-ui",ownerPackage:"@rancher/shell"}];function B(e){return P.find(n=>n.id===e)||P[0]}function H(e){return e.split("/").pop()||e}function I(e,n){return e.fork||(n?`${n}/${H(e.repo)}`:"")}function j(e,n){return e.prTarget||I(e,n)}const W="settings",L="gh_token",q="vuln-console.rancher.io/owns",U="vuln-console.rancher.io/board",G="snapshot-",M="vuln-workspace",F="/workspaces",K="local",z=`/k8s/clusters/${K}/v1`,Y=`/k8s/clusters/${K}/api/v1`;function J(){const e=document.cookie.match(/(?:^|;\s*)CSRF=([^;]*)/);return{"X-Api-Csrf":e?decodeURIComponent(e[1]):"CSRF"}}async function X(e,n){const t=!!(null===n||void 0===n?void 0:n.method)&&"GET"!==n.method,a=await fetch(e,{...n,headers:{"Content-Type":"application/json",Accept:"application/json",...t?J():{},...(null===n||void 0===n?void 0:n.headers)||{}}});if(404===a.status)return null;const o=await a.json().catch(()=>({}));if(!a.ok)throw new Error(o.message||o.error||`HTTP ${a.status}`);return o}function Z(e,n){return X(`${z}${e}`,n)}async function Q(){const e=await Z(`/namespaces/${V}`).catch(()=>null);e||await Z("/namespaces",{method:"POST",body:JSON.stringify({apiVersion:"v1",kind:"Namespace",metadata:{name:V}})}).catch(e=>{if(!/409|already exists|alreadyexists/i.test((null===e||void 0===e?void 0:e.message)||""))throw e})}function ee(e){return`/configmaps/${V}/${e}`}async function ne(e,n,t){const a={apiVersion:"v1",kind:"ConfigMap",metadata:{name:e,namespace:V,labels:{[q]:"true",...t}},data:n},o=await Z(ee(e)).catch(()=>null);o?await Z(ee(e),{method:"PUT",body:JSON.stringify({...a,metadata:{...o.metadata,labels:a.metadata.labels}})}):await Z("/configmaps",{method:"POST",body:JSON.stringify(a)}).catch(async n=>{if(!/409|already exists|alreadyexists/i.test((null===n||void 0===n?void 0:n.message)||""))throw n;const t=await Z(ee(e));await Z(ee(e),{method:"PUT",body:JSON.stringify({...a,metadata:{...t.metadata,labels:a.metadata.labels}})})})}async function te(e,n){var t;const a=await Z(ee(e)).catch(()=>null),o=null===(t=null===a||void 0===a?void 0:a.data)||void 0===t?void 0:t[n];if(!o)return null;try{return JSON.parse(o)}catch(r){return null}}function ae(e){return`${G}${e}`}function oe(e){return te(ae(e),"snapshot.json")}function re(e){return e.toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-+|-+$/g,"")}function se(e,n){return`job-${e}-${re(n).slice(0,40)||"unnamed"}`}async function ie(e){const n=`${q}=true,${U}=${e}`,t=await Z(`/configmaps?labelSelector=${encodeURIComponent(n)}`).catch(()=>null);return((null===t||void 0===t?void 0:t.data)||[]).filter(e=>{var n;return((null===(n=null===e||void 0===e?void 0:e.metadata)||void 0===n?void 0:n.name)||"").startsWith("job-")}).map(e=>{var n;try{return JSON.parse((null===(n=e.data)||void 0===n?void 0:n["job.json"])||"null")}catch(t){return null}}).filter(e=>!!(null===e||void 0===e?void 0:e.library))}async function le(e){await Q(),await ne(se(e.board,e.library),{"job.json":JSON.stringify(e)},{[U]:e.board})}function de(e){return String(e||"").replace(/[^a-zA-Z0-9._-]+/g,"-").replace(/^-+|-+$/g,"").slice(0,200)||"unknown"}function ce(e){return`${L}-${de(e)}`}function he(e){return`vuln-console.rancher.io/gh-${de(e)}`.slice(0,253)}const pe={Accept:"application/json;as=PartialObjectMetadata;g=meta.k8s.io;v=v1"};function ue(){return`${Y}/namespaces/${V}/secrets/${W}`}async function me(){const e=await X(ue(),{headers:pe}).catch(()=>null);return(null===e||void 0===e?void 0:e.metadata)||null}function fe(e){const n=new Set;return((null===e||void 0===e?void 0:e.managedFields)||[]).forEach(e=>{var t;Object.keys((null===(t=null===e||void 0===e?void 0:e.fieldsV1)||void 0===t?void 0:t["f:data"])||{}).forEach(e=>{e.startsWith("f:")&&n.add(e.slice(2))})}),[...n]}async function ve(e){const n=await me();if(!n)return{stored:!1,others:0,unreadable:!1};const t=fe(n),a=n.annotations||{},o="set"===a[he(e)]||t.includes(ce(e)),r=new Set([...t.filter(e=>e.startsWith(`${L}-`)),...Object.keys(a).filter(e=>e.startsWith("vuln-console.rancher.io/gh-")).map(e=>`${L}-${e.split("/")[1].replace(/^gh-/,"")}`)]);return{stored:o,others:Math.max(0,r.size-(o?1:0)),unreadable:!1}}async function ge(){const e=await me();e||(await X(`${Y}/namespaces`,{method:"POST",body:JSON.stringify({apiVersion:"v1",kind:"Namespace",metadata:{name:V}})}).catch(e=>{if(!/409|already exists|alreadyexists/i.test((null===e||void 0===e?void 0:e.message)||""))throw e}),await X(`${Y}/namespaces/${V}/secrets`,{method:"POST",headers:pe,body:JSON.stringify({apiVersion:"v1",kind:"Secret",type:"Opaque",metadata:{name:W,namespace:V}})}).catch(e=>{if(!/409|already exists|alreadyexists/i.test((null===e||void 0===e?void 0:e.message)||""))throw e}))}function be(e){return btoa(String.fromCharCode(...(new TextEncoder).encode(e)))}async function we(e,n){await ge();const t=""===n;await X(ue(),{method:"PATCH",headers:{"Content-Type":"application/merge-patch+json",...pe},body:JSON.stringify({metadata:{annotations:{[he(e)]:t?null:"set"}},data:{[ce(e)]:t?null:be(n)}})})}function ye(e){return e.stored}const ke={"workspace/config.yaml":'# The scripts the workspace pod runs, mounted rather than baked into an image.\n#\n# No image is ever built for any of this: a stock `node:24` reads its whole behaviour out of\n# this ConfigMap. Editing a script is editing this object, and the pod picks it up on its next\n# start - which is why the Deployment carries a hash of these in its pod template.\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  namespace: ${namespace}\n  name: vuln-workspace-config\ndata:\n  boot.sh: |\n    #!/bin/sh\n    # Bring one fix workspace up: a rancher/dashboard checkout with its dependencies installed and\n    # the dev server supervised on top of it.\n    #\n    # The container\'s main process ends up being the supervisor, not the server, because a fix run\n    # does things that kill a dev server - switches branch, regenerates a lockfile, runs a build -\n    # and a pod whose main process was the server would restart on each of those and take the run\'s\n    # terminal with it.\n    #\n    # `${install}`, `${repo}` and `${port}` are apps-plus substitutions, filled in when the App is\n    # rendered. Everything written `$LIKE_THIS` is ordinary shell and is left alone: apps-plus only\n    # substitutes the braced form, and only for names its App declares.\n    set -e\n\n    # Force apt onto IPv4 before anything runs one. The node has no working IPv6 route to the Debian\n    # mirror, so an `apt-get update` left alone resolves an IPv6 address and hangs on it for minutes\n    # while holding the apt lock - which is how a workspace comes up with no tmux and no terminal.\n    mkdir -p /etc/apt/apt.conf.d\n    printf \'Acquire::ForceIPv4 "true";\\nAcquire::Retries "3";\\n\' > /etc/apt/apt.conf.d/99vuln-ipv4\n\n    WS=/workspaces/${install}\n    mkdir -p "$WS/.home"\n    chown node:node "$WS" "$WS/.home" 2>/dev/null || true\n    [ -f "$WS/.owned" ] || { chown -R node:node "$WS" 2>/dev/null; touch "$WS/.owned"; }\n\n    # The browser sidecar mounts these as subPaths, and the kubelet creates a missing subPath as\n    # root - leaving the seed unable to write into it.\n    mkdir -p "$WS/.a11y/opt" "$WS/.a11y/init"\n    chown -R node:node "$WS/.a11y" 2>/dev/null || true\n\n    # What every workspace on this node shares rather than keeping its own copy. Measured before\n    # this existed: Cypress 813 MB, yarn 749 MB, and a gigabyte of node_modules - most of a\n    # workspace\'s 3 GB, per workspace.\n    SHARED=/workspaces/.shared\n    mkdir -p "$SHARED/yarn" "$SHARED/cypress" "$SHARED/npm" "$SHARED/template"\n    chown node:node "$SHARED" "$SHARED/yarn" "$SHARED/cypress" "$SHARED/npm" "$SHARED/template" 2>/dev/null || true\n\n    # tmux and kubectl, which the terminal needs. No claude: the conversation runs in the agents pod\n    # and reaches this one through a tunnel, so a claude here is hundreds of megabytes of hostPath\n    # that nothing executes.\n    [ -f /seed/terminal-tools.sh ] \\\n      && (TOOLS_NO_CLAUDE=1 HOME_DIR="$WS/.home" /bin/sh /seed/terminal-tools.sh >"$WS/.terminal-tools.log" 2>&1 &) \\\n      || true\n\n    # What a recording and a lockfile bump need and the image lacks. Backgrounded so the dev server\n    # is not a minute later for it; the lock timeout lets it wait for the tmux install above rather\n    # than colliding with it.\n    (command -v ffmpeg >/dev/null 2>&1 && command -v lsof >/dev/null 2>&1) || (\n      apt-get -o DPkg::Lock::Timeout=300 update -qq \\\n        && DEBIAN_FRONTEND=noninteractive apt-get -o DPkg::Lock::Timeout=300 install -y -qq \\\n          ffmpeg jq lsof iproute2 >"$WS/.apt.log" 2>&1 &\n    ) || true\n\n    # /workspace-config, not /seed. This extension\'s scripts are mounted from its own ConfigMap;\n    # /seed is the agents extension\'s, which is optional here and usually absent - so pointing at it\n    # crash-looped the pod with "cannot open /seed/checkout.sh".\n    exec setpriv --reuid=1000 --regid=1000 --init-groups /bin/sh /workspace-config/checkout.sh\n  checkout.sh: |\n    #!/bin/sh\n    # The workspace\'s checkout and install, as the node user. Split from boot.sh so the privileged\n    # half is short enough to read and this half can be run again by hand.\n    set -e\n\n    WS=/workspaces/${install}\n    SHARED=/workspaces/.shared\n\n    export HOME="$WS/.home"\n    export YARN_CACHE_FOLDER="$SHARED/yarn"\n    export CYPRESS_CACHE_FOLDER="$SHARED/cypress"\n    export npm_config_cache="$SHARED/npm"\n\n    # init + fetch rather than `git clone`, because clone refuses a non-empty directory and this one\n    # is not reliably empty: the agent seed writes `.claude` into the checkout, and if that lands\n    # first - a separate trigger racing this boot - a clone would fail and leave the pod serving\n    # nothing.\n    if [ ! -d "$WS/src/.git" ]; then\n      mkdir -p "$WS/src"\n      cd "$WS/src"\n      git init -q\n      git remote add origin https://github.com/${repo} 2>/dev/null || true\n      DEFAULT=$(git ls-remote --symref origin HEAD | sed -n \'s@^ref: refs/heads/\\(.*\\)[[:space:]]HEAD@\\1@p\')\n      git fetch --depth 1 origin "$DEFAULT"\n      git checkout -f -B "$DEFAULT" FETCH_HEAD\n    fi\n\n    cd "$WS/src"\n\n    # The fork is where fixes are pushed, so it is a remote from the start rather than something the\n    # agent has to remember to add. `upstream` is what a branch is diffed against - never the fork\'s\n    # own master, which is chronically stale and makes a clean fix look like it carries half the\n    # repository with it.\n    git remote add fork "https://github.com/${fork}" 2>/dev/null || true\n    git remote set-url fork "https://github.com/${fork}" 2>/dev/null || true\n    git remote add upstream "https://github.com/${repo}" 2>/dev/null || true\n\n    # node_modules, hard-linked from a template of the same lockfile if this node already has one.\n    # The files are the same inodes, so a second workspace\'s node_modules costs directory entries\n    # and nothing else. Keyed by the lockfile hash, so a template is only ever used by a checkout\n    # that would have installed exactly it.\n    HASH=$(sha1sum yarn.lock 2>/dev/null | cut -c1-12)\n\n    if [ -n "$HASH" ] && [ ! -d node_modules ] && [ -d "$SHARED/template/$HASH/node_modules" ]; then\n      cp -al "$SHARED/template/$HASH/node_modules" node_modules && touch .install-done\n    fi\n\n    [ -f .install-done ] || {\n      yarn install --mutex "file:$SHARED/yarn/.mutex" --network-timeout 600000\n      touch .install-done\n    }\n\n    # The first workspace to install a given lockfile leaves the template for the next one.\n    if [ -n "$HASH" ] && [ -d node_modules ] && [ ! -d "$SHARED/template/$HASH" ]; then\n      mkdir -p "$SHARED/template/$HASH"\n      cp -al node_modules "$SHARED/template/$HASH/node_modules" || rm -rf "$SHARED/template/$HASH"\n    fi\n\n    exec /bin/bash /workspace-config/serve.sh ${port}\n  serve.sh: |\n    #!/bin/bash\n    # The dev server, supervised: the container\'s process is this loop, not the server.\n    #\n    # A fix run does things that kill a dev server - switches branch, regenerates a lockfile, runs a\n    # build. When the pod\'s main process was the server itself, each of those restarted the\n    # container and took the run\'s terminal with it, mid-fix. So the server is restarted when it\n    # exits, and left alone while something else holds the port.\n    PORT=${1:-8005}\n\n    # The boot script runs this from the checkout, so the tree is its parent - which is where the\n    # log and the retarget file live.\n    WS=${WS:-$(dirname "$PWD")}\n    RETARGET=$WS/.dev-server.env\n    LOG=$WS/.dev-server.log\n\n    echo "$PORT" > "$WS/.dev-server.port" 2>/dev/null || true\n    export PORT WS RETARGET LOG IONICE\n\n    held() {\n      node -e "require(\'net\').connect(Number(process.argv[1]),\'127.0.0.1\').on(\'connect\',()=>process.exit(0)).on(\'error\',()=>process.exit(1))" "$PORT" 2>/dev/null\n    }\n\n    # Not all at once. Rancher\'s embedded k3s runs beside these servers, and when it restarts every\n    # workspace pod restarts with it and every webpack compiles at the same moment: a dozen of them\n    # on twelve cores, k3s\'s controllers fail to renew their lease, k3s exits, Rancher restarts -\n    # and the herd forms again. So a server waits, with jitter so they do not all wake together,\n    # while the node\'s one-minute load is above what the cores can carry.\n    CORES=$(nproc 2>/dev/null || echo 4)\n    gate() {\n      load=$(cut -d. -f1 /proc/loadavg 2>/dev/null || echo 0)\n      [ "$load" -le $(( CORES * 3 / 2 )) ]\n    }\n\n    # One dev server per pod, whoever started it. A second webpack is another two gigabytes at every\n    # rebuild and the pod\'s limit is five, so an agent that starts its own gets the port until it is\n    # done and this one comes back afterwards. Only node processes, matched by command line: a shell\n    # or a grep that merely mentions the server would otherwise count and stop this one for nothing.\n    servers() { pgrep -f "^[^ ]*node .*vue-cli-service serve" 2>/dev/null; }\n    foreign() {\n      local p\n      for p in $(servers); do\n        [ "$(ps -o pgid= -p "$p" 2>/dev/null | tr -d \' \')" = "$SERVER" ] || echo "$p"\n      done\n    }\n\n    SERVER=""\n    stop_ours() {\n      if [ -n "$SERVER" ]; then\n        kill -TERM -- -"$SERVER" 2>/dev/null\n        wait "$SERVER" 2>/dev/null\n        SERVER=""\n      fi\n    }\n    trap "stop_ours; exit 0" TERM INT\n\n    while :; do\n      if held || [ -n "$(foreign)" ]; then sleep 10; continue; fi\n\n      sleep $(( RANDOM % 20 ))\n\n      waited=0\n      while ! gate && [ "$waited" -lt 300 ]; do\n        [ "$waited" -eq 0 ] && echo "[workspace] the node is busy; waiting to start the dev server"\n        sleep 10\n        waited=$((waited + 10))\n      done\n\n      # Below the cluster\'s own processes: a compile that takes every core has taken k3s down with\n      # it on a busy node, and a slow first page is the better failure.\n      IONICE=""\n      command -v ionice >/dev/null 2>&1 && IONICE="ionice -c 3"\n\n      # What the server runs against can be changed without a new pod: writing the retarget file\n      # restarts it onto a different Rancher. Read in the server\'s own shell, so it replaces the\n      # pod\'s environment for the server alone.\n      STAMP=$(stat -c %Y "$RETARGET" 2>/dev/null || echo 0)\n      [ -f "$RETARGET" ] && echo "[workspace] the dev server runs against $(grep \'^API=\' "$RETARGET" | cut -d= -f2-)"\n\n      # setsid, so the server and everything it starts are one process group a stop can signal as a\n      # whole; tee, so the log is in the tree as well as in the pod\'s.\n      setsid bash -c \'if [ -f "$RETARGET" ]; then set -a; . "$RETARGET"; set +a; fi; nice -n 15 $IONICE env VUE_CLI_SERVICE_CONFIG_PATH=/workspace-config/vue.config.js yarn dev --port "$PORT" 2>&1 | tee "$LOG"\' &\n      SERVER=$!\n\n      # `sleep & wait`, not `sleep`: a trap runs once the foreground command returns, and a stop\n      # that waited out the sleep left the server running for the check that followed.\n      while kill -0 "$SERVER" 2>/dev/null; do\n        sleep 10 & wait $!\n        if [ -n "$(foreign)" ]; then\n          echo "[workspace] another dev server is running in this pod; standing down until it is gone"\n          stop_ours; sleep 10; continue 2\n        fi\n        if [ "$(stat -c %Y "$RETARGET" 2>/dev/null || echo 0)" != "$STAMP" ]; then\n          echo "[workspace] the dev server\'s target changed; restarting it"\n          stop_ours; continue 2\n        fi\n      done\n\n      SERVER=""\n      echo "[workspace] the dev server exited; starting it again in 5s"\n      sleep 5\n    done\n  vue.config.js: |\n    // Written by the Dev extension. See pkg/dev-extension/workspace-config.ts.\n    //\n    // This dev server is reached one of two ways, and everything below is the consequence of which.\n    //\n    //   - behind the apiserver\'s service proxy, at DEV_PROXY_PATH, on Rancher\'s own origin. The Host\n    //     header is the proxy\'s, the TLS the browser sees is Rancher\'s, the prefix is stripped on the\n    //     way in and has to be put back on every URL handed out.\n    //   - at an origin of its own, on a node port, with no prefix at all.\n    //\n    // A dev server built for one does not work at the other: the router base is baked in. Which one\n    // this is comes from DEV_PROXY_PATH being set or empty, and changing it restarts the pod.\n    //\n    // The second mode exists because of what the first one does to a dashboard. rancher/dashboard\n    // builds its API URLs absolute from the page origin, so behind the prefix its /v1 and /v3 calls\n    // never reach this dev server at all: they go to the Rancher this page was served from, using\n    // that session, against that cluster. Measured, on a workspace with a Rancher of its own: two\n    // requests arrived here and a hundred and thirty-six went to the host. An origin of its own is\n    // the only thing that fixes it, because there is no browser-side API base to set.\n    const proxyPath = (process.env.DEV_PROXY_PATH || \'\').replace(/\\/$/, \'\');\n\n    // The prefix on every in-app link, which has to be the path the page was loaded at. Set before\n    // the shell\'s config runs, because the shell reads it out of the environment as it does.\n    process.env.ROUTER_BASE = proxyPath ? proxyPath + \'/\' : \'/\';\n\n    // The repo\'s own config, untouched in the checkout, rather than a reimplementation of it.\n    // rancher/dashboard is a monorepo whose shell is ./shell rather than a node_modules package,\n    // and which arguments its root config passes to that shell is the repo\'s business and changes\n    // with the repo. Wrapping it means this file only has to know about the proxy. This file is\n    // handed to vue-cli-service as VUE_CLI_SERVICE_CONFIG_PATH (see apps.ts, WORKSPACE_SCRIPT), so\n    // it lives outside the checkout and the checkout\'s own vue.config.js is never rewritten: the\n    // tree stays clean, a branch switch never trips over it, and nothing an agent commits carries it.\n    const base = require(require(\'path\').join(process.cwd(), \'vue.config.js\'));\n\n    // The proxy rewrites absolute URLs in HTML as they pass through, which is what lets a naive UI\n    // work behind it at all, so an asset URL that already carried the prefix would carry it twice.\n    // index.html therefore asks for \'/js/...\' and lets the proxy add the prefix, while webpack\'s\n    // runtime works its own base out from the script URL it was loaded from.\n    base.publicPath = \'auto\';\n\n    const previousChainWebpack = base.chainWebpack;\n\n    base.chainWebpack = (webpackConfig) => {\n      if (typeof previousChainWebpack === \'function\') {\n        previousChainWebpack(webpackConfig);\n      }\n\n      webpackConfig.plugin(\'html-index\').tap((args) => {\n        args[0].publicPath = \'/\';\n\n        return args;\n      });\n    };\n\n    base.devServer = {\n      ...base.devServer,\n\n      // Plain http only behind the proxy, where Rancher terminates TLS and will not talk to the\n      // shell\'s self-signed dev certificate on the way through.\n      //\n      // At its own origin the browser talks to this server directly, and http there is not a smaller\n      // problem than a certificate warning, it is a broken login: the shipped proxy sets\n      // x-forwarded-proto: https on every request it makes, so Rancher issues its session cookie with\n      // Secure, and a browser on http throws that cookie away. What that looks like is a login that\n      // returns 200 and a page that bounces straight back to the login form, which is what it did.\n      // So https here, with the shell\'s own certificate, and a warning to click through once.\n      ...(proxyPath ? { server: { type: \'http\' } } : {}),\n\n      // Requests arrive with whatever Host the proxy chain last set, never this server\'s own.\n      // Without this the proxy answers \'Invalid Host header\'.\n      allowedHosts: \'all\',\n\n      // The proxy cannot rewrite URLs in a compressed body: the response comes back as a stream\n      // error and the browser shows a blank page.\n      compress: false,\n\n      // The prefix is stripped before a request lands here, so everything is served from the root.\n      devMiddleware: { publicPath: \'/\' },\n      static:        { publicPath: \'/\' },\n\n      // A deep link into the dashboard has to load the app rather than 404. disableDotRule matters\n      // in Rancher, where a route routinely carries a resource name with dots in it.\n      historyApiFallback: {\n        index:             \'/index.html\',\n        disableDotRule:    true,\n        htmlAcceptHeaders: [\'text/html\', \'application/xhtml+xml\']\n      },\n\n      hot:             true,\n      webSocketServer: { type: \'ws\', options: { path: \'/ws\' } },\n      client:          {\n        // Hot reload rides the same proxy when there is one, and the page\'s own origin when there is\n        // not. Either way this has to be written out, because the shell\'s config has an address of its\n        // own in it and that address is not where this server is.\n        //\n        // Behind the proxy only the path is ours; the rest are webpack-dev-server\'s "infer it from\n        // window.location" sentinels. \'auto\' is one the *server* substitutes as it serves the client.\n        //\n        // Served at its own origin the sentinels are still needed, and leaving them out was a real\n        // bug rather than a tidy default. The shell\'s own config sets a fixed\n        // wss://localhost:8005/ws, which is right on a laptop running the dev server and wrong\n        // everywhere else: the browser then opened a hot-reload socket to whatever was on port 8005\n        // of the machine the browser is on, and displayed that server\'s compile errors over this\n        // dashboard. Seen here as a webpack overlay full of a completely different project\'s module\n        // resolution failures, sitting on top of the workspace\'s login form and swallowing clicks.\n        //\n        // 0.0.0.0 and 0 are webpack-dev-server\'s own "take it from window.location" sentinels. The\n        // protocol is written out rather than left as \'auto\', because \'auto\' is substituted by the\n        // server as it serves the client and is handed to the WebSocket constructor unchanged when\n        // the page was loaded directly. This server is https at its own origin, so wss.\n        webSocketURL: proxyPath ? {\n          protocol: \'auto\',\n          hostname: \'0.0.0.0\',\n          port:     0,\n          pathname: proxyPath + \'/ws\'\n        } : {\n          protocol: \'wss\',\n          hostname: \'0.0.0.0\',\n          port:     0,\n          pathname: \'/ws\'\n        },\n        // The compile-error overlay would cover the whole framed dashboard for whoever is looking\n        // when an edit fails to compile.\n        overlay: false\n      }\n    };\n\n    module.exports = base;\n',"workspace/deployment.yaml":'# The workspace: a checkout with a supervised dev server, and a Chromium beside it.\n#\n# Two containers in one pod so they share localhost - the browser drives the dev server at\n# 127.0.0.1 and the agent drives the browser at 127.0.0.1:9222, with nothing to route and no\n# service to resolve between them.\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  namespace: ${namespace}\n  name: ${namespace}\n  labels:\n    vuln-console.rancher.io/workspace: ${install}\nspec:\n  replicas: 1\n  # Recreate, not RollingUpdate: two of these would both want the hostPath tree and the same\n  # node_modules, and the second would fight the first for the yarn mutex.\n  strategy:\n    type: Recreate\n  selector:\n    matchLabels:\n      vuln-console.rancher.io/workspace: ${install}\n  template:\n    metadata:\n      labels:\n        vuln-console.rancher.io/workspace: ${install}\n    spec:\n      serviceAccountName: vuln-workspace\n      containers:\n        - name: workspace\n          image: ${image}\n          command: ["/bin/sh", "/workspace-config/boot.sh"]\n          env:\n            # The path this dev server is reached at. The dashboard bakes its router base in at\n            # build time, so it has to know the prefix the proxy strips on the way in.\n            - name: DEV_PROXY_PATH\n              value: /k8s/clusters/${hostCluster}/api/v1/namespaces/${namespace}/services/${scheme}:${namespace}:${port}/proxy\n            - name: API\n              value: ${rancherUrl}\n            - name: RANCHER_URL\n              value: ${rancherUrl}\n            - name: VULN_BROWSER_CDP\n              value: http://localhost:9222\n            # This cluster is k3s inside the Rancher container, so the node\'s own address is\n            # how the dev server reaches the Rancher it belongs to.\n            - name: NODE_IP\n              valueFrom:\n                fieldRef:\n                  fieldPath: status.hostIP\n            # A dashboard build is a large one; the default heap is not enough for it.\n            - name: NODE_OPTIONS\n              value: --max_old_space_size=4096\n          ports:\n            - containerPort: ${port}\n              protocol: TCP\n          # The first start is a clone, a yarn install and a first compile - minutes, not\n          # seconds - so the startup probe is allowed a long time before it gives up, and only\n          # then does the readiness probe take over.\n          startupProbe:\n            httpGet:\n              path: /index.html\n              port: ${port}\n              scheme: HTTPS\n            periodSeconds: 10\n            failureThreshold: 90\n          readinessProbe:\n            httpGet:\n              path: /index.html\n              port: ${port}\n              scheme: HTTPS\n            periodSeconds: 30\n            failureThreshold: 3\n          resources:\n            requests:\n              cpu: 250m\n              memory: 1Gi\n            limits:\n              cpu: "2"\n              memory: 5Gi\n          volumeMounts:\n            - name: work\n              mountPath: /workspaces\n            - name: workspace-config\n              mountPath: /workspace-config\n            - name: seed\n              mountPath: /seed\n        - name: browser\n          image: lscr.io/linuxserver/chromium:latest\n          env:\n            - name: CHROME_CLI\n              value: --remote-debugging-port=9222 --remote-allow-origins=* --ignore-certificate-errors ${scheme}://localhost:${port}\n            - name: DOCKER_MODS\n              value: linuxserver/mods:universal-package-install\n            - name: INSTALL_PACKAGES\n              value: ${a11yPackages}\n          ports:\n            - containerPort: 3000\n              protocol: TCP\n            - containerPort: 9222\n              protocol: TCP\n          resources:\n            requests:\n              cpu: 100m\n              memory: 512Mi\n            limits:\n              cpu: "1"\n              memory: 3Gi\n          volumeMounts:\n            # Chromium needs more shared memory than the default 64 MiB or it dies rendering\n            # anything substantial.\n            - name: dshm\n              mountPath: /dev/shm\n            - name: work\n              mountPath: /artifacts\n              subPath: ${install}/artifacts\n      volumes:\n        # The PARENT of the workspaces, deliberately: the checkout\'s node_modules is hard-linked\n        # from a template shared between workspaces, and a hard link cannot cross mounts.\n        - name: work\n          hostPath:\n            path: /var/lib/rancher/vuln-workspaces\n            type: DirectoryOrCreate\n        - name: workspace-config\n          configMap:\n            name: vuln-workspace-config\n            defaultMode: 365\n        # The agents extension\'s own seed, for the terminal tools. Optional: a workspace still\n        # builds and serves without it, it just has no tmux.\n        - name: seed\n          configMap:\n            name: agent-seed\n            defaultMode: 365\n            optional: true\n        - name: dshm\n          emptyDir:\n            medium: Memory\n            sizeLimit: 1Gi\n',"workspace/namespace.yaml":"# The workspace's own namespace. One per fix in flight, named for the installation, so a run's\n# pod, service and config are deleted together by deleting the installation.\napiVersion: v1\nkind: Namespace\nmetadata:\n  name: ${namespace}\n  labels:\n    vuln-console.rancher.io/workspace: ${install}\n  annotations:\n    vuln-console.rancher.io/port: \"${port}\"\n    vuln-console.rancher.io/scheme: ${scheme}\n","workspace/rbac.yaml":"# What the workspace pod may do, which is only ever within its own namespace.\n#\n# Deliberately not cluster-admin. The agents pod is cluster-admin because it edits the cluster;\n# a workspace only ever builds a branch and serves it, so a RoleBinding - not a ClusterRoleBinding\n# - is what it gets. A fix that needs more than this is a fix worth looking at.\napiVersion: v1\nkind: ServiceAccount\nmetadata:\n  namespace: ${namespace}\n  name: vuln-workspace\n---\napiVersion: rbac.authorization.k8s.io/v1\nkind: RoleBinding\nmetadata:\n  namespace: ${namespace}\n  name: vuln-workspace\nroleRef:\n  apiGroup: rbac.authorization.k8s.io\n  kind: ClusterRole\n  name: edit\nsubjects:\n  - kind: ServiceAccount\n    name: vuln-workspace\n    namespace: ${namespace}\n","workspace/service.yaml":"# Two ports, two ways in.\n#\n# The dev server (${port}) is reached through the Kubernetes apiserver's service proxy, on\n# Rancher's own origin - so there is no Ingress, no hostname to choose and no certificate to\n# wait for, and the browser's existing Rancher session authenticates it. The browser sidecar\n# (3000) is framed the same way.\napiVersion: v1\nkind: Service\nmetadata:\n  namespace: ${namespace}\n  name: ${namespace}\n  labels:\n    vuln-console.rancher.io/workspace: ${install}\nspec:\n  selector:\n    vuln-console.rancher.io/workspace: ${install}\n  ports:\n    - name: http\n      port: ${port}\n      targetPort: ${port}\n      protocol: TCP\n    - name: browser\n      port: 3000\n      targetPort: 3000\n      protocol: TCP\n"};const $e="appsplus.io.app",xe="appsplus.io.appinstance",_e="vuln-console.rancher.io/library",Ee="vuln-console.rancher.io/board";function Se(e){const n=e.getters["management/schemaFor"];return!!(null===n||void 0===n?void 0:n($e))&&!!(null===n||void 0===n?void 0:n(xe))}function Re(e,n){const t=n.toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-+|-+$/g,"").slice(0,30);return`vuln-${e}-${t||"fix"}`}function Ne(){const e=Object.keys(ke).filter(e=>e.startsWith("workspace/")).sort().map(e=>({name:e.replace("workspace/",""),content:ke[e]}));return{type:$e,apiVersion:"appsplus.io/v1alpha1",kind:"App",metadata:{name:M},spec:{description:"A rancher/dashboard checkout with its dependencies installed, the dev server running, and a browser beside it — where one Dependabot fix is made and verified. The first start is minutes: a clone, a yarn install and a first compile.",values:{repo:"rancher/dashboard",fork:"marcelofukumoto/dashboard",port:8005,scheme:"https",image:"node:24",hostCluster:"local",rancherUrl:"https://$(NODE_IP)",a11yPackages:"at-spi2-core|dbus-x11|gir1.2-atspi-2.0|python3-gi|python3-pyatspi"},valueLabels:{repo:"Repository the fix is made against",fork:"Fork the branch is pushed to",port:"Port the dev server listens on",scheme:"http or https",image:"Container image",hostCluster:"Cluster the workspace runs on",rancherUrl:"Rancher the dev server points at",a11yPackages:"What the browser installs (pipe separated)"},templates:e}}}async function Ae(e){var n;const t=Ne(),a=await e.dispatch("management/find",{type:$e,id:M}).catch(()=>null);if(!a){const n=await e.dispatch("management/create",t);return void await n.save()}JSON.stringify(null===(n=a.spec)||void 0===n?void 0:n.templates)!==JSON.stringify(t.spec.templates)&&(a.spec={...a.spec,...t.spec},await a.save())}async function Te(e,n,t){const a=Re(n.id,t),o=await e.dispatch("management/find",{type:xe,id:a}).catch(()=>null);if(o)return a;await Ae(e);const r=await e.dispatch("management/create",{type:xe,metadata:{name:a,labels:{[_e]:t.replace(/[^A-Za-z0-9_.-]/g,"-").slice(0,63),[Ee]:n.id}},spec:{app:M,namespace:a,targets:[{clusterName:"local"}],values:{repo:n.repo,fork:n.fork},provisionCluster:{enabled:!1}}});return await r.save(),a}const Oe={"address-comments.prompt.md":'# Address the review comments on a pull request\n\nRe-read the pull request\'s review comments, make any changes that are actually requested,\nre-verify, and **stage a reply for the human to send**.\n\n## The rule that matters most\n\n**Do not post a comment on the pull request.** Not a reply, not a summary, not an\nacknowledgement. Marcelo answers reviewers himself, in his own words — an auto-posted reply reads\nas his and is not.\n\nWhat you do instead: write the per-point reply as a draft and record where it is, so the board\nshows a **reply draft** pill he can read and then post himself.\n\nEditing the pull request **body** is still wanted — the description should stay true as the\nbranch changes, and the video belongs there.\n\n## What to do\n\n1. Read every review comment and every review state on the pull request.\n2. For each point: decide whether it asks for a change. If it does, make it in the checkout.\n3. If you changed anything: re-run the verification against the dev server, capture a fresh\n   video, and update the `### Screenshot/Video` section of the body.\n4. Write the reply draft — one short paragraph per point, saying what you did or why nothing\n   changed. Never a bare `#<number>` in it.\n5. Record:\n\n```\n$ROOT/job.sh "<library>" phase=Done replyUrl=<path> message="<one line: what changed>"\n```\n\nIf nothing needed changing, say so in the draft and record it the same way.\n',"fix.prompt.md":'# Fix one Dependabot vulnerability\n\nYou are the vulnerability console\'s fix agent. Somebody pressed **Fix** on one library. Bump it,\nprove the bump did not break where it is used, and record what you did. **Do not open a pull\nrequest** — that is a separate button and a separate run.\n\n## Where you are\n\nEvery command you run lands inside this fix\'s workspace, at the path it already has. You do not\nneed to `kubectl exec`, `ssh` or `cd` anywhere unusual; `yarn`, `git` and `node` are simply there.\n\n- the checkout is `$WSD/src`, on the repository\'s default branch\n- `origin` is the upstream repository, `fork` is where branches are pushed. The fork belongs to\n  whoever owns the stored token, and it has been created already if it did not exist\n- `gh` and `jq` are on `$PATH`, `GH_TOKEN` is in the environment of every command you run, and\n  git has a credential helper — so `gh` and `git push` work without you setting anything up\n- the dev server is **already running** and supervised. Never start a second one — the pod\'s\n  memory limit fits one, and two have OOM-killed the container out from under a run. Its URL is\n  in the job; a branch you check out is serving within a minute or two of compiling.\n\n## ⛔ You do not get woken up\n\nYou run one-shot. There is no notification, no wake-up, no re-invocation. The moment you stop to\n"wait for a background task" or "await completion", the run ends and everything you did is lost.\n\nRun every slow command **synchronously in the foreground**. If one might time out, block inline\nwith a single foreground loop over its log until a done marker appears. Never hand work to a\nbackground task and yield.\n\nDo **not** run the whole monorepo\'s `yarn lint` or `yarn test` — that is the pull request\'s CI\njob, it is slow, and it will strand you.\n\n## 1. Decide whether to fix, and on which branch\n\nA package can need fixing more than once over time, so branches are **numbered** and every\nre-fix gets its own. **Never reuse or force-push a branch that already has a pull request** —\nthat corrupts the earlier one and makes the new fix link to it.\n\nList what already exists:\n\n```\ngh pr list --repo <fork> --state all --json number,state,title,headRefName \\\n  | jq \'[.[] | select(.title | test("<library>"; "i"))]\'\ngh api repos/<fork>/branches --paginate --jq \'.[].name\' | grep -E "/<library>(-|$)"\n```\n\nThen:\n\n- **an open pull request of ours already covers every still-open line** — stop, and record\n  `phase=Done message="already covered by pull request <n>"`. Do not open another.\n- **an open pull request exists but does not cover this case** (it fixed a different manifest or\n  major line) — this is a separate fix: take the next free numbered branch.\n- **only merged pull requests exist and the advisory is open again** — always a new numbered\n  branch. Never touch the merged one\'s.\n- **nothing exists** — fix it on the base branch name.\n\nBranch names: `dependabot/npm_and_yarn/multi/<library>` for the first fix, then `<library>-1`,\n`<library>-2`, … — the first name not already in use.\n\n## 2. Make the fix\n\nStart from a clean tree on the repository\'s default branch, then bump the vulnerable dependency\nacross **every** manifest it touches and regenerate each lockfile.\n\n**Prefer a real update; a `resolutions` override is the last resort.** When a stale lockfile\nmerely holds a transitive at an old patch and the consuming range already admits a patched\nversion, `yarn upgrade <library>` lets it climb in place. Reach for a root `resolutions` pin only\nwhen neither an in-range update nor bumping the pinning parent can get there.\n\n### The three things reviewers actually catch\n\n1. **Cover every affected manifest, enumerated from the LIVE alerts** — not from the board,\n   which can be hours old:\n   ```\n   gh api repos/<repo>/dependabot/alerts --paginate \\\n     --jq \'.[]|select(.state=="open")|select(.security_vulnerability.package.name=="<library>")|.dependency.manifest_path\' \\\n     | sort -u\n   ```\n   Then grep the repository for every other `yarn.lock` pinning the vulnerable range. Bump them\n   all — root, `shell/`, **`cypress/`**, `storybook/`, `docusaurus/`, `pkg/*`. `cypress/yarn.lock`\n   is the commonly missed one.\n\n2. **Each affected major line needs that line\'s own first patched version.** They differ per\n   line: one advisory can require `1.1.16` *and* `2.1.2` *and* `5.0.7`, and a lower patch in one\n   line can still be vulnerable. Read each alert\'s `vulnerable_version_range` and\n   `first_patched_version`, and confirm the consuming `^x.y` ranges admit what you resolved to.\n\n3. **Introduce no new vulnerable package.** After regenerating, diff each lockfile for *added*\n   entries and check every newly added package and version — including a **different** package\n   the resolver pulled in (the classic is `braces < 3.0.3`). If the regeneration added a\n   vulnerable one, pin it forward, reinstall, and re-diff until the net change adds zero\n   vulnerabilities.\n\n### The lockfile will refuse to change\n\nThe checkout carries a root `.yarnrc` containing `--frozen-lockfile true`, and it is inherited by\nevery subdirectory. `yarn install` will install into `node_modules` and leave `yarn.lock`\nuntouched, so the bump appears to do nothing. Move the root `.yarnrc` aside, clear the package\nfrom `node_modules` and remove `.yarn-integrity`, install, then put `.yarnrc` back. Look for\n`success Saved lockfile.` and confirm the new version in the lock. **Never commit a `.yarnrc`\nchange.**\n\nCommit **only** `package.json` and `yarn.lock` files. No attribution trailers, no co-author\nlines, and no pull request or issue numbers in the message.\n\nRecord the branch as soon as it is pushed:\n\n```\n$ROOT/job.sh "<library>" branch=<branch>\n```\n\n## 3. Prove it still works\n\nThe point is not that it builds — it is that the place this library is actually used still\nbehaves. Grep the checkout for where it is imported, pick two to four real interactions that\nexercise it, and drive them in the browser against the running dev server.\n\nUse the browser that is already in this pod at `$VULN_BROWSER_CDP` — connect over CDP, do not\ninstall a browser. Capture an annotated video and take screenshots of each check.\n\nWrite the verification up as numbered checks, each ending ✅ or ❌, followed by a one-line\nverdict. Then record it:\n\n```\n$ROOT/job.sh "<library>" phase=Fixed previewUrl=<url> videoUrl=<path> infoUrl=<path> \\\n  message="<one line: what was bumped, in how many manifests, and what was verified>"\n```\n\nIf a check fails, that is a real result: record `phase=Failed` with what broke. A fix that\nbreaks the feature is worse than the advisory.\n\n## What you must not do\n\n- do not open a pull request\n- do not comment on anything on GitHub\n- do not touch any repository other than the checkout in your workspace\n- do not start a second dev server\n',"gather.mjs":"#!/usr/bin/env node\n// Gather the Dependabot picture for one repository: every alert, Dependabot's own pull\n// requests, and ours.\n//\n// Dependency-free and shell-free on purpose. This runs inside the agents pod, a stock\n// `node:24` with neither `jq` nor `gh` in it, so the laptop pipeline it replaces\n// (scripts/gather-vulnerabilities.sh, which is `gh api | jq` from end to end) is re-expressed\n// in the one runtime the pod is guaranteed to have.\n//\n// It writes ALL alert states, not just the open ones. The board needs the closed ones to show\n// what we shipped, and - more importantly - closing is the ONLY thing that moves a row out of\n// the actionable list. A gather that dropped them would leave the board unable to tell \"fixed\"\n// from \"never seen\".\n\nimport { readFileSync, writeFileSync } from 'node:fs';\n\nconst REPO = process.env.VULN_REPO || 'rancher/dashboard';\n\n/**\n * The package this repository gets most of its tree from, if it has one.\n *\n * Empty for a repository that is its own tree. Set, it turns on the lockfile walk below.\n */\nconst OWNER_PACKAGE = process.env.OWNER_PACKAGE || '';\nconst OUT = process.env.OUT || './snapshot.json';\nconst API = process.env.GITHUB_API || 'https://api.github.com';\n\nfunction fail(message) {\n  process.stderr.write(`gather: ${ message }\\n`);\n  process.exit(1);\n}\n\n/**\n * The token, read from a file rather than the environment.\n *\n * A pod is a place where `ps` and `/proc/<pid>/environ` are readable, and this one is shared:\n * every conversation in it runs as the same user. A token on a command line or in an exported\n * variable is a token every pane can read for as long as the process lives. The file is written\n * 0600, read once here, and removed when the run ends.\n */\nfunction token() {\n  const file = process.env.CREDS_FILE;\n\n  if (!file) {\n    return process.env.GH_TOKEN || '';\n  }\n\n  try {\n    return JSON.parse(readFileSync(file, 'utf8')).GH_TOKEN || '';\n  } catch (e) {\n    fail(`could not read the credentials at ${ file }: ${ e?.message || e }`);\n  }\n}\n\nconst GH_TOKEN = token();\n\nif (!GH_TOKEN) {\n  fail('GH_TOKEN is not set. Dependabot alerts cannot be read without it.');\n}\n\nconst HEADERS = {\n  Authorization: `Bearer ${ GH_TOKEN }`,\n  Accept:        'application/vnd.github+json',\n  'User-Agent':  'vuln-console',\n};\n\nasync function request(url, what) {\n  let resp;\n\n  try {\n    resp = await fetch(url, { headers: HEADERS });\n  } catch (e) {\n    throw new Error(`${ what }: ${ e?.message || e }`, { cause: e });\n  }\n\n  const text = await resp.text();\n\n  if (!resp.ok) {\n    const detail = text.slice(0, 300).replace(/\\s+/g, ' ').trim();\n\n    throw new Error(`${ what }: HTTP ${ resp.status }${ detail ? ` - ${ detail }` : '' }`);\n  }\n\n  try {\n    return { body: JSON.parse(text), link: resp.headers.get('link') || '' };\n  } catch {\n    throw new Error(`${ what }: the answer was not JSON`);\n  }\n}\n\n/** Follow `Link: rel=\"next\"` rather than counting pages - GitHub decides where the end is. */\nasync function paginate(url, what) {\n  const all = [];\n  let next = url;\n\n  while (next) {\n    const { body, link } = await request(next, what);\n\n    if (!Array.isArray(body)) {\n      throw new Error(`${ what }: expected a list, got ${ typeof body }`);\n    }\n\n    all.push(...body);\n\n    const more = /<([^>]+)>;\\s*rel=\"next\"/.exec(link);\n\n    next = more ? more[1] : null;\n  }\n\n  return all;\n}\n\nfunction severityOf(value) {\n  const known = ['critical', 'high', 'medium', 'low'];\n\n  return known.includes(value) ? value : 'low';\n}\n\nasync function fetchAlerts() {\n  // No `state` filter, deliberately. The parameter takes a comma-separated list of the four\n  // real states and has no \"all\" - and an unrecognised value is not an error, it is an empty\n  // list. Asking for `state=all` returns zero alerts with HTTP 200, which is indistinguishable\n  // from a repository that has none.\n  const raw = await paginate(\n    `${ API }/repos/${ REPO }/dependabot/alerts?per_page=100`,\n    'reading the Dependabot alerts',\n  );\n\n  // Trimmed, because this ends up in a ConfigMap and a ConfigMap holds a megabyte. The full\n  // record for all 969 of rancher/dashboard's alerts is 521 KiB, which is half the ceiling on a\n  // list that only grows - so the snapshot carries what the BOARD reads and nothing else:\n  //\n  //   * `url` is derived (`<repo>/security/dependabot/<id>`), not stored.\n  //   * the vulnerable range, scope and relationship are not stored at all. They belong to\n  //     fixing, and the fix prompt already enumerates them from the LIVE alerts rather than the\n  //     snapshot - precisely because a snapshot can be hours old and a manifest list must not be.\n  //   * a CLOSED alert keeps only what the shipped list shows. 959 of the 969 are closed, and\n  //     what they are for is \"we fixed this\" - not the advisory text.\n  return raw.map((a) => {\n    const open = a.state === 'open';\n    const alert = {\n      id:       a.number,\n      library:  a.security_vulnerability?.package?.name || a.dependency?.package?.name || '?',\n      ghsa:     a.security_advisory?.ghsa_id || '',\n      severity: severityOf(a.security_advisory?.severity),\n      manifest: a.dependency?.manifest_path || '',\n      state:    a.state,\n      patched:  a.security_vulnerability?.first_patched_version?.identifier || null,\n      fixedAt:  a.fixed_at || null,\n    };\n\n    if (!open) {\n      return alert;\n    }\n\n    return {\n      ...alert,\n      ecosystem: a.security_vulnerability?.package?.ecosystem || 'npm',\n      cve:       a.security_advisory?.cve_id || null,\n      summary:   a.security_advisory?.summary || '',\n      createdAt: a.created_at,\n    };\n  });\n}\n\nfunction normalisePr(pr) {\n  return {\n    number:      pr.number,\n    url:         pr.html_url,\n    title:       pr.title || '',\n    headRefName: pr.head?.ref || '',\n    status:      pr.merged_at ? 'merged' : (pr.state === 'closed' ? 'closed' : 'open'),\n    author:      pr.user?.login || '',\n    createdAt:   pr.created_at,\n    mergedAt:    pr.merged_at || null,\n    videoUrl:    videoIn(pr.body || ''),\n  };\n}\n\n/**\n * A verification video already attached to a pull request body.\n *\n * Detecting it means the board's recording pill survives losing our own record of the run -\n * the pull request is the durable copy, and a GitHub-hosted URL is the published one.\n */\nfunction videoIn(body) {\n  const found = /https:\\/\\/github\\.com\\/user-attachments\\/assets\\/[0-9a-f-]+/i.exec(body);\n\n  return found ? found[0] : null;\n}\n\n/**\n * Our pull requests, in one GraphQL search.\n *\n * Not by paging `/pulls?state=closed`: rancher/dashboard has tens of thousands and the ones\n * that matter are a few hundred by one author. Not by the REST search either - that returns\n * issue records carrying neither the head branch nor a body, so every hit needed a second\n * call and a few hundred of those take longer than the whole rest of the gather. GraphQL\n * answers the question and returns the fields in the same request.\n *\n * Both fields it adds are load-bearing: a closed alert is attributed to a merge by TIMING, and\n * the branch is the fallback when a title says nothing.\n */\n/**\n * Who the stored token belongs to.\n *\n * Everything \"ours\" is defined by this and not by a name written into the extension: our pull\n * requests are the ones this account authored, and our fork is this account's fork. A hard-coded\n * login makes an extension that works for exactly one person and fails at `git push` for\n * everybody else, three minutes into a run.\n */\nasync function tokenOwner() {\n  const { body } = await request(`${ API }/user`, 'identifying the stored token');\n\n  if (!body?.login) {\n    throw new Error('identifying the stored token: the answer carried no login');\n  }\n\n  return body.login;\n}\n\nasync function fetchOurPulls(owner) {\n  const query = `repo:${ REPO } author:${ owner } is:pr`;\n  const found = [];\n  let cursor = null;\n\n  for (;;) {\n    const page = await graphql(\n      `query($q: String!, $after: String) {\n         search(query: $q, type: ISSUE, first: 100, after: $after) {\n           pageInfo { hasNextPage endCursor }\n           nodes {\n             ... on PullRequest {\n               number url title headRefName state createdAt mergedAt body\n               author { login }\n             }\n           }\n         }\n       }`,\n      { q: query, after: cursor },\n    );\n    const { nodes = [], pageInfo = {} } = page.search || {};\n\n    found.push(...nodes.filter((n) => n && n.number));\n\n    if (!pageInfo.hasNextPage) {\n      break;\n    }\n\n    cursor = pageInfo.endCursor;\n  }\n\n  return found\n    .map((n) => ({\n      number:      n.number,\n      url:         n.url,\n      title:       n.title || '',\n      headRefName: n.headRefName || '',\n      status:      n.mergedAt ? 'merged' : (n.state === 'CLOSED' ? 'closed' : 'open'),\n      author:      n.author?.login || '',\n      createdAt:   n.createdAt,\n      mergedAt:    n.mergedAt || null,\n      videoUrl:    videoIn(n.body || ''),\n    }))\n    .filter((p) => p.status !== 'closed');\n}\n\nasync function graphql(query, variables) {\n  let resp;\n\n  try {\n    resp = await fetch(`${ API }/graphql`, {\n      method:  'POST',\n      headers: { ...HEADERS, 'Content-Type': 'application/json' },\n      body:    JSON.stringify({ query, variables }),\n    });\n  } catch (e) {\n    throw new Error(`searching for our pull requests: ${ e?.message || e }`, { cause: e });\n  }\n\n  const text = await resp.text();\n\n  if (!resp.ok) {\n    throw new Error(`searching for our pull requests: HTTP ${ resp.status } - ${ text.slice(0, 300) }`);\n  }\n\n  const body = JSON.parse(text);\n\n  // GraphQL answers 200 with an `errors` array, so a failure here looks like a success to\n  // anything that only checks the status.\n  if (body.errors?.length) {\n    throw new Error(`searching for our pull requests: ${ body.errors.map((e) => e.message).join('; ') }`);\n  }\n\n  return body.data;\n}\n\n/** Dependabot's own open pull requests - the \"there is already one upstream\" signal. */\nasync function fetchDependabotPulls() {\n  const open = await paginate(\n    `${ API }/repos/${ REPO }/pulls?state=open&per_page=100`,\n    'reading the open pull requests',\n  );\n\n  return open.map(normalisePr).filter((p) => p.author === 'dependabot[bot]');\n}\n\n// ── Who owns a vulnerable library ──────────────────────────────────────────────────────────\n\n/**\n * Parse a yarn v1 lockfile into `spec -> { version, deps }`.\n *\n * The keys are exactly the `name@range` specs a package asks for, which is what makes the walk\n * below simple: a dependency's declared range IS the key of the entry that satisfies it, so\n * nothing has to resolve semver.\n */\nfunction parseLock(text) {\n  const entries = new Map();\n  let specs = null;\n  let version = '';\n  let deps = [];\n  let inDeps = false;\n\n  const flush = () => {\n    if (specs) {\n      for (const spec of specs) {\n        entries.set(spec, { version, deps });\n      }\n    }\n\n    specs = null;\n    version = '';\n    deps = [];\n    inDeps = false;\n  };\n\n  for (const raw of text.split('\\n')) {\n    if (!raw.trim() || raw.startsWith('#')) {\n      continue;\n    }\n\n    const indent = raw.length - raw.trimStart().length;\n    const line = raw.trim();\n\n    if (indent === 0) {\n      flush();\n      specs = line.replace(/:$/, '').split(',').map((part) => part.trim().replace(/^\"|\"$/g, ''));\n      continue;\n    }\n\n    if (indent === 2) {\n      inDeps = /^(dependencies|optionalDependencies):$/.test(line);\n\n      const found = /^version \"?([^\"]+)\"?$/.exec(line);\n\n      if (found) {\n        version = found[1];\n      }\n      continue;\n    }\n\n    if (indent >= 4 && inDeps) {\n      const found = /^\"?(@?[^\"\\s]+)\"?\\s+\"?([^\"]+)\"?$/.exec(line);\n\n      if (found) {\n        deps.push(`${ found[1] }@${ found[2] }`);\n      }\n    }\n  }\n\n  flush();\n\n  return entries;\n}\n\nfunction reachable(entries, roots) {\n  const seen = new Set();\n  const queue = [...roots];\n\n  while (queue.length) {\n    const spec = queue.pop();\n\n    if (seen.has(spec)) {\n      continue;\n    }\n\n    seen.add(spec);\n\n    const entry = entries.get(spec);\n\n    if (entry) {\n      queue.push(...entry.deps);\n    }\n  }\n\n  return seen;\n}\n\n/** Package names out of a set of `name@range` specs. */\nfunction namesOf(specs) {\n  const names = new Set();\n\n  for (const spec of specs) {\n    const at = spec.lastIndexOf('@');\n\n    if (at > 0) {\n      names.add(spec.slice(0, at));\n    }\n  }\n\n  return names;\n}\n\nasync function repoFile(path) {\n  const resp = await fetch(`${ API }/repos/${ REPO }/contents/${ path }`, {\n    headers: { ...HEADERS, Accept: 'application/vnd.github.raw' },\n  });\n\n  if (!resp.ok) {\n    throw new Error(`reading ${ path }: HTTP ${ resp.status }`);\n  }\n\n  return resp.text();\n}\n\n/**\n * Which vulnerable libraries are in the tree ONLY because of the owner package.\n *\n * Walked from the lockfile rather than guessed from the alert's `relationship`, because every\n * alert on a repository like this one is `transitive` - that field says the library is not a\n * direct dependency, not who pulled it in. rancher-ai-ui's `extract-zip` and `tmp` are\n * transitive too, and they come from Cypress and Jest; calling them shell's would be wrong in a\n * way somebody would only discover by trying to fix one.\n */\nasync function ownedLibraries() {\n  if (!OWNER_PACKAGE) {\n    return { ownerOnly: [], ownerVersion: '' };\n  }\n\n  const [manifest, lock] = await Promise.all([repoFile('package.json'), repoFile('yarn.lock')]);\n  const pkg = JSON.parse(manifest);\n  const direct = { ...(pkg.dependencies || {}), ...(pkg.devDependencies || {}) };\n  const entries = parseLock(lock);\n\n  const ownerRoots = [];\n  const otherRoots = [];\n\n  for (const [name, range] of Object.entries(direct)) {\n    (name === OWNER_PACKAGE ? ownerRoots : otherRoots).push(`${ name }@${ range }`);\n  }\n\n  if (!ownerRoots.length) {\n    return { ownerOnly: [], ownerVersion: '' };\n  }\n\n  const viaOwner = namesOf(reachable(entries, ownerRoots));\n  const viaOther = namesOf(reachable(entries, otherRoots));\n  const ownerOnly = [...viaOwner].filter((name) => !viaOther.has(name)).sort();\n\n  return { ownerOnly, ownerVersion: entries.get(ownerRoots[0])?.version || '' };\n}\n\nasync function main() {\n  // First, because everything \"ours\" is defined by it.\n  const owner = await tokenOwner();\n  const [alerts, dependabotPrs, ourPrs, owned] = await Promise.all([\n    fetchAlerts(),\n    fetchDependabotPulls(),\n    fetchOurPulls(owner),\n    // Never fatal: a board that cannot read its own lockfile is a board with no owner\n    // information, which is the same board it was before this existed - not a failed gather.\n    ownedLibraries().catch((e) => {\n      process.stderr.write(`gather: could not work out what ${ OWNER_PACKAGE } owns (${ e?.message || e })\\n`);\n\n      return { ownerOnly: [], ownerVersion: '' };\n    }),\n  ]);\n\n  // A transient failure must never be written as an empty snapshot. The console this replaces\n  // piped `gh api | jq ... || echo \"[]\"`, so a rate-limited page produced a valid 0-alert file\n  // that the watcher then reused for the rest of the day - the board simply went blank while\n  // the repository had 156 open alerts. Any failure above has already thrown; an empty result\n  // here is treated as one too.\n  if (!alerts.length) {\n    fail('no alerts came back at all. Refusing to write a snapshot that would blank the board.');\n  }\n\n  // Only the vulnerable libraries are worth storing, not the whole reachable set - the set is\n  // thousands of names and the board only ever asks about the ones it draws.\n  const vulnerable = new Set(alerts.map((a) => a.library));\n  const snapshot = {\n    gatheredAt: new Date().toISOString(),\n    repo:       REPO,\n    tokenLogin: owner,\n    alerts,\n    dependabotPrs,\n    ourPrs,\n    ...(OWNER_PACKAGE ? {\n      ownerPackage: OWNER_PACKAGE,\n      ownerVersion: owned.ownerVersion,\n      ownerOnly:    owned.ownerOnly.filter((name) => vulnerable.has(name)),\n    } : {}),\n  };\n\n  writeFileSync(OUT, JSON.stringify(snapshot));\n\n  const open = alerts.filter((a) => a.state === 'open').length;\n\n  const owns = snapshot.ownerOnly?.length\n    ? `, ${ snapshot.ownerOnly.length } only via ${ OWNER_PACKAGE } ${ snapshot.ownerVersion }`\n    : '';\n\n  process.stderr.write(\n    `gather: ${ alerts.length } alerts (${ open } open), ${ dependabotPrs.length } Dependabot pull requests, ${ ourPrs.length } by ${ owner }${ owns } -> ${ OUT }\\n`,\n  );\n}\n\nmain().catch((e) => fail(e?.message || String(e)));\n","gather.sh":'#!/bin/sh\n# Refresh the board: gather the Dependabot picture and put it in the cluster.\n#\n# Deterministic from end to end - there is no agent in this path and nothing is judged. The old\n# console needed a watcher on somebody\'s laptop to do this on a timer; here it is a script the\n# extension runs in a pod it already has, and the result is a ConfigMap rather than a file in an\n# emptyDir that a pod rollout wiped.\n#\n# It resolves the token itself rather than being handed one. A token pasted into a page and\n# written through the exec socket is a token the browser held; read here, with the pod\'s own\n# ServiceAccount, at the moment it is needed, the browser never has it at all.\n#\n# usage: gather.sh <work-dir> <board-id> <repo> <token-key> [owner-package]\nset -e\n\nDIR=${1:?gather.sh needs a working directory}\nBOARD=${2:?gather.sh needs a board id}\nREPO=${3:?gather.sh needs a repository}\n# The Secret key holding the token of the person who asked for this. Per user, so a gather is\n# done as them and "our pull requests" means theirs.\nTOKEN_KEY=${4:?gather.sh needs the token key}\n# Optional: the package this repository gets most of its tree from. Empty for one that is its own.\nOWNER_PACKAGE=${5:-}\nSEED=$(dirname "$0")\n\n[ -d "$DIR" ] || { echo "gather.sh: no such directory: $DIR" >&2; exit 2; }\n\nNS=vuln-console\nSECRET=settings\n\n# kubectl as the pod rather than as whoever opened a terminal in it. shell.sh writes a kubeconfig\n# carrying the Rancher identity of the person who opened the pane, and that identity may not be\n# allowed to read these Secrets - the pod\'s ServiceAccount is. A KUBECONFIG naming nothing is\n# what sends kubectl to the in-cluster config.\nkube() {\n  KUBECONFIG=/dev/null kubectl "$@"\n}\n\nsecret_key() {\n  kube get secret "$2" -n "$1" -o "jsonpath={.data.$3}" 2>/dev/null | base64 -d 2>/dev/null | tr -d \'\\r\\n\'\n}\n\nGH_TOKEN=$(secret_key "$NS" "$SECRET" "$TOKEN_KEY")\n\nif [ -z "$GH_TOKEN" ]; then\n  echo "gather.sh: no GitHub token is stored for this user. Set one from the Credentials dialog." >&2\n  exit 2\nfi\n\n# 0600 before anything is in it, so it is never briefly readable. Removed however this ends.\nCREDS="$DIR/creds.json"\nSNAPSHOT="$DIR/snapshot.json"\n\ncleanup() { rm -f "$CREDS"; }\ntrap cleanup EXIT INT TERM\n\n: > "$CREDS"\nchmod 600 "$CREDS"\n\n# Through node rather than printf, so a token containing a quote or a backslash is JSON-encoded\n# rather than pasted into a string and hoped for.\nGH_TOKEN="$GH_TOKEN" node -e \\\n  \'require("fs").writeFileSync(process.argv[1], JSON.stringify({ GH_TOKEN: process.env.GH_TOKEN }))\' \\\n  "$CREDS"\n\nCREDS_FILE="$CREDS" OUT="$SNAPSHOT" VULN_REPO="$REPO" OWNER_PACKAGE="$OWNER_PACKAGE" \\\n  node "$SEED/gather.mjs"\n\ncleanup\n\n# A ConfigMap holds a megabyte. A snapshot that does not fit has to fail as a gather somebody can\n# see, not as a kubectl error nobody reads.\nSIZE=$(wc -c < "$SNAPSHOT")\n\nif [ "$SIZE" -gt 950000 ]; then\n  echo "gather.sh: the snapshot is $((SIZE / 1024)) KiB, over what a ConfigMap can hold" >&2\n  exit 2\nfi\n\nkube get namespace "$NS" >/dev/null 2>&1 || kube create namespace "$NS" >/dev/null\n\n# Server-side apply, NOT the client-side default. A client-side `apply` records the whole object\n# it sent in a `kubectl.kubernetes.io/last-applied-configuration` annotation, and annotations are\n# capped at 256 KiB - so a snapshot large enough to be worth having is one the apiserver rejects\n# outright ("metadata.annotations: Too long"). Server-side apply keeps its bookkeeping in\n# managedFields instead, where there is no such limit.\n\nkube create configmap "snapshot-$BOARD" \\\n  --namespace "$NS" \\\n  --from-file=snapshot.json="$SNAPSHOT" \\\n  --dry-run=client -o json |\n  node -e \'\n    let raw = "";\n\n    process.stdin.on("data", (c) => (raw += c));\n    process.stdin.on("end", () => {\n      const cm = JSON.parse(raw);\n\n      cm.metadata.labels = {\n        "vuln-console.rancher.io/owns": "true",\n        "vuln-console.rancher.io/board": process.argv[1],\n      };\n      process.stdout.write(JSON.stringify(cm));\n    });\n  \' "$BOARD" |\n  kube apply --server-side --force-conflicts -f - >/dev/null\n\necho "gather.sh: published a $((SIZE / 1024)) KiB snapshot for $BOARD ($REPO)"\n',"job.sh":'#!/bin/sh\n# Record what a run achieved, as fields on its job.\n#\n# The agent calls this rather than composing kubectl itself, for the same reason the report\n# console has a publish script: the object name and the label are what the board selects on, so\n# a job written with a typo\'d label is a job that exists and cannot be found.\n#\n# It MERGES. Each call sets the fields it is given and leaves the rest, so recording a branch\n# early and a pull request later does not erase the branch - which is the failure mode of every\n# "write the whole file" version of this.\n#\n# usage: job.sh <board> <library> phase=Done branch=... prNumber=123 message="..."\nset -e\n\nBOARD=${1:?job.sh needs the board}\nLIBRARY=${2:?job.sh needs the library}\nshift 2\n\nNS=vuln-console\n\nkube() { KUBECONFIG=/dev/null kubectl "$@"; }\n\n# The object name, by the same rule the extension uses: lowercased, anything that is not a\n# letter or a digit becomes a hyphen. `@scope/name` is not a legal object name and this is only\n# an address - the record keeps the real library name in a field.\nNAME=job-$BOARD-$(printf \'%s\' "$LIBRARY" | tr \'[:upper:]\' \'[:lower:]\' | sed \'s/[^a-z0-9]\\{1,\\}/-/g; s/^-*//; s/-*$//\' | cut -c1-40)\n\nCURRENT=$(kube get configmap "$NAME" -n "$NS" -o "jsonpath={.data.job\\.json}" 2>/dev/null || true)\n\n# Read the current record, apply the assignments, write it back. Through node because the values\n# are arbitrary text - a message can contain a quote, a URL contains slashes - and sed would\n# mangle both.\nUPDATED=$(CURRENT="$CURRENT" LIBRARY="$LIBRARY" BOARD="$BOARD" node -e \'\n  const assignments = process.argv.slice(1);\n  let job = {};\n\n  try {\n    job = JSON.parse(process.env.CURRENT || "{}") || {};\n  } catch {\n    job = {};\n  }\n\n  job.library = job.library || process.env.LIBRARY;\n  job.board = job.board || process.env.BOARD;\n\n  // Numbers stay numbers and the empty string clears a field, so `prNumber=` is how a run says\n  // "there is no pull request" without writing the string "null" into the board.\n  for (const pair of assignments) {\n    const at = pair.indexOf("=");\n\n    if (at < 1) {\n      continue;\n    }\n\n    const key = pair.slice(0, at);\n    const raw = pair.slice(at + 1);\n\n    if (raw === "") {\n      job[key] = null;\n    } else if (/^(prNumber|startedAt|updatedAt)$/.test(key)) {\n      job[key] = Number(raw);\n    } else if (key === "vulnIds") {\n      job[key] = raw.split(",").map((n) => Number(n.trim())).filter((n) => !Number.isNaN(n));\n    } else {\n      job[key] = raw;\n    }\n  }\n\n  job.updatedAt = Date.now();\n  process.stdout.write(JSON.stringify(job));\n\' "$@")\n\n[ -n "$UPDATED" ] || { echo "job.sh: refusing to write an empty job" >&2; exit 1; }\n\nkube get namespace "$NS" >/dev/null 2>&1 || kube create namespace "$NS" >/dev/null\n\nprintf \'%s\' "$UPDATED" > /tmp/job-$$.json\n\nkube create configmap "$NAME" \\\n  --namespace "$NS" \\\n  --from-file=job.json=/tmp/job-$$.json \\\n  --dry-run=client -o json |\n  node -e \'\n    let raw = "";\n\n    process.stdin.on("data", (c) => (raw += c));\n    process.stdin.on("end", () => {\n      const cm = JSON.parse(raw);\n\n      cm.metadata.labels = {\n        "vuln-console.rancher.io/owns": "true",\n        "vuln-console.rancher.io/board": process.argv[1],\n      };\n      process.stdout.write(JSON.stringify(cm));\n    });\n  \' "$BOARD" |\n  kube apply --server-side --force-conflicts -f - >/dev/null\n\nrm -f /tmp/job-$$.json\n\necho "job.sh: recorded $*"\n',"pr.prompt.md":"# Open the pull request for a verified fix\n\nThe fix is made, pushed and verified. Open the pull request for it, and nothing else.\n\n## Rules that are not negotiable\n\n- **Open it as a draft, and leave it a draft.** The human marks it ready for review themselves.\n  Never run `gh pr ready`.\n- **Never write a bare `#<number>`** in the body. GitHub auto-links it to an unrelated pull\n  request or issue, which is a false cross-reference somebody then has to explain. Use a full URL\n  or a link label.\n- **No attribution trailers** and no reviewer names in the commit history.\n\n## What to do\n\n1. Confirm the branch is pushed and its diff is what you expect. Diff it against **upstream's**\n   default branch, never the fork's own — the fork's is chronically stale, and judging by it\n   makes a clean fix look like it carries half the repository with it.\n\n2. Open the draft pull request from the fix branch. Title it the way Dependabot does:\n   `Bump <library> from <old> to <new>` — the board reads the library out of that title, and a\n   title it cannot read is a row that loses its pull request.\n\n3. The body carries: what was bumped and in which manifests, a `### Vulnerabilities fixed`\n   section listing each advisory by GHSA id with a full URL, and a `### Screenshot/Video`\n   section. Put the verification video's URL **on its own line** — that is what makes GitHub\n   render a player rather than a link.\n\n4. Request the reviewers the board has selected, if any are given to you.\n\n5. Record it, including the alert ids this pull request covers — that record is what ties them\n   together afterwards, and it beats any guess the board could make from titles:\n\n```\n$ROOT/job.sh \"<library>\" phase=Done prUrl=<url> prNumber=<n> vulnIds=<id,id,id>\n```\n\n## What you must not do\n\n- do not mark it ready for review\n- do not merge it\n- do not comment on it\n","publish.prompt.md":'# Attach a staged recording to its pull request\n\nThe recording exists and the pull request exists; put one in the other.\n\n1. Upload the video as a GitHub user-attachment so it renders as an inline player. A release\n   asset link renders as a link, not a player, which is not what this is for.\n2. Splice it into the pull request body\'s `### Screenshot/Video` section, **on its own line**.\n3. Leave the staged copy alone until the upload is confirmed, then record the published URL:\n\n```\n$ROOT/job.sh "<library>" videoUrl=<github-url>\n```\n\nDo not comment on the pull request. Do not mark it ready for review.\n',"record.prompt.md":'# Capture the verification recording again\n\nThe fix exists but its recording does not, or needs redoing. Produce it, and change nothing else.\n\n1. Check out the fix branch in the workspace and let the supervised dev server compile it. Do not\n   start a second server.\n2. Grep the checkout for where the library is used and pick two to four real interactions that\n   exercise it.\n3. Drive them against the dev server through the browser already in this pod\n   (`$VULN_BROWSER_CDP`), connecting over CDP. Capture an annotated video and screenshots.\n4. Write the checks up as a numbered list, each ending ✅ or ❌, then a one-line verdict.\n5. Record:\n\n```\n$ROOT/job.sh "<library>" videoUrl=<path> infoUrl=<path> message="<one line verdict>"\n```\n\nDo not change the branch, do not open or edit a pull request.\n',"resolve-conflict.prompt.md":'# Rebase a fix branch onto the latest default branch\n\nThe pull request has drifted from its base, or conflicts with it. Bring it up to date.\n\n## What to do\n\n1. Fetch upstream and check whether the branch actually conflicts. GitHub computes mergeability\n   asynchronously, so a "conflict" flag can be one poll stale — look for yourself.\n2. If it is clean and merely behind, rebase it onto the latest default branch.\n3. If it conflicts, rebase and **re-apply the fix** rather than resolving by hand where the\n   resolution is a lockfile: regenerate the lockfiles from the rebased tree using the recipe in\n   the fix prompt, so what lands is a real resolution and not a merge artefact.\n4. Re-check that no vulnerable version remains and that the diff adds no new vulnerable package.\n5. Force-push **only this branch**, and only when it is the branch this job owns.\n6. Record:\n\n```\n$ROOT/job.sh "<library>" phase=Done message="<one line: rebased onto <sha>, lockfiles regenerated>"\n```\n\n## What you must not do\n\n- do not force-push any branch other than this one\n- do not comment on the pull request\n- do not mark it ready for review\n',"workspace/boot.sh":'#!/bin/sh\n# Bring one fix workspace up: a rancher/dashboard checkout with its dependencies installed and\n# the dev server supervised on top of it.\n#\n# The container\'s main process ends up being the supervisor, not the server, because a fix run\n# does things that kill a dev server - switches branch, regenerates a lockfile, runs a build -\n# and a pod whose main process was the server would restart on each of those and take the run\'s\n# terminal with it.\n#\n# `${install}`, `${repo}` and `${port}` are apps-plus substitutions, filled in when the App is\n# rendered. Everything written `$LIKE_THIS` is ordinary shell and is left alone: apps-plus only\n# substitutes the braced form, and only for names its App declares.\nset -e\n\n# Force apt onto IPv4 before anything runs one. The node has no working IPv6 route to the Debian\n# mirror, so an `apt-get update` left alone resolves an IPv6 address and hangs on it for minutes\n# while holding the apt lock - which is how a workspace comes up with no tmux and no terminal.\nmkdir -p /etc/apt/apt.conf.d\nprintf \'Acquire::ForceIPv4 "true";\\nAcquire::Retries "3";\\n\' > /etc/apt/apt.conf.d/99vuln-ipv4\n\nWS=/workspaces/${install}\nmkdir -p "$WS/.home"\nchown node:node "$WS" "$WS/.home" 2>/dev/null || true\n[ -f "$WS/.owned" ] || { chown -R node:node "$WS" 2>/dev/null; touch "$WS/.owned"; }\n\n# The browser sidecar mounts these as subPaths, and the kubelet creates a missing subPath as\n# root - leaving the seed unable to write into it.\nmkdir -p "$WS/.a11y/opt" "$WS/.a11y/init"\nchown -R node:node "$WS/.a11y" 2>/dev/null || true\n\n# What every workspace on this node shares rather than keeping its own copy. Measured before\n# this existed: Cypress 813 MB, yarn 749 MB, and a gigabyte of node_modules - most of a\n# workspace\'s 3 GB, per workspace.\nSHARED=/workspaces/.shared\nmkdir -p "$SHARED/yarn" "$SHARED/cypress" "$SHARED/npm" "$SHARED/template"\nchown node:node "$SHARED" "$SHARED/yarn" "$SHARED/cypress" "$SHARED/npm" "$SHARED/template" 2>/dev/null || true\n\n# tmux and kubectl, which the terminal needs. No claude: the conversation runs in the agents pod\n# and reaches this one through a tunnel, so a claude here is hundreds of megabytes of hostPath\n# that nothing executes.\n[ -f /seed/terminal-tools.sh ] \\\n  && (TOOLS_NO_CLAUDE=1 HOME_DIR="$WS/.home" /bin/sh /seed/terminal-tools.sh >"$WS/.terminal-tools.log" 2>&1 &) \\\n  || true\n\n# What a recording and a lockfile bump need and the image lacks. Backgrounded so the dev server\n# is not a minute later for it; the lock timeout lets it wait for the tmux install above rather\n# than colliding with it.\n(command -v ffmpeg >/dev/null 2>&1 && command -v lsof >/dev/null 2>&1) || (\n  apt-get -o DPkg::Lock::Timeout=300 update -qq \\\n    && DEBIAN_FRONTEND=noninteractive apt-get -o DPkg::Lock::Timeout=300 install -y -qq \\\n      ffmpeg jq lsof iproute2 >"$WS/.apt.log" 2>&1 &\n) || true\n\n# /workspace-config, not /seed. This extension\'s scripts are mounted from its own ConfigMap;\n# /seed is the agents extension\'s, which is optional here and usually absent - so pointing at it\n# crash-looped the pod with "cannot open /seed/checkout.sh".\nexec setpriv --reuid=1000 --regid=1000 --init-groups /bin/sh /workspace-config/checkout.sh\n',"workspace/checkout.sh":'#!/bin/sh\n# The workspace\'s checkout and install, as the node user. Split from boot.sh so the privileged\n# half is short enough to read and this half can be run again by hand.\nset -e\n\nWS=/workspaces/${install}\nSHARED=/workspaces/.shared\n\nexport HOME="$WS/.home"\nexport YARN_CACHE_FOLDER="$SHARED/yarn"\nexport CYPRESS_CACHE_FOLDER="$SHARED/cypress"\nexport npm_config_cache="$SHARED/npm"\n\n# init + fetch rather than `git clone`, because clone refuses a non-empty directory and this one\n# is not reliably empty: the agent seed writes `.claude` into the checkout, and if that lands\n# first - a separate trigger racing this boot - a clone would fail and leave the pod serving\n# nothing.\nif [ ! -d "$WS/src/.git" ]; then\n  mkdir -p "$WS/src"\n  cd "$WS/src"\n  git init -q\n  git remote add origin https://github.com/${repo} 2>/dev/null || true\n  DEFAULT=$(git ls-remote --symref origin HEAD | sed -n \'s@^ref: refs/heads/\\(.*\\)[[:space:]]HEAD@\\1@p\')\n  git fetch --depth 1 origin "$DEFAULT"\n  git checkout -f -B "$DEFAULT" FETCH_HEAD\nfi\n\ncd "$WS/src"\n\n# The fork is where fixes are pushed, so it is a remote from the start rather than something the\n# agent has to remember to add. `upstream` is what a branch is diffed against - never the fork\'s\n# own master, which is chronically stale and makes a clean fix look like it carries half the\n# repository with it.\ngit remote add fork "https://github.com/${fork}" 2>/dev/null || true\ngit remote set-url fork "https://github.com/${fork}" 2>/dev/null || true\ngit remote add upstream "https://github.com/${repo}" 2>/dev/null || true\n\n# node_modules, hard-linked from a template of the same lockfile if this node already has one.\n# The files are the same inodes, so a second workspace\'s node_modules costs directory entries\n# and nothing else. Keyed by the lockfile hash, so a template is only ever used by a checkout\n# that would have installed exactly it.\nHASH=$(sha1sum yarn.lock 2>/dev/null | cut -c1-12)\n\nif [ -n "$HASH" ] && [ ! -d node_modules ] && [ -d "$SHARED/template/$HASH/node_modules" ]; then\n  cp -al "$SHARED/template/$HASH/node_modules" node_modules && touch .install-done\nfi\n\n[ -f .install-done ] || {\n  yarn install --mutex "file:$SHARED/yarn/.mutex" --network-timeout 600000\n  touch .install-done\n}\n\n# The first workspace to install a given lockfile leaves the template for the next one.\nif [ -n "$HASH" ] && [ -d node_modules ] && [ ! -d "$SHARED/template/$HASH" ]; then\n  mkdir -p "$SHARED/template/$HASH"\n  cp -al node_modules "$SHARED/template/$HASH/node_modules" || rm -rf "$SHARED/template/$HASH"\nfi\n\nexec /bin/bash /workspace-config/serve.sh ${port}\n',"workspace/serve.sh":'#!/bin/bash\n# The dev server, supervised: the container\'s process is this loop, not the server.\n#\n# A fix run does things that kill a dev server - switches branch, regenerates a lockfile, runs a\n# build. When the pod\'s main process was the server itself, each of those restarted the\n# container and took the run\'s terminal with it, mid-fix. So the server is restarted when it\n# exits, and left alone while something else holds the port.\nPORT=${1:-8005}\n\n# The boot script runs this from the checkout, so the tree is its parent - which is where the\n# log and the retarget file live.\nWS=${WS:-$(dirname "$PWD")}\nRETARGET=$WS/.dev-server.env\nLOG=$WS/.dev-server.log\n\necho "$PORT" > "$WS/.dev-server.port" 2>/dev/null || true\nexport PORT WS RETARGET LOG IONICE\n\nheld() {\n  node -e "require(\'net\').connect(Number(process.argv[1]),\'127.0.0.1\').on(\'connect\',()=>process.exit(0)).on(\'error\',()=>process.exit(1))" "$PORT" 2>/dev/null\n}\n\n# Not all at once. Rancher\'s embedded k3s runs beside these servers, and when it restarts every\n# workspace pod restarts with it and every webpack compiles at the same moment: a dozen of them\n# on twelve cores, k3s\'s controllers fail to renew their lease, k3s exits, Rancher restarts -\n# and the herd forms again. So a server waits, with jitter so they do not all wake together,\n# while the node\'s one-minute load is above what the cores can carry.\nCORES=$(nproc 2>/dev/null || echo 4)\ngate() {\n  load=$(cut -d. -f1 /proc/loadavg 2>/dev/null || echo 0)\n  [ "$load" -le $(( CORES * 3 / 2 )) ]\n}\n\n# One dev server per pod, whoever started it. A second webpack is another two gigabytes at every\n# rebuild and the pod\'s limit is five, so an agent that starts its own gets the port until it is\n# done and this one comes back afterwards. Only node processes, matched by command line: a shell\n# or a grep that merely mentions the server would otherwise count and stop this one for nothing.\nservers() { pgrep -f "^[^ ]*node .*vue-cli-service serve" 2>/dev/null; }\nforeign() {\n  local p\n  for p in $(servers); do\n    [ "$(ps -o pgid= -p "$p" 2>/dev/null | tr -d \' \')" = "$SERVER" ] || echo "$p"\n  done\n}\n\nSERVER=""\nstop_ours() {\n  if [ -n "$SERVER" ]; then\n    kill -TERM -- -"$SERVER" 2>/dev/null\n    wait "$SERVER" 2>/dev/null\n    SERVER=""\n  fi\n}\ntrap "stop_ours; exit 0" TERM INT\n\nwhile :; do\n  if held || [ -n "$(foreign)" ]; then sleep 10; continue; fi\n\n  sleep $(( RANDOM % 20 ))\n\n  waited=0\n  while ! gate && [ "$waited" -lt 300 ]; do\n    [ "$waited" -eq 0 ] && echo "[workspace] the node is busy; waiting to start the dev server"\n    sleep 10\n    waited=$((waited + 10))\n  done\n\n  # Below the cluster\'s own processes: a compile that takes every core has taken k3s down with\n  # it on a busy node, and a slow first page is the better failure.\n  IONICE=""\n  command -v ionice >/dev/null 2>&1 && IONICE="ionice -c 3"\n\n  # What the server runs against can be changed without a new pod: writing the retarget file\n  # restarts it onto a different Rancher. Read in the server\'s own shell, so it replaces the\n  # pod\'s environment for the server alone.\n  STAMP=$(stat -c %Y "$RETARGET" 2>/dev/null || echo 0)\n  [ -f "$RETARGET" ] && echo "[workspace] the dev server runs against $(grep \'^API=\' "$RETARGET" | cut -d= -f2-)"\n\n  # setsid, so the server and everything it starts are one process group a stop can signal as a\n  # whole; tee, so the log is in the tree as well as in the pod\'s.\n  setsid bash -c \'if [ -f "$RETARGET" ]; then set -a; . "$RETARGET"; set +a; fi; nice -n 15 $IONICE env VUE_CLI_SERVICE_CONFIG_PATH=/workspace-config/vue.config.js yarn dev --port "$PORT" 2>&1 | tee "$LOG"\' &\n  SERVER=$!\n\n  # `sleep & wait`, not `sleep`: a trap runs once the foreground command returns, and a stop\n  # that waited out the sleep left the server running for the check that followed.\n  while kill -0 "$SERVER" 2>/dev/null; do\n    sleep 10 & wait $!\n    if [ -n "$(foreign)" ]; then\n      echo "[workspace] another dev server is running in this pod; standing down until it is gone"\n      stop_ours; sleep 10; continue 2\n    fi\n    if [ "$(stat -c %Y "$RETARGET" 2>/dev/null || echo 0)" != "$STAMP" ]; then\n      echo "[workspace] the dev server\'s target changed; restarting it"\n      stop_ours; continue 2\n    fi\n  done\n\n  SERVER=""\n  echo "[workspace] the dev server exited; starting it again in 5s"\n  sleep 5\ndone\n',"workspace/vue.config.js":"// Written by the Dev extension. See pkg/dev-extension/workspace-config.ts.\n//\n// This dev server is reached one of two ways, and everything below is the consequence of which.\n//\n//   - behind the apiserver's service proxy, at DEV_PROXY_PATH, on Rancher's own origin. The Host\n//     header is the proxy's, the TLS the browser sees is Rancher's, the prefix is stripped on the\n//     way in and has to be put back on every URL handed out.\n//   - at an origin of its own, on a node port, with no prefix at all.\n//\n// A dev server built for one does not work at the other: the router base is baked in. Which one\n// this is comes from DEV_PROXY_PATH being set or empty, and changing it restarts the pod.\n//\n// The second mode exists because of what the first one does to a dashboard. rancher/dashboard\n// builds its API URLs absolute from the page origin, so behind the prefix its /v1 and /v3 calls\n// never reach this dev server at all: they go to the Rancher this page was served from, using\n// that session, against that cluster. Measured, on a workspace with a Rancher of its own: two\n// requests arrived here and a hundred and thirty-six went to the host. An origin of its own is\n// the only thing that fixes it, because there is no browser-side API base to set.\nconst proxyPath = (process.env.DEV_PROXY_PATH || '').replace(/\\/$/, '');\n\n// The prefix on every in-app link, which has to be the path the page was loaded at. Set before\n// the shell's config runs, because the shell reads it out of the environment as it does.\nprocess.env.ROUTER_BASE = proxyPath ? proxyPath + '/' : '/';\n\n// The repo's own config, untouched in the checkout, rather than a reimplementation of it.\n// rancher/dashboard is a monorepo whose shell is ./shell rather than a node_modules package,\n// and which arguments its root config passes to that shell is the repo's business and changes\n// with the repo. Wrapping it means this file only has to know about the proxy. This file is\n// handed to vue-cli-service as VUE_CLI_SERVICE_CONFIG_PATH (see apps.ts, WORKSPACE_SCRIPT), so\n// it lives outside the checkout and the checkout's own vue.config.js is never rewritten: the\n// tree stays clean, a branch switch never trips over it, and nothing an agent commits carries it.\nconst base = require(require('path').join(process.cwd(), 'vue.config.js'));\n\n// The proxy rewrites absolute URLs in HTML as they pass through, which is what lets a naive UI\n// work behind it at all, so an asset URL that already carried the prefix would carry it twice.\n// index.html therefore asks for '/js/...' and lets the proxy add the prefix, while webpack's\n// runtime works its own base out from the script URL it was loaded from.\nbase.publicPath = 'auto';\n\nconst previousChainWebpack = base.chainWebpack;\n\nbase.chainWebpack = (webpackConfig) => {\n  if (typeof previousChainWebpack === 'function') {\n    previousChainWebpack(webpackConfig);\n  }\n\n  webpackConfig.plugin('html-index').tap((args) => {\n    args[0].publicPath = '/';\n\n    return args;\n  });\n};\n\nbase.devServer = {\n  ...base.devServer,\n\n  // Plain http only behind the proxy, where Rancher terminates TLS and will not talk to the\n  // shell's self-signed dev certificate on the way through.\n  //\n  // At its own origin the browser talks to this server directly, and http there is not a smaller\n  // problem than a certificate warning, it is a broken login: the shipped proxy sets\n  // x-forwarded-proto: https on every request it makes, so Rancher issues its session cookie with\n  // Secure, and a browser on http throws that cookie away. What that looks like is a login that\n  // returns 200 and a page that bounces straight back to the login form, which is what it did.\n  // So https here, with the shell's own certificate, and a warning to click through once.\n  ...(proxyPath ? { server: { type: 'http' } } : {}),\n\n  // Requests arrive with whatever Host the proxy chain last set, never this server's own.\n  // Without this the proxy answers 'Invalid Host header'.\n  allowedHosts: 'all',\n\n  // The proxy cannot rewrite URLs in a compressed body: the response comes back as a stream\n  // error and the browser shows a blank page.\n  compress: false,\n\n  // The prefix is stripped before a request lands here, so everything is served from the root.\n  devMiddleware: { publicPath: '/' },\n  static:        { publicPath: '/' },\n\n  // A deep link into the dashboard has to load the app rather than 404. disableDotRule matters\n  // in Rancher, where a route routinely carries a resource name with dots in it.\n  historyApiFallback: {\n    index:             '/index.html',\n    disableDotRule:    true,\n    htmlAcceptHeaders: ['text/html', 'application/xhtml+xml']\n  },\n\n  hot:             true,\n  webSocketServer: { type: 'ws', options: { path: '/ws' } },\n  client:          {\n    // Hot reload rides the same proxy when there is one, and the page's own origin when there is\n    // not. Either way this has to be written out, because the shell's config has an address of its\n    // own in it and that address is not where this server is.\n    //\n    // Behind the proxy only the path is ours; the rest are webpack-dev-server's \"infer it from\n    // window.location\" sentinels. 'auto' is one the *server* substitutes as it serves the client.\n    //\n    // Served at its own origin the sentinels are still needed, and leaving them out was a real\n    // bug rather than a tidy default. The shell's own config sets a fixed\n    // wss://localhost:8005/ws, which is right on a laptop running the dev server and wrong\n    // everywhere else: the browser then opened a hot-reload socket to whatever was on port 8005\n    // of the machine the browser is on, and displayed that server's compile errors over this\n    // dashboard. Seen here as a webpack overlay full of a completely different project's module\n    // resolution failures, sitting on top of the workspace's login form and swallowing clicks.\n    //\n    // 0.0.0.0 and 0 are webpack-dev-server's own \"take it from window.location\" sentinels. The\n    // protocol is written out rather than left as 'auto', because 'auto' is substituted by the\n    // server as it serves the client and is handed to the WebSocket constructor unchanged when\n    // the page was loaded directly. This server is https at its own origin, so wss.\n    webSocketURL: proxyPath ? {\n      protocol: 'auto',\n      hostname: '0.0.0.0',\n      port:     0,\n      pathname: proxyPath + '/ws'\n    } : {\n      protocol: 'wss',\n      hostname: '0.0.0.0',\n      port:     0,\n      pathname: '/ws'\n    },\n    // The compile-error overlay would cover the whole framed dashboard for whoever is looking\n    // when an edit fails to compile.\n    overlay: false\n  }\n};\n\nmodule.exports = base;\n","workspace-setup.sh":'#!/bin/sh\n# Make a fix workspace able to do the work: give it a GitHub credential, the `gh` CLI, and a\n# fork to push to.\n#\n# This runs in the AGENTS pod, not the workspace, and that is the point. The agents pod\'s\n# ServiceAccount can read the token out of the Secret; the workspace\'s cannot, and should not -\n# it is a pod running a build. So the token is read here and handed across the one channel that\n# already exists between them, an exec into the workspace.\n#\n# The token is never on a command line. `kubectl exec` argv travels through the apiserver as URL\n# query parameters, which are logged; it goes over STDIN instead.\n#\n# usage: workspace-setup.sh <workspace-namespace> <repo> <fork> <token-key> <board> <library>\nset -e\n\nNS=${1:?workspace-setup.sh needs the workspace namespace}\nREPO=${2:?workspace-setup.sh needs the upstream repository}\nFORK=${3:?workspace-setup.sh needs the fork}\n# The Secret key holding the token of the person who pressed the button. The branch is pushed\n# with THEIR credential, to THEIR fork, so the work is attributable to them.\nTOKEN_KEY=${4:?workspace-setup.sh needs the token key}\n# So each step can name itself on the job, and the board can say what it is waiting for rather\n# than showing "Running" through five minutes of clone and install.\nBOARD=${5:-}\nLIBRARY=${6:-}\n\nSECRET_NS=vuln-console\nSECRET=settings\nWS=/workspaces/$NS\n\nkube() { KUBECONFIG=/dev/null kubectl "$@"; }\n\nstage() {\n  [ -n "$BOARD" ] && [ -n "$LIBRARY" ] || return 0\n  sh "$(dirname "$0")/job.sh" "$BOARD" "$LIBRARY" "stage=$1" >/dev/null 2>&1 || true\n}\n\n# Wait for the workspace to exist before trying to talk to it.\n#\n# A workspace is minutes old before it is usable: Fleet has to render the Bundle, the kubelet has\n# to pull node:24, and boot.sh then clones the repository and runs a yarn install. Exec\'ing into\n# it before any of that has happened fails with `container not found ("workspace")`, which is\n# what the first real run did.\nstage waiting\necho "workspace-setup.sh: waiting for $NS to come up"\ni=0\nwhile [ "$i" -lt 240 ]; do\n  ready=$(kube get deployment "$NS" -n "$NS" -o jsonpath=\'{.status.readyReplicas}\' 2>/dev/null || true)\n  [ "${ready:-0}" -ge 1 ] 2>/dev/null && break\n  # The dev server is what makes a pod READY, and it is the last thing to start. Being able to\n  # exec is what this actually needs, so a running container is enough to get on with.\n  phase=$(kube get pods -n "$NS" -l "vuln-console.rancher.io/workspace=$NS" \\\n    -o jsonpath=\'{.items[0].status.containerStatuses[?(@.name=="workspace")].ready}\' 2>/dev/null || true)\n  [ "$phase" = "true" ] && break\n  i=$((i + 1))\n  sleep 5\ndone\n\nif [ "$i" -ge 240 ]; then\n  echo "workspace-setup.sh: $NS did not come up within 20 minutes" >&2\n  exit 2\nfi\n\nsecret_key() {\n  kube get secret "$2" -n "$1" -o "jsonpath={.data.$3}" 2>/dev/null | base64 -d 2>/dev/null | tr -d \'\\r\\n\'\n}\n\nGH_TOKEN=$(secret_key "$SECRET_NS" "$SECRET" "$TOKEN_KEY")\n\nif [ -z "$GH_TOKEN" ]; then\n  echo "workspace-setup.sh: no GitHub token is stored for this user, so nothing could be pushed" >&2\n  exit 2\nfi\n\nstage preparing\n\n# The fork has to exist before the agent needs it. Creating it is one call and it is idempotent;\n# discovering it is missing happens at `git push`, three minutes into a run, after the work.\nFORK_OWNER=${FORK%%/*}\nFORK_NAME=${FORK##*/}\n\nstatus=$(curl -s -o /dev/null -w \'%{http_code}\' \\\n  -H "Authorization: Bearer $GH_TOKEN" -H "Accept: application/vnd.github+json" \\\n  "https://api.github.com/repos/$FORK_OWNER/$FORK_NAME")\n\nif [ "$status" = "404" ]; then\n  echo "workspace-setup.sh: $FORK does not exist yet - forking $REPO"\n  curl -s -o /dev/null -X POST \\\n    -H "Authorization: Bearer $GH_TOKEN" -H "Accept: application/vnd.github+json" \\\n    "https://api.github.com/repos/$REPO/forks"\n  # A fork is created asynchronously; it is usually there within a few seconds.\n  i=0\n  while [ "$i" -lt 30 ]; do\n    status=$(curl -s -o /dev/null -w \'%{http_code}\' \\\n      -H "Authorization: Bearer $GH_TOKEN" -H "Accept: application/vnd.github+json" \\\n      "https://api.github.com/repos/$FORK_OWNER/$FORK_NAME")\n    [ "$status" = "200" ] && break\n    i=$((i + 1))\n    sleep 2\n  done\nfi\n\n[ "$status" = "200" ] || { echo "workspace-setup.sh: $FORK is not reachable (HTTP $status)" >&2; exit 2; }\n\n# Everything below runs inside the workspace, as its own user, reading the token off stdin.\nprintf \'%s\' "$GH_TOKEN" | kube exec -i -n "$NS" "deploy/$NS" -c workspace -- \\\n  setpriv --reuid=1000 --regid=1000 --init-groups \\\n  /usr/bin/env WS="$WS" FORK="$FORK" REPO="$REPO" /bin/sh -s <<\'INNER\'\nset -e\nTOKEN=$(cat)\nmkdir -p "$WS/bin"\n\n# git, for the push. A credential store file rather than a URL with the token embedded in it:\n# a remote carrying a token is a token in `git remote -v`, in every error message, and in the\n# repository\'s own config on disk.\numask 077\nprintf \'https://x-access-token:%s@github.com\\n\' "$TOKEN" > "$WS/.git-credentials"\ngit config --global credential.helper "store --file=$WS/.git-credentials"\ngit config --global user.name "${GIT_NAME:-Vulnerability Console}"\ngit config --global user.email "${GIT_EMAIL:-noreply@rancher.com}"\n\n# Never attribute a console fix to the agent. The commits are the human\'s, opened on their fork.\ngit config --global --unset-all trailer.co-authored-by 2>/dev/null || true\n\n# gh, which the fix prompt uses to read alerts and list pull requests. Not in the image and not\n# in Debian\'s repositories, so the released static binary.\nif ! [ -x "$WS/bin/gh" ]; then\n  GH_VERSION=2.76.1\n  ARCH=$(uname -m)\n  case "$ARCH" in\n    x86_64) GH_ARCH=amd64 ;;\n    aarch64|arm64) GH_ARCH=arm64 ;;\n    *) GH_ARCH=amd64 ;;\n  esac\n  URL="https://github.com/cli/cli/releases/download/v${GH_VERSION}/gh_${GH_VERSION}_linux_${GH_ARCH}.tar.gz"\n  curl -sL "$URL" -o /tmp/gh.tgz\n  tar -xzf /tmp/gh.tgz -C /tmp\n  mv "/tmp/gh_${GH_VERSION}_linux_${GH_ARCH}/bin/gh" "$WS/bin/gh"\n  chmod 755 "$WS/bin/gh"\n  rm -rf /tmp/gh.tgz "/tmp/gh_${GH_VERSION}_linux_${GH_ARCH}"\nfi\n\n# gh reads GH_TOKEN from the environment; the shell wrapper sources this for every command the\n# agent runs. 0600, because it is a token in a file.\nprintf \'GH_TOKEN=%s\\nGITHUB_TOKEN=%s\\nVULN_FORK=%s\\nVULN_REPO=%s\\n\' "$TOKEN" "$TOKEN" "$FORK" "$REPO" > "$WS/.env"\nchmod 600 "$WS/.env"\n\necho "workspace-setup: credential, gh $("$WS/bin/gh" --version | head -1 | awk \'{print $3}\') and $FORK ready"\nINNER\n'},Ce="/workspace/.vuln-console",De="1000:1000",Ve="/workspace/conversations",Pe="/workspace/.home",Be="workspace-setup.sh",He=132e4,Ie={fix:"fix.prompt.md",pr:"pr.prompt.md",record:"record.prompt.md",publish:"publish.prompt.md",addresscomment:"address-comments.prompt.md",resolveconflict:"resolve-conflict.prompt.md"},je={fix:"Fix",pr:"Open the pull request for",record:"Record the verification for",publish:"Attach the recording for",addresscomment:"Address the review comments on",resolveconflict:"Rebase"};function We(e){const n=g();return{pod:e,namespace:(null===n||void 0===n?void 0:n.agent.namespace)||"extension-studio",container:(null===n||void 0===n?void 0:n.agent.container)||"agent"}}function Le(e){return["#!/bin/sh","# Written by the vulnerability console. Runs one command inside a fix workspace.","set -e",`NS=${A(e)}`,`WS=${A(`${F}/${e}`)}`,"DIR=${PWD}",'KUBECONFIG=/dev/null exec kubectl exec -i -n "$NS" "deploy/$NS" -c workspace -- \\',"  setpriv --reuid=1000 --regid=1000 --init-groups \\",'  /usr/bin/env HOME="$WS/.home" WSD="$WS" DIR="$DIR" \\','  /bin/bash -lc \'cd "$DIR" 2>/dev/null || cd "$WSD/src"; PATH="$WSD/bin:$PATH"; if [ -f "$WSD/.env" ]; then set -a; . "$WSD/.env"; set +a; fi; eval "$1"\' bash "$1"',""].join("\n")}function qe(e,n,t,a,o,r,s){const i=((null===r||void 0===r?void 0:r.vulns)||[]).filter(e=>"open"===e.state).map(e=>`#${e.id} (${e.severity}${e.patched?`, needs ${e.patched}`:", no fix published"}) in ${e.manifest}`);return[`${je[e]} the dependency **${o}** in ${n.repo}.`,"",`Read ${Ce}/${Ie[e]} IN FULL before you do anything. It is the specification`,"for this action and it is authoritative.","","Facts for this run:",`  library        ${o}`,`  workspace      ${s.workspace} (your commands already run inside it)`,`  checkout       ${F}/${s.workspace}/src`,`  repository     ${n.repo}`,`  push to        ${t} (remote "fork")`,`  pull requests  ${a}`,s.branch?`  branch         ${s.branch}`:"",s.prNumber?`  pull request   ${s.prNumber}`:"",i.length?`  open alerts    ${i.join("; ")}`:"","","Enumerate the affected manifests from the LIVE alerts, not from the list above - this board","can be hours old and a missed lockfile is the most common thing a reviewer catches.","",`Record what you did by writing the job: ${Ce}/job.sh ${n.id} ${A(o)} <field>=<value> ...`,"Call it when you finish and whenever something durable happens (a branch, a preview, a pull","request). If you cannot finish, record why:",`  ${Ce}/job.sh ${A(o)} phase=Failed message="one line saying what went wrong"`,"","Then stop."].filter(e=>""!==e).join("\n")}async function Ue(e,n){const t=["job.sh",Be,...Object.values(Ie)];for(const a of t){const n=Oe[a];if(!n)throw new Error(`This build is missing its ${a} - run "yarn gen-seed" and rebuild.`);await D(e,`${Ce}/${a}`,n,{mode:"644",owner:De})}await D(e,`${Ce}/shell-${n}.sh`,Le(n),{mode:"755",owner:De}),await S(e,["/bin/sh","-c",`chown ${De} ${Ce} 2>/dev/null || true`],{timeoutMs:15e3})}async function Ge(e){const{store:n,board:t,tokenLogin:a,principalId:o,library:r,action:s,group:i=null,by:l}=e,d=I(t,a),c=j(t,a);if(!d)throw new Error("There is no fork to push to: the board sets none and the stored token's account is not known yet. Refresh the board first.");const h=g();if(!h)throw new Error("The Agents extension is not available on this page, so there is no agent to do the work.");const p=(await ie(t.id)).find(e=>e.library===r&&"Running"===e.phase);if(p&&Date.now()-p.updatedAt<9e5)throw new Error(`${r} already has a run going. Stop it first, or wait for it to finish.`);const u=await h.agent.pod();if(!u)throw new Error("The agent pod is not running, so there is nowhere to do the work.");const m=(await ie(t.id)).find(e=>e.library===r),v=Date.now(),b={board:t.id,library:r,phase:"Running",action:s,by:l||"unknown",startedAt:v,updatedAt:v,stage:"workspace",sessionId:null,workspace:Re(t.id,r),branch:(null===m||void 0===m?void 0:m.branch)||null,previewUrl:(null===m||void 0===m?void 0:m.previewUrl)||null,prUrl:(null===m||void 0===m?void 0:m.prUrl)||null,prNumber:(null===m||void 0===m?void 0:m.prNumber)||null,videoUrl:(null===m||void 0===m?void 0:m.videoUrl)||null,infoUrl:(null===m||void 0===m?void 0:m.infoUrl)||null,replyUrl:(null===m||void 0===m?void 0:m.replyUrl)||null,message:null,vulnIds:(null===m||void 0===m?void 0:m.vulnIds)||[]};await le(b);try{const e=await Te(n,t,r),a=We(u);await Ue(a,e);const l=await h.agent.startInProject(f(`${e}-${s}`),`${je[s]} ${r}`,qe(s,t,d,c,r,i,{...b,workspace:e})),p={...b,workspace:e,sessionId:l,stage:"waiting",updatedAt:Date.now()};return await le(p),(async()=>{try{await N(a,[`sh ${A(`${Ce}/${Be}`)}`,A(e),A(t.repo),A(d),A(ce(o)),A(t.id),A(r)].join(" "),`prepare the workspace for ${t.repo}`,He),await le({...p,stage:"starting",updatedAt:Date.now()}).catch(()=>{}),await N(a,["/bin/sh /seed/shell.sh",A(l),A(Ve),A(Pe),"start",A(`${Ce}/shell-${e}.sh`)].join(" "),"start the conversation in the agent pod",12e4)}catch(n){await le({...p,phase:"Failed",message:(null===n||void 0===n?void 0:n.message)||String(n),updatedAt:Date.now()}).catch(()=>{})}})(),p}catch(w){const e=(null===w||void 0===w?void 0:w.message)||String(w);throw await le({...b,phase:"Failed",message:e,updatedAt:Date.now()}).catch(()=>{}),new Error(e,{cause:w})}}async function Me(e){const n=g();n&&e.sessionId&&await n.agent.end(e.sessionId).catch(()=>{}),await le({...e,phase:"Cancelled",message:"Stopped.",sessionId:null,updatedAt:Date.now()})}function Fe(e,n=Date.now()){return"Running"===e.phase&&n-e.updatedAt>9e5}const Ke={class:"steps"},ze=["href","title"],Ye=["href"],Je=["href"],Xe=["href"];var Ze=(0,a.defineComponent)({__name:"StepPills",props:{row:{},job:{},fork:{},ownerPackage:{},ownerVersion:{},busy:{type:Boolean}},emits:["act","stop","session"],setup(e,{emit:n}){const t=e,o=n,r=(0,a.computed)(()=>t.job),s=(0,a.computed)(()=>{var e;return"Running"===(null===(e=r.value)||void 0===e?void 0:e.phase)&&!Fe(r.value)}),i=(0,a.computed)(()=>!!r.value&&Fe(r.value)),d=(0,a.computed)(()=>{var e;if(null===(e=r.value)||void 0===e?void 0:e.prUrl)return{url:r.value.prUrl,number:r.value.prNumber};const n=t.row.pr;return"open"===(null===n||void 0===n?void 0:n.status)?{url:n.url,number:n.number}:null}),c=(0,a.computed)(()=>{var e,n;return(null===(e=r.value)||void 0===e?void 0:e.branch)||("open"===(null===(n=t.row.pr)||void 0===n?void 0:n.status)?t.row.pr.headRefName:null)||null}),p=(0,a.computed)(()=>{var e,n;return(null===(e=r.value)||void 0===e?void 0:e.videoUrl)||(null===(n=t.row.pr)||void 0===n?void 0:n.videoUrl)||null}),u=(0,a.computed)(()=>!!p.value&&/^https?:\/\//.test(p.value)),m=(0,a.computed)(()=>t.row.unfixable),f=(0,a.computed)(()=>t.row.ownerOnly&&!!t.ownerPackage),v=(0,a.computed)(()=>[`Only in this repository because of ${t.ownerPackage}`,t.ownerVersion?` ${t.ownerVersion}`:"",". The fix is a ",t.ownerPackage," bump, not a lockfile change here."].join("")),g=(0,a.computed)(()=>c.value?`https://github.com/${t.fork}/tree/${c.value}`:null);return(n,t)=>{var b,w;return(0,a.openBlock)(),(0,a.createElementBlock)("div",Ke,[f.value?((0,a.openBlock)(),(0,a.createBlock)((0,a.unref)(l.BN),{key:0,status:"info",title:v.value,"data-testid":"vc-owned-elsewhere"},{default:(0,a.withCtx)(()=>[(0,a.createTextVNode)(" Comes from "+(0,a.toDisplayString)(e.ownerPackage),1)]),_:1},8,["title"])):m.value?((0,a.openBlock)(),(0,a.createBlock)((0,a.unref)(l.BN),{key:1,status:"unknown",title:"No patched version is available — Dependabot has no fix for this advisory, so there is nothing to bump to."},{default:(0,a.withCtx)(()=>[...t[11]||(t[11]=[(0,a.createTextVNode)(" No fix published ",-1)])]),_:1})):f.value?(0,a.createCommentVNode)("",!0):((0,a.openBlock)(),(0,a.createElementBlock)(a.Fragment,{key:2},[s.value?((0,a.openBlock)(),(0,a.createElementBlock)(a.Fragment,{key:0},[(0,a.createVNode)(h.A,{variant:"secondary",size:"small",onClick:t[0]||(t[0]=e=>o("session"))},{default:(0,a.withCtx)(()=>{var e;return[t[12]||(t[12]=(0,a.createElementVNode)("i",{class:"icon icon-spinner icon-spin"},null,-1)),(0,a.createElementVNode)("span",null,(0,a.toDisplayString)("fix"===(null===(e=r.value)||void 0===e?void 0:e.action)?"Fixing":"Working")+"…",1)]}),_:1}),(0,a.createVNode)(h.A,{variant:"secondary",size:"small","data-testid":"vc-stop",onClick:t[1]||(t[1]=e=>o("stop"))},{default:(0,a.withCtx)(()=>[...t[13]||(t[13]=[(0,a.createElementVNode)("span",null,"Stop",-1)])]),_:1})],64)):i.value?((0,a.openBlock)(),(0,a.createElementBlock)(a.Fragment,{key:1},[(0,a.createVNode)((0,a.unref)(l.BN),{status:"warning",title:"This run has not reported for a while. The tab that started it may have been closed, or its pod replaced."},{default:(0,a.withCtx)(()=>[...t[14]||(t[14]=[(0,a.createTextVNode)(" Stalled ",-1)])]),_:1}),(0,a.createVNode)(h.A,{variant:"secondary",size:"small",onClick:t[2]||(t[2]=e=>o("session"))},{default:(0,a.withCtx)(()=>[...t[15]||(t[15]=[(0,a.createElementVNode)("span",null,"Session",-1)])]),_:1}),(0,a.createVNode)(h.A,{variant:"secondary",size:"small",onClick:t[3]||(t[3]=e=>o("stop"))},{default:(0,a.withCtx)(()=>[...t[16]||(t[16]=[(0,a.createElementVNode)("span",null,"Stop",-1)])]),_:1})],64)):((0,a.openBlock)(),(0,a.createElementBlock)(a.Fragment,{key:2},[g.value?((0,a.openBlock)(),(0,a.createElementBlock)("a",{key:0,class:"steps__link",href:g.value,target:"_blank",rel:"noopener",title:`fork branch: ${c.value}`},[(0,a.createVNode)((0,a.unref)(l.BN),{status:"success"},{default:(0,a.withCtx)(()=>[(0,a.createTextVNode)((0,a.toDisplayString)(c.value),1)]),_:1})],8,ze)):((0,a.openBlock)(),(0,a.createBlock)(h.A,{key:1,variant:"primary",size:"small",disabled:e.busy,"data-testid":"vc-fix",onClick:t[4]||(t[4]=e=>o("act","fix"))},{default:(0,a.withCtx)(()=>[...t[17]||(t[17]=[(0,a.createElementVNode)("span",null,"Fix",-1)])]),_:1},8,["disabled"])),d.value?((0,a.openBlock)(),(0,a.createElementBlock)("a",{key:2,class:"steps__link",href:d.value.url,target:"_blank",rel:"noopener"},[(0,a.createVNode)((0,a.unref)(l.BN),{status:"success"},{default:(0,a.withCtx)(()=>[(0,a.createTextVNode)("Pull request "+(0,a.toDisplayString)(d.value.number),1)]),_:1})],8,Ye)):c.value?((0,a.openBlock)(),(0,a.createBlock)(h.A,{key:3,variant:"secondary",size:"small",disabled:e.busy,onClick:t[5]||(t[5]=e=>o("act","pr"))},{default:(0,a.withCtx)(()=>[...t[18]||(t[18]=[(0,a.createElementVNode)("span",null,"Create pull request",-1)])]),_:1},8,["disabled"])):(0,a.createCommentVNode)("",!0),u.value?((0,a.openBlock)(),(0,a.createElementBlock)("a",{key:4,class:"steps__link",href:p.value||"#",target:"_blank",rel:"noopener"},[(0,a.createVNode)((0,a.unref)(l.BN),{status:"success"},{default:(0,a.withCtx)(()=>[...t[19]||(t[19]=[(0,a.createTextVNode)("Recording",-1)])]),_:1})],8,Je)):p.value&&d.value?((0,a.openBlock)(),(0,a.createBlock)(h.A,{key:5,variant:"secondary",size:"small",disabled:e.busy,onClick:t[6]||(t[6]=e=>o("act","publish"))},{default:(0,a.withCtx)(()=>[...t[20]||(t[20]=[(0,a.createElementVNode)("span",null,"Add recording to pull request",-1)])]),_:1},8,["disabled"])):c.value||d.value?((0,a.openBlock)(),(0,a.createBlock)(h.A,{key:6,variant:"secondary",size:"small",disabled:e.busy,onClick:t[7]||(t[7]=e=>o("act","record"))},{default:(0,a.withCtx)(()=>[...t[21]||(t[21]=[(0,a.createElementVNode)("span",null,"Record",-1)])]),_:1},8,["disabled"])):(0,a.createCommentVNode)("",!0),d.value?((0,a.openBlock)(),(0,a.createElementBlock)(a.Fragment,{key:7},[(0,a.createVNode)(h.A,{variant:"secondary",size:"small",disabled:e.busy,onClick:t[8]||(t[8]=e=>o("act","addresscomment"))},{default:(0,a.withCtx)(()=>[...t[22]||(t[22]=[(0,a.createElementVNode)("span",null,"Address comments",-1)])]),_:1},8,["disabled"]),(0,a.createVNode)(h.A,{variant:"secondary",size:"small",disabled:e.busy,onClick:t[9]||(t[9]=e=>o("act","resolveconflict"))},{default:(0,a.withCtx)(()=>[...t[23]||(t[23]=[(0,a.createElementVNode)("span",null,"Rebase",-1)])]),_:1},8,["disabled"])],64)):(0,a.createCommentVNode)("",!0),(null===(b=r.value)||void 0===b?void 0:b.replyUrl)?((0,a.openBlock)(),(0,a.createElementBlock)("a",{key:8,class:"steps__link",href:r.value.replyUrl,target:"_blank",rel:"noopener",title:"the per-point reply this run staged — read it here and post it yourself; the console never comments for you"},[(0,a.createVNode)((0,a.unref)(l.BN),{status:"info"},{default:(0,a.withCtx)(()=>[...t[24]||(t[24]=[(0,a.createTextVNode)("Reply draft",-1)])]),_:1})],8,Xe)):(0,a.createCommentVNode)("",!0),(null===(w=r.value)||void 0===w?void 0:w.sessionId)?((0,a.openBlock)(),(0,a.createBlock)(h.A,{key:9,variant:"link",size:"small",onClick:t[10]||(t[10]=e=>o("session"))},{default:(0,a.withCtx)(()=>[...t[25]||(t[25]=[(0,a.createElementVNode)("span",null,"Session",-1)])]),_:1})):(0,a.createCommentVNode)("",!0)],64))],64))])}}}),Qe=(t(5081),t(7433));const en=(0,Qe.A)(Ze,[["__scopeId","data-v-54f21300"]]);var nn=en;const tn=["critical","high","medium","low"],an=36e5,on=6048e5,rn=432e5;function sn(e){switch(e){case"critical":case"high":return"error";case"medium":return"warning";case"low":return"info";default:return"unknown"}}function ln(e){const n=tn.indexOf(e);return-1===n?tn.length:n}function dn(e){if(!e)return null;const n=Date.parse(e);return Number.isNaN(n)?null:n}function cn(e){const n=/^Bump\s+([@/A-Za-z0-9._-]+)/.exec(e.title||"");return n?n[1]:null}function hn(e){const n=e.replace(/^@/,"");return[...new Set([e,n,n.replace(/\//g,"-")])]}function pn(e,n){const t=hn(n),a=cn(e);if(a)return t.includes(a);const o=e.headRefName||"";return t.some(e=>o.endsWith(`/${e}`)||o.includes(`/${e}-`))}function un(e,n,t){var a;const o=n.locks||{};for(const[i,l]of Object.entries(o))if(l.includes(e.id)){const e=t.find(e=>e.number===Number(i));if(e)return e}const r=t.filter(n=>pn(n,e.library)),s=null!==(a=n.now)&&void 0!==a?a:Date.now();if("open"===e.state){const e=r.find(e=>"open"===e.status);if(e)return e;const n=r.filter(e=>"merged"===e.status).filter(e=>{const n=dn(e.mergedAt);return null!==n&&s-n<=rn}).sort((e,n)=>(dn(n.mergedAt)||0)-(dn(e.mergedAt)||0))[0];return n||null}if("fixed"===e.state){const n=dn(e.fixedAt);return null===n?null:r.filter(e=>"merged"===e.status).filter(e=>{const t=dn(e.mergedAt);return null!==t&&(n>=t-an&&n<=t+on)}).sort((e,n)=>(dn(n.mergedAt)||0)-(dn(e.mergedAt)||0))[0]||null}return null}function mn(e,n){const t=new Map;for(const o of e){const e=t.get(o.alert.library);e?e.push(o):t.set(o.alert.library,[o])}const a=[];for(const[o,r]of t){const e=r.map(e=>e.alert).sort((e,n)=>n.id-e.id),t=e.map(e=>e.severity).sort((e,n)=>ln(e)-ln(n))[0]||"low";a.push({library:o,severity:t,pr:r.map(e=>e.pr).find(e=>null!==e)||null,vulns:e,unfixable:e.length>0&&e.every(e=>!e.patched),ownerOnly:n.has(o)})}return a.sort((e,n)=>ln(e.severity)-ln(n.severity)||e.library.localeCompare(n.library))}function fn(e){const{snapshot:n}=e,t=n.ourPrs||[],a=(n.alerts||[]).map(n=>({alert:n,pr:un(n,e,t)})),o=a.filter(e=>{var n;return"open"===e.alert.state&&"open"!==(null===(n=e.pr)||void 0===n?void 0:n.status)}),r=a.filter(e=>{var n;return"open"===e.alert.state&&"open"===(null===(n=e.pr)||void 0===n?void 0:n.status)}),s=a.filter(e=>{var n;return"open"!==e.alert.state&&"merged"===(null===(n=e.pr)||void 0===n?void 0:n.status)}),i=new Set(n.ownerOnly||[]),l={openNoPr:mn(o,i),openPrOpen:mn(r,i),prMerged:mn(s,i)};return{generatedAt:n.gatheredAt,lists:l,counts:{openNoPr:o.length,openPrOpen:r.length,prMerged:s.length}}}function vn(e){var n;return"merged"===(null===(n=e.pr)||void 0===n?void 0:n.status)&&e.vulns.some(e=>"open"===e.state)}function gn(e,n){return`https://github.com/${e}/security/dependabot/${n}`}const bn={class:"vuln-ids"},wn=["href","title"];var yn=(0,a.defineComponent)({__name:"VulnIds",props:{vulns:{},repo:{}},setup(e){const n=e,t=(0,a.computed)(()=>n.vulns.map(e=>({id:e.id,url:gn(n.repo,e.id),status:"open"===e.state?sn(e.severity):"success",title:[e.ghsa,e.manifest,e.patched?`needs ${e.patched}`:"no fix published","open"===e.state?"open":"closed"].filter(Boolean).join(" · ")})));return(e,n)=>((0,a.openBlock)(),(0,a.createElementBlock)("div",bn,[((0,a.openBlock)(!0),(0,a.createElementBlock)(a.Fragment,null,(0,a.renderList)(t.value,e=>((0,a.openBlock)(),(0,a.createElementBlock)("a",{key:e.id,class:"vuln-ids__link",href:e.url,target:"_blank",rel:"noopener",title:e.title},[(0,a.createVNode)((0,a.unref)(l.BN),{status:e.status},{default:(0,a.withCtx)(()=>[(0,a.createTextVNode)("#"+(0,a.toDisplayString)(e.id),1)]),_:2},1032,["status"])],8,wn))),128))]))}});t(9657);const kn=(0,Qe.A)(yn,[["__scopeId","data-v-501df710"]]);var $n=kn;function xn(e,n=Date.now()){const t=e.startedAt;if(!t)return"";const a="Running"===e.phase?n:e.updatedAt||n,o=Math.max(0,Math.round((a-t)/1e3));if(o<60)return`${o}s`;const r=Math.floor(o/60);return r<60?`${r}m ${o%60}s`:`${Math.floor(r/60)}h ${r%60}m`}const _n=["workspace","waiting","preparing","starting","fixing","verifying","recording"],En={workspace:"Creating the workspace",waiting:"Waiting for it to start (clone and install)",preparing:"Preparing the credential and the fork",starting:"Starting the agent",fixing:"Bumping and regenerating the lockfiles",verifying:"Serving the branch and checking it",recording:"Recording the verification"};function Sn(e){if(e.stage&&_n.includes(e.stage)){const n=e.stage;if(_n.indexOf(n)>=_n.indexOf("starting")||!e.branch)return n}return e.sessionId?e.branch?e.videoUrl?"recording":"verifying":"fixing":"workspace"}const Rn={class:"progress","data-testid":"vc-progress"},Nn={class:"progress__steps"},An={class:"progress__marker"},Tn={key:0,class:"icon icon-checkmark"},On={key:1,class:"icon icon-spinner icon-spin"},Cn={class:"progress__label"},Dn={class:"progress__foot"},Vn={class:"progress__elapsed"};var Pn=(0,a.defineComponent)({__name:"RunProgress",props:{phase:{},elapsed:{},canOpenSession:{type:Boolean}},emits:["open-session"],setup(e){const n=e,t=(0,a.computed)(()=>{const e=_n.indexOf(n.phase);return _n.map((n,t)=>({phase:n,label:En[n]||n,state:t<e?"done":t===e?"active":"todo"}))});return(n,o)=>((0,a.openBlock)(),(0,a.createElementBlock)("div",Rn,[(0,a.createElementVNode)("ol",Nn,[((0,a.openBlock)(!0),(0,a.createElementBlock)(a.Fragment,null,(0,a.renderList)(t.value,e=>((0,a.openBlock)(),(0,a.createElementBlock)("li",{key:e.phase,class:(0,a.normalizeClass)(["progress__step",`is-${e.state}`])},[(0,a.createElementVNode)("span",An,["done"===e.state?((0,a.openBlock)(),(0,a.createElementBlock)("i",Tn)):"active"===e.state?((0,a.openBlock)(),(0,a.createElementBlock)("i",On)):(0,a.createCommentVNode)("",!0)]),(0,a.createElementVNode)("span",Cn,(0,a.toDisplayString)(e.label),1)],2))),128))]),(0,a.createElementVNode)("div",Dn,[(0,a.createElementVNode)("span",Vn,(0,a.toDisplayString)(e.elapsed),1),e.canOpenSession?((0,a.openBlock)(),(0,a.createElementBlock)("button",{key:0,type:"button",class:"progress__toggle","data-testid":"vc-open-session",onClick:o[0]||(o[0]=(0,a.withModifiers)(e=>n.$emit("open-session"),["stop"]))},[...o[1]||(o[1]=[(0,a.createElementVNode)("i",{class:"icon icon-terminal"},null,-1),(0,a.createTextVNode)(" Watch the agent ",-1)])])):(0,a.createCommentVNode)("",!0)])]))}});t(4105);const Bn=(0,Qe.A)(Pn,[["__scopeId","data-v-c97aac58"]]);var Hn=Bn;const In={class:"board"},jn={class:"board__running-head"},Wn=["onClick"],Ln={key:0,class:"board__loading"},qn={key:1,class:"board__empty","data-testid":"vc-empty"},Un={class:"board__toolbar"},Gn={class:"board__counts"},Mn={class:"board__stamp"},Fn=["href"],Kn={class:"board__lib"},zn=["href"],Yn={key:0,class:"board__none"},Jn=["href"],Xn={key:1,class:"board__none"},Zn={key:0,class:"board__message"};var Qn=(0,a.defineComponent)({__name:"BoardPanel",props:{board:{}},emits:["act","stop","session","shipped","loaded"],setup(e,{expose:n,emit:t}){const o=e,r=t,s=(0,a.ref)(null),i=(0,a.ref)([]),h=(0,a.ref)(!0);let p=null;const u=(0,a.computed)(()=>s.value?fn({snapshot:s.value}):null),m=(0,a.computed)(()=>i.value.filter(e=>"Running"===e.phase&&!Fe(e)).sort((e,n)=>n.startedAt-e.startedAt));function f(e,n){var t;return n||"open"===(null===(t=e.pr)||void 0===t?void 0:t.status)?0:e.ownerOnly&&o.board.ownerPackage?2:e.unfixable?3:1}function v(e){return[...new Set(e.vulns.map(e=>e.manifest).filter(Boolean))].sort()}const g=(0,a.computed)(()=>{const e=u.value;return e?[...e.lists.openPrOpen,...e.lists.openNoPr].map(e=>({...e,id:e.library,job:i.value.find(n=>n.library===e.library)||null,stale:vn(e),severityRank:ln(e.severity),actionRank:f(e,i.value.find(n=>n.library===e.library)||null),manifests:v(e)})):[]}),b=[{name:"severity",label:"Severity",value:"severity",width:110,sort:["actionRank","severityRank","library"]},{name:"library",label:"Library",value:"library",sort:["library"]},{name:"files",label:"Files",value:"manifests"},{name:"vulns",label:"Vulnerability",value:"vulns"},{name:"dependabot",label:"Dependabot",value:"dependabotPr",width:120},{name:"actions",label:"Status / actions",value:"actions",sort:["actionRank","severityRank","library"]}];function w(e){var n;return((null===(n=s.value)||void 0===n?void 0:n.dependabotPrs)||[]).find(n=>n.title.startsWith(`Bump ${e} `))||null}async function y(){const[e,n]=await Promise.all([oe(o.board.id).catch(()=>null),ie(o.board.id).catch(()=>[])]);s.value=e,i.value=n,h.value=!1,r("loaded",{board:o.board.id,rows:g.value,jobs:n,hasSnapshot:!!e,tokenLogin:(null===e||void 0===e?void 0:e.tokenLogin)||""})}return n({load:y}),(0,a.onMounted)(()=>{y().catch(()=>{}),p=setInterval(()=>y().catch(()=>{}),5e3)}),(0,a.onUnmounted)(()=>{p&&clearInterval(p)}),(n,t)=>{var o,i,p;return(0,a.openBlock)(),(0,a.createElementBlock)("div",In,[((0,a.openBlock)(!0),(0,a.createElementBlock)(a.Fragment,null,(0,a.renderList)(m.value,e=>((0,a.openBlock)(),(0,a.createElementBlock)("section",{key:e.library,class:"board__running","data-testid":"vc-running"},[(0,a.createElementVNode)("div",jn,[t[1]||(t[1]=(0,a.createElementVNode)("i",{class:"icon icon-spinner icon-spin"},null,-1)),(0,a.createElementVNode)("strong",null,"Fixing "+(0,a.toDisplayString)(e.library),1),(0,a.createElementVNode)("button",{type:"button",class:"board__running-stop",title:"Stop this run",onClick:n=>r("stop",e)}," Stop ",8,Wn),(0,a.createElementVNode)("span",null,(0,a.toDisplayString)((0,a.unref)(xn)(e)),1)]),(0,a.createVNode)(Hn,{phase:(0,a.unref)(Sn)(e),elapsed:(0,a.unref)(xn)(e),"can-open-session":!!e.sessionId,onOpenSession:n=>r("session",e)},null,8,["phase","elapsed","can-open-session","onOpenSession"])]))),128)),h.value?((0,a.openBlock)(),(0,a.createElementBlock)("div",Ln,[...t[2]||(t[2]=[(0,a.createElementVNode)("i",{class:"icon icon-spinner icon-spin"},null,-1),(0,a.createElementVNode)("span",null,"Loading the board…",-1)])])):s.value?((0,a.openBlock)(),(0,a.createElementBlock)(a.Fragment,{key:2},[(0,a.createElementVNode)("div",Un,[(0,a.createElementVNode)("div",Gn,[(0,a.createVNode)(d.A,{name:"In flight",count:(null===(o=u.value)||void 0===o?void 0:o.counts.openPrOpen)||0,"primary-color-var":"--sizzle-info"},null,8,["count"]),(0,a.createVNode)(d.A,{name:"To fix",count:(null===(i=u.value)||void 0===i?void 0:i.counts.openNoPr)||0,"primary-color-var":"--sizzle-error"},null,8,["count"]),(0,a.createVNode)(d.A,{name:"Shipped",count:(null===(p=u.value)||void 0===p?void 0:p.counts.prMerged)||0,"primary-color-var":"--sizzle-success",clickable:"",onClick:t[0]||(t[0]=e=>{var n;return r("shipped",(null===(n=u.value)||void 0===n?void 0:n.lists.prMerged)||[])})},null,8,["count"])]),(0,a.createElementVNode)("span",Mn,[(0,a.createTextVNode)(" gathered "+(0,a.toDisplayString)(new Date(s.value.gatheredAt).toLocaleString())+" ",1),s.value.tokenLogin?((0,a.openBlock)(),(0,a.createElementBlock)(a.Fragment,{key:0},[t[5]||(t[5]=(0,a.createTextVNode)(" · fixes push to ",-1)),(0,a.createElementVNode)("a",{href:`https://github.com/${(0,a.unref)(I)(e.board,s.value.tokenLogin)}`,target:"_blank",rel:"noopener"},(0,a.toDisplayString)((0,a.unref)(I)(e.board,s.value.tokenLogin)),9,Fn)],64)):(0,a.createCommentVNode)("",!0)])]),(0,a.createVNode)(c.A,{rows:g.value,headers:b,"key-field":"id","table-actions":!1,"row-actions":!1,search:!0,"default-sort-by":"actions","no-rows-key":"No open vulnerabilities."},{"col:severity":(0,a.withCtx)(({row:e})=>[(0,a.createElementVNode)("td",null,[(0,a.createVNode)((0,a.unref)(l.BN),{status:(0,a.unref)(sn)(e.severity)},{default:(0,a.withCtx)(()=>[(0,a.createTextVNode)((0,a.toDisplayString)(e.severity.toUpperCase()),1)]),_:2},1032,["status"])])]),"col:library":(0,a.withCtx)(({row:e})=>{var n;return[(0,a.createElementVNode)("td",null,[(0,a.createElementVNode)("div",Kn,(0,a.toDisplayString)(e.library),1),e.stale?((0,a.openBlock)(),(0,a.createElementBlock)("a",{key:0,class:"board__stale",href:null===(n=e.pr)||void 0===n?void 0:n.url,target:"_blank",rel:"noopener",title:"That pull request merged but this alert is still open — the merge did not resolve it. It needs a fresh fix."},[(0,a.createVNode)((0,a.unref)(l.BN),{status:"warning"},{default:(0,a.withCtx)(()=>[...t[6]||(t[6]=[(0,a.createTextVNode)("merged, alert still open",-1)])]),_:1})],8,zn)):(0,a.createCommentVNode)("",!0)])]}),"col:files":(0,a.withCtx)(({row:e})=>[(0,a.createElementVNode)("td",null,[((0,a.openBlock)(!0),(0,a.createElementBlock)(a.Fragment,null,(0,a.renderList)(e.manifests,e=>((0,a.openBlock)(),(0,a.createElementBlock)("span",{key:e,class:"board__file"},(0,a.toDisplayString)(e),1))),128)),e.manifests.length?(0,a.createCommentVNode)("",!0):((0,a.openBlock)(),(0,a.createElementBlock)("span",Yn,"—"))])]),"col:vulns":(0,a.withCtx)(({row:n})=>[(0,a.createElementVNode)("td",null,[(0,a.createVNode)($n,{vulns:n.vulns,repo:e.board.repo},null,8,["vulns","repo"])])]),"col:dependabot":(0,a.withCtx)(({row:e})=>{var n,t;return[(0,a.createElementVNode)("td",null,[w(e.library)?((0,a.openBlock)(),(0,a.createElementBlock)("a",{key:0,href:null===(n=w(e.library))||void 0===n?void 0:n.url,target:"_blank",rel:"noopener"},(0,a.toDisplayString)(null===(t=w(e.library))||void 0===t?void 0:t.number),9,Jn)):((0,a.openBlock)(),(0,a.createElementBlock)("span",Xn,"—"))])]}),"col:actions":(0,a.withCtx)(({row:n})=>{var t,o,i,l;return[(0,a.createElementVNode)("td",null,[(0,a.createVNode)(nn,{row:n,job:n.job,fork:(0,a.unref)(I)(e.board,(null===(t=s.value)||void 0===t?void 0:t.tokenLogin)||""),"owner-package":null===(o=s.value)||void 0===o?void 0:o.ownerPackage,"owner-version":null===(i=s.value)||void 0===i?void 0:i.ownerVersion,busy:!1,onAct:e=>r("act",n,e),onStop:e=>r("stop",n.job),onSession:e=>r("session",n.job)},null,8,["row","job","fork","owner-package","owner-version","onAct","onStop","onSession"]),(null===(l=n.job)||void 0===l?void 0:l.message)?((0,a.openBlock)(),(0,a.createElementBlock)("div",Zn,(0,a.toDisplayString)(n.job.message),1)):(0,a.createCommentVNode)("",!0)])]}),_:1},8,["rows"])],64)):((0,a.openBlock)(),(0,a.createElementBlock)("section",qn,[(0,a.createElementVNode)("h2",null,(0,a.toDisplayString)(e.board.repo)+" has not been gathered yet",1),(0,a.createElementVNode)("p",null,[t[3]||(t[3]=(0,a.createTextVNode)(" Refreshing reads every Dependabot alert on ",-1)),(0,a.createElementVNode)("code",null,(0,a.toDisplayString)(e.board.repo),1),t[4]||(t[4]=(0,a.createTextVNode)(" and the pull requests that relate to them, and stores the result in the cluster. It takes a few seconds and happens in the agent pod, not in this page. ",-1))])]))])}}});t(5645);const et=(0,Qe.A)(Qn,[["__scopeId","data-v-7ccb6cc6"]]);var nt=et,tt=t(3238);const at={class:"agent-terminal"},ot={key:2,class:"agent-terminal__waiting"};var rt=(0,a.defineComponent)({__name:"AgentTerminal",props:{session:{},mode:{default:"claude"}},emits:["state"],setup(e,{emit:n}){const t=e,o=n,s=(0,a.ref)(null),i=(0,a.ref)(!1),l=(0,a.computed)(()=>{var e,n;return(null===(n=null===(e=s.value)||void 0===e?void 0:e.terminal)||void 0===n?void 0:n.component)||null}),d=(0,a.computed)(()=>s.value&&t.session?s.value.agent.command(t.session,t.mode):null),c=(0,a.computed)(()=>i.value&&!l.value);return(0,a.onMounted)(async()=>{s.value=await y(15e3),i.value=!0,s.value||o("state","closed")}),(n,t)=>((0,a.openBlock)(),(0,a.createElementBlock)("div",at,[c.value?((0,a.openBlock)(),(0,a.createBlock)((0,a.unref)(r.l),{key:0,color:"warning",class:"agent-terminal__missing"},{default:(0,a.withCtx)(()=>[...t[1]||(t[1]=[(0,a.createTextVNode)(" Nothing here draws a terminal: install the ",-1),(0,a.createElementVNode)("b",null,"Agents",-1),(0,a.createTextVNode)(" extension, which brings the agent pod and the terminal every extension borrows, then reload. ",-1)])]),_:1})):l.value&&d.value?((0,a.openBlock)(),(0,a.createBlock)((0,a.resolveDynamicComponent)(l.value),{key:1,session:e.session,command:d.value,mode:e.mode,class:"agent-terminal__pane",onState:t[0]||(t[0]=e=>o("state",e))},null,40,["session","command","mode"])):((0,a.openBlock)(),(0,a.createElementBlock)("div",ot,[...t[2]||(t[2]=[(0,a.createElementVNode)("i",{class:"icon icon-spinner icon-spin"},null,-1),(0,a.createElementVNode)("span",null,"Waiting for the Agents extension…",-1)])]))]))}});t(9120);const st=(0,Qe.A)(rt,[["__scopeId","data-v-5584bb48"]]);var it=st;const lt={class:"session__id"},dt={class:"session"},ct={key:0,class:"session__waiting"},ht={key:2,class:"session__note session__note--ended"},pt={key:3,class:"session__note"};var ut=(0,a.defineComponent)({__name:"AgentSessionPanel",props:{job:{},onBack:{type:Function},width:{},height:{},triggerFocusTrap:{type:Boolean},closeOnRouteChange:{}},emits:["close"],setup(e,{emit:n}){const t=e,o=n,s=(0,a.ref)(""),i=(0,a.computed)(()=>{const e=Sn(t.job);return"workspace"!==e&&"waiting"!==e&&"preparing"!==e});return(n,l)=>((0,a.openBlock)(),(0,a.createBlock)(tt.A,{"aria-target":`the agent session for ${e.job.library}`,onClose:l[2]||(l[2]=e=>o("close"))},{title:(0,a.withCtx)(()=>[(0,a.createTextVNode)(" Agent session · "+(0,a.toDisplayString)(e.job.library)+" ",1),(0,a.createElementVNode)("span",lt,(0,a.toDisplayString)(e.job.sessionId),1)]),body:(0,a.withCtx)(()=>[(0,a.createElementVNode)("div",dt,[e.job.sessionId&&i.value?((0,a.openBlock)(),(0,a.createBlock)(it,{key:e.job.sessionId||"none",session:e.job.sessionId||"",class:"session__terminal",onState:l[0]||(l[0]=e=>s.value=e)},null,8,["session"])):((0,a.openBlock)(),(0,a.createElementBlock)("div",ct,[(0,a.createVNode)((0,a.unref)(r.l),{color:"info"},{default:(0,a.withCtx)(()=>[...l[3]||(l[3]=[(0,a.createElementVNode)("strong",null,"Setting the workspace up.",-1),(0,a.createTextVNode)(" The first fix for a repository clones it and installs its dependencies, which takes several minutes. The agent starts as soon as that is done — you can close this and come back. ",-1)])]),_:1}),(0,a.createVNode)(Hn,{phase:(0,a.unref)(Sn)(e.job),elapsed:(0,a.unref)(xn)(e.job),"can-open-session":!1},null,8,["phase","elapsed"])])),"closed"===s.value?((0,a.openBlock)(),(0,a.createElementBlock)("p",ht," This conversation has ended — the run it belonged to is over, and the agent pod has released it. ")):e.job.sessionId?((0,a.openBlock)(),(0,a.createElementBlock)("p",pt," Closing this drawer leaves the conversation running — it is the pod that holds it, not this page. ")):(0,a.createCommentVNode)("",!0)])]),"additional-actions":(0,a.withCtx)(()=>[e.onBack?((0,a.openBlock)(),(0,a.createBlock)(h.A,{key:0,variant:"secondary",size:"large","data-testid":"vc-session-back",onClick:l[1]||(l[1]=n=>{var a;return null===(a=t.onBack)||void 0===a?void 0:a.call(t,e.job)})},{default:(0,a.withCtx)(()=>[...l[4]||(l[4]=[(0,a.createTextVNode)(" Back to the board ",-1)])]),_:1})):(0,a.createCommentVNode)("",!0)]),_:1},8,["aria-target"]))}});t(1809);const mt=(0,Qe.A)(ut,[["__scopeId","data-v-0b9da457"]]);var ft=mt;const vt={class:"creds",role:"dialog","aria-modal":"true","aria-labelledby":"vc-creds-title","data-testid":"vc-credentials"},gt={class:"creds__lede"},bt={class:"creds__field"},wt={class:"creds__label"},yt={class:"creds__hint"},kt={class:"creds__input"},$t=["type","placeholder"],xt=["title"],_t=["disabled"],Et={class:"creds__actions"},St=["disabled"],Rt=["disabled"];var Nt=(0,a.defineComponent)({__name:"CredentialsDialog",props:{status:{},principalId:{},blocking:{type:Boolean},busy:{type:Boolean}},emits:["cancel","saved"],setup(e,{emit:n}){const t=e,o=n,s=(0,a.ref)(""),i=(0,a.ref)(!1),l=(0,a.ref)(!1),d=(0,a.ref)(""),c=(0,a.computed)(()=>t.status.stored),h=(0,a.computed)(()=>c.value||!!s.value.trim());async function p(){if(h.value&&!l.value){l.value=!0,d.value="";try{await we(t.principalId,s.value.trim()),s.value="",o("saved")}catch(e){d.value=(null===e||void 0===e?void 0:e.message)||String(e)}finally{l.value=!1}}}async function u(){l.value=!0,d.value="";try{await we(t.principalId,""),o("saved")}catch(e){d.value=(null===e||void 0===e?void 0:e.message)||String(e)}finally{l.value=!1}}return(n,t)=>((0,a.openBlock)(),(0,a.createElementBlock)("div",{class:"creds-backdrop",onClick:t[4]||(t[4]=(0,a.withModifiers)(n=>!l.value&&!e.busy&&o("cancel"),["self"]))},[(0,a.createElementVNode)("div",vt,[t[21]||(t[21]=(0,a.createElementVNode)("h2",{id:"vc-creds-title",class:"creds__title"}," Credentials ",-1)),(0,a.createElementVNode)("p",gt,[e.blocking?((0,a.openBlock)(),(0,a.createElementBlock)(a.Fragment,{key:0},[t[5]||(t[5]=(0,a.createTextVNode)(" A fix pushes a branch and opens a pull request, and both are done by ",-1)),t[6]||(t[6]=(0,a.createElementVNode)("em",null,"you",-1)),t[7]||(t[7]=(0,a.createTextVNode)(" — so this needs your own GitHub token. It is stored once — you will not be asked again. ",-1))],64)):((0,a.openBlock)(),(0,a.createElementBlock)(a.Fragment,{key:1},[(0,a.createTextVNode)(" Yours, not the installation's: a fix pushes to your fork and the pull request is authored by you. Stored in a Secret and read by the pod that needs it — it never comes back out to this page, so a stored one can be replaced but not shown. ")],64))]),d.value?((0,a.openBlock)(),(0,a.createBlock)((0,a.unref)(r.l),{key:0,color:"error"},{default:(0,a.withCtx)(()=>[(0,a.createTextVNode)((0,a.toDisplayString)(d.value),1)]),_:1})):(0,a.createCommentVNode)("",!0),(0,a.createElementVNode)("label",bt,[(0,a.createElementVNode)("span",wt,[t[8]||(t[8]=(0,a.createTextVNode)(" GitHub token ",-1)),(0,a.createElementVNode)("span",{class:(0,a.normalizeClass)(["creds__state",{"is-set":c.value}])},(0,a.toDisplayString)(c.value?"Stored":"Not set"),3)]),(0,a.createElementVNode)("span",yt,[t[9]||(t[9]=(0,a.createTextVNode)(" It reads Dependabot alerts and pushes branches, so: a classic token with ",-1)),t[10]||(t[10]=(0,a.createElementVNode)("code",null,"repo",-1)),t[11]||(t[11]=(0,a.createTextVNode)(" and ",-1)),t[12]||(t[12]=(0,a.createElementVNode)("code",null,"security_events",-1)),t[13]||(t[13]=(0,a.createTextVNode)(", or a fine-grained token with ",-1)),t[14]||(t[14]=(0,a.createElementVNode)("em",null,"Dependabot alerts (read)",-1)),t[15]||(t[15]=(0,a.createTextVNode)(" plus ",-1)),t[16]||(t[16]=(0,a.createElementVNode)("em",null,"Contents",-1)),t[17]||(t[17]=(0,a.createTextVNode)(" and ",-1)),t[18]||(t[18]=(0,a.createElementVNode)("em",null,"Pull requests",-1)),t[19]||(t[19]=(0,a.createTextVNode)(" (read and write) on the fork. ",-1)),c.value?((0,a.openBlock)(),(0,a.createElementBlock)(a.Fragment,{key:0},[(0,a.createTextVNode)(" Leave blank to keep the stored one.")],64)):(0,a.createCommentVNode)("",!0)]),(0,a.createElementVNode)("span",kt,[(0,a.withDirectives)((0,a.createElementVNode)("input",{"onUpdate:modelValue":t[0]||(t[0]=e=>s.value=e),type:i.value?"text":"password",autocomplete:"off",spellcheck:"false","data-testid":"vc-gh-token",placeholder:c.value?"••••••••  (stored)":"GH_TOKEN",onKeyup:(0,a.withKeys)(p,["enter"])},null,40,$t),[[a.vModelDynamic,s.value]]),(0,a.createElementVNode)("button",{type:"button",class:"creds__peek",title:i.value?"Hide":"Show",onClick:t[1]||(t[1]=e=>i.value=!i.value)},[(0,a.createElementVNode)("i",{class:(0,a.normalizeClass)(["icon",i.value?"icon-hide":"icon-show"])},null,2)],8,xt)]),c.value?((0,a.openBlock)(),(0,a.createElementBlock)("button",{key:0,type:"button",class:"creds__clear",disabled:l.value,onClick:t[2]||(t[2]=e=>u())}," Remove the stored token ",8,_t)):(0,a.createCommentVNode)("",!0)]),(0,a.createVNode)((0,a.unref)(r.l),{color:"info",class:"creds__note"},{default:(0,a.withCtx)(()=>[...t[20]||(t[20]=[(0,a.createTextVNode)(" Written straight into the Secret under a key of your own and never read back by this page — replacing it is possible, seeing it is not. Saving touches your key alone, so nobody else's token is read or rewritten. The pod reads it with its own ServiceAccount at the moment it is needed. ",-1)])]),_:1}),(0,a.createElementVNode)("div",Et,[(0,a.createElementVNode)("button",{type:"button",class:"btn role-secondary",disabled:l.value||e.busy,onClick:t[3]||(t[3]=e=>o("cancel"))},(0,a.toDisplayString)(e.blocking?"Cancel":"Close"),9,St),(0,a.createElementVNode)("button",{type:"button",class:"btn role-primary",disabled:!h.value||l.value||e.busy,"data-testid":"vc-creds-confirm",onClick:p},(0,a.toDisplayString)(l.value?"Saving…":e.busy?"Starting…":e.blocking?"Save and fix":"Save"),9,Rt)])])]))}});t(2909);const At=(0,Qe.A)(Nt,[["__scopeId","data-v-3758a73c"]]);var Tt=At;const Ot={key:0,class:"shipped__empty"},Ct={class:"shipped__body"},Dt={class:"shipped__lib"},Vt=["href"];var Pt=(0,a.defineComponent)({__name:"ShippedDrawer",props:{rows:{},repo:{},width:{},height:{},triggerFocusTrap:{type:Boolean},closeOnRouteChange:{}},emits:["close"],setup(e,{emit:n}){const t=n;return(n,o)=>((0,a.openBlock)(),(0,a.createBlock)(tt.A,{"aria-target":"the vulnerabilities we have fixed",onClose:o[0]||(o[0]=e=>t("close"))},{title:(0,a.withCtx)(()=>[...o[1]||(o[1]=[(0,a.createTextVNode)(" Shipped ",-1)])]),body:(0,a.withCtx)(()=>[o[2]||(o[2]=(0,a.createElementVNode)("p",{class:"shipped__sub"}," Fixes of ours that merged and whose Dependabot alert has since closed. They age out of this list on their own as GitHub stops reporting them. ",-1)),e.rows.length?(0,a.createCommentVNode)("",!0):((0,a.openBlock)(),(0,a.createElementBlock)("p",Ot," Nothing shipped yet. ")),((0,a.openBlock)(!0),(0,a.createElementBlock)(a.Fragment,null,(0,a.renderList)(e.rows,n=>((0,a.openBlock)(),(0,a.createElementBlock)("div",{key:n.library,class:"shipped__row"},[(0,a.createVNode)((0,a.unref)(l.BN),{status:(0,a.unref)(sn)(n.severity)},{default:(0,a.withCtx)(()=>[(0,a.createTextVNode)((0,a.toDisplayString)(n.severity),1)]),_:2},1032,["status"]),(0,a.createElementVNode)("div",Ct,[(0,a.createElementVNode)("div",Dt,(0,a.toDisplayString)(n.library),1),(0,a.createVNode)($n,{vulns:n.vulns,repo:e.repo},null,8,["vulns","repo"])]),n.pr?((0,a.openBlock)(),(0,a.createElementBlock)("a",{key:0,class:"shipped__pr",href:n.pr.url,target:"_blank",rel:"noopener"},(0,a.toDisplayString)(n.pr.number),9,Vt)):(0,a.createCommentVNode)("",!0)]))),128))]),_:1}))}});t(6732);const Bt=(0,Qe.A)(Pt,[["__scopeId","data-v-398ab162"]]);var Ht=Bt;const It="/workspace/.vuln-console",jt="1000:1000",Wt=18e4;async function Lt(e,n){const t=g();if(!t)throw new Error("The Agents extension is not available on this page, so there is no pod to gather in.");const a=await t.agent.pod();if(!a)throw new Error("The agent pod is not running, so there is nowhere to gather.");const o={pod:a,namespace:t.agent.namespace||"extension-studio",container:t.agent.container||"agent"};for(const s of["gather.mjs","gather.sh"]){const e=Oe[s];if(!e)throw new Error(`This build is missing its ${s} - run "yarn gen-seed" and rebuild.`);await D(o,`${It}/${s}`,e,{mode:"644",owner:jt})}const r=`${It}/gather-${e.id}`;await N(o,`mkdir -p ${A(r)} && chown ${jt} ${A(It)} ${A(r)} 2>/dev/null || true`,"make the gather directory in the agent pod",2e4),await N(o,[`sh ${A(`${It}/gather.sh`)}`,A(r),A(e.id),A(e.repo),A(ce(n)),A(e.ownerPackage||"")].join(" "),`gather the Dependabot alerts for ${e.repo}`,Wt)}const qt={class:"vuln"},Ut={class:"vuln__head"},Gt={class:"vuln__actions"},Mt=["disabled","title"],Ft=["disabled","title"];var Kt=(0,a.defineComponent)({__name:"VulnPage",setup(e){const n=(0,o.Pj)(),t=(0,a.ref)({state:"checking",version:null,pod:null,detail:""}),l=(0,a.ref)({stored:!1,others:0,unreadable:!1}),d=(0,a.computed)(()=>n.getters["auth/principalId"]||""),c=(0,a.ref)(""),h=(0,a.ref)(!1),p=(0,a.ref)(!1),u=(0,a.ref)(!1),m=(0,a.ref)(P[0].id),f=(0,a.ref)({}),v=(0,a.ref)({}),g=(0,a.ref)({}),b=(0,a.computed)(()=>B(m.value)),k=(0,a.computed)(()=>"ready"===t.value.state&&ye(l.value)&&Se(n));function $(e){f.value={...f.value,[e.board]:e.rows},v.value={...v.value,[e.board]:e.tokenLogin}}function x(e){(null===e||void 0===e?void 0:e.sessionId)&&n.commit("slideInPanel/open",{component:ft,componentProps:{width:"wide",height:"full",triggerFocusTrap:!0,closeOnRouteChange:["name","params","query"],onClose:()=>n.commit("slideInPanel/close"),job:e}})}function _(e){n.commit("slideInPanel/open",{component:Ht,componentProps:{width:"wide",height:"full",triggerFocusTrap:!0,closeOnRouteChange:["name","params","query"],onClose:()=>n.commit("slideInPanel/close"),rows:e,repo:b.value.repo}})}function E(){u.value=!1,p.value=!0,ve(d.value).then(e=>{l.value=e}).catch(()=>{})}async function S(){l.value=await ve(d.value).catch(()=>l.value),u.value?ye(l.value)&&(p.value=!1,await T()):p.value=!1}async function R(){var e,n;await(null===(n=null===(e=g.value[m.value])||void 0===e?void 0:e.load)||void 0===n?void 0:n.call(e))}async function N(){h.value=!0,c.value="";try{await Lt(b.value,d.value),await R()}catch(e){c.value=(null===e||void 0===e?void 0:e.message)||String(e)}finally{h.value=!1}}async function A(e,t){c.value="";try{const a=await Ge({store:n,board:b.value,tokenLogin:v.value[m.value]||"",principalId:d.value,library:e.library,action:t,group:e,by:d.value});await R(),x(a)}catch(a){c.value=(null===a||void 0===a?void 0:a.message)||String(a)}}async function T(){if(!ye(l.value))return u.value=!0,void(p.value=!0);const e=(f.value[m.value]||[]).filter(e=>{var n,t;return!e.unfixable&&!e.ownerOnly&&!(null===(n=e.job)||void 0===n?void 0:n.branch)&&"Running"!==(null===(t=e.job)||void 0===t?void 0:t.phase)}).sort((e,n)=>ln(e.severity)-ln(n.severity))[0];e?await A(e,"fix"):c.value=`Nothing on ${b.value.repo} is waiting for a fix.`}async function O(e){if(e)try{await Me(e),await R()}catch(n){c.value=(null===n||void 0===n?void 0:n.message)||String(n)}}return y().then(async()=>{const[e,n]=await Promise.all([w(),ve(d.value).catch(()=>({stored:!1,others:0,unreadable:!0}))]);t.value=e,l.value=n}),(e,o)=>((0,a.openBlock)(),(0,a.createElementBlock)("div",qt,[(0,a.createElementVNode)("header",Ut,[o[5]||(o[5]=(0,a.createElementVNode)("div",{class:"vuln__titles"},[(0,a.createElementVNode)("h1",{class:"vuln__title"}," Vulnerabilities "),(0,a.createElementVNode)("p",{class:"vuln__lede"}," Actionable Dependabot alerts — each with the lockfiles it touches, and a fix that runs in a workspace of its own. One board per repository; the actions here act on the one you are looking at. ")],-1)),(0,a.createElementVNode)("div",Gt,[(0,a.createElementVNode)("button",{type:"button",class:"btn role-primary",disabled:"ready"!==t.value.state||!(0,a.unref)(Se)((0,a.unref)(n)),"data-testid":"vc-fix-worst",title:k.value?`Fix the highest-severity vulnerability waiting for one on ${b.value.repo}`:t.value.detail||"Not ready yet",onClick:T},[...o[2]||(o[2]=[(0,a.createElementVNode)("i",{class:"icon icon-play"},null,-1),(0,a.createElementVNode)("span",null,"Fix the worst one",-1)])],8,Mt),(0,a.createElementVNode)("button",{type:"button",class:"btn role-secondary",disabled:!k.value||h.value,"data-testid":"vc-refresh",title:`Re-read ${b.value.repo}'s Dependabot alerts and our pull requests`,onClick:N},[o[3]||(o[3]=(0,a.createElementVNode)("i",{class:"icon icon-refresh"},null,-1)),(0,a.createElementVNode)("span",null,(0,a.toDisplayString)(h.value?"Refreshing…":"Refresh"),1)],8,Ft),(0,a.createElementVNode)("button",{type:"button",class:"btn role-secondary","data-testid":"vc-credentials-open",title:"The GitHub token the boards are read and fixed with",onClick:E},[...o[4]||(o[4]=[(0,a.createElementVNode)("i",{class:"icon icon-key"},null,-1),(0,a.createElementVNode)("span",null,"Credentials",-1)])])])]),"ready"!==t.value.state&&"checking"!==t.value.state?((0,a.openBlock)(),(0,a.createBlock)((0,a.unref)(r.l),{key:0,color:"no-pod"===t.value.state?"warning":"error","data-testid":"vc-agents-banner"},{default:(0,a.withCtx)(()=>[o[6]||(o[6]=(0,a.createElementVNode)("strong",null,"Agents is not ready.",-1)),(0,a.createTextVNode)(" "+(0,a.toDisplayString)(t.value.detail),1)]),_:1},8,["color"])):(0,a.unref)(Se)((0,a.unref)(n))?(0,a.unref)(ye)(l.value)?(0,a.createCommentVNode)("",!0):((0,a.openBlock)(),(0,a.createBlock)((0,a.unref)(r.l),{key:2,color:"warning"},{default:(0,a.withCtx)(()=>[o[8]||(o[8]=(0,a.createElementVNode)("strong",null,"You have not stored a GitHub token.",-1)),o[9]||(o[9]=(0,a.createTextVNode)(" Tokens are per person — a fix pushes to your fork and opens the pull request as you — so yours is needed before you can refresh a board or fix anything. ",-1)),l.value.others?((0,a.openBlock)(),(0,a.createElementBlock)(a.Fragment,{key:0},[(0,a.createTextVNode)((0,a.toDisplayString)(l.value.others)+" other "+(0,a.toDisplayString)(1===l.value.others?"person has":"people have")+" stored one. ",1)],64)):(0,a.createCommentVNode)("",!0)]),_:1})):((0,a.openBlock)(),(0,a.createBlock)((0,a.unref)(r.l),{key:1,color:"warning"},{default:(0,a.withCtx)(()=>[...o[7]||(o[7]=[(0,a.createElementVNode)("strong",null,"Apps Plus is not installed.",-1),(0,a.createTextVNode)(" A fix runs in a workspace, and a workspace is an Apps Plus installation — without it there is nowhere for the work to happen. ",-1)])]),_:1})),c.value?((0,a.openBlock)(),(0,a.createBlock)((0,a.unref)(r.l),{key:3,color:"error"},{default:(0,a.withCtx)(()=>[(0,a.createTextVNode)((0,a.toDisplayString)(c.value),1)]),_:1})):(0,a.createCommentVNode)("",!0),(0,a.createVNode)(s.A,{"default-tab":(0,a.unref)(P)[0].id,"use-hash":!0,onChanged:o[0]||(o[0]=e=>m.value=e.selectedName)},{default:(0,a.withCtx)(()=>[((0,a.openBlock)(!0),(0,a.createElementBlock)(a.Fragment,null,(0,a.renderList)((0,a.unref)(P),e=>((0,a.openBlock)(),(0,a.createBlock)(i.A,{key:e.id,name:e.id,label:e.label,weight:(0,a.unref)(P).length-(0,a.unref)(P).indexOf(e)},{default:(0,a.withCtx)(()=>[(0,a.createVNode)(nt,{ref_for:!0,ref:n=>g.value[e.id]=n,board:e,onAct:A,onStop:O,onSession:x,onShipped:_,onLoaded:$},null,8,["board"])]),_:2},1032,["name","label","weight"]))),128))]),_:1},8,["default-tab"]),p.value?((0,a.openBlock)(),(0,a.createBlock)(Tt,{key:4,status:l.value,"principal-id":d.value,blocking:u.value,onCancel:o[1]||(o[1]=e=>p.value=!1),onSaved:S},null,8,["status","principal-id","blocking"])):(0,a.createCommentVNode)("",!0)]))}});t(2593);const zt=(0,Qe.A)(Kt,[["__scopeId","data-v-0a6882d6"]]);var Yt=zt},7553:function(e,n,t){"use strict";t.r(n);var a=t(6758),o=t.n(a),r=t(6173),s=t.n(r),i=s()(o());i.push([e.id,".session[data-v-0b9da457]{height:100%;min-height:0;overflow:hidden}.session[data-v-0b9da457],.session__waiting[data-v-0b9da457]{display:flex;flex-direction:column}.session__waiting[data-v-0b9da457]{gap:8px}.session__id[data-v-0b9da457]{margin-left:10px;font-family:var(--font-family-mono,monospace);font-size:11px;font-weight:400;color:var(--muted)}.session__terminal[data-v-0b9da457]{flex:1 1 auto;min-height:0;border:1px solid var(--border);border-radius:6px;overflow:hidden;background:var(--body-bg)}.session__note[data-v-0b9da457]{margin:10px 0 0;font-size:11px;color:var(--muted)}.session__note--ended[data-v-0b9da457]{color:var(--warning)}",""]),n["default"]=i},8899:function(e,n,t){"use strict";t.r(n);var a=t(6758),o=t.n(a),r=t(6173),s=t.n(r),i=s()(o());i.push([e.id,".agent-terminal[data-v-5584bb48]{display:flex;flex-direction:column;min-width:0}.agent-terminal[data-v-5584bb48],.agent-terminal__pane[data-v-5584bb48]{flex:1 1 auto;min-height:0}.agent-terminal__waiting[data-v-5584bb48]{display:flex;align-items:center;gap:8px;padding:16px;color:var(--muted);font-size:13px}",""]),n["default"]=i},6477:function(e,n,t){"use strict";t.r(n);var a=t(6758),o=t.n(a),r=t(6173),s=t.n(r),i=s()(o());i.push([e.id,".board__running[data-v-7ccb6cc6]{margin-bottom:18px;padding:12px 16px;border:1px solid var(--info);border-radius:8px;background:var(--body-bg)}.board__running-head[data-v-7ccb6cc6]{display:flex;align-items:center;gap:8px;font-size:13px}.board__running-head .icon[data-v-7ccb6cc6]{color:var(--info)}.board__running-head span[data-v-7ccb6cc6]{margin-left:auto;color:var(--muted);font-size:11px;font-variant-numeric:tabular-nums}.board__running-stop[data-v-7ccb6cc6]{border:none;background:transparent;color:var(--link);font-size:11px;cursor:pointer;padding:0}.board__running-stop[data-v-7ccb6cc6]:hover{text-decoration:underline}.board__loading[data-v-7ccb6cc6]{display:flex;align-items:center;gap:10px;padding:40px 0;color:var(--muted)}.board__empty[data-v-7ccb6cc6]{margin:24px auto 0;max-width:560px;padding:28px 32px;border:1px dashed var(--border);border-radius:10px;text-align:center}.board__empty h2[data-v-7ccb6cc6]{margin:0 0 8px;font-size:16px}.board__empty p[data-v-7ccb6cc6]{margin:0;color:var(--muted);font-size:13px;line-height:19px}.board__toolbar[data-v-7ccb6cc6]{display:flex;align-items:center;justify-content:space-between;gap:16px;flex-wrap:wrap;margin-bottom:12px}.board__counts[data-v-7ccb6cc6]{display:flex;flex-wrap:wrap;gap:12px}.board__counts[data-v-7ccb6cc6] .count-container{min-width:132px}.board__counts[data-v-7ccb6cc6] .count-container.clickable .count{transition:background-color .1s ease-in-out}.board__counts[data-v-7ccb6cc6] .count-container.clickable .count:hover{background:var(--accent-btn)}.board__stamp[data-v-7ccb6cc6]{font-size:11px;color:var(--muted)}.board__lib[data-v-7ccb6cc6]{font-weight:600;white-space:nowrap}.board__stale[data-v-7ccb6cc6]{display:inline-block;margin-top:4px;text-decoration:none}.board__file[data-v-7ccb6cc6]{display:block;font-family:var(--font-family-mono,monospace);font-size:11px;white-space:nowrap}.board__file[data-v-7ccb6cc6],.board__message[data-v-7ccb6cc6],.board__none[data-v-7ccb6cc6]{color:var(--muted)}.board__message[data-v-7ccb6cc6]{margin-top:4px;font-size:11px}",""]),n["default"]=i},9101:function(e,n,t){"use strict";t.r(n);var a=t(6758),o=t.n(a),r=t(6173),s=t.n(r),i=s()(o());i.push([e.id,"[data-v-3758a73c] code{padding:1px 5px;font-size:.92em;line-height:inherit;vertical-align:baseline;border-radius:3px}.creds-backdrop[data-v-3758a73c]{position:fixed;inset:0;z-index:1000;display:flex;align-items:center;justify-content:center;padding:24px;background:rgba(0,0,0,.5)}.creds[data-v-3758a73c]{width:100%;max-width:580px;max-height:90vh;overflow-y:auto;padding:24px;border-radius:8px;background:var(--body-bg);border:1px solid var(--border);box-shadow:0 18px 48px rgba(0,0,0,.35)}.creds__title[data-v-3758a73c]{margin:0 0 8px;font-size:20px}.creds__lede[data-v-3758a73c]{margin:0 0 20px;color:var(--muted);font-size:13px;line-height:19px}.creds__field[data-v-3758a73c]{display:block;margin-bottom:18px}.creds__label[data-v-3758a73c]{display:flex;align-items:center;gap:10px;font-weight:600;font-size:13px;margin-bottom:2px}.creds__state[data-v-3758a73c]{padding:1px 8px;border-radius:10px;border:1px solid var(--border);font-size:10px;font-weight:700;letter-spacing:.04em;text-transform:uppercase;color:var(--muted)}.creds__state.is-set[data-v-3758a73c]{color:var(--success);border-color:var(--success)}.creds__hint[data-v-3758a73c]{display:block;color:var(--muted);font-size:12px;line-height:17px;margin-bottom:6px}.creds__hint em[data-v-3758a73c]{font-style:normal;color:var(--body-text)}.creds__input[data-v-3758a73c]{display:flex;align-items:stretch;border:1px solid var(--border);border-radius:4px;background:var(--input-bg);overflow:hidden}.creds__input[data-v-3758a73c]:focus-within{border-color:var(--link)}.creds__input input[data-v-3758a73c]{flex:1;min-width:0;padding:8px 10px;outline:none;color:var(--input-text);font-family:var(--font-family-mono,monospace);font-size:13px}.creds__input input[data-v-3758a73c],.creds__peek[data-v-3758a73c]{border:none;background:transparent}.creds__peek[data-v-3758a73c]{color:var(--muted);cursor:pointer;padding:0 10px}.creds__peek[data-v-3758a73c]:hover{color:var(--body-text)}.creds__clear[data-v-3758a73c]{margin-top:6px;border:none;background:transparent;padding:0;color:var(--error);font-size:11px;cursor:pointer}.creds__clear[data-v-3758a73c]:hover{text-decoration:underline}.creds__note[data-v-3758a73c]{font-size:12px;line-height:18px}.creds__actions[data-v-3758a73c]{display:flex;justify-content:flex-end;gap:10px;margin-top:20px}",""]),n["default"]=i},2057:function(e,n,t){"use strict";t.r(n);var a=t(6758),o=t.n(a),r=t(6173),s=t.n(r),i=s()(o());i.push([e.id,'.progress[data-v-c97aac58]{margin-top:10px}.progress__steps[data-v-c97aac58]{display:flex;flex-wrap:wrap;gap:6px 4px;margin:0;padding:0;list-style:none}.progress__step[data-v-c97aac58]{display:inline-flex;align-items:center;gap:6px;padding:3px 10px 3px 6px;border-radius:12px;font-size:11px;color:var(--muted);background:var(--nav-bg)}.progress__step+.progress__step[data-v-c97aac58]{position:relative;margin-left:8px}.progress__step+.progress__step[data-v-c97aac58]:before{content:"›";position:absolute;left:-10px;color:var(--muted);opacity:.6}.progress__step.is-done[data-v-c97aac58]{color:var(--success)}.progress__step.is-active[data-v-c97aac58]{color:var(--body-text);background:var(--accent-btn);font-weight:600}.progress__step.is-todo[data-v-c97aac58]{opacity:.55}.progress__marker[data-v-c97aac58]{display:inline-flex;align-items:center;justify-content:center;width:13px;height:13px}.progress__marker .icon[data-v-c97aac58]{font-size:11px}.progress__step.is-todo .progress__marker[data-v-c97aac58]:before{content:"";width:5px;height:5px;border-radius:50%;border:1px solid currentColor}.progress__foot[data-v-c97aac58]{display:flex;align-items:center;gap:12px;margin-top:8px;font-size:11px;color:var(--muted)}.progress__elapsed[data-v-c97aac58]{font-variant-numeric:tabular-nums}.progress__toggle[data-v-c97aac58]{border:none;background:transparent;color:var(--link);font-size:11px;cursor:pointer;padding:0;display:inline-flex;align-items:center;gap:4px}.progress__toggle[data-v-c97aac58]:hover{text-decoration:underline}',""]),n["default"]=i},1324:function(e,n,t){"use strict";t.r(n);var a=t(6758),o=t.n(a),r=t(6173),s=t.n(r),i=s()(o());i.push([e.id,".shipped__sub[data-v-398ab162]{margin:0 0 16px}.shipped__empty[data-v-398ab162],.shipped__sub[data-v-398ab162]{color:var(--muted)}.shipped__row[data-v-398ab162]{display:flex;align-items:flex-start;gap:12px;padding:10px 0;border-bottom:1px solid var(--border)}.shipped__body[data-v-398ab162]{flex:1 1 auto;min-width:0}.shipped__lib[data-v-398ab162]{font-weight:600;margin-bottom:4px}.shipped__pr[data-v-398ab162]{white-space:nowrap}",""]),n["default"]=i},617:function(e,n,t){"use strict";t.r(n);var a=t(6758),o=t.n(a),r=t(6173),s=t.n(r),i=s()(o());i.push([e.id,".steps[data-v-54f21300]{display:flex;flex-wrap:wrap;align-items:center;gap:6px}.steps__link[data-v-54f21300]{text-decoration:none;max-width:260px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}",""]),n["default"]=i},8361:function(e,n,t){"use strict";t.r(n);var a=t(6758),o=t.n(a),r=t(6173),s=t.n(r),i=s()(o());i.push([e.id,".vuln-ids[data-v-501df710]{display:flex;flex-wrap:wrap;gap:4px}.vuln-ids__link[data-v-501df710]{text-decoration:none}",""]),n["default"]=i},1729:function(e,n,t){"use strict";t.r(n);var a=t(6758),o=t.n(a),r=t(6173),s=t.n(r),i=s()(o());i.push([e.id,".vuln[data-v-0a6882d6]{padding:20px}.vuln__head[data-v-0a6882d6]{display:flex;align-items:flex-start;justify-content:space-between;gap:24px;flex-wrap:wrap;margin-bottom:16px}.vuln__titles[data-v-0a6882d6]{min-width:0}.vuln__title[data-v-0a6882d6]{margin:0 0 4px;font-size:22px;font-weight:600}.vuln__lede[data-v-0a6882d6]{margin:0;max-width:66ch;color:var(--muted);font-size:13px;line-height:19px}.vuln__actions[data-v-0a6882d6]{display:flex;gap:8px;flex-shrink:0}",""]),n["default"]=i},1809:function(e,n,t){var a=t(7553);a.__esModule&&(a=a.default),"string"===typeof a&&(a=[[e.id,a,""]]),a.locals&&(e.exports=a.locals);var o=t(4825).A;o("6d0ea9d1",a,!0,{sourceMap:!1,shadowMode:!1})},9120:function(e,n,t){var a=t(8899);a.__esModule&&(a=a.default),"string"===typeof a&&(a=[[e.id,a,""]]),a.locals&&(e.exports=a.locals);var o=t(4825).A;o("c0eec4c2",a,!0,{sourceMap:!1,shadowMode:!1})},5645:function(e,n,t){var a=t(6477);a.__esModule&&(a=a.default),"string"===typeof a&&(a=[[e.id,a,""]]),a.locals&&(e.exports=a.locals);var o=t(4825).A;o("516a2528",a,!0,{sourceMap:!1,shadowMode:!1})},2909:function(e,n,t){var a=t(9101);a.__esModule&&(a=a.default),"string"===typeof a&&(a=[[e.id,a,""]]),a.locals&&(e.exports=a.locals);var o=t(4825).A;o("1ef95828",a,!0,{sourceMap:!1,shadowMode:!1})},4105:function(e,n,t){var a=t(2057);a.__esModule&&(a=a.default),"string"===typeof a&&(a=[[e.id,a,""]]),a.locals&&(e.exports=a.locals);var o=t(4825).A;o("317dcfb8",a,!0,{sourceMap:!1,shadowMode:!1})},6732:function(e,n,t){var a=t(1324);a.__esModule&&(a=a.default),"string"===typeof a&&(a=[[e.id,a,""]]),a.locals&&(e.exports=a.locals);var o=t(4825).A;o("4b198b39",a,!0,{sourceMap:!1,shadowMode:!1})},5081:function(e,n,t){var a=t(617);a.__esModule&&(a=a.default),"string"===typeof a&&(a=[[e.id,a,""]]),a.locals&&(e.exports=a.locals);var o=t(4825).A;o("55b84ba8",a,!0,{sourceMap:!1,shadowMode:!1})},9657:function(e,n,t){var a=t(8361);a.__esModule&&(a=a.default),"string"===typeof a&&(a=[[e.id,a,""]]),a.locals&&(e.exports=a.locals);var o=t(4825).A;o("9e7c0046",a,!0,{sourceMap:!1,shadowMode:!1})},2593:function(e,n,t){var a=t(1729);a.__esModule&&(a=a.default),"string"===typeof a&&(a=[[e.id,a,""]]),a.locals&&(e.exports=a.locals);var o=t(4825).A;o("86c5fcc8",a,!0,{sourceMap:!1,shadowMode:!1})}}]);
//# sourceMappingURL=vuln-console-0.8.2.umd.min.914.js.map